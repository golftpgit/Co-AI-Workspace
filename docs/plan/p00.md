# P0 — Scaffold

> ส่วนหนึ่งของ [แผนการสร้าง](README.md) · สเปกอยู่ที่ [`ARCHITECTURE.md`](../../ARCHITECTURE.md) · กติกาว่าอะไรเรียกว่าผ่านอยู่ที่ [`TEST_PROTOCOL.md`](../TEST_PROTOCOL.md)
>
> **ตารางข้างล่างคือสถานะ** · หัวข้อ "เหตุผลและสิ่งที่วัดได้" ข้างใต้คือ*ที่มา*ของสถานะนั้น

---


**สถานะ: ✅ ครบ 5/5 (2026-08-10)**

**เป้าหมาย**: โครงที่ build/test/run ได้ ก่อนเขียน logic ใดๆ

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P0.1** SwiftPM package + targets | `AgentKit` / `Config` / `Observability` / `Sidecar` / `CoAIWorkspaceApp` + test target ครบ ([ARCH §3](../architecture/01-foundations.md#3-system-hierarchy) · [§4](../architecture/01-foundations.md#4-module-catalog--ภาพรวมทั้งระบบ)) | `swift build` + app เปิดหน้าต่างได้ | ✅ build ผ่าน, แอปเปิดเป็น Foreground app จริง (`lsappinfo` ยืนยัน) |
| **P0.2** Swift Testing + check script | `scripts/check.sh` = build + test + structural rules | รัน 1 คำสั่งแล้วได้ผลสรุป pass/fail | ✅ **30 tests / 7 suites ผ่านหมด** + structural rule 3 ข้อ (กัน `Scope` ซ้ำ, `print()` ใน library, `[String: Any]` บน Sendable) |
| **P0.3** Bootstrap config | `bootstrap.plist` + `AppPaths` สร้าง directory เอง + validate + ซ่อมไฟล์เสีย | ลบ dir แล้วเปิดแอปใหม่ → สร้างเองได้ ไม่ crash | ✅ ทดสอบกับแอปจริง: ลบทั้งโฟลเดอร์ → เปิดใหม่ → สร้างครบ 8 รายการ; เปิดซ้ำ → **ไม่เขียนทับ** |
| **P0.4** Sidecar manager | spawn/monitor/restart/terminate + health probe + **reap orphan จาก pid file** | kill จากภายนอก → restart ภายใน 5 วิ; ปิดแอป → ไม่มี process ค้าง | ✅ test พิสูจน์ครบ: restart **0.36 วิ**, `stopAll` ไม่เหลือ process, restart มีเพดานไม่วนไม่จำกัด, orphan ถูกเก็บกวาด |
| **P0.5** App Sandbox + entitlements | เปิด sandbox ตั้งแต่ต้น + `build-app.sh` ประกอบ+เซ็น bundle | แอปรันใน sandbox ได้ + เข้าถึง data dir/network ได้ | ✅ `lsappinfo` แสดง **sandboxed**, container ถูกสร้าง, `codesign --verify` ผ่าน |

**สิ่งที่ได้จริง**: `swift build && ./scripts/check.sh` ผ่านทั้งหมด · `./scripts/build-app.sh` ได้ `.app` ที่เซ็นแล้วเปิดใช้ได้จริง · แอปแสดงหน้า Boot Status (config, paths, sidecar health) ซึ่งจะถูกแทนที่ด้วย Chat view ใน P1.10

---


---

## เหตุผลและสิ่งที่วัดได้ — รายละเอียดต่อ task


### P0.1 — SwiftPM package + targets

**สถานะ**

✅ build ผ่าน, แอปเปิดเป็น Foreground app จริง (`lsappinfo` ยืนยัน)


---
### P0.2 — Swift Testing + check script

**สถานะ**

✅ **30 tests / 7 suites ผ่านหมด** + structural rule 3 ข้อ (กัน `Scope` ซ้ำ, `print()` ใน library, `[String: Any]` บน Sendable)


---
### P0.3 — Bootstrap config

**สถานะ**

✅ ทดสอบกับแอปจริง: ลบทั้งโฟลเดอร์ → เปิดใหม่ → สร้างครบ 8 รายการ; เปิดซ้ำ → **ไม่เขียนทับ**


---
### P0.4 — Sidecar manager

**สถานะ**

✅ test พิสูจน์ครบ: restart **0.36 วิ**, `stopAll` ไม่เหลือ process, restart มีเพดานไม่วนไม่จำกัด, orphan ถูกเก็บกวาด


---
### P0.5 — App Sandbox + entitlements

**สถานะ**

✅ `lsappinfo` แสดง **sandboxed**, container ถูกสร้าง, `codesign --verify` ผ่าน


---


---

[↩ แผนการสร้าง](README.md) · [ของค้าง](README.md#ค้างไว้--ยังไม่ได้พิสูจน์) · [ความเสี่ยง](risks.md)
