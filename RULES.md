# กฎของโปรเจกต์ Co-AI Workspace

แต่ละกฎบอกสามอย่าง: **กฎคืออะไร · บังคับด้วยอะไร · ทำไมถึงมีกฎนี้** — ข้อที่ยังไม่มีตัวบังคับอยู่ที่ [§9](#9-กฎที่ยังบังคับไม่ได้)

| | |
|---|---|
| **บังคับด้วย** | `scripts/check.sh` (95 กฎ) · `scripts/accessibility-audit.py` · Swift type system · โครง `Package.swift` |
| **รันด้วย** | `./scripts/check.sh` ทุก commit · `./scripts/check.sh --full` ก่อนปิดทุก task |
| **อัปเดตล่าสุด** | 2026-08-17 |

| § | เนื้อหา |
|---|---|
| [1](#1-กฎแม่-8-ข้อ) | **กฎแม่ 8 ข้อ** — ตัดสินทุกข้อขัดแย้ง |
| [2](#2-ความปลอดภัยของการเรียกทูล) | ความปลอดภัยของการเรียกทูล (8) |
| [3](#3-สิ่งที่สร้างแล้วต้องต่อถึงคน) | สิ่งที่สร้างแล้วต้องต่อถึงคน (7) |
| [4](#4-ความถูกต้องของข้อมูลและตัวเลข) | ความถูกต้องของข้อมูลและตัวเลข (12) |
| [5](#5-ความเป็นส่วนตัวและความลับ) | ความเป็นส่วนตัวและความลับ (7) |
| [6](#6-หน้าจอ) | หน้าจอ (14) |
| [7](#7-โมเดลและ-tier) | โมเดลและ tier (10) |
| [8](#8-เทส--การทดสอบ-end-to-end--เอกสาร) | เทส · การทดสอบ end-to-end · เอกสาร (14) |
| [9](#9-กฎที่ยังบังคับไม่ได้) | **กฎที่ยังบังคับไม่ได้** |
| [10](#10-วิธีเพิ่มกฎใหม่) | วิธีเพิ่มกฎใหม่ |

---

## 1. กฎแม่ 8 ข้อ

**ข้อบนชนะข้อล่างเมื่อขัดกัน**

| # | กฎ | ทำไม |
|---|---|---|
| **M1** | **"มี implementation" ≠ "มี feature"** — ความสามารถที่ไม่มีเส้นทางจากผู้ใช้ถึงมัน ไม่นับว่ามี | v1 มี MCP client ครบแต่ไม่เคยต่อเข้า tool list · เกิดซ้ำอีก 8 ครั้ง รวมถึง `ConflictDetector` ที่ผ่าน 7 เทสขณะที่หน้าจอว่างถาวร |
| **M2** | **"เทสเขียว" ≠ "ใช้งานได้"** — สิ่งที่มีหน้าจอต้องถูกกดจริงบน `.app` ที่ build + sandbox แล้ว | ทุกรอบที่ขับหน้าจอเจอบั๊กที่เทสทั้งชุดมองไม่เห็น (8 · 10 · 5 · 7 · 7 · 3 ตัว) |
| **M3** | **"เขียว" ≠ "ตรวจครบ"** — รอบตรวจต้องบอกทุกครั้งว่า*ข้ามอะไรไป* | `check.sh` เคยขึ้น `ALL CHECKS PASSED` โดยข้ามด่าน Tier 1 ทั้งด่าน ขณะที่ GX10 เปิดอยู่ |
| **M4** | **กฎที่ไม่มีเครื่องบังคับ = ความตั้งใจ ไม่ใช่กฎ** | `KnowledgePolicyGate` ผ่าน 11 เทสขณะที่แอปจริงไม่ได้ต่อมันเลย หลายเดือน (R14) |
| **M5** | **ความเงียบคือความล้มเหลวที่แพงที่สุดที่นี่** — อะไรที่พลาดแล้วไม่มีใครรู้ ต้องมีกฎ | เซิร์ฟเวอร์ตอบ 200 ทุกครั้งขณะที่ `tool_calls` เป็น null และระบบ agent ลดชั้นเป็นแชตบอตเงียบ ๆ |
| **M6** | **ตัวเลขที่อ้าง ต้องวัดเอง** — ห้ามเขียน "น่าจะเร็วขึ้น" | เพดานของ GX10 เคยเข้าใจว่า 4.7 tok/s · วัดใหม่ได้ 11.4 และ 16 สายรวม 128.5 ⇒ ข้อสรุปเชิงสถาปัตยกรรมที่ตั้งบนเลขเก่าผิดทั้งหมด |
| **M7** | **ทุกอย่างต้องมีเทส และต้องมีเทสที่วิ่งครบวง (end-to-end)** — เทสหน่วยพิสูจน์ว่าชิ้นส่วนถูก **ไม่ได้พิสูจน์ว่าชิ้นส่วนยังต่อกันอยู่** | โปรเจกต์นี้แก้จุดหนึ่งแล้วอีกจุดพังมาแล้วซ้ำ ๆ — `HookChain(stageGate:)` ผ่าน 11 เทสขณะที่ policy ในแอปดับ · `TeamOrchestrator` เสีย span sink แล้วเทสทุกตัวยังเขียว · endpoint ที่เพิ่มบนหน้าจอไม่ถึง router · **ทั้งสามกรณีมีเทสหน่วยครบ และไม่มีกรณีไหนมีเทสที่วิ่งจากปลายถึงปลาย** |
| **M8** | **ที่นี่ขับหน้าจอเองได้ และต้องขับ** — สิทธิ์ Accessibility เปิดแล้ว · `ScreenDriver` กดและพิมพ์ได้จริงตั้งแต่ P17 | "ทดสอบไม่ได้เพราะขับไม่ได้" หมดอายุแล้วตั้งแต่ 2026-08-16 ([E.33](docs/VERIFICATION_LOG.md)) · ของค้างที่อ้างเหตุผลนี้ต้องถูกขับ ไม่ใช่ถูกเลื่อน |

---

## 2. ความปลอดภัยของการเรียกทูล

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| T1 | **`ToolGateway` เป็นทางเดียวที่เรียกทูลได้** — `.call(argumentsJSON` ปรากฏนอกไฟล์นั้นไม่ได้ | `check.sh` | ทางที่สองคือทางที่ข้าม Critic/Risk/Policy/HITL · v1 มีทางแบบนี้จริงและมันคือบั๊ก |
| T2 | **แอปต้องติดตั้ง policy gate ตอนต่อสาย** — `HookChain(` ใน `Engine.swift` ต้องมี `policyGate:` | `check.sh` | ค่า default คือ `NoPolicyGate` ซึ่งถูกสำหรับเทสและหายนะสำหรับแอป |
| T3 | **ช่องทางแชตเรียกทูลไม่ได้ — บังคับด้วย module graph** ไม่ใช่ด้วยความระมัดระวัง | `check.sh` (ตรวจทั้ง `import` และ `Package.swift`) | v1 bug B2: Telegram bridge รันทูลโดยไม่ผ่าน hook chain |
| T4 | **ทูลที่ `RiskScorer` จำแนกไว้ ต้องมีจริง** หรือประกาศใน `notBuiltYet` | `check.sh` | ชื่อในตารางความเสี่ยง = มีคนตั้งใจจะสร้าง · ชื่อที่ไม่มีของอยู่หลัง = ความตั้งใจที่ไม่เคยลงจอด |
| T5 | **ทูลทุกตัวถูกจำแนกทั้ง *ความเสี่ยง* และ *ผลต่อขั้นโครงการ*** — อยู่ตารางเดียวไม่ได้ | `check.sh` | อยู่ตารางเดียวแล้วจะตกไป `.mutating` เงียบ ๆ หรือข้ามพื้นความเสี่ยงไปเลย |
| T6 | **`install_package` และ `r_install_package` ถามคนเสมอ ไม่ว่าโหมดอัตโนมัติจะเปิดแค่ไหน** | `check.sh` | sdist รันโค้ดตอน*ติดตั้ง* — สิ่งที่มันทำจะไม่โผล่ใน output ไหนเลย |
| T7 | **`r_eval` ลักลอบติดตั้งแพ็กเกจไม่ได้** — `RTool` ต้องมี `refuseInstalls` | `check.sh` | รายการ "ถามเสมอ" ผูกกับ*ชื่อทูล* และ `install.packages(...)` ในบล็อก R ไม่ใช่ชื่อทูล |
| T8 | **ข้อความบนหน้าจอที่ตัวขับอ่านมา เข้าสู่โมเดลในฐานะ *ผลลัพธ์ของทูล* เท่านั้น ไม่ใช่ system message** | `check.sh` | ข้อความบนจออีกฝั่งคือ prompt injection ที่มีเมาส์ |

---

## 3. สิ่งที่สร้างแล้วต้องต่อถึงคน

**หมวดนี้ทั้งหมดคือ M1 ในรูปกฎที่ตรวจได้** — เกิดซ้ำมาแล้ว 9 ครั้ง แต่ละครั้งเพิ่มกฎหนึ่งข้อ

| # | กฎ | บังคับด้วย | เคยเกิดว่า |
|---|---|---|---|
| W1 | **ทุกความสามารถในรายชื่อ ~60 ตัว ต้องถูกอ้างถึงจาก `Sources/CoAIWorkspaceApp`** | `check.sh` | `ConflictDetector` ผ่าน 7 เทส · ไม่มีอะไรในแอปสร้างมัน · หน้าจอว่างถาวร |
| W2 | **ทูลที่สร้างแล้ว ต้องถูก register ที่ gateway** | `check.sh` | ทูลใหม่ 9 ตัวพร้อมกัน ไม่มีตัวไหนเรียกได้ |
| W3 | **ของบน `Engine` ต้องมีหน้าจออ่าน** — ไม่ใช่แค่มี store | `check.sh` | `ChannelAccountStore` อยู่บน engine ตั้งแต่ P7.3 และไม่มี view ไหนอ่าน ⇒ ตั้งค่าบอทได้ทางเดียวคือแก้ JSON ข้างฐานข้อมูล |
| W4 | **ทุกหน้าจอใน §14.2 ต้องมีแถวใน `IAInventory`** | `check.sh` (parse ตาราง §14.2 ออกมาเทียบ) | ความเสี่ยง R13 — ยุบ 14 หน้าเป็น 4 พื้นที่แล้วของหายเงียบ ๆ |
| W5 | **`TeamOrchestrator` ต้องถูกสร้างพร้อม span sink** | `check.sh` (ดูเฉพาะบล็อกโรงงาน ไม่ใช่ทั้งไฟล์) | `spans:` เป็น argument ที่มี default `nil` ⇒ ลืมแล้วคอมไพล์ผ่าน เทสผ่าน และฟีเจอร์ดับเงียบ |
| W6 | **ปุ่มเริ่มงานบนแผน ต้องส่ง `workPackage:` เข้าไปในการรันจริง** | `check.sh` | `LedgerRow.work_package` ว่างมาสี่รายการของแผน เพราะ picker ที่ไม่มีใครอ่าน |
| W7 | **แถบแท็บ workspace ต้องถูกวาดที่ไหนสักที่** | `check.sh` | `OpenWorkspaces` ถือได้สิบ workspace และเข้าถึงไม่ได้เลย |

---

## 4. ความถูกต้องของข้อมูลและตัวเลข

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| D1 | **`Scope` ประกาศได้ครั้งเดียวทั้ง repo** | `check.sh` | v1 มีสามชุดและมันคือรากของบั๊กชุดใหญ่ |
| D2 | **ตัวตรวจ SQL ที่เปลี่ยนข้อมูล อยู่ใน `SQLGuard` ที่เดียว** | `check.sh` | v1 มีสองชุด แล้ว `DELETE` เดียวกันเตือนที่หนึ่ง และรันเงียบอีกที่หนึ่ง |
| D3 | **เลขคณิตสถิติ (tail ของการแจกแจง · eigen · LAPACK) มีชุดเดียว** | `check.sh` | continued fraction สองชุดจะตรงกันหลายปี แล้วต่างกันที่ทศนิยมตำแหน่งที่สี่ ในตารางเดียวที่มีคนตีพิมพ์ |
| D4 | **manifest มี parser ตัวเดียว** — มีแต่ `Manifest.swift` ที่แกะ frontmatter ได้ | `check.sh` | parser ที่สองจะ drift และสิ่งแรกที่ drift คือฟิลด์ที่ตัดสินว่าโปรเจกต์เริ่มด้วยอะไร |
| D5 | **ชนิดของเอกสารมาจากคนที่เพิ่มมัน ห้ามเดาจากเนื้อหา** | `check.sh` | ทางลัดคือดูจากชื่อเรื่อง แล้ว literature review ที่มีคำว่า "โครงร่าง" จะกลายเป็นแผนวิเคราะห์ของใครบางคน |
| D6 | **ใบงานถึง `.done` ผ่านการตรวจหลักฐานเท่านั้น** — `status = .done` เขียนได้ที่เดียว | `check.sh` | สถานะที่กำหนดได้จากทุกที่ คือสถานะที่จะถูกกำหนดจากทุกที่ |
| D7 | **ขั้นของโปรเจกต์เปลี่ยนได้ที่ `ProjectService.advance/terminate` เท่านั้น** | `check.sh` | ไม่งั้นการผ่าน gate กลายเป็นเรื่องของการจำได้ว่าต้องถาม |
| D8 | **การ์ดที่ถูกลากบนกระดาน เดินผ่านการปฏิเสธชุดเดียวกับเมนู** | `check.sh` | drop handler ที่ตั้ง status ตรง ๆ คือประตูที่สองที่ไม่มีกลอน — และเป็นวิธีเขียนที่เป็นธรรมชาติที่สุด |
| D9 | **ประวัติการตัดสินข้อขัดแย้งเป็น append-only** — ห้าม `UPDATE`/`DELETE` | `check.sh` | การกลับคำตัดสินด้วยการเขียนทับ ทำให้การ์ดบอกได้แต่ว่าตอนนี้คิดอะไร บอกไม่ได้ว่าเคยคิดอะไร |
| D10 | **การประมาณตารางเวลา ห้ามมีค่า default** — งานที่ไม่มีประวัติเทียบได้ ไม่ได้วันจบ | `check.sh` (จับ `?? <ตัวเลข>`) | `?? 3600` ตัวเดียวทำให้ทุกใบที่ไม่มีประวัติได้วันจบที่ดูมั่นใจ (R9) |
| D11 | **เส้นความสัมพันธ์ที่คนลบ ต้องไม่ถูกเขียนกลับตอน ingest รอบหน้า** | `check.sh` | ลูกศรเดิมกลับมา และการแก้ของคนถูกยกเลิกด้วย ingest ที่เขาไม่เคยเห็น |
| D12 | **ไฟล์รายการทุกชุดเขียนพร้อม schema version** — ห้าม `encoder.encode(list)` เปล่า | `check.sh` | มัน round-trip วันนี้ และ release ที่เพิ่มฟิลด์คือ release ที่กินบอตของใครบางคน |

---

## 5. ความเป็นส่วนตัวและความลับ

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| S1 | **แอปติดตั้ง `SecretStore` ก่อนอะไรก็ตามอ่านความลับ** | `check.sh` | ไม่มีบรรทัดนี้ ความลับทุกตัวตกไปอ่าน environment ที่ `.app` เปิดจาก Finder ไม่มี (วัดแล้ว: `launchctl getenv` ว่าง) |
| S2 | **โมดูลที่ถือความลับ ห้ามอ่าน `processInfo.environment` เอง** | `check.sh` | `EndpointRegistry.apiKey` เคยทำ ⇒ "ที่เดียวที่หาความลับ" กลายเป็นสองที่ และที่สองไม่เห็นทั้ง Keychain และค่า override ของเทส |
| S3 | **`SecretsAuditTests` ต้องอยู่ใน `Package.swift` เสมอ** | `check.sh` | เป็นตัวเดียวที่ตรวจคุณสมบัติที่ไม่มีโมดูลไหนเป็นเจ้าของ — ลบ target ทิ้งแล้วจะปลด Done-when ของ P9.3 เงียบ ๆ |
| S4 | **M16 (FieldServer) เขียนได้แค่ SQLite** — เอื้อมถึง DuckDB ไม่ได้เลยผ่าน dependency | `check.sh` + `Package.swift` | DuckDB มีผู้เขียนคนเดียว · ไม่พังบนเครื่องนี้ แต่พังในวันที่ยี่สิบคนกำลังตอบ ซึ่งเป็นวันเดียวที่งานภาคสนามทำซ้ำไม่ได้ |
| S5 | **ห้าม `UPDATE`/`DELETE` คำตอบดิบ** — การแก้ค่าคือระเบียนการแก้ไข | `check.sh` | ข้อมูลวิจัยที่ถูกเขียนทับเงียบ ๆ ได้ คือข้อมูลที่ไม่มีใครพิสูจน์ได้ว่าไม่ถูกเขียนทับ |
| S6 | **ใครตอบ อยู่คนละไฟล์กับสิ่งที่เขาตอบ และเซิร์ฟเวอร์เอื้อมไม่ถึงไฟล์แรก** | `check.sh` (2 กฎ: ไฟล์แยก + ไม่มี dependency) | สำเนาข้อมูลคำตอบต้องไม่พกตัวตนไปด้วย · และหน้าเดียวที่รับ input จากคนแปลกหน้า ต้องไม่มีทางถามว่าใครเป็นใคร |
| S7 | **ตารางคำตอบขึ้นจอได้โดยไม่ต้องรอ Keychain** | `check.sh` (ตรวจลำดับใน `loadResponses`) | เคยเกิด: Keychain ปฏิเสธ ⇒ ขั้นตอนตัวตนไม่คืนค่า ⇒ ตารางขึ้น "ยังไม่มีคำตอบ" ข้างหัวข้อที่บอกว่าสี่สิบ ทั้งที่มีสี่สิบแถวในฐานข้อมูล |

---

## 6. หน้าจอ

> รายละเอียดเต็มของระบบการออกแบบอยู่ที่ [`docs/UX_UI_DESIGN.md`](docs/UX_UI_DESIGN.md) — ตรงนี้เก็บเฉพาะข้อที่**บังคับด้วยเครื่อง**

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| U1 | **ตัวเลขอยู่บนพื้นทึบเสมอ** — ไฟล์ที่มี `Surface.floating` ห้ามมี `%.Nf` | `check.sh` | ค่า p ที่อ่านผิดเพราะพื้นหลังโปร่งแสง คือความเสียหายที่แก้ไม่ได้ในงานวิจัย |
| U2 | **ความโปร่งแสงประกาศผ่าน `Surface` เท่านั้น** ห้ามใส่ material/glass ดิบ | `check.sh` | ทำให้คำถาม "อันนี้อยู่ชั้นไหน" ตอบได้ด้วย grep เดียวทั้งแอป |
| U3 | **view ใช้ระยะ/มุม/สีจาก `DesignTokens`** | `check.sh` *(มีช่องโหว่ — ดู [§9](#9-กฎที่ยังบังคับไม่ได้))* | เคยมีระยะ 4, 6, 8, 10, 12 อยู่พร้อมกันโดยไม่มีค่าไหนถูกเลือก |
| U4 | **`Optional(…)` ห้ามโผล่บนหน้าจอ** — warning "debug description" = build แดง | `check.sh` | หลุดมาแล้วสองครั้ง คอมไพเลอร์บอกทั้งสองครั้ง และข้อความหายไปใน build log |
| U5 | **markdown ใน `Text` ที่ต่อด้วย `+` = แดง** — ใช้ `Text(markdown:)` | `check.sh` | SwiftUI แกะ markdown เฉพาะเมื่อ argument เป็น *literal* · ต่อสตริงแล้วดอกจันจะขึ้นจอ (เจอกลางประโยคที่อธิบายการรับประกันความเป็นส่วนตัว) |
| U6 | **ห้าม `Binding($…)` ใน view** | `check.sh` | sheet ที่ force-unwrap optional ที่ใช้เปิดตัวเอง จะ trap ตอนกดบันทึก · ขึ้นจริงสองหน้าจอ และเทสทุกตัวมองไม่เห็น |
| U7 | **view model ห้ามอ่าน/เขียนไฟล์บน main actor** | `check.sh` | วัดแล้ว: decode archive 24 MB บน main actor ค้าง 81.6 ms (ห้าเฟรม) เทียบกับ 2.6 ms เมื่อย้ายออก |
| U8 | **หน้าจอโปรเจกต์ buffer การพิมพ์ แล้ว commit หลังหยุด** | `check.sh` | เขียนทุก keystroke ⇒ ประโยคไทยหายราวหนึ่งตัวอักษรต่อรอบ round-trip |
| U9 | **แถบ Gantt ลากไม่ได้** — ห้าม `DragGesture` ใน `ProjectsView` | `check.sh` | วันจบคือ*ผล*ของลำดับงานกับความเร็วจริง · การลากมันคือการแก้เครื่องวัด |
| U10 | **A ใน RACI มีคนเดียว** — ไม่มี case ที่รับ `Role` และไม่มีในแถวปุ่ม | Swift type + `check.sh` | "สอง A" ต้องเป็นสถานะที่แทนค่าไม่ได้ ไม่ใช่สถานะที่มี validator คอยปฏิเสธ |
| U11 | **ทุกปุ่มบนแถบสถานะเดินผ่าน `model.perform` ที่บันทึกเสมอ** | `check.sh` | แถบนี้เปลี่ยนงบหรือปิด exception ได้ในคลิกเดียวจากทุกหน้าจอ = ที่ที่การตัดสินใจถูกลืมง่ายที่สุด |
| U12 | **การเน้นแผงอ่านจาก project type ที่คนเลือกเท่านั้น และ *เน้น* ไม่ *เรียงใหม่*** | `check.sh` (3 กฎ) | หน้าจอที่ย้ายของเองตามที่มันคิดว่าคุณกำลังทำอะไร คือหน้าจอที่จำตำแหน่งอะไรไม่ได้ |
| U13 | **ทุก action มีชื่อที่บอกว่ามันทำอะไร · ไม่ซ้ำ · ไม่ยาวเกิน** | `accessibility-audit.py` | v1 ไม่มี `aria-*` เลย แล้วต้องไล่แก้ 16 ปุ่มใน 8 ไฟล์ทีหลัง |
| U14 | **เหตุผลของการเลือกทูล/tier มาจากกลไกที่เลือกจริง** ไม่ใช่คำอธิบายย้อนหลังของโมเดล | `check.sh` (2 กฎ) | คำอธิบายที่โมเดลเขียนเองคือเรื่องเล่า ไม่ใช่หลักฐาน |
| U15 | **key ที่คอมไพเลอร์บอกว่าถูกเรียก ต้องมีคำแปลครบทุกภาษา และตัวแปรต้องตรงกัน** | `check.sh` | คำแปลที่ขาด = คนอ่านภาษานั้นเห็นอังกฤษโดยไม่มีอะไรบอก · `%.0f` ที่หายไปจากคำแปล = `String(format:)` พิมพ์เลขผิด ไม่ใช่ error |
| U16 | **key มาจาก `-emit-localized-strings` เท่านั้น ห้ามอ่านจากซอร์สเอง** | `Package.swift` + `LocalisationTests` | `\(count)` เป็น `%lld` แต่ `\(name)` เป็น `%@` — แยกได้ต้องใช้ type checker · เดาผิดจะ*ผ่าน*การตรวจของตัวเอง แล้วพลาดทุก lookup ตอนรัน |
| U17 | **ทุก `.lproj` ที่ส่งไปกับแอป ต้องประกาศใน `CFBundleLocalizations`** | `check.sh` | ไม่ประกาศ ⇒ บันเดิลหลักบอกว่าไม่รองรับภาษาไหนเลย ⇒ ภาษานั้น**ไม่มีวันถูกเลือก** บนเครื่องไหนก็ตาม โดยไม่มีอะไรเตือน ([E.48](docs/VERIFICATION_LOG.md)) |
| U18 | **ห้าม `Bundle.module` ใน `Sources/`** — ใช้ `Localisation.bundle(named:)` | `check.sh` | accessor ที่ SwiftPM สร้างมองที่รากของ `.app` ซึ่ง `codesign` ห้ามมีของหลวม · candidate ที่เหลือคือ path ของเครื่องที่คอมไพล์ แล้ว `fatalError` ⇒ **แอปที่แพ็กแล้วตายที่ `Text` ตัวแรกที่แปล** ขณะที่ไบนารีเดียวกันรันจากเทอร์มินัลปกติ ([E.49](docs/VERIFICATION_LOG.md)) |
| U24 | **ตารางคำที่โปรแกรมใช้ *จับคู่* ต้องทำเครื่องหมาย `LOCALISATION: matching data` และห้ามมีการเรียกคำแปลข้างใน** | `check.sh` | ไม่ใช่ "ยังไม่ได้แปล" แต่**แปลแล้วฟีเจอร์หาย** · คำใน `Classifier.vocabulary` ถูกค้นหา*ในตัวเอกสาร* ⇒ ต้องอยู่ในภาษาที่เอกสารเขียน · ตัวแทนข้อความเขียนทับไป 8 คำ (ซ้ำอีกที่ `RoleMemory.vocabulary` และ `appliesMarkers`) ⇒ เอกสารไทยจะหยุดถูกจัดหมวดทันทีที่สลับหน้าจอเป็นอังกฤษ · **เพิ่มภาษาได้ สลับออกไม่ได้** ([E.57](docs/VERIFICATION_LOG.md)) |
| U22 | **กฎที่ตรวจ "call site ที่แปลแล้ว" ต้องรู้จักชื่อ helper ทุกสะกด** | `check.sh` | `Analysis` เปลี่ยนชื่อ helper เป็น `localised` เพราะ `let t` (ค่า t) บังหน้า ⇒ กฎ U20 ที่มองหาแค่ `t(` **เลิกตรวจทั้งโมดูลโดยไม่แดง** · กฎที่ผูกกับชื่อของสิ่งที่มันตรวจ จะเงียบตอนสิ่งนั้นเปลี่ยนชื่อ ([E.56](docs/VERIFICATION_LOG.md)) |
| U23 | **ตัวแทนข้อความห้ามแตะ literal ที่อยู่ในซอร์สภาษาอื่นที่ฝังไว้** | `check.sh` (U20 จับให้) | สคริปต์ Python ใน raw string ถูกเขียนทับเป็นการเรียก Swift ⇒ เคอร์เนลพังทั้งตัว · ข้อความที่ต้องแปลในซอร์สฝังตัว ให้เจาะช่องแล้วแทนด้วย **JSON-encoded string** (เป็น Python literal ที่ถูกต้องอยู่แล้ว) ([E.56](docs/VERIFICATION_LOG.md)) |
| U21 | **key ห้ามซ้ำในไฟล์แคตตาล็อกเดียวกัน** | `check.sh` | `.strings` เอาตัวหลังเงียบ ๆ ⇒ สองคำแปลของ key เดียวดูเหมือนแคตตาล็อกที่ทำงานได้ ขณะที่ครึ่งหนึ่งของ call site ได้คำแปลที่ไม่มีใครเลือก — สะสม 6 คู่ก่อนถูกจับ ([E.54](docs/VERIFICATION_LOG.md)) |
| U20 | **ทุก call site ที่เรียก `t()` ต้องมี key ที่คอมไพเลอร์ emit ออกมาได้** | `check.sh` | key ที่ไม่คงที่ (เช่นแทรก `any Error`) คอมไพล์ผ่าน แสดงภาษาอังกฤษถูกต้อง และ**แปลไม่ได้ตลอดไป** · กฎเดิมมองไม่เห็นเพราะมันถามแค่ key ที่คอมไพเลอร์รายงาน — เจอ 17 จุดตอนวัด ([E.53](docs/VERIFICATION_LOG.md)) |
| U19 | **ชนิดที่เป็นรากของ `Scene` ห้ามเป็น `private`** | ยังไม่มีกฎเครื่อง — ดู [§9](#9-กฎที่ยังบังคับไม่ได้) | ชื่อ mangled ของชนิด `private` มี**ที่อยู่หน่วยความจำ**อยู่ข้างใน ⇒ identifier ของ state restoration เปลี่ยนทุกครั้งที่เปิด ⇒ macOS กู้ไม่เจอ คืน `window=0x0` **โดยไม่มี error** แล้วไม่เปิดหน้าต่างใหม่ ⇒ **แอปขึ้นมาโดยไม่มีอะไรเลย** ([E.49](docs/VERIFICATION_LOG.md)) |

---

## 7. โมเดลและ tier

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| L1 | **เพดานบริบทอ่านจากเซิร์ฟเวอร์ ห้ามเขียนตายในโค้ด** | `check.sh` (2 กฎ) | เคยอ่าน `32_768` ให้ทุก endpoint ⇒ เพิ่ม `--max-model-len` แล้วไม่มีอะไรเปลี่ยน และลดแล้วแอป overflow หน้าต่างที่มันเชื่อว่าใหญ่กว่า |
| L2 | **ชื่อโมเดลมาจาก `/v1/models`** — คอนฟิกที่ปักชื่อไว้จะพังเงียบเมื่อสลับ checkpoint | `EndpointProbe` + เทส | เซิร์ฟเวอร์รับคำขอสำหรับโมเดลที่มันไม่มี แล้วพลาดจะโผล่ทีหลังในรูปที่อธิบายไม่ได้ |
| L3 | **ความคิดของโมเดลไม่ใช่คำตอบ** — ห้าม `text +=` ใน `reasoningDelta` | `check.sh` (2 กฎ) | พอรวมกับ `text` มันจะถูกบันทึกเป็นคำตอบ และถูกส่งเข้า structured-output parser |
| L4 | **มีที่เดียวที่รู้ว่า `<think>` หน้าตายังไง** — `ReasoningSplitter` | `check.sh` | ตัวแยกสองชุดจะ drift และผลคือบทพูดกับตัวเองภาษาอังกฤษบนหน้าแชทภาษาไทย |
| L5 | **การเลือกโมเดลต่อเทิร์นคือ *ลำดับ* ไม่ใช่ *ตัวกรอง*** — เลือกแล้วไม่ตัดตัวอื่นทิ้ง · ไม่ปลดล็อกความสามารถ · ไม่ปลดล็อกการใช้เงิน · ชื่อที่ไม่ตรงอะไรเลยไม่ใช่ error | เทส 5 ข้อ | endpoint ที่เลือกไว้ล่มกลางคำถาม ต้องแปลว่าตอบช้าลง ไม่ใช่แอปตาย |
| L6 | **การรันที่หยุดเพราะชนเพดานโทเคน ต้องบอก** | `check.sh` | การรันที่คืนของน้อยลงเงียบ ๆ หน้าตาเหมือนการรันที่เจอของน้อย |
| L7 | **สถานะ Busy อ่านจาก `/metrics` ของเซิร์ฟเวอร์ ไม่ใช่จากคำกล่าวอ้างของ agent** | ยังไม่มีตัวบังคับ *(ดู [§9](#9-กฎที่ยังบังคับไม่ได้))* | agent ที่รายงานตัวเองคือ agent ที่รายงานผิดได้ |
| L8 | **สาย executor เปลี่ยนได้ขณะรัน** — `executors` เป็น `var` · มี `replaceExecutors` · การสลับสายล้าง availability cache | `check.sh` (3 กฎ) | เป็น `let` มาหลายเดือน ⇒ endpoint ที่เพิ่มบนหน้าจอไม่มีอยู่จริงจนกว่าจะรีสตาร์ท และไม่มีอะไรบอก (F-1) |
| L9 | **บันทึก endpoint บนหน้าจอ ต้องสร้าง chain ใหม่ ไม่ใช่แค่เขียนไฟล์** | `check.sh` | เขียนไฟล์คือครึ่งที่ทำงานมาตลอด · เรียก router คือครึ่งที่ไม่เคยมี |
| L10 | **executor สร้างที่เดียว** — `EndpointExecutors` เท่านั้น | `check.sh` | สองจุดสร้างจะ drift และฟิลด์แรกที่ drift คือเพดานบริบท ซึ่ง §17.1 บังคับว่าต้องมาจากเซิร์ฟเวอร์ |

---

## 8. เทส · การทดสอบ end-to-end · เอกสาร

### 8.1 การทดสอบ (M7 · M8 ในรูปกฎที่ตรวจได้)

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| E1 | **ทุกความสามารถต้องมีเทส** — ไม่มีข้อยกเว้นสำหรับ "โค้ดนิดเดียว" | รีวิว + `check.sh` §3 | ทุกกฎในหมวด [§3](#3-สิ่งที่สร้างแล้วต้องต่อถึงคน) เกิดจากของที่คนคิดว่าเล็กพอที่จะไม่ต้องตรวจ |
| E2 | **ทุก task ที่แตะเส้นทางของผู้ใช้ ต้องมีเทสที่วิ่งครบวง** — จากจุดที่ผู้ใช้เริ่ม ถึงผลที่ผู้ใช้เห็น ผ่านชั้นจริงทุกชั้น ไม่ mock ตรงกลาง | รีวิว · [`TEST_PROTOCOL §5`](docs/TEST_PROTOCOL.md#5-l4-end-to-end--scenario-ที่ต้องผ่าน) | **เทสหน่วยพิสูจน์ว่าชิ้นส่วนถูก ไม่ได้พิสูจน์ว่าชิ้นส่วนยังต่อกันอยู่** — และการต่อกันคือสิ่งที่พังทุกครั้งที่แก้อย่างอื่น |
| E3 | **แก้บั๊กแล้ว ต้องเพิ่มเทสที่จะแดงถ้าบั๊กนั้นกลับมา** ก่อนแก้ | รีวิว | ไม่งั้นการแก้คือความเชื่อ ไม่ใช่หลักฐาน |
| E4 | **ที่นี่ขับหน้าจอเองได้ — ห้ามเลื่อนงานด้วยเหตุผลว่าทดสอบไม่ได้** | [`TEST_PROTOCOL §4`](docs/TEST_PROTOCOL.md#4-l3-ขับหน้าจอ) | สิทธิ์ Accessibility เปิดแล้ว · `ScreenDriver` กดและพิมพ์ได้จริง · ของค้างที่ยังอ้างเหตุผลนี้อยู่ ต้องถูกขับ |
| E5 | **เทสห้ามเดาพอร์ตแล้ว bind** — ใช้ port 0 แล้วถามระบบว่าได้อะไร | `check.sh` | `UInt16.random(in:)` ชนบ่อยพอที่จะทำให้หนึ่งในสามรอบแดงขณะที่ผ่านตอนรันเดี่ยว = แดงที่คนเรียนรู้จะรันใหม่แทนที่จะอ่าน |
| E6 | **เทสฐานข้อมูลทุกตัวมีพอร์ตของตัวเอง** | `check.sh` | เซิร์ฟเวอร์สองตัวบนพอร์ตเดียวไม่ชนกันดัง ๆ — client ตัวที่สองต่อเข้าข้อมูลของตัวแรก |
| E7 | **เทสที่แดงแล้วรันใหม่ผ่าน = ยังไม่ผ่าน** | [`TEST_PROTOCOL §8`](docs/TEST_PROTOCOL.md#8-การจัดการเทสที่ไม่เสถียร-flaky) | การรันซ้ำจนเขียวคือการซ่อนตัวแปรที่ทำให้มันแดง — ที่นี่มักคือ RAM |
| E8 | **ห้ามปิดเทสที่ flaky** — แก้ให้ไม่ขึ้นกับเวลาแทน | [`TEST_PROTOCOL §8`](docs/TEST_PROTOCOL.md#8-การจัดการเทสที่ไม่เสถียร-flaky) | เทสที่ปิดคือเทสที่หายไป และปัญหาที่มันจับได้จะกลับมาเงียบ ๆ |
| E9 | **สิ่งที่ทดสอบจริงไม่ได้ ต้องเขียนว่าทดสอบไม่ได้ และเพราะอะไร** — หรือทำ mock server แทน ห้ามเงียบ | [`TEST_PROTOCOL §0`](docs/TEST_PROTOCOL.md#0-สี่คำที่ห้ามใช้ปนกัน) | ไม่มี paid LLM endpoint บนเครื่องนี้ ⇒ เส้นทาง Tier 2 ทดสอบกับของจริงไม่ได้ · ปล่อยเงียบแล้วมันจะอ่านเหมือนผ่าน |

### 8.2 เอกสาร

| # | กฎ | บังคับด้วย | ทำไม |
|---|---|---|---|
| E10 | **ไฟล์เอกสารห้ามเกิน 200 KB** — โตกว่านั้นให้แยกเป็นโฟลเดอร์ | `check.sh` | แผนเคยโต 555 KB โดยมีเซลล์สถานะเดียวยาว 5,254 ตัวอักษร ในตารางที่มีหน้าที่ให้กวาดตา |
| E11 | **ลิงก์ระหว่างเอกสารต้องชี้ถึงจริง รวม anchor** | `check.sh` | ลิงก์เสียในชุดเอกสารขนาดนี้ ไม่ใช่ความไม่สะดวก แต่คือวิธีที่คนไปอ่านไฟล์ผิดแล้วลงมือตามนั้น |
| E13 | **ด่านที่รันโปรแกรมภายนอก ต้องตัดสินจาก exit code ไม่ใช่จากการ grep ข้อความ** | รีวิว | ด่าน Tier 0.5 เคยเขียน `if swift run MLXCheck \| tail -16; then` ซึ่งทดสอบ `tail` **จึงไม่เคยแดงได้เลย** · พอแก้เป็น grep หา `✗` ก็แย่กว่า เพราะโปรแกรมนั้นพิมพ์ ✗ กำกับโมเดลที่ใหญ่เกิน RAM ว่างตอนนั้น ⇒ ด่านแดง/เขียวตาม RAM โดยทุกเคสข้างในผ่านหมด (E.40) |
| E14 | **รอบตรวจต้องบอกชื่อสิ่งที่ตก ไม่ใช่แค่จำนวน** | `check.sh` | รอบหนึ่งแดงแล้วรอบถัดไปเขียวโดยไม่มีการแก้โค้ด และสรุปเขียนได้แค่ "ตก ดูข้างบน" ในล็อก 400 บรรทัด |
| E12 | **สคริปต์ทุกตัวใน `scripts/` ต้อง parse ผ่าน** · **ช่องสถานะในแผนต้องเปิดด้วยเครื่องหมายที่จริง** | `check.sh` | `gx10-serve.sh` รันบนอีกเครื่อง syntax error จะนอนรอจนถึงนาทีที่ต้องใช้ · การ mark done ด้วยการ*ต่อท้าย* ทำให้แถวเปิดด้วย "— ยังไม่เริ่ม" |

---

## 9. กฎที่ยังบังคับไม่ได้

**หมวดนี้มีไว้เพื่อไม่ให้ §1–§8 โกหก** — ทุกข้อคือกฎที่เขียนไว้จริงแต่ยังไม่มีเครื่องบังคับ

| # | กฎที่เขียนไว้ | ทำไมยังบังคับไม่ได้ | ผลจริงวันนี้ |
|---|---|---|---|
| X1 | **U3** design token ครอบคลุมระยะทุกแบบ | กฎจับ `.padding(N)` **ไม่จับ `.padding(.ทิศทาง, N)`** | ระยะดิบ **~114 จุด ใน 27 ไฟล์** ใช้ค่า 11 แบบ ขณะที่ `check.sh` บอกว่า "ทุก view ใช้ token" |
| ~~X2~~ | ~~รอบตรวจต้องบอกว่าข้ามอะไร~~ | ✅ **ปิดแล้ว 2026-08-17** — skip สองชั้น · สรุปความครอบคลุมทุกรอบ · ชื่อสิ่งที่ตก | — |
| ~~X3~~ | ~~ค่าที่ตั้งบนหน้าจอ ต้องมีผล~~ | ✅ **ปิดแล้ว 2026-08-17** — endpoint และเพดานบริบทมีผลทันที บังคับด้วยกฎ 3 ข้อ ([L8–L10](#7-โมเดลและ-tier)) | เหลือ `surrealPort` · `modelQuotaGigabytes` ที่ยังไงก็ต้องรีสตาร์ท — `StaleConfigBanner` อยู่ในคิวข้อ 9c |
| X4 | **L7** Busy มาจาก `/metrics` เท่านั้น | หน้าจอที่ใช้มันยังรอ P16 | ตัวอ่านทำแล้ว · ยังไม่มี code path ให้ตรวจ |
| X5 | **ปัญหาเดียวกันใช้ control ตัวเดียวกัน** (หลักการ 5 ของ UX) | "อันนี้เป็นปัญหาเดียวกันไหม" ตอบด้วย grep ไม่ได้ | ยังมี `GroupBox`/`List`/แถวปุ่ม ปนกัน |
| X6 | **รายการว่างต้องต่างจากหน้าโหลดล้มเหลว** | ไม่มี `EmptyState` กลาง จึงไม่มีอะไรให้ grep | แก้ทีละที่ตอนเจอ |
| X7 | **U19** ชนิดที่เป็นรากของ `Scene` ห้ามเป็น `private` | "ชนิดไหนเป็นรากของ scene" ต้องอ่านโครง `body` ของ `App` ซึ่ง grep ตอบไม่ได้ · วันนี้มีรากเดียวคือ `RootView` และมันถูกแก้แล้ว | สิ่งที่จะจับได้จริงคือ**เปิดแอปที่แพ็กแล้วแล้วดูว่ามีหน้าต่างไหม** — ไม่มีเทสใดใน 1,746 ตัวทำแบบนั้น ([X8](#9-กฎที่ยังบังคับไม่ได้)) |
| X8 | **แอปที่แพ็กแล้วต้องเปิดขึ้นมาแล้วมีหน้าต่าง** | ต้องเปิดแอปจริงและอ่านหน้าจอ ⇒ ต้องมีสิทธิ์ Accessibility ซึ่งระบบให้ตัวเองไม่ได้ (§23.2) | **บั๊กสองตัวที่ทำให้แอปที่แพ็กแล้วใช้ไม่ได้เลย ผ่านรอบตรวจทั้งชุด** ([E.49](docs/VERIFICATION_LOG.md)) · ควรเป็น probe แบบเดียวกับ `ScreenCheck` ใน `--full` |

**การมีตารางนี้ ไม่ใช่การยอมรับสภาพ** — แต่ละแถวมีงานผูกอยู่ใน [`docs/plan/README.md`](docs/plan/README.md#งานถัดไป) แล้ว

---

## 10. วิธีเพิ่มกฎใหม่

**กฎใหม่เกิดจากความล้มเหลวจริง ไม่ใช่จากการนึกออก** — ทุกกฎใน §2–§8 ตามหลังเหตุการณ์ที่เกิดขึ้นแล้ว

| ขั้น | ทำอะไร |
|---|---|
| 1 | **เขียนอาการก่อน** ลง [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md) หรือ [`docs/VERIFICATION_LOG.md`](docs/VERIFICATION_LOG.md) — อาการคือสิ่งที่ทำให้กฎเถียงได้ |
| 2 | **ถามว่า "อะไรที่ถ้าเปลี่ยนกลับ อาการจะกลับมา"** — นั่นคือสิ่งที่กฎต้องจับ ไม่ใช่ตัวอาการ |
| 3 | **เขียนกฎให้ fail ได้จริง** แล้ว**พิสูจน์ว่ามัน fail** โดยย้อนโค้ดชั่วคราว — กฎที่ไม่เคยเห็นตัวเองแดง คือกฎที่อาจไม่ทำงาน (เกิดมาแล้ว: กฎรุ่นแรกของ `install_package` grep ทั้งไฟล์และผ่านสบายตอนรายการถูกล้าง) |
| 4 | **เขียนคอมเมนต์ว่าทำไม** ไว้เหนือกฎใน `check.sh` — กฎที่ไม่มีเหตุผลกำกับ คือกฎที่คนถัดไปจะลบเมื่อมันขวาง |
| 5 | **เพิ่มแถวในไฟล์นี้** พร้อมคอลัมน์ "ทำไม" |

### สามอย่างที่ทำให้กฎกลายเป็นภาระ — หลีกเลี่ยง

| อย่า | เพราะ | เคยเกิด |
|---|---|---|
| อย่าให้กฎ fail บนข้อความอธิบาย | คนจะเลิกเขียนคำอธิบาย | กฎ `print()` เคยแดงใส่คอมเมนต์ที่อธิบายว่าทำไมห้าม `print()` |
| อย่าให้กฎ fail บน*ชื่อ* | คนจะเรียนรู้ที่จะเลี่ยงชื่อ | `InstrumentFootprint(` ลงท้ายเหมือน `print(` |
| อย่าเขียนกฎที่ grep ทั้งไฟล์ ถ้าสิ่งที่ต้องตรวจอยู่บรรทัดเดียว | มันจะผ่านทั้งที่ของหายไปแล้ว | รายการ "ถามเสมอ" ถูกล้างจนว่าง และกฎยังเขียว |

---

## เอกสารที่เกี่ยวข้อง

| ถ้าอยากรู้ | อ่านที่ |
|---|---|
| **ไม่รู้จะเริ่มตรงไหน** | [`START_HERE.md`](START_HERE.md) |
| ระบบคืออะไร ทำไมถึงเป็นแบบนั้น | [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| อะไรเรียกว่าผ่าน | [`docs/TEST_PROTOCOL.md`](docs/TEST_PROTOCOL.md) |
| หน้าตาและการใช้งาน | [`docs/UX_UI_DESIGN.md`](docs/UX_UI_DESIGN.md) |
| สร้างถึงไหนแล้ว | [`docs/plan/README.md`](docs/plan/README.md) |
| เรื่องที่ตัดสินไปแล้ว อย่าเสนอซ้ำ | [`docs/DECISIONS.md`](docs/DECISIONS.md) |
