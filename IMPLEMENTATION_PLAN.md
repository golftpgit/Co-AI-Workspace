# Co-AI Workspace — Implementation Plan

> คู่กับ [`ARCHITECTURE.md`](ARCHITECTURE.md) (spec) — เอกสารนี้ตอบว่า **สร้างอะไรก่อนหลัง และรู้ได้ยังไงว่าเสร็จจริง**
>
> **กติกาของเอกสารนี้** (เรียนจากความผิดพลาดของ v1):
> - ทุก Task มี **Done-when** ที่เป็นหลักฐานตรวจได้ ไม่ใช่ "เขียนโค้ดเสร็จ" — ห้าม mark DONE ด้วยความรู้สึก
> - **"มี implementation" ≠ "มี feature"** — v1 เคยมี MCP client ครบแต่ไม่เคยถูกต่อเข้า tool list จริง (D6) ทุก Task ที่สร้างความสามารถให้ agent ต้องมี **integration test ที่พิสูจน์ว่ามันถูกเรียกใช้จริงใน path ของผู้ใช้**
> - **เทสเขียวไม่เท่ากับใช้งานได้** — Task ที่มีหน้าจอต้องถูก *กดด้วยมือ* บน `.app` ที่ build + sandbox แล้ว บันทึกอยู่ที่ [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md)
> - ช่อง "ค้าง" ในทุกแถวบอกตรง ๆ ว่าอะไรยังไม่จริง — แถวที่ไม่มีช่องนี้แปลว่าไม่มีอะไรค้าง ไม่ใช่ว่าลืมเขียน

## สารบัญ

| ส่วน | เนื้อหา |
|---|---|
| [สถานะปัจจุบัน](#สถานะปัจจุบัน) | **ตารางเดียวที่ตอบว่าไปถึงไหนแล้ว** — P0–P21 |
| [หลักการเรียงงาน](#หลักการเรียงงาน) | ทำไมเรียงแบบนี้ |
| [ค้างไว้ / ยังไม่ได้พิสูจน์](#ค้างไว้--ยังไม่ได้พิสูจน์) | รายการที่ยังเปิดอยู่จริง (6 ข้อ) |
| [Phase P0–P21](#phase-p0p21) | Phase → Task → Done-when → สถานะ · **P15–P21 เพิ่ม 2026-08-15 ยังไม่เริ่มทั้งหมด** |
| [Risk Register](#risk-register) | ความเสี่ยง 18 ข้อ พร้อม **สถานะจริง** — ปิดแล้ว 6 · คุมแล้ว 10 · **เปิดโดยยังไม่มีอะไรคุม 2 ข้อ (R15 · R16) ซึ่งเป็นความเสี่ยงที่ P16/P17 สร้างขึ้นเอง และต้องคุมพร้อมกับที่สร้าง** |
| [งานถัดไป](#งานถัดไป) | คิวถัดไป |

**เอกสารที่แยกออกไป**: [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md) (บันทึกการขับหน้าจอ + รายการค้างที่ปิดแล้ว) · [`docs/LEGACY_V1.md`](docs/LEGACY_V1.md) (completeness checklist เทียบ v1) · [`docs/VERIFICATION_LOG.md`](docs/VERIFICATION_LOG.md) (ผลที่วัดจริง)

---

## สถานะปัจจุบัน

**อัปเดต 2026-08-16** · **1,659 เทสผ่าน · `scripts/check.sh` เขียวทั้งหมด** · `.app` build + sandbox + launch ได้ sidecar ขึ้นและถูกเก็บตอน quit

### ไปถึงไหนแล้ว — ต่อ Phase

`✅` = ผ่าน Done-when ครบ · `🔶` = ใช้งานได้แต่มีของค้างที่เขียนไว้ในแถว · `—` = ยังไม่เริ่ม

| Phase | ขอบเขต | Task | สถานะ |
|---|---|---|---|
| [P0](#p0--scaffold) | Scaffold — build/test/run/sandbox | 5 | **✅ ครบ 5/5** |
| [P1](#p1--walking-skeleton) | Walking skeleton — UI→router→tool→hook→approval→span→DB | 10 | **✅ ครบ 10/10** |
| [P2](#p2--knowledge) | Knowledge — ingestion · ไทย · hybrid search · policy gate | 8 | ✅ **ครบ 8/8** — graph view ของ entity/relation ทำแล้ว 2026-08-15 (ของค้างที่เก่าที่สุดในแผน) · ค้าง: แก้ความสัมพันธ์จากหน้ากราฟโดยตรง |
| [P3](#p3--web--conflict) | Web & Conflict — tier registry · `fetch_page` · conflict ledger | 7 | 🔶 4✅ · 3 มีของค้าง · P3.1 ถูกแทนที่ด้วย P13.1 · ประวัติที่กลับคำตัดสินได้ + หัวการ์ด (U12) ทำแล้ว 2026-08-16 |
| [P4](#p4--ai-team) | AI Team — specialist actors · orchestrator · QA loop · context manager | 9 | 🔶 5✅ · 4 มีของค้าง · ค้าง: manual mode บางแถว (P4.6) · การกดสวิตช์ P4.8 บนหน้าจอจริงกับงานค้างจริง (เพดาน token ทำแล้ว 2026-08-16) |
| [P5](#p5--llm-tiers-เต็มรูปแบบ) | LLM tiers — MLX local · model manager · admission control · budget | 7 | 🔶 4✅ · 3 มีของค้าง · ค้าง: ขับหน้าจอบางจุด · ยังไม่มี paid endpoint จริงให้ทดสอบงบ |
| [P6](#p6--analysis) | Analysis — DuckDB · connectors · notebook · statistical gate | 8 | 🔶 3✅ · 5 มีของค้าง · ค้าง: PG/MySQL ยังไม่ยืนยันกับ server จริง · survival analysis |
| [P7](#p7--channels--docgen) | Channels & DocGen — Telegram/Discord/LINE/Siri · docx/pptx · citation | 9 | 🔶 6✅ · 3 มีของค้าง · ค้าง: ยังไม่ได้ยิงกับแพลตฟอร์มจริง (ต้องมีโทเคนของผู้ใช้) |
| [P8](#p8--roster--ui-ครบ) | Roster — manifest · MCP · plugin · `write_skill` · accessibility | 7 | 🔶 5✅ · 1 ถูกกลืนเข้า P10.12 · 1 มีของค้าง · **File Viewer กับ Workflow Builder ทำแล้ว (2026-08-15)** · ค้าง: VoiceOver จริง (P8.7) · หน้า Settings ยังไม่ครบทุกหมวด |
| [P9](#p9--hardening--release) | Hardening & Release | 6 | **🔶 3✅ · 1 มีของค้าง · 2 ยังไม่เริ่ม** — P9.2/P9.3/P9.4 ✅ · P9.6 🔶 · **P9.1/P9.5 ยังไม่เริ่ม** |
| [P10](#p10--project-เป็น-first-class--project-management) | Project เป็น first-class + PM ([ARCH §19](ARCHITECTURE.md#19-project-environment--project-management-m14-projectkit)) | 16 | 🔶 11✅ · 5 มีของค้าง · ค้าง: Kanban ลากไม่ได้ · popover บางช่อง (P10.15) — **แกนเวลาจริง + แกนคาดการณ์ ครบแล้ว 2026-08-16** |
| [P11](#p11--research-program--instruments) | Research Program & Instruments ([ARCH §20](ARCHITECTURE.md#20-research-program--งานวิจัยที่เดินบนโครง-pm-m15-instruments)) | 13 | 🔶 **11✅ · 2 มีของค้าง · ขับบนหน้าจอแล้ว 2 ใน 3** — ประกอบต้นฉบับกับส่งบทถอดเทปเข้าคลัง **ขับจริงแล้ว 2026-08-15** (เจอบั๊ก 5 ตัว รวมถึงการตรวจตัวเลขล้าสมัยที่ไม่ทำงานในแอปจริง) · **ค้าง: เดินโปรเจกต์ที่มีคำตอบจริงจนกดปิด** — ต้องผ่าน G1–G4 และตอบ practice 17 ข้อก่อน ซึ่งเป็นกลไกของ P10 |
| [P12](#p12--agent-competence--knowledge-view) | Agent Competence & Knowledge View ([ARCH §21](ARCHITECTURE.md#21-agent-competence-model--อะไรทำให้-agent-แต่ละตัวต่างกัน)) | 8 | ✅ **8/8** — ครบทุก task · เหลือของค้างข้อเดียว: บันทึก view ที่ใช้ลง evidence (P12.6) ซึ่งควรไปอยู่บน span ไม่ใช่ `Evidence` ที่บังคับ pass/fail |
| [P13](#p13--ค้นเว็บชั้น-t5) | ค้นเว็บชั้น T5 — สะพาน WKWebView | 2 | **✅ ครบ 2/2** |
| [P14](#p14--r-ผ่านสะพานภายนอก) | R ผ่านสะพานภายนอก | 4 | ✅ **ครบทั้ง phase (2026-08-16)** — สะพานขับกับ R 4.5.3 จริง (E.28) · `r_eval` เดินประตูเดิม · ผลลง DuckDB · การติดตั้งแพ็กเกจหยุดถามคนเสมอ |

**Phase ที่เพิ่มเมื่อ 2026-08-15** — จากทิศทางที่ผู้ใช้กำหนด ยังไม่เริ่มทั้งหมด **ยกเว้น P21 (3 ใน 4 ทำแล้ว)** และ [ARCH §22–§24](ARCHITECTURE.md#22-ai-organization--จากทีมเดียวเป็นองค์กร-m17-command) คือสเปกของมัน

| Phase | ขอบเขต | Task | ขึ้นกับอะไร |
|---|---|---|---|
| [P15](#p15--gx10-เป็นสมองหลักที่ใช้ได้จริง) | GX10 เป็นสมองหลักที่ใช้ได้จริง | 9 | **ไม่ขึ้นกับอะไร และปลดล็อกที่เหลือ** — งานหลายทีมทดสอบจริงบนโมเดล 3B ไม่ได้ |
| [P21](#p21--project-เป็นแท็บจริง) | Project เป็นแท็บจริง | 4 | ควรทำก่อน P16 — องค์กรหลายทีมจะยิ่งผูกกับ scope เดียวที่สลับได้ทีละอัน |
| [P17](#p17--machine-control-m18-screendriver) | Machine Control (M18) | 5 | ปลดล็อกของค้าง "ขับไม่ได้" หลายข้อใน P8/P9/P11 |
| [P16](#p16--ai-organization-m17-command) | AI Organization (M17) | 7 | P15 (จำนวนสายขนานจริงคือเพดานจริงของ span of control) · P21 |
| [P18](#p18--knowledge-ที่เป็นหอสมุดจริง) | Knowledge ที่เป็นหอสมุดจริง | 5 | ไม่ขึ้นกับใคร — P18.1/P18.2 แก้อาการที่เจอจริงแล้ว |
| [P19](#p19--สถิติการแพทย์และสาธารณสุขให้ครบ) | สถิติการแพทย์/สาธารณสุข | 8 | ไม่ขึ้นกับใคร · **P19.0 ต้องทำก่อนข้ออื่นในกลุ่ม** |
| [P20](#p20--design-system--hig--liquid-glass) | Design System · HIG · Liquid Glass | 6 | หลัง P16/P17 — สองอันนั้นเปลี่ยนโครงหน้าจอมากที่สุด |

### ฐานรากที่พิสูจน์แล้ว (ไม่ต้องกลับไปตรวจอีก)

| เรื่อง | สถานะ | หลักฐาน |
|---|---|---|
| Guided generation (Tier 0) | ✅ 0.6–0.9 วิ | [E.6](docs/VERIFICATION_LOG.md#e6-d-7-spike--guided-generation-ใน-app-target-จริง) |
| SurrealClient (KB) | ✅ ครบ BM25/HNSW/graph/hybrid | [E.8](docs/VERIFICATION_LOG.md#e8-surrealclient-spike--เขียน-client-เอง-กับ-surrealdb-v320) · [`spikes/SurrealClient/`](spikes/SurrealClient/) |
| VLLMExecutor (Tier 1) | ✅ ครบ streaming/tool/schema/cancel | [E.9](docs/VERIFICATION_LOG.md#e9-vllmexecutor-spike--tier-1-ผ่าน-openai-compatible-endpoint) · [`spikes/LLMExecutor/`](spikes/LLMExecutor/) |
| Embedding model | ✅ **`bge-m3` @ 1024 มิติ** — ตัวเดียวที่ทำ cross-lingual ไทย↔อังกฤษได้ | [E.10](docs/VERIFICATION_LOG.md#e10-d-2--เลือก-embedding-model-วัดจริง-ปิดแล้ว) · [`spikes/EmbeddingEval/`](spikes/EmbeddingEval/) |
| Tier 0.5 เป็นพื้นรับประกัน | ✅ ปิด endpoint แล้วแอปยังตอบได้จากโมเดลบนเครื่อง | P5.4 · [E.17](docs/VERIFICATION_LOG.md) |
| สถิติที่เขียนเอง (α/ω/ICC/κ/EFA) | ✅ ตรวจกับค่าที่ตีพิมพ์ + implementation อิสระ | P11.3 · [driving log 2026-08-14](docs/DRIVING_LOG.md) |

### ข้อจำกัดที่ทราบและยอมรับไว้

| เรื่อง | สถานะ | ผลต่อแผน |
|---|---|---|
| Guardrail ปฏิเสธงานการแพทย์ ~12.5% | ⚠️ มีแผนรับมือ (escalate อัตโนมัติ) | [E.7](docs/VERIFICATION_LOG.md#e7-guardrail-characterization--โดเมนการแพทย์) · R1 |
| Thai tokenization | ⚠️ ใช้ได้ · ต้องมี dictionary merge layer | [E.3](docs/VERIFICATION_LOG.md#e3-thai-tokenizer--รันจริงกับประโยคงานวิจัยการแพทย์) · R4 |
| macOS 27 API (custom LLM provider) | ⏳ รอ ก.ย. 2026 — มี abstraction คั่นแล้ว | [ARCH §9.1](ARCHITECTURE.md#91-llm-abstraction-ของเราเอง-รองรับทั้งสองยุค) · R2 |
| แพ็กเกจ Python ภายนอกในแอปที่ sandbox | 🔴 `/opt/homebrew` · `/usr/local` มองไม่เห็นเลย (วัดแล้ว) | notebook ได้ Python 3.9 เปล่า · SearXNG แพ็กเข้า `.app` ไม่ได้ → P9.6 |
| Notarization | 🔴 ทำแทนไม่ได้ ต้องมี Developer ID ของเจ้าของแอป | ตอนนี้เซ็น ad-hoc · Gatekeeper เครื่องอื่นปฏิเสธ |

**สภาพแวดล้อมที่ยืนยันแล้วบนเครื่อง**: macOS 26.6.1 · Swift 6.3.3 · Xcode 26.6 · Apple Intelligence `available` · LM Studio :1234 (Llama 3.1 8B + nomic-embed) · SurrealDB v3.2.0 binary

---

## หลักการเรียงงาน

## หลักการเรียงงาน

1. **Walking skeleton ก่อน breadth** — P1 สร้างเส้นทางบางที่สุดที่วิ่งครบตั้งแต่ UI → Router → Tool → Hook chain → Approval → Span → DB **ให้ใช้งานได้จริงตั้งแต่สัปดาห์แรกๆ** แล้วค่อยขยายทีละ module ไม่ใช่สร้างทุก module ให้เสร็จแล้วค่อยต่อกันตอนท้าย (v1 เจอปัญหานี้: workflow execution ไม่เคยผ่าน gate เลยจนมาเจอตอน Phase F)
2. **อะไรที่ spike แล้วเอาโค้ดมาใช้เลย** — [`spikes/`](spikes/) มีโค้ดที่รันผ่านจริงแล้ว 6 ชิ้น ไม่ต้องเริ่มจากศูนย์ (`SurrealClient` · `LLMExecutor` · `EmbeddingEval` · `EmbeddingRuntime` · `EFAReference` · `ScreenDriver`)
3. **Invariant ก่อน feature** — hook chain, approval broker, span store ต้องมาก่อนสิ่งที่ต้องใช้มัน ไม่ใช่ตามมาแก้ทีหลัง (v1 bug B2: Telegram bridge ข้าม hook chain เพราะ bridge มาก่อน invariant)
4. **ทุก Phase จบด้วยสิ่งที่ใช้งานได้** ไม่ใช่ half-built layer


---

## ค้างไว้ / ยังไม่ได้พิสูจน์

รายการที่ **โค้ดลงแล้วแต่ยังไม่ได้พิสูจน์** หรือ **ยังไม่ได้ทำ** — ห้าม mark task ที่มีรายการค้างอยู่เป็น ✅
รายการที่ปิดไปแล้ว (U1–U20 อีก 14 ข้อ) ย้ายไป [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md#รายการค้างที่ปิดไปแล้ว-u1u20) พร้อมเหตุผลที่มันเคยเปิดอยู่

| # | เรื่อง | สถานะจริง | ต้องทำอะไรถึงจะปิดได้ |
|---|---|---|---|
| **U4** | **Graph view ของ entity/relation** | ยังไม่ได้ทำเลย | UI แสดง entity + relation แก้/ลบได้ (P2.7) |
| **U6** | `EmbeddingRuntime` ทดสอบผ่าน `swift test` ไม่ได้ | MLX หา metallib ผ่าน main bundle ซึ่งตอนรันเทสคือ helper ของ SwiftPM — ย้ายไปตรวจใน `Sources/EmbeddingCheck` ที่ `check.sh` เรียกแทน | ยอมรับข้อจำกัดนี้ หรือหาวิธี host bundle ให้ xctest เห็น |
| **U8** | KB โหลดทั้ง scope เข้า memory | พอสำหรับคลังส่วนตัว แต่ไม่ได้วัดว่าที่กี่หมื่น chunk จะเริ่มมีปัญหา | วัดจริงเมื่อมีข้อมูลจริงพอ แล้วค่อยตัดสินใจเรื่อง paging |
| **U12** | **คุณภาพที่ยังไม่ดีพอ (ไม่ใช่บั๊ก แต่ยังไม่ควรปิด)** | 🔶 **entity ไทยได้ clause ปนมา** — ช่องว่างถูกอุดด้วย subject/object จาก `RelationExtractor` เอกสารไทยจาก 0 เป็น 8 entity ได้ของจริงอย่าง `โรงพยาบาลสระบุรี` แต่ก็ติด `ให้ต่ออีก 24 ชั่วโมง` มาด้วย · **`finding.question` คืนประโยคคำสั่งจาก prompt** แทนคำถามที่ขัดกัน หัวการ์ดเลยอ่านว่า "เปรียบเทียบข้อความสองชิ้นว่า ขัดแย้งกันหรือไม่" · **เทส `ProcessRegistryTests` flaky สองตัว** ตอนรันทั้งชุด (ผ่านตอนรันเดี่ยว): "output streams…" (assert `chunks.count >= 2`) และ "pause suspends the process…" (assert ว่าโปรเซสที่ถูก pause ยังไม่จบ) — ทั้งคู่ขึ้นกับ wall-clock และล้มเฉพาะตอนเครื่องมีงานหนักอย่างอื่น เช่นตอนรันโมเดล MLX ไปด้วย | entity: ต้องมี prompt สกัด entity แยก = model call เพิ่ม — เป็นการตัดสินใจเชิงออกแบบ · question: บีบ schema หรือปฏิเสธคำตอบที่ซ้ำ prompt · เทส: เขียนใหม่ให้ไม่ขึ้นกับเวลา |
| **U17** | **โปรเซส `surreal` ค้างเมื่อเทส/แอปถูกฆ่าด้วย SIGTERM** *(เจอตอน P5.3, 2026-08-12)* | เทสที่จบเองไม่ทิ้งอะไรไว้ (วัดแล้ว: ก่อนรัน 0 หลังรัน 0) แต่รันที่**ถูกฆ่ากลางคัน** (ครั้งนี้คือ MLX crash ใน `swift test`) ทิ้ง sidecar ไว้ 11 ตัว ตัวละ ~200 MB ค้างอยู่หลายชั่วโมง — ซึ่งบน 16 GB คือสาเหตุที่ P5.3 รายงานว่าไม่มี RAM ให้โมเดล · `applicationShouldTerminate` ครอบเฉพาะการปิดแอปตามปกติ ไม่ครอบ SIGTERM/SIGKILL | ล้างด้วย `pkill -f 'vendor/helpers/surreal'` · ทางแก้จริงคือให้ sidecar ตายตามพ่อ (process group + `PR_SET_PDEATHSIG` ไม่มีบน macOS → ต้องใช้ watchdog หรือให้ลูกเช็ค parent pid) — ยังไม่ได้ทำ |
| **U18** | **หน้าตาที่ยังไม่ได้ทำ — จาก Bionic/LM Studio** *(ดูของจริงตอน P5.4, 2026-08-12)* | สามอย่างที่ Bionic ทำแล้วเรายังไม่มี: (1) **ตัวเลือกโมเดลอยู่ในช่องพิมพ์** ไม่ใช่ในหน้า settings — โมเดลเป็นการตัดสินใจต่อเทิร์น (2) **`Context: 5.8K` ค้างอยู่ท้ายจอตลอด** — ของเราแสดงตอนย่อครั้งเดียวแล้วหาย (3) **`Worked for 12s ›` พับ reasoning ไว้** กดแล้วค่อยกาง — ตอนนี้ของเราสตรีม reasoning ทิ้งไปเฉยๆ · จาก LM Studio: **ปุ่ม eject/สถานะ "โหลดอยู่ในหน่วยความจำ"** ต่อโมเดล ซึ่งตรงกับที่ §9.4 ต้องการอยู่แล้ว (คืน RAM เมื่อไม่ใช้) | 🔶 **ทำแล้วสามในสี่ (2026-08-12)** — (1) **ตัวเลือกโมเดลอยู่ในช่องพิมพ์แล้ว** เห็นชื่อโมเดล Tier 0.5 และสลับได้โดยไม่ออกจากหน้าแชท (ยืนยันบนหน้าจอ: `Qwen3-VL-4B-Instruct-4bit ⌄`) (2) **มิเตอร์บริบทค้างอยู่ท้ายช่องพิมพ์** — `TurnEvent.usage` ใหม่รายงานทุกครั้งที่โมเดลตอบ ไม่ใช่โผล่ครั้งเดียวตอนย่อ (เปลี่ยนเป็นสีส้มที่ 75% ซึ่งคือจุดที่ §5.6 เริ่มย่อ) (3) **ปุ่มปลดจากหน่วยความจำ** โผล่เมื่อโมเดลโหลดอยู่ — คืน RAM โดยไม่ต้องปิดอะไร · **(4) `คิดอยู่ N วินาที ›` ทำแล้ว (2026-08-16, 2 เทส + 2 กฎ)** — runner เคย**ทิ้ง `reasoningDelta` ทุกตัว** พร้อมคอมเมนต์ว่ายังไม่มีที่แสดง ตอนนี้สะสมแล้วส่งเป็น event ของตัวเอง การ์ดพับไว้เหนือคำตอบและกางได้ · **กฎที่ทำให้การทิ้งมันเคยปลอดภัย ต้องอยู่ต่อ**: ความคิดไม่ใช่คำตอบ — พอมันไปรวมกับ `text` เมื่อไหร่ มันจะถูกบันทึกเป็นคำตอบและถูกส่งเข้า structured-output parser (เทสยืนยันว่าข้อความที่เก็บคือคำตอบล้วน · check.sh fail ถ้ามี `text +=` ในเคสนั้น) · ไม่นับเป็นเทิร์นในบทสรุป และไม่ถูกบันทึกลงบทสนทนา — การ์ดบอกไว้ตรงนั้น (ตั้งใจ ตาม §14.2 ว่าเป็นงานของ Live Monitor) · ยังไม่ได้เห็นมิเตอร์กับปุ่ม eject บนหน้าจอ (ต้องรันเทิร์นก่อน มิเตอร์ถึงมีตัวเลข) |

---

## Phase P0–P21

## P0 — Scaffold

**สถานะ: ✅ ครบ 5/5 (2026-08-10)**

**เป้าหมาย**: โครงที่ build/test/run ได้ ก่อนเขียน logic ใดๆ

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P0.1** SwiftPM package + targets | `AgentKit` / `Config` / `Observability` / `Sidecar` / `CoAIWorkspaceApp` + test target ครบ ([ARCH §3](ARCHITECTURE.md#3-system-hierarchy) · [§4](ARCHITECTURE.md#4-module-catalog--ภาพรวมทั้งระบบ)) | `swift build` + app เปิดหน้าต่างได้ | ✅ build ผ่าน, แอปเปิดเป็น Foreground app จริง (`lsappinfo` ยืนยัน) |
| **P0.2** Swift Testing + check script | `scripts/check.sh` = build + test + structural rules | รัน 1 คำสั่งแล้วได้ผลสรุป pass/fail | ✅ **30 tests / 7 suites ผ่านหมด** + structural rule 3 ข้อ (กัน `Scope` ซ้ำ, `print()` ใน library, `[String: Any]` บน Sendable) |
| **P0.3** Bootstrap config | `bootstrap.plist` + `AppPaths` สร้าง directory เอง + validate + ซ่อมไฟล์เสีย | ลบ dir แล้วเปิดแอปใหม่ → สร้างเองได้ ไม่ crash | ✅ ทดสอบกับแอปจริง: ลบทั้งโฟลเดอร์ → เปิดใหม่ → สร้างครบ 8 รายการ; เปิดซ้ำ → **ไม่เขียนทับ** |
| **P0.4** Sidecar manager | spawn/monitor/restart/terminate + health probe + **reap orphan จาก pid file** | kill จากภายนอก → restart ภายใน 5 วิ; ปิดแอป → ไม่มี process ค้าง | ✅ test พิสูจน์ครบ: restart **0.36 วิ**, `stopAll` ไม่เหลือ process, restart มีเพดานไม่วนไม่จำกัด, orphan ถูกเก็บกวาด |
| **P0.5** App Sandbox + entitlements | เปิด sandbox ตั้งแต่ต้น + `build-app.sh` ประกอบ+เซ็น bundle | แอปรันใน sandbox ได้ + เข้าถึง data dir/network ได้ | ✅ `lsappinfo` แสดง **sandboxed**, container ถูกสร้าง, `codesign --verify` ผ่าน |

**สิ่งที่ได้จริง**: `swift build && ./scripts/check.sh` ผ่านทั้งหมด · `./scripts/build-app.sh` ได้ `.app` ที่เซ็นแล้วเปิดใช้ได้จริง · แอปแสดงหน้า Boot Status (config, paths, sidecar health) ซึ่งจะถูกแทนที่ด้วย Chat view ใน P1.10

---

## P1 — Walking Skeleton

**เป้าหมาย**: คุยกับ agent ได้จริง 1 เส้นทาง ครบทุก invariant — นี่คือกระดูกสันหลังที่ทุก phase ต่อยอด

**ความคืบหน้า**: **P1 ครบทั้ง 10 task ✅ (2026-08-10)** — **139 tests ผ่านหมด** โดย Persistence ทดสอบกับ SurrealDB จริง · LLM ทดสอบกับโมเดลจริงทั้ง on-device และ endpoint · Execution ทดสอบกับ process/สัญญาณ/seatbelt ของจริง · และมี **end-to-end test หนึ่งตัวที่วิ่งครบเส้นทาง** user → DB → router → tool call → hook chain → approval broker → `run_shell` จริง → span ([`WalkingSkeletonTests`](Tests/ToolBeltTests/WalkingSkeletonTests.swift))

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P1.1** `AgentKit` protocols | `AgentTool`, `Channel`, `Specialist`, `Scope`, `Assignment`, `Deliverable`, `RiskLevel`, `OpaqueID` ([ARCH §6](ARCHITECTURE.md#6-m2-agentkit)) — **type ล้วน ไม่มี logic** | module อื่น import ได้โดยไม่เกิด dependency cycle | ✅ `Assignment` บังคับ `acceptanceCriteria` ตั้งแต่ type; `Channel` มี approval เป็น method บังคับ |
| **P1.2** `SurrealClient` + schema | ย้ายโค้ดจาก spike เข้า target จริง + schema bootstrap แบบ **idempotent** | เปิดแอป 3 ครั้งติดกันไม่ error; test ครอบ reconnect + concurrent query | ✅ พร้อมแก้บั๊กจริง 5 ข้อที่เจอตอนเทสกับ engine ([ARCH C.0](docs/ENGINEERING_NOTES.md#c0-surrealdb-v320-quirks--ยืนยันซ้ำค้นพบใหม่จาก-spike-ฝั่ง-swift-2026-08-10)) — **3 ใน 5 เป็นบั๊กของเราเอง** |
| **P1.3** Conversation persistence | ตาราง `conversation`/`message`, เขียน user message **ก่อน** เรียก LLM เสมอ, โหลด history จาก DB ทุกครั้ง | ปิดแอปกลางบทสนทนา → เปิดใหม่เห็นครบ; agent error → user message ยังอยู่ | ✅ เทสยืนยัน reconnect แล้ว history ครบ, ลำดับถูก, scope filter ไม่รั่ว, delete cascade |
| **P1.6** Span store | ทุก step เขียน span เข้า DB — **แหล่งเดียว** ([ARCH §16](ARCHITECTURE.md#16-m12-observability--eval)) | สลับหน้าไปมาแล้ว event ไม่หาย (v1 bug B5); span ย้อนหลังอ่านได้หลัง restart | ✅ `SurrealSpanSink` + `SpanRecorder` ปิด span เสมอแม้ body throw; parent/child reassemble ได้ |
| **P1.4** `LLMExecutor` + 2 impl | `OnDeviceExecutor` (Foundation Models) + `VLLMExecutor` (OpenAI-compatible) หลัง protocol เดียว ([ARCH §9.1](ARCHITECTURE.md#91-llm-abstraction-ของเราเอง-รองรับทั้งสองยุค)) | ทั้งสอง executor ผ่าน contract เดียวกันกับ backend จริง | ✅ **Tier 0 ทำ structured output ได้จริง** ผ่าน `DynamicGenerationSchema` (JSON Schema ของเรา → schema ของ Apple ตอน runtime); tool calling ประกาศว่าไม่รองรับตามตรง เพื่อให้ router ส่งขึ้น tier บนแทนที่จะพังกลางเทิร์น |
| **P1.5** Model Router | เลือก tier จาก capability/impact/availability/latency + **fallback chain** + **refusal = escalate** ([ARCH §9.2](ARCHITECTURE.md#92-model-router-tier-0--05--1)) | test: บังคับให้ Tier 0 refuse → งานสำเร็จที่ tier ถัดไป **โดยไม่มี error โผล่ถึง caller** | ✅ 12 เทสคุมกติกา: refusal/overflow/offline escalate, decoding ไม่ escalate, งาน high-impact ข้าม Tier 0, **paid tier ต้องขอใช้ก่อนเสมอ**, streaming escalate ก่อน token แรก, เส้นทางที่ escalate ถูกบันทึกลง span |
| **P1.7** Hook chain + Risk scorer | Critic → Risk → HITL รอบทุก tool call ([ARCH §5.3](ARCHITECTURE.md#53-hook-chain-gate-sub-module)) + risk classification ต่อ tool | test พิสูจน์ว่า **ไม่มีทางเรียก tool โดยไม่ผ่าน gate** (v1 ใช้ test แบบนี้จับ bug ได้จริง) | ✅ invariant บังคับด้วย**โครงสร้าง** ไม่ใช่วินัย: `ToolGateway` ถือ tool ไว้เป็น private และแจก `ToolAdvert` ที่เรียกไม่ได้ + `check.sh` fail ถ้า `.call(argumentsJSON` โผล่นอก `ToolGateway.swift` · เทสทุกสาขาที่ปฏิเสธยืนยันว่า **body ของ tool ไม่ถูกเรียก** ไม่ใช่แค่ค่าที่คืนมา · tool ลดความเสี่ยงของตัวเองไม่ได้ (declared เป็นพื้น ไม่ใช่เพดาน), tool ที่ไม่รู้จัก = High, policy hard stop ชนะ full-autonomous |
| **P1.8** Approval Broker | broadcast ไปทุก channel, first-response-wins, กัน double-resolve ([ARCH §5.4](ARCHITECTURE.md#54-approval-broker-sub-module)) | test: 2 channel ตอบพร้อมกัน → resolve ครั้งเดียว, อีกฝั่งได้สถานะ "resolved แล้ว" | ✅ resolve ทุกทาง (ตอบ/หมดเวลา/ยกเลิก) ลอดผ่านจุดเดียว จึง "resolve ครั้งเดียว" เป็นสมบัติของ actor ไม่ใช่กติกาที่แต่ละ channel ต้องจำ · **ไม่มี channel = ปฏิเสธ ไม่ใช่ปล่อยผ่าน** และไม่ใช่รอค้าง · channel ที่ต่อเข้ามาทีหลังเห็นคำขอที่ค้างอยู่ |
| **P1.9** `run_shell` + Execution (แกน) | `Process` + sandbox profile + process registry + pause/stop ([ARCH §13](ARCHITECTURE.md#13-m9-execution)) | รันคำสั่งจริงได้, กด stop แล้วตายจริง, ไม่มี zombie | ✅ ใช้ **`posix_spawn` แทน `Process`** เพราะต้องได้ process group ของลูกแบบ atomic — เทสยืนยันว่า `sleep` ที่ shell แตกออกมาตายไปด้วยตอนกด Stop · pause/resume/timeout/reap ครบ (SIGTERM ต้อง SIGCONT ก่อน ไม่งั้น process ที่ pause ไว้ฆ่าไม่ตาย) · **seatbelt ปิดจริงระดับ kernel** (เขียนนอก project root ไม่ได้, network ปิด) และรายงานตรงๆ เมื่อ profile ใช้ไม่ได้แทนที่จะแกล้งว่าใช้ |
| **P1.10** Chat UI (แกน) | หน้าคุย + streaming + conversation list + approval banner inline | คุยกับ agent จริงจนจบเทิร์นที่มี tool call + approval ได้ | ✅ `AgentTurnRunner` วิ่งครบเทิร์น (user→DB→history จาก DB→router→stream→tool→gate→approval→DB) + Chat UI ครบ sidebar/streaming/3 สวิตช์/approval banner ที่แก้ argument ก่อนอนุมัติได้ · **เทิร์นที่มี tool call + approval พิสูจน์ด้วย end-to-end test กับ DB/process/broker ของจริง และขับผ่านหน้าจอจริงแล้ว** (Llama 3.1 8B ผ่าน LM Studio) — ขับจริง 3 รอบ เจอ 8 ข้อที่เทสไม่เห็น แก้ครบพร้อม regression test: การ์ด tool ขึ้นซ้ำ, แถบ approval บวมเต็มจอ, ขออนุมัติคำสั่งที่รันไม่ได้อยู่แล้ว, ถามซ้ำหลังกดไม่อนุมัติ, routing error กลืนสาเหตุจริง, **สถานะ "ไม่ได้รัน" หายตอนโหลด history** (ประวัติโกหกว่าทุกคำสั่งสำเร็จ), โมเดลเล็กมั่วชื่อ tool, **view model ถูกสร้างใหม่ทุก body pass จน approval ถูกส่งไปหา instance ที่ไม่มีใครแสดง → เทิร์นค้างโดยไม่มีแถบขึ้น** |

**🎯 Milestone P1**: พิมพ์ "ดูไฟล์ในโฟลเดอร์นี้แล้วสรุปให้หน่อย" → agent เรียก tool → ขอ approval → รัน → ตอบ → ทุกอย่างถูกบันทึกและดูย้อนหลังได้

---

## P2 — Knowledge

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P2.1** เลือก embedding model ✅ | วัด 4 ตัวเลือกบนชุดไทย+อังกฤษเดียวกัน (recall@1/@3, MRR, cross-lingual) | มีตัวเลขเทียบ + บันทึกเหตุผลใน ARCH; **มิติถูกล็อกก่อนเริ่ม index** | ✅ **`bge-m3` @ 1024 มิติ** — 100% ทุกมิติ ([ARCH E.10](docs/VERIFICATION_LOG.md#e10-d-2--เลือก-embedding-model-วัดจริง-ปิดแล้ว)); พบว่า embedding ของ Apple แยก vector space ตามสคริปต์ จึงทำ cross-lingual ไม่ได้เลย |
| **P2.2** Chunker + Thai tokenizer | `NLTokenizer` + **dictionary merge layer** สำหรับคำทับศัพท์ ([ARCH E.3](docs/VERIFICATION_LOG.md#e3-thai-tokenizer--รันจริงกับประโยคงานวิจัยการแพทย์)) | `โลจิสติก`/`โควิด` ไม่ถูกแตก; วัด BM25 recall เทียบก่อน/หลัง merge layer | ✅ target `Knowledge` (tokenizer + chunker + BM25) **14 เทส** · `โลจิสติก`/`โควิด` ไม่แตกแล้ว และมีเทสยืนยันว่าถ้าปิด merge layer มันแตกจริง · **วัดแล้วได้ผลค้านข้อสันนิษฐานของ Done-when เอง**: recall@1/MRR เท่ากันเป๊ะทั้งเปิดและปิด (1.00) เพราะ index/query ใช้ tokenizer เดียวกัน — ของจริงที่ layer นี้แก้คือ **precision** (ค้น `สติ` แล้วไม่ได้เปเปอร์ logistic regression ติดมา) บันทึกไว้ที่ [ARCH E.3.1](docs/VERIFICATION_LOG.md#e31-merge-layer-ให้ผลอะไรจริง--วัดตอน-p22-แก้ข้อสันนิษฐานเดิม) · chunker ไม่ตัดกลางประโยค + overlap แบบ best-effort |
| **P2.3** Ingestion pipeline | PDF/DOCX/PPTX/รูป → OCR (Vision) → chunk → dedup (sha256 + semantic) → entity/relation → embed → index | ingest ไฟล์สแกนจริงแล้วค้นเจอ; ingest ซ้ำ → ไม่เพิ่ม chunk ซ้ำ | ✅ **ทั้งสอง Done-when ผ่านกับไฟล์สแกนจริง** — เทสวาดข้อความไทยลง bitmap แล้วยัดเป็นหน้า PDF (ไม่มี text layer เลย) OCR จึงเป็นทางเดียวที่ข้อความจะเข้า index ได้ · ค้นเจอทั้งฝั่ง BM25 และฝั่ง vector (bge-m3 @ **1024 มิติ** ตามที่ P2.1 ล็อก) · ingest ซ้ำ = เพิ่ม 0 chunk · dedup 2 ชั้น: sha256 หลัง normalize ช่องว่าง + near-duplicate ด้วย cosine ≥ 0.97 (กันเคสสแกนใหม่แล้ว OCR เพี้ยนนิดหน่อย) · reader ครบ PDF (text layer บาง = ส่งเข้า OCR), รูป, docx/pptx (unzip + OOXML), plain text · **ปฏิเสธการ index ถ้า embedder อ่านสคริปต์ไม่ออก** ([E.11](docs/VERIFICATION_LOG.md#e11-embedding-model-ที่-ตาบอดภาษาไทย--เจอตอน-p24-2026-08-11)) · พบว่า Vision รองรับไทยเฉพาะ `.accurate` ([E.12](docs/VERIFICATION_LOG.md#e12-vision-ocr-รองรับไทยแค่โหมดเดียว--ตรวจตอน-p23-2026-08-11)) · **ค้าง**: relation extraction (ต้องใช้โมเดลอ่านประโยค) เลื่อนไปพร้อม graph view ใน P2.7 — ตอนนี้สกัดเฉพาะ entity ด้วย `NLTagger` |
| **P2.4** Hybrid search | BM25 + vector + RRF fusion (โค้ดมีแล้วจาก spike) + scope filter | ผลลัพธ์คืน provenance + tier ครบทุกแถว | ✅ `KnowledgeIndex` — RRF (k=60) fuse BM25 + cosine, ทุกแถวคืน provenance + tier + บอกได้ว่าอันดับมาจากฝั่งไหน (`lexicalRank`/`semanticRank`) · chunk ที่ยังไม่มี embedding ยังแข่งฝั่ง lexical ได้ ไม่หายไปเฉยๆ · scope filter เทสครบ central/project ก/project ข/policy ไม่รั่วข้ามกัน · **เจอของจริงระหว่างทำ**: embedding model บนเครื่องนี้คืน vector เดียวกันหมดสำหรับภาษาไทย → เพิ่ม `diagnose(_:)` กันไม่ให้ index ผ่าน embedder ที่ตาบอดสคริปต์ ([ARCH E.11](docs/VERIFICATION_LOG.md#e11-embedding-model-ที่-ตาบอดภาษาไทย--เจอตอน-p24-2026-08-11)) |
| **P2.5** Provenance + credibility | `tier/origin/accessedAt/supersedes` ต่อ chunk ([ARCH §11.3](ARCHITECTURE.md#113-provenance--credibility-บังคับตั้งแต่-ingestion)) | ไม่มี chunk ไหนเข้า index ได้โดยไม่มี provenance (บังคับด้วย type ไม่ใช่ convention) | ✅ `IndexedChunk` ไม่มี initialiser ที่ไม่รับ `Provenance` และ `Provenance` ของแหล่งภายนอก **บังคับ `tier` แบบไม่ optional** (ตัว optional มีทางเดียวคือ `.authored` ของงานที่ระบบเขียนเอง) — บังคับด้วย type ตาม Done-when |
| **P2.6** Policy scope + Policy Gate | `scope: policy`, chunking atomic ต่อกฎ, `hard_constraint` → hard stop ใน hook chain | test: action ที่ขัด hard constraint ถูกหยุด **พร้อมแสดง chunk ที่ขัด** ไม่ใช่แค่ "เสี่ยง" | ✅ `PolicyDocumentParser` ตัด 1 กฎ/chunk (ข้ามหัวข้อ) และแยก **ห้าม/must not = hard** ออกจาก **ควร = คำแนะนำ** · `KnowledgePolicyGate` แทน `NoPolicyGate` ที่ P1 วางไว้ · เทสยิงผ่าน `ToolGateway` จริงและพิสูจน์ว่า**ทูลไม่ถูกเรียก** (ไม่ใช่แค่ค่าที่คืนมา) แม้เปิด full-autonomous · ข้อความที่คืนคือ**ตัวกฎคำต่อคำ + ชื่อเอกสารต้นทาง** · matching ตั้งใจให้แคบ (ทุก content term ของกฎต้องปรากฏใน action) เพราะ gate ที่ fire มั่วจะบล็อกงานจริงจนคนเลี่ยงมัน · 11 เทส |
| **P2.8** Embedding profile + reindex *(ใหม่ 2026-08-11)* | โมเดล embedding ผูกกับ **index** ไม่ใช่ tier — เก็บ `modelID/revision/dimensions/pooling/normalised/tokenizerVersion/chunkerVersion` เป็น profile เดียว + job re-embed ตอนเปลี่ยนโมเดล | index ปฏิเสธ vector ที่มาจากโมเดลอื่น (ทั้งตอนเขียนและตอนค้น); เปลี่ยนโมเดลแล้ว re-embed จากข้อความที่เก็บไว้ได้โดยไม่ต้อง OCR ใหม่ และวัด recall เทียบก่อนสลับ | ✅ `EmbeddingProfile` + ปฏิเสธ vector ข้าม profile ทั้งตอนเขียนและตอนค้น · `Reindexer` re-embed จากข้อความที่เก็บไว้ (ไม่แตะไฟล์ต้นฉบับ, resume ได้, chunk id/provenance/entity คงเดิม) + **golden-query gate ที่วัดก่อนสลับ** โมเดลที่แย่ลงถูกปฏิเสธและ index เดิมยังทำงานต่อ · **เจอระหว่างทำ**: gate ที่วัดแค่ผล fused ยอมรับโมเดลที่คืน vector เหมือนกันหมดด้วย recall@1 = 1.00 เพราะ BM25 แบกไว้ทั้งหมด → ต้องวัดฝั่ง vector แยกด้วย (`searchSemantically`) + เช็ค `diagnose` ก่อนรับ · รวม 15 เทส |
| **P2.7** KB UI | list, upload, graph view, edit/delete entity+relation, tier badge, export/import | แก้ entity แล้วผลค้นหาเปลี่ยนตามจริง | 🔶 **ตรรกะเสร็จ+พิสูจน์แล้ว · หน้าจอยังไม่ได้ขับจริง** — entity ถูก index ไปพร้อมเนื้อความ ทำให้ Done-when เป็นจริงในระดับ index (เทส: ก่อนแก้ค้นไม่เจอ → แก้ entity → เจอ → ลบ entity → หายอีกครั้ง) · `KnowledgeView` มี list + tier badge + ค้นหาที่บอกว่าอันดับมาจากฝั่งไหน + อัปโหลดผ่าน NSOpenPanel + แก้ entity + ลบเอกสาร + export/import (ไม่ส่ง vector ไปด้วย เพราะเครื่องปลายทางอาจใช้โมเดลคนละตัว) · · **KB ลง SurrealDB แล้ว** — ตาราง `chunk` + unique index บน `content_hash` (กัน chunk ซ้ำที่ชั้นที่ bypass ไม่ได้) · หน้าจอเขียนผ่านลง DB ทันทีทั้งตอน ingest / แก้ entity / ลบ / import และโหลดกลับตอนเปิดหน้าจอ · vector ที่สร้างด้วยโมเดลอื่นถูกตัดทิ้งตอนโหลด (เก็บข้อความไว้ให้ re-embed) ไม่ปนเข้า index · 7 เทสกับ SurrealDB จริง · **ค้าง**: graph view ของ entity/relation, relation extraction (P2.3 ยกยอดมา), และ**การขับผ่านหน้าจอจริง** ซึ่ง P1.10 พิสูจน์แล้วว่าเจอบั๊กที่เทสมองไม่เห็น 8 ข้อ · ✅ **กราฟ entity/relation ทำแล้ว (2026-08-15, 13 เทส)** — แท็บ "กราฟ" ในคลังความรู้ · **ความสัมพันธ์ถูกสกัดและเก็บมาตั้งแต่ P2.7 และไม่มี view ไหนอ่านเลย** — `KnowledgeViewModel.relations` โหลดตอน attach แล้วไม่ถูกเรนเดอร์ที่ไหน · **เป็นย่านรอบสิ่งที่เลือก ไม่ใช่ทั้งกราฟ** เพราะ 500 โหนดพร้อมกันคือก้อนขนที่ดูเหมือนมีความรู้เยอะแต่ไม่ตอบอะไร — มีจุดกึ่งกลางเสมอ มีระยะเสมอ และ**บอกจำนวนที่อยู่ไกลกว่าที่แสดง**เสมอ เพราะภาพที่แสดงหนึ่งในสามของกราฟเงียบ ๆ แย่กว่ารายการ · โหนดอยู่ที่ระยะ*สั้นที่สุด* (BFS) ไม่ใช่ตามเส้นแรกที่เจอ ไม่งั้นกราฟเดียวกันจะวาดต่างกันตามลำดับแถวที่ฐานข้อมูลคืนมา · **เลย์เอาต์คงที่** ข้อมูลเดิมได้ภาพเดิมทุกครั้ง — กราฟ force-directed ที่ลงตัวคนละที่ทุกรอบคือกราฟที่ไม่มีใครชี้คุยกันได้ · **ทุกเส้นพกไปยัง chunk ที่พูดมัน** เพราะลูกศรระหว่างคำสองคำอ่านได้ว่าเป็นข้อเท็จจริง กดแล้วเห็นประโยคต้นทางกับเอกสาร · โหนดเป็น `Button` ไม่ใช่รูปวงกลม (canvas คือที่ที่ accessibility ตายบ่อยที่สุด และสคริปต์ของโปรเจกต์เองก็ fail ให้) · **ขับจริงแล้ว เจอ 2 ข้อ**: กราฟไม่บอกว่ากำลังวาดคลังไหน (เพิ่มตัวเลือกขอบเขต) · และ `changeScope` โหลดเอกสารใหม่แต่**ไม่โหลดความสัมพันธ์ใหม่** ซึ่งไม่มีใครเห็นได้ตราบใดที่ไม่มีอะไรเรนเดอร์มัน — สลับไปโปรเจกต์แล้วยังเห็นความสัมพันธ์ของคลังกลาง โดยตัวเลือกบอกความจริงแต่หน้าจอไม่ · **ลบความสัมพันธ์จากหน้ากราฟได้แล้ว (2026-08-16, 2 เทสกับ SurrealDB จริง + 1 กฎ)** — ปุ่มอยู่ในชีต "ที่มาของความสัมพันธ์" ซึ่งเป็นที่ที่คนกำลังยืนอยู่ตอนไม่เห็นด้วยกับสิ่งที่โมเดลอ่าน · **ลบการอ่าน ไม่ใช่ลบประโยค** — chunk ต้นทางอยู่ที่เดิม · **และการแก้ต้องรอดจากการ ingest ครั้งถัดไป**: การลบถูกบันทึกเป็น `relation_rejection` และ `save` กรองออกให้ ไม่งั้นการอ่านเอกสารเดิมซ้ำจะวาดเส้นเดิมกลับมา และการแก้ของคนถูกลบทิ้งโดย ingest ที่เขาไม่รู้ว่าเกิดขึ้น (กฎใน check.sh บังคับว่าตัวกรองต้องอยู่ใน `save` ซึ่งเป็นทางเดียวที่ทุกการสกัดผ่าน) |

---

## P3 — Web & Conflict

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P3.1** SearXNG sidecar | ติดตั้ง native/binary + lifecycle ผ่าน `SidecarManager` ([ARCH §1.2](ARCHITECTURE.md#12-web-search--มีของฟรีถาวรไหม-apple-ให้ด้วยไหม)) | ค้นได้จริงจากในแอปโดย user ไม่ต้องติดตั้งอะไรเอง | 🔶 **ติดตั้งและค้นได้จริงแล้ว** — SearXNG 2026.8.10 ใน `vendor/searxng/venv` + `scripts/fetch-searxng.sh`/`run-searxng.sh` · `SearXNGSource` คืนผลพร้อม tier ของ**ผู้เผยแพร่** (ไม่ใช่ของ search engine) · **ค้นภาษาไทยได้จริง** · 🔴 **PyPI ชื่อ `searxng` ไม่ใช่ SearXNG** — เป็น MCP server ของบุคคลที่สามที่ใช้ชื่อซ้ำ (ลองติดตั้งแล้วได้ 40 แพ็กเกจที่ไม่เกี่ยว ไม่มี search engine) ต้องลงจาก git เท่านั้น · build ต้องปิด isolation + ลง `msgspec`/requirements ก่อน เพราะ backend ของมัน import ตัวเองตอน build · · **ต่อเข้า `SidecarManager` แล้ว** (health probe/restart/teardown เหมือน `surreal`, readiness 45 วิเพราะโหลด engine ~70 ตัวก่อนตอบ) ตั้ง path ผ่าน `searxngPython` ใน `bootstrap.plist` · 🔄 **ถูกแทนที่แล้วโดย P13.1 (2026-08-14)** — venv ของ Python **ย้ายที่ไม่ได้** (สคริปต์ข้างในฝัง absolute path) จึงก๊อปเข้า `.app` ตรงๆ ไม่ได้ → Done-when ข้อ "user ไม่ต้องติดตั้งอะไรเอง" **ไม่มีวันผ่านด้วยทางนี้** จึงไม่ใช่งาน packaging ของ P9.6 อีกต่อไป · T5 เดินผ่านสะพาน `WKWebView` แทน ซึ่งผ่าน Done-when ข้อนั้นแล้ว · `SearXNGSource` **ยังอยู่และใช้ได้** ถ้าผู้ใช้รัน SearXNG เองไว้ — เก็บไว้ในฐานะ provider ทางเลือก ไม่ใช่ทางหลัก |
| **P3.2** Source registry + tiering | T1–T5 ครอบทุกสาขา, แก้ผ่าน Settings ไม่ต้องแก้โค้ด | เพิ่มแหล่งใหม่ 1 แถว → agent ใช้ทันทีโดยไม่ recompile | ✅ `SourceRegistry` — 27 แหล่งตั้งต้นครบ T1–T5 ทุกสาขาตาม §1.2 · เลือกแหล่งจาก**หัวข้อของงาน** ไม่ใช่จาก role (v1 hardcode WHO/CDC/PubMed จนตอบคำถามเขียนโค้ดไม่ได้) · domain ที่ไม่รู้จัก = T5 ไม่ใช่ "ไม่มี tier" · match แบบ host/subdomain จริง (`notwho.int` ไม่ได้ T1) · rule ที่เจาะจงกว่าชนะ · ปิดแหล่งได้โดยไม่ลบแถว · JSON round-trip · 11 เทส |
| **P3.3** Tier 1–3 API clients | PubMed E-utilities, medRxiv, OpenAlex, Crossref, Semantic Scholar | คืนผลพร้อม `{tier, url, accessedAt}` ครบ | ✅ **ยิง API จริงผ่านหมด** — OpenAlex, Crossref, PubMed (esearch+esummary 2 ครั้ง), medRxiv (by DOI) · ทุกแถวมี tier/url/accessedAt ครบตาม Done-when · **เจอจากการยิงจริง 3 ข้อ**: (1) `doi.org` เป็นแค่ตัว redirect ไม่มีตัวตนเชิงบรรณาธิการ → เปเปอร์ peer-reviewed จะได้ T5 เท่าบล็อก แก้ด้วยการใช้ tier ของ index ที่หาเจอแทนเมื่อ URL เป็น DOI (ตรงกับตาราง §1.2 ที่บอกว่า OpenAlex/Crossref/PubMed = T2) แล้ว `fetch_page` จะประเมินใหม่ตอนตาม DOI ไปถึงสำนักพิมพ์จริง · (2) **medRxiv ไม่มี API ค้นด้วยคำค้นเลย** ตอบได้แค่ตาม DOI/ช่วงวันที่ → `search` โยน error ตรงๆ แทนที่จะคืน preprint ล่าสุดมั่วๆ ให้ดูเหมือนผลค้นหา · (3) **Semantic Scholar ติด 429 ตั้งแต่ request แรก** สำหรับผู้เรียกที่ไม่มี key → แยกเป็น `rateLimited` ให้ต่างจาก "ไม่มีผลลัพธ์" · preprint เป็น T3 เสมอไม่ว่าจะหาเจอจากที่ไหน (การถูก index ไม่ใช่การ review) · 7 เทส (live เป็น opt-in) |
| **P3.4** `fetch_page` | readability extraction (ตัด nav/ads), รองรับ PDF, provenance ระดับย่อหน้า | อ่านหน้าจริงได้ ≥5 เว็บที่โครงสร้างต่างกัน; agent อ้างอิงได้ระดับย่อหน้า | ✅ **ยิงเว็บจริงผ่านครบ 5 แห่ง**: RFC (`text/plain`), Wikipedia (317 ย่อหน้า), arXiv, MDN (63), WHO (131) + PDF ของ arXiv · คืนเป็น**ย่อหน้า** ไม่ใช่ก้อนเดียว · tier คิดจาก URL **ปลายทางหลัง redirect** · บล็อก `file://` · **เจอจากการยิงจริง 3 ข้อ**: (1) `text/plain` อ่านไม่ได้เลยเพราะโยนเข้า HTML extractor — แก้แล้ว (2) `ietf.org/rfc/*` redirect ไป `rfc-editor.org` ที่ไม่มีในทะเบียน — เพิ่มแล้ว (3) **หน้าที่ render ด้วย JS อ่านไม่ได้** (swift.org, developer.apple.com ส่ง HTML เปล่า) → เพิ่ม `FetchError.needsJavaScript` บอกตรงๆ แทนที่จะรายงานว่าเป็นหน้าบาง · เทสยิงเน็ตเป็น opt-in (`COAI_TEST_NETWORK=1`), เทส extractor 7 ตัวรัน offline เสมอ |
| **P3.5** `ingest_url` | ดึงหน้าเว็บเข้า KB ผ่าน pipeline เดียวกับ upload | หน้าที่ ingest แล้วค้นเจอใน KB พร้อม tier ที่ถูกต้อง | ✅ **(แก้สถานะ 2026-08-12: เคยถูก mark ✅ ทั้งที่ทูลชื่อเดียวกับ task ไม่มีอยู่จริง — `grep URLIngestor` เจอไฟล์ตัวเองไฟล์เดียว ไม่มีทูล ไม่มี UI เอื้อมถึงจากที่ไหนไม่ได้เลย D6 ครั้งที่หก · ตอนนี้มี `IngestURLTool` เรียกผ่าน gateway ได้จริงและอยู่ใน tool list ของ Researcher)** `URLIngestor` — ค้นเจอใน KB พร้อม tier ที่มาจาก **source registry ผ่านหน้าเว็บ ไม่ใช่จาก caller** (ถ้าให้ caller ส่ง tier มาได้ คำว่า T1 จะแปลว่า "คนที่ ingest บอกว่าใช่") · citation ชี้ระดับย่อหน้าได้ (`page` + `section`) · ingest ซ้ำเพิ่ม 0 chunk · scope ตรงตามที่สั่ง · หน้าที่อ่านไม่ได้ไม่ทิ้ง chunk ครึ่งๆ ไว้ · แยก `PageReading` เป็น protocol ให้เทสได้โดยไม่ต้องพึ่งเว็บคนอื่น · 6 เทส |
| **P3.6** Conflict Ledger | detect → ประเมินน้ำหนัก → auto/HITL → precedent ([ARCH §11.6](ARCHITECTURE.md#116-conflict-ledger--เมื่อความรู้ขัดกัน)) | test: ป้อนเอกสารขัดกัน 2 ฉบับ → เกิด Conflict Card; ตัดสินแล้วครั้งถัดไปไม่ถามซ้ำ; มีแหล่ง tier สูงกว่าเข้ามา → เปิด conflict ใหม่ | 🔶 **ตรรกะครบ 10 เทส ผ่าน Done-when ทั้ง 3 ข้อ** — ต่างกันชัด (T1/2026 vs T5/2019) ตัดสินเอง แต่ยัง**บันทึกว่าเคยขัดกัน**, ก้ำกึ่งยกให้คน, ตัดสินแล้วครั้งถัดไปคืนคำตัดสินเดิม, แหล่ง tier สูงกว่าเปิดใหม่ได้/แหล่งอ่อนกว่าเปิดไม่ได้ · น้ำหนักคืนเป็น**เหตุผลรายข้อ**ไม่ใช่แค่ตัวเลข · แหล่งอ่อน 10 แหล่งไม่ชนะ T1 หนึ่งแหล่ง และปีใหม่ไม่ชนะ tier · · **`ConflictDetector` เสร็จแล้ว** (7 เทส) — ถามโมเดลด้วย structured output, ถามเฉพาะคู่ที่ retrieval หยิบมาคู่กันอยู่แล้ว, **ไม่แน่ใจ = ไม่ขัดแย้ง** (การ์ดปลอมทำให้ผู้ใช้เลิกอ่านการ์ด รวมถึงใบจริง), **โมเดลล่ม ≠ สองแหล่งตรงกัน**, ข้าม chunk จากเอกสารเดียวกัน · · **persist ลง SurrealDB แล้ว** (ตาราง `conflict`, 4 เทสกับ DB จริง) — เก็บข้อความทั้งสองฝั่ง **verbatim** ไม่ใช่อ้างอิงถึง chunk เพราะข้อขัดแย้งอยู่ได้นานกว่าเอกสารที่ทำให้เกิด และเก็บ**เหตุผลของน้ำหนักตามที่เขียนไว้ตอนนั้น** ไม่ใช่คำนวณใหม่ — ข้อนี้เคยเป็นแค่ความตั้งใจ ตอนนี้บังคับด้วย `recordDecision` ที่เขียนเฉพาะคำตัดสิน และมีเทสที่จำลอง "ชั่งไว้เมื่อปีก่อน" คอยจับ (U11) · ✅ **ต่อเข้าเส้นทางจริงแล้ว (2026-08-11)** — `Engine` สร้าง detector, `KnowledgeViewModel` review ทุกครั้งที่ ingest โดยยิงคำถามจาก chunk ใหม่แล้วให้ detector ดูเฉพาะเพื่อนบ้านที่ retrieval คืนมา (จำกัด 5 chunk × 3 เพื่อนบ้าน เพราะทุกคู่คือ model call ระดับ high-impact) ที่ตัดสินไปแล้วไม่ถูกยกซ้ำ · **พิสูจน์ Done-when ข้อแรกบนหน้าจอจริงแล้ว**: ป้อนเอกสารขัดกัน 2 ฉบับ (ไทย/อังกฤษ เรื่องระยะเวลาให้ยาต่อหลังผ่าตัด) → ขึ้น "พบความรู้ที่ขัดกัน 1 จุด" และการ์ดโผล่เอง |
| **P3.7** Conflict UI | Conflict Card (verbatim 2 ฝั่ง + ที่มา + น้ำหนัก + 4 ทางเลือก) + ประวัติที่กลับคำตัดสินได้ | ผู้ใช้ตัดสินได้โดยไม่ต้องเปิดเอกสารต้นฉบับเอง | 🔶 **โค้ดครบทุกแถวของตาราง §11.6 · ขับหน้าจอจริงแล้ว (2026-08-11)** — การ์ดแสดงข้อความ**ทั้งสองฝั่งคำต่อคำ** + ที่มาพร้อม tier/ปี/วันที่เข้าถึง + เหตุผลของน้ำหนักรายข้อ + ข้อเสนอของระบบที่**ระบุฝั่งและเหตุผล** พร้อมกำกับว่าเป็นข้อเสนอ ไม่ใช่ข้อสรุป + 4 ทางเลือก (A / B / ถูกทั้งคู่ในบริบทต่างกัน **บังคับระบุเงื่อนไข** / ยังไม่ตัดสิน) + เลือกได้ว่าผูกเฉพาะโปรเจกต์หรือเป็นคำตัดสินกลาง · กดตัดสินจริงแล้วบันทึกและไม่ถามซ้ำข้ามการรีสตาร์ท (U9) · sheet เปิดที่ฝั่งที่ระบบเสนอ ไม่ใช่ฝั่ง A เสมอ · **ประวัติที่กลับคำตัดสินได้ทำแล้ว (2026-08-16, 2 เทสกับ SurrealDB จริง + 1 กฎ)**: ตาราง `conflict_decision` **เขียนเพิ่มอย่างเดียว** — การกลับคำตัดสินคือการเขียนแถวใหม่ ไม่ใช่การ UPDATE ทับ เพราะประวัติที่แก้ได้ไม่ใช่ประวัติ มันคือความเห็นปัจจุบันที่มีเวลาแปะไว้ · **การเปิดใหม่ก็เป็นคำตัดสินหนึ่ง** และ**ต้องมีเหตุผล** (ปฏิเสธถ้าเว้นว่าง) เพราะการกลับคำตัดสินที่ไม่มีเหตุผลแยกไม่ออกจากการกดผิดเมื่อมีคนมาอ่านในอีกหกเดือน · การ์ดกางประวัติได้และมีปุ่มกลับคำตัดสินตรงนั้น เพราะการ์ดคือที่ที่คนยืนอยู่ตอนรู้ว่าตัดสินผิด · โหลดประวัติเมื่อกางเท่านั้น ไม่ใช่ query ต่อการ์ดทุกครั้งที่ reload · **หัวการ์ดแก้แล้ว (2026-08-16, 6 เทส)**: `ConflictHeadline` — หัวการ์ดต้อง*เกี่ยวกับสองข้อความนั้น* ไม่ใช่สิ่งที่ผู้ใช้พิมพ์ลงช่องค้นหา · ประโยคคำสั่ง ("จง…", "ช่วย…หน่อย", "summarize…") ไม่ใช่คำถามเกี่ยวกับแหล่ง และหัวเรื่องที่ไม่มีคำร่วมกับข้อความทั้งสองเลยคือหัวเรื่องของเรื่องอื่น — ทั้งสองกรณีถูกแทนด้วย**ข้อความจริงของสองฝั่ง** ซึ่งอ้างคำพูดมาตรง ๆ จึงพูดผิดเกี่ยวกับสิ่งที่อยู่บนการ์ดไม่ได้ · เป็น computed property ⇒ การ์ดที่ถูกบันทึกไว้ก่อนกฎนี้ถูกซ่อมตอนขึ้นจอ ไม่ใช่ค้างชื่อผิดถาวรเพราะเคยเขียนลงไปครั้งหนึ่ง |

---

## P4 — AI Team

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P4.1** Specialist actors | Researcher/Analyst/Engineer/Writer เป็น `actor` แยก, คืนแค่ `Deliverable` ([ARCH §2.3](ARCHITECTURE.md#23-context-isolation--กติกาการคืนงาน)) | compiler บังคับ isolation จริง — test พิสูจน์ว่า context ไม่รั่วข้าม actor | ✅ 4 role เป็น `actor` แยก คืนได้แค่ `Deliverable` — **compiler การันตี ไม่ใช่วินัย** · **หลักฐานมาจากสิ่งที่รันจริงเท่านั้น**: คำสั่งที่ exit code ≠ 0 ให้ evidence ที่ `passed == false` แม้โมเดลจะสรุปว่า "ทุกอย่างผ่านแล้ว" (external-truth-gated done ตาม §2.5) · **แต่ละ role ใช้ได้เฉพาะทูลของตัวเอง** — Writer ขอ `run_shell` แล้วทูลไม่ถูกเรียกเลย · ทุกการเรียกทูลยังผ่าน hook chain เดิม ไม่มีทางลัด · artifacts เป็น pointer ไม่ใช่เนื้อหา · โมเดลล่ม = โยน error ไม่ใช่คืน deliverable ว่าง (ไม่งั้นจะถูกตรวจว่าเป็นงานที่ทำแล้วไม่ได้ผล แทนที่จะเป็นงานที่ไม่ได้ทำ) · 6 เทส |
| **P4.2** Team Orchestrator | plan → assign → review → rework → escalate + cap fan-out + task ledger ใน DB ([ARCH §2.2](ARCHITECTURE.md#22-กติกาของหัวหน้าทีม-supervisor-contract)) | หัวหน้าทีมมอบหมายชื่อ role นอก enum ไม่ได้ (compile-time); assignment ทุกอันมี acceptance criteria | 🔶 `TeamOrchestrator` ครบวง plan → assign → review → rework → escalate + cap fan-out + ledger ต่อ assignment · role นอก enum เป็นไปไม่ได้ตั้งแต่ type · แผนที่ไม่มี acceptance criteria ถูกทิ้งตอน parse · ✅ **ledger ลง SurrealDB แล้ว** (ตาราง `task`, 5 เทสกับ DB จริง) เขียนทุกครั้งที่สถานะเปลี่ยน ไม่ใช่ตอนจบ — เพราะ run ที่ถูกขัดจังหวะคือตอนที่คนอยากอ่าน ledger ที่สุด · `unfinished(scope:)` ตอบว่ามีงานไหนยังต้องการคน · ✅ **ปล่อย span แล้ว (2026-08-15)** — 1 span ต่อ assignment + 1 ต่อรอบ พร้อม `deliverable_kind` และใบงาน (รายละเอียดอยู่ที่ P10.15) · `scope` เปลี่ยนตามโปรเจกต์ที่เปิดอยู่ได้แล้ว แต่ **เปลี่ยนกลางรันไม่ได้** เพราะครึ่งหนึ่งของรันลงบัญชีโปรเจกต์หนึ่ง อีกครึ่งลงอีกโปรเจกต์ คือ ledger ที่อ่านไม่ได้ทั้งสองฝั่ง |
| **P4.3** QA agent + Definition of Done | ตรวจด้วย**หลักฐาน** ต่อ role ([ARCH §2.5](ARCHITECTURE.md#25-qa-loop--ตรวจตามมาตรฐาน)) | Engineer task ที่ไม่มี exit code 0 ในทรานสคริปต์ → QA ตีกลับ **แม้โมเดลจะบอกว่าเสร็จ** | ✅ `QAReviewer` ตรวจจาก evidence ล้วน ไม่เคยถามโมเดลว่า "ดีไหม" · **เทสยิงเคสตรงตาม Done-when**: deliverable ที่สรุปว่า "เรียบร้อย ทุกอย่างผ่านแล้ว" แต่ไม่มี exit code 0 → ตีกลับทั้ง 3 รอบแล้ว escalate · Researcher ต้องมี ≥2 citation ที่อ่านจริง (มี URL) |
| **P4.4** Rework loop + escalation | retry cap → escalate หา user พร้อมเหตุผล | test: งานที่ผ่านไม่ได้ → escalate ที่ครั้งที่ N ไม่วนไม่จำกัด | ✅ หยุดที่ retry cap พอดี (เทสให้ script 4 รอบ แต่ถูกเรียกแค่ 3) · **rework ส่งเหตุผลของ QA กลับไปในโจทย์รอบถัดไป** — ส่งโจทย์เดิมซ้ำคือวิธีที่ loop วนซ้ำตัวเอง · specialist ที่ throw ถูกนับเป็นความพยายามหนึ่งครั้ง ไม่ใช่ผลตรวจ |
| **P4.5** Engineer ห้ามแตกทีม | บังคับว่า role นี้ทำงาน context เดียว ([ARCH §2.4](ARCHITECTURE.md#24-ข้อยกเว้น-งานที่ห้ามแตกทีม)) | test พิสูจน์ว่า orchestrator fan-out งาน code ไม่ได้ | ✅ แผนที่มีงาน engineer มากกว่า 1 ถูกปฏิเสธ **ก่อนใครเริ่มทำงาน** (เทสยืนยันว่า specialist ไม่ถูกเรียกเลยสักครั้ง) พร้อมเหตุผลอ้าง §2.4 |
| **P4.6** Manual mode ทุกชั้น | คุยกับ specialist ตรง, แก้ plan รายขั้น, แก้ query ก่อนรัน ([ARCH §2.6](ARCHITECTURE.md#26-manual-mode--ไม่ใช่ทุกอย่างต้องผ่านทีม)) | ทุก artifact ที่ agent สร้าง มีปุ่มแก้เอง | 🔶 **สองแถวของ §2.6 ที่ทำได้ตอนนี้ เสร็จและกดจริงแล้ว** — **ไม่มีทางลัดจากเป้าหมายไปสู่การลงมือทำอีกแล้ว**: ทั้งสองเส้นทางจบที่แผนที่แก้ได้ และไม่มีอะไรเริ่มจนกว่าจะกดอนุมัติ · **แถว "ข้ามหัวหน้าทีม"**: เลือก role จาก dropdown แล้วเขียนงานเอง — dropdown แบบ v1 ยังอยู่ตามที่ §2.6 กำหนด และไม่ใช่ทางหนีตอนวางแผนพลาด · **แถว "แก้ระหว่างทาง"**: `propose()` แยกออกจาก `run()` แผนที่ได้มาแก้ได้ทุกช่อง (role · งาน · ผลงานที่ต้องส่ง · เกณฑ์ตรวจรับ) เพิ่ม/ลบงานได้ · **ข้อห้าม §2.4 ขึ้นในตัวแก้ขณะที่ยังแก้ได้** พร้อมเหตุผลเต็ม ("แผนแตกงาน Engineer เป็น 2 ก้อน…") และปิดปุ่มอนุมัติ — เดิมกฎนี้มาถึงผู้ใช้เป็น error หลังกดเริ่มไปแล้ว · เปลี่ยน role หนึ่งงาน → คำเตือนหาย ปุ่มเปิด · งานที่ไม่มีเกณฑ์ตรวจรับเริ่มไม่ได้ (§2.5 บังคับที่ UI ไม่ใช่แค่ที่ type) · **ค้าง**: แถว "ข้าม agent ทั้งหมด" (Notebook / DB Explorer / editor) เป็นของ P6/P8 ยังไม่มีเครื่องมือให้ข้ามไปหา · แถว "หยุด" มีแล้วบางส่วน (Plan-only, หยุดทั้งรัน, stop ราย process) ยังไม่มี pause/rework ราย assignment · **การกดจริงครั้งแรกพิสูจน์คุณค่าของขั้นนี้ทันที**: แผนที่หัวหน้าทีมเสนอมามีเกณฑ์ตรวจรับข้อเดียวกันวนซ้ำสิบกว่าบรรทัด (โมเดลวนใน structured output) และใส่ชื่องานลงช่อง "ผลงานที่ต้องส่ง" — ถ้าไม่มีขั้นให้แก้ แผนนี้จะถูกส่งให้ specialist ทั้งอย่างนั้น |
| **P4.7** Team View | เห็นทั้งทีม ใครทำอะไร สถานะ QA + สั่ง rework/ยกเลิกราย assignment | ดูงานที่กำลังวิ่งขนานกันได้จากหน้าเดียว | 🔶 **แท็บที่สี่ในแอป และ P4.1–P4.5 เข้าถึงได้จริงเป็นครั้งแรก** — ก่อนหน้านี้ `TeamOrchestrator` และ specialist ทั้งสี่ถูกสร้างเฉพาะในเทสของตัวเอง (ดู U14) · หน้าจอรวมสองแหล่ง: event สดจากหัวหน้าทีม (อย่างเดียวที่บอกได้ว่างานไหน**กำลัง**ทำอยู่) กับ ledger ที่บันทึกไว้ (อย่างเดียวที่รอดข้ามการปิดแอป) merge ด้วย assignment id · ทุกแถวบอก role · จำนวนรอบ · และเหตุผลที่ QA ตีกลับ — "escalate" เปล่าๆ คือสิ่งที่ทำให้ loop ของ v1 อ่านไม่รู้เรื่อง · **ขับจริงบนหน้าจอแล้ว**: สั่งเป้าหมาย → หัวหน้าทีมวางแผน → มอบหมาย Engineer → รอบ 1 → ตีกลับ → รอบ 2 → รอบ 3 → escalate ตาม retry cap โดยหน้าจอตามทันทุกสถานะ · ledger ที่เขียนลง DB ตรงกับหน้าจอแล้ว (U15 ปิด) · ✅ **ปิดส่วนที่ค้างแล้ว (2026-08-12)**: ทุกแถวมีปุ่ม **สั่งแก้** (บังคับให้พิมพ์เหตุผล — ส่งเข้าไปทางช่องเดียวกับ findings ของ QA เพราะ "ทำใหม่อีกรอบ" เฉยๆ คือคำสั่งที่ทำให้ loop ของ v1 วน) และ **ยกเลิก** (บันทึกเป็น `cancelled` ไม่ใช่ลบทิ้ง — "ตัดสินใจไม่ทำ" เป็นสิ่งที่เกิดขึ้นจริงและ Run-until-done ต้องไม่รื้อขึ้นมาเอง เหมือนกับ escalate) · รอบของการ rework นับต่อจากเดิม ไม่รีเซ็ต (ไม่งั้นเท่ากับแจกโควตา retry ใหม่ฟรีๆ) · การสั่งแก้จะดึงงานออกจากกอง "รอคน" ให้เอง เพราะคนที่กดคือคนคนนั้น · 4 เทส |
| **P4.8** Operating modes | Autonomy Slider / Plan-only / Run-until-done — อิสระต่อกัน มองเห็นชัดในหัว conversation | ทั้ง 3 สวิตช์ทำงานถูกต้องและไม่กวนกัน | 🔶 **โค้ดครบและมีเทส** — `OperatingModes` แยกสามสวิตช์ออกจากกันจริง (v1 รวม "ไม่รัน" กับ "ไม่ถาม" เข้าด้วยกันจนปิดอันหนึ่งแล้วอีกอันเปลี่ยนตาม), `planOnly` บังคับใน `ToolGateway` ไม่ใช่ที่ตัว agent, `runUntilDone` เป็นสวิตช์ชัดเจนไม่ใช่การเดา · ทั้งสามอยู่บนหัวหน้าแชทแล้ว · **สวิตช์ที่สามเคยไม่ทำอะไรเลย — ตอนนี้ทำแล้ว**: `runUntilDone` ถูกประกาศ ส่งเข้า `ToolGateway` และมี toggle บนหน้าแชท แต่เดิม**ไม่มีโค้ดบรรทัดไหนใน `Sources` อ่านค่ามัน และไม่มีเทสตัวไหนแตะมัน** · **เกณฑ์ "จบงาน" อ่านจาก ledger ไม่ใช่ถามโมเดล** — สิ่งที่เหลือเป็นข้อเท็จจริง ส่วนความเห็นของโมเดลว่าตัวเองเสร็จหรือยังคือสิ่งที่ทำให้ loop ของ v1 วน · **ข้อสำคัญ: งานที่ escalate ไปแล้วห้ามถูกรื้อขึ้นมาทำเอง** — §2.5 จบด้วยการขอให้คนตัดสิน การ retry อัตโนมัติคือสิ่งที่คำตัดสินนั้นถูกเลือกมาแทน เดิม ledger แยกไม่ออกเพราะ "ถูกขัดจังหวะ" กับ "escalate" เป็น `!passed` เหมือนกัน จึงเพิ่ม `needs_human` และแยก `resumable(scope:)` ออกจาก `unfinished(scope:)` · เก็บเกณฑ์ตรวจรับลง ledger ด้วย ไม่งั้นสร้าง `Assignment` กลับมาไม่ได้เลย (type ปฏิเสธถ้าไม่มีเกณฑ์) · เพดาน: `continuationCap` 3 รอบต่อการรันหนึ่งครั้ง · **เพดาน token ต่อการรันทำแล้ว (2026-08-16, 5 เทส)**: `RunBudget` ตัวเดียวใช้ร่วมกันระหว่างหัวหน้าทีมกับ `SpecialistEnvironment` — สองตัวนับคือสองตัวที่จะไม่ตรงกันครั้งแรกที่มีใครลืมอัปเดตตัวหนึ่ง · **โทเคนของรอบที่ไม่ผ่านก็นับ** เพราะมันถูกใช้ไปแล้ว เพดานที่นับเฉพาะงานที่สำเร็จจะหลวมที่สุดตอนที่การรันกำลังไปได้ไม่ดี ซึ่งคือตอนที่มันใช้มากที่สุด · **หยุดระหว่างการเรียก ไม่ใช่กลางการเรียก** — request ที่ถูกตัดครึ่งจ่าย prompt ไปแล้วและไม่ได้อะไรกลับมา · **หมดงบแล้วต้องพูด**: มี event ของตัวเอง และงานที่ยังไม่ได้เริ่ม**ถูกเขียนลง ledger ว่ารอคนตัดสิน** เพราะการรันที่คืนผลงานน้อยลงเงียบ ๆ หน้าตาเหมือนการรันที่เจองานน้อย · ช่องกรอกอยู่บนหน้าทีม เว้นว่าง = ไม่จำกัด (ไม่มีตัวเลขที่ระบบเลือกให้เอง) · ยังไม่ได้กดสวิตช์นี้บนหน้าจอจริงกับรันที่มีงานค้างจริง (เช็คระดับ ledger ผ่านแล้ว) |
| **P4.9** Context Manager | budget-aware compaction 70–80% + structured handoff (**ปิด D-5**: สกัด `key_decisions`/`open_issues`/`file_pointers` จริงด้วย Tier 0 + heuristic) | session ยาวจน compact แล้ว **ทำงานต่อถูกต้อง** และ 3 field นั้นไม่ว่างเปล่า | ✅ **ผ่าน Done-when ทั้งสองข้อ กดจริงบนหน้าจอแล้ว** — `ContextManager` ย่อที่ 75% ของงบ ไม่ใช่รอเต็ม เพราะ handoff ที่เขียนตอนไม่เหลือที่ คือ handoff ที่เขียนไม่ดีหรือไม่ได้เขียน · ลำดับการทิ้งเรียงตามความเสียดายน้อยสุดก่อน: ผลลัพธ์เครื่องมือดิบ → บทสนทนาเก่าที่ถูกย่อเข้า handoff → **durable rules ไม่ถูกทิ้งเลย** เก็บแยกจาก transcript ไม่ใช่เป็นข้อความแรกในนั้น (v1 ทิ้งคำสั่งตอนต้น session ก่อนเพื่อน เพราะมันคือข้อความที่เก่าที่สุดพอดี) · **ปิดหนี้ D-5**: สามฟิลด์ที่ v1 ทำให้ไม่ว่างไม่ได้ ถูกสกัดจาก transcript **ไม่ใช่จากการถามโมเดล** — โมเดลที่ถูกถามว่า "ตัดสินอะไรไปบ้าง" จะแต่งคำตอบที่ฟังดูดี ส่วนการอนุมัติที่เกิดขึ้นจริง คำสั่งที่ exit ไม่ใช่ 0 และ path ที่ถูกเปิดจริง เป็นข้อเท็จจริงที่นอนอยู่ในข้อความอยู่แล้ว · โมเดล (Tier 0, impact ต่ำ) ใช้เฉพาะสองฟิลด์เชิงเล่าเรื่อง ถ้าเรียกไม่ได้ ครึ่งที่เป็นหลักฐานยังมาครบ · `file_pointers` เก็บ path ไม่เก็บเนื้อหา มีเทสยืนยันว่าเนื้อไฟล์ไม่รั่วเข้ามา · การตัดไม่ทำให้ `tool` message กำพร้าจาก `tool_calls` ของมัน (E.9) · **หลักฐานบนหน้าจอ**: บทสนทนา 33 ข้อความ → ย่อ **18,595 → 3,637 tokens** แล้วโมเดลตอบคำถาม "ตอนนี้ค้างอะไรอยู่" ได้ถูกต้องโดยอ้าง `ChunkerTests.swift` กับอาการ expectation failed ซึ่งมาจาก `file_pointers`/`open_issues` ไม่ใช่จากข้อความดิบที่ถูกย่อไปแล้ว · 7 เทส |

---

## P5 — LLM Tiers เต็มรูปแบบ

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P5.1** `MLXRuntime` | โหลด/รันโมเดล MLX ([ARCH §9.4](ARCHITECTURE.md#94-mlx-local-tier-05--model-management)) | รันโมเดล local ผ่าน `LLMExecutor` เดียวกันได้ ผ่าน test suite ชุดเดียวกับ executor อื่น | 🔶 **รันโมเดลบนเครื่องได้จริงผ่าน `LLMExecutor` ตัวเดียวกัน และผ่าน contract ชุดเดียวกับ executor อื่น** — contract ย้ายออกจากไฟล์เทสมาเป็น target `ExecutorContract` เพราะ Tier 0.5 รันใน `swift test` ไม่ได้เลย (MLX หา metallib ผ่าน main bundle — E.13 เดิม) ถ้าเขียนแยกกันสองชุด ชุดที่อ่อนกว่าจะกลายเป็นชุดที่คุ้ม tier ที่ทุกอย่างตกลงมาพอดี · `Tests/LLMProvidersTests` รัน 8 เคสนี้กับ Tier 0/Tier 1, `MLXCheck` รันเคสเดียวกันกับน้ำหนักจริงใน `check.sh` · **ผลจริงบน qwen3.5-9B-4bit**: สตรีม 29 text + 456 reasoning delta · reasoning แยกจากคำตอบ · structured output 1.3 วิ · tool call ประกอบกลับเป็น JSON ที่ใช้ได้ · prompt เกิน context ถูกปฏิเสธก่อนยิง · **สามอย่างที่ tier นี้ต้องทำเอง** เพราะไม่มีโปรโตคอลไหนทำให้: (1) **`<think>` ไม่มีแท็กเปิด** — chat template ของ Qwen เปิด think block ไว้ใน*พรอมป์ต์* ผลลัพธ์จึงเริ่มกลางความคิด จึง**ถาม template ตอนรัน** ไม่ใช่เดาจาก output (เดาไม่ทัน — กว่า `</think>` จะพิสูจน์ว่าที่ผ่านมาคือความคิด ความคิดนั้นก็ถูกสตรีมออกไปเป็นคำตอบแล้ว เป็น E.9 เคส 8c เวอร์ชัน local) (2) **ไม่มี guided generation ใน mlx-swift-lm** — schema จึงเป็นคำสั่งใน prompt แล้วดึง JSON object แรกออกมา; ไม่มี JSON = error ที่ escalate ได้ ไม่ใช่สตริงว่างที่ปลายทางอ่านว่า "ไม่มีข้อขัดแย้ง" (3) **ปิด thinking เมื่อขอ schema** — วัดจริง: ขอ routing object เพดาน 2,048 token โมเดลคิดจนหมดเพดานแล้วไม่ตอบอะไรเลย **102.7 วิ ได้สตริงว่าง** → ปิด thinking เหลือ **1.3 วิ** · **context window ประกาศ 32,768 ไม่ใช่ 262,144 ที่ config บอก** — บน 16GB prompt 7.6k โทเคนกิน ~7.4GB (บันทึกไว้ใน `Engine.swift`) ตัวเลขที่ใจกว้างจะดึงพรอมป์ต์ที่ทำเครื่องล่มเข้ามาพอดี (P5.3 จะวัดจาก RAM จริง) · **residency**: โหลดครั้งแรกแล้วค้างไว้ ปลดเมื่อ idle แล้วโหลดกลับได้ — เช็คทั้งสามข้อกับของจริง · **ค้าง**: **แอปที่ sandbox มองไม่เห็น `~/.lmstudio`** — โมเดลสองตัวบนเครื่องนี้อยู่นอก container ทั้งคู่ แอปจริงจึงยังไม่มี Tier 0.5 (หน้าบูตบอกไว้แล้วว่า "พื้นรับประกันหายไป") เข้าถึงโฟลเดอร์ของผู้ใช้ต้องผ่าน open panel = งาน P5.2 · ยังไม่ได้กดใช้บนหน้าแชทจริง (ผ่าน contract กับน้ำหนักจริงแล้ว) |
| **P5.2** Model manager UI | โหลดจาก HuggingFace (progress/resume) + เลือกจากที่มีอยู่ + ลบ + quota | โหลดโมเดลใหม่จบในแอปโดยไม่ใช้ terminal | ✅ **ผ่าน Done-when — กดโหลดจริงในแอปที่ sandbox แล้ว (ปิด U16)** · แท็บที่ห้า "โมเดล": รายการที่ติดตั้งแล้ว + รายการแนะนำพร้อมขนาด/RAM ที่ต้องใช้ + แถบโควตา · **หลักฐานบนหน้าจอ**: แอปขึ้น "ยังไม่มีโมเดลบนเครื่องนี้" (ยืนยัน U16 ว่าโมเดลใน `~/.lmstudio` มองไม่เห็นจากใน container) → กดโหลด Qwen3-0.6B → progress bar เดิน → เสร็จแล้วขึ้นในรายการ "ใช้อยู่" 335 MB context 32768 → โควตาอัปเดตเป็น 335 MB/60 GB → **ปิดแอปเปิดใหม่ยังจำได้** (`localModel` ลง bootstrap.plist) → กดยกเลิกกลางคัน ได้ข้อความว่าไฟล์ที่โหลดเสร็จยังอยู่ · **เลือกโมเดลแล้วใช้ได้เลยไม่ต้องรีสตาร์ท** — router ถือ `LocalTier` ซึ่งเป็น "ช่อง" ไม่ใช่โมเดลตายตัว (การโหลดครั้งแรกบนเครื่องที่ไม่เคยมีโมเดล คือจังหวะที่คนกำลังดูอยู่พอดีว่ามันใช้ได้ไหม) · **รายการแนะนำ = สิ่งที่ runtime โหลดได้จริง** ไม่ใช่ทุกอย่างบน Hub — อ่านจาก type registry ของ mlx-swift-lm เอง · **ห้าบั๊กที่เจอเพราะกดจริง**: (1) `Qwen3-VL-4B` มีไฟล์ครบทุกอย่างของโมเดลแชทแต่ `unsupportedModelType("qwen3_vl")` ตอนโหลด — และมันเป็นโมเดลที่ใหญ่ที่สุดบนเครื่อง จึงเป็น Tier 0.5 พอดี (2) `HubApi` เก็บ **สำเนาที่สองในแคชกลาง** คู่กับ snapshot ที่มันคลายให้ (335 MB กลายเป็น 670 MB) และสำเนาที่โควตามองไม่เห็นคือสำเนาที่ทำดิสก์เต็ม → `cache: nil` (3) ยกเลิกดาวน์โหลดขึ้นเป็น **error สีแดง `NSURLErrorDomain -999`** เพราะ URLSession โยน error ก่อน `CancellationError` จะเกิด (4) แถบ progress ขยับแต่ตัวเลขค้าง "0 MB / 0 MB" — Progress ของ Hub นับ**ไฟล์** ไม่ใช่ byte (5) โมเดลใน HF cache รายงานขนาด ~0 เพราะเป็น symlink ไป `blobs/` ทำให้ `preferred()` แพ้ให้ไฟล์จริงทุกครั้ง · **ดาวน์โหลดที่ยกเลิกไว้ต้องไม่ถูกนับว่าติดตั้งแล้ว** — ไฟล์เล็กมาก่อน โมเดล 17 GB ที่ค้างจึงมี config+template+shard เดียวและอ่านว่าพร้อมใช้ จนไปพังตอนโหลด (เช็ค shard จาก `model.safetensors.index.json`) · **ลบไฟล์ที่ค้างได้แล้ว (2026-08-16, 5 เทส)** — `incompleteDownloads` หาไดเรกทอรีที่หน้าตาเป็นโมเดลแต่โหลดไม่ได้ แล้วบอก**ว่าขาดอะไรและใหญ่แค่ไหน** ("ขาดไฟล์น้ำหนัก 2 ชิ้น · 4.1 GB") เพราะคำว่า "ไม่สมบูรณ์" ไม่บอกว่าเกือบเสร็จหรือเพิ่งเริ่ม · แยกจากรายการที่ติดตั้งแล้วโดยตั้งใจ — มันไม่ใช่โมเดล และการเอาไปรวมกันคือวิธีที่ไดเรกทอรีพัง 17 GB ถูกเลือกเป็น Tier 0.5 · **ลบได้เฉพาะของที่แอปนี้โหลดเอง** — ของที่ค้างอยู่ในคลัง LM Studio หรือ HF cache ขึ้นรายการพร้อมพาธแต่ไม่มีปุ่มลบ เพราะการเก็บกวาดที่เอื้อมเข้าไปในของโปรแกรมอื่น สุดท้ายจะลบของที่คนอื่นใช้อยู่ (กฎอยู่ใน catalog ไม่ใช่ในหน้าจอ) · resume เป็นระดับไฟล์ไม่ใช่ระดับ byte (ไฟล์ที่โหลดเสร็จไม่ถูกดึงซ้ำ ไฟล์ที่กำลังโหลดเริ่มใหม่) · quota ยังแก้ในแอปไม่ได้ ต้องแก้ `modelQuotaGigabytes` ใน bootstrap.plist |
| **P5.3** Admission control | เทียบขนาดโมเดล vs RAM ว่าง, ตารางจับคู่ RAM→ขนาด→งาน | เลือกโมเดลใหญ่เกิน RAM → เตือนและไม่ให้ตั้ง default | 🔶 **บังคับแล้วสามชั้น มีเทส 12 ตัว — ยังไม่ได้กดบนหน้าจอ (จอเครื่องหลับ)** · สิ่งที่กันคือ**เครื่องค้าง ไม่ใช่คำตอบผิด**: โมเดลที่ใหญ่กว่าที่ว่างไม่ได้ตอบช้า แต่ทำให้ทั้งเครื่องใช้ไม่ได้เป็นนาทีระหว่าง swap · **ประเมินจากรูปร่างของโมเดลเอง ไม่ใช่กฎคร่าวๆ**: weights + KV cache ที่คำนวณจาก `num_hidden_layers`/`num_key_value_heads`/`head_dim` ใน config (qwen3.5-9B = **128 KB ต่อ token**) + overhead 0.5 GB · ยึดกับการวัดจริงที่เดียวที่มี: 9B-4bit บนดิสก์ 5.6 GB, prompt 7.6k โทเคนใช้ ~7.4 GB → ประเมินได้ 7.1 GB (คลาด <10% มีเทสยืนยัน) · **สามจุดที่บังคับ**: (1) `select()` บนหน้าโมเดล — เกินแล้ว**กดไม่ได้** พร้อมตัวเลขทั้งสองข้าง (2) `isAvailable()` ถามทุกครั้ง ไม่ใช่ตอนบูต — โมเดลที่พอดีตอนเปิดแอปไม่พอดีตอนงานวิเคราะห์ถือ 10 GB และ tier ที่ตอบว่า "ไม่ว่าง" ทำให้ router ขึ้น tier ถัดไป ซึ่งดีกว่า tier ที่ตอบว่าได้แล้วทำเครื่องล่ม (3) `preferred()` เลือก**ตัวใหญ่ที่สุดที่พอดี** ไม่ใช่ตัวใหญ่ที่สุด · **ตาราง §9.4 อยู่ในโค้ดจริง**: เครื่องนี้ 16 GB → แนะนำ 7–8B → รับ routing/extraction/สรุป/งานเขียนสั้น · **เห็นผลทันทีบนเครื่องนี้**: ตอน LM Studio ถือ RAM อยู่ เหลือว่าง 6.1 GB → 9B (ต้องการ 7.1 GB) ถูกปฏิเสธ contract จึงข้ามแบบดังๆ แทนที่จะรัน — และนั่นคือพฤติกรรมที่ถูก · **ค้าง**: ยังไม่ได้กดปุ่มที่ถูก disable บนหน้าจอจริง (จอเครื่องหลับตอนถึงคิว) · ยังไม่มีทางกด "ใช้ทั้งที่เตือน" ถ้าผู้ใช้ยืนยันเอง — §9.4 บอกว่าห้ามตั้ง default ก็ยังห้ามอยู่ |
| **P5.4** Tier 0.5 เป็นพื้นรับประกัน | fallback สุดท้ายเมื่อ tier อื่นใช้ไม่ได้ | **ตัดเน็ตทั้งหมด → ระบบยังทำงานได้** (นี่คือ acceptance test หลัก) | ✅ **ผ่าน — ปิด LM Studio จริงแล้วแอปยังตอบได้จากโมเดลบนเครื่อง** · หัวแชทขึ้น `mlx:mlx-community/Qwen3-VL-4B-Instruct-4bit · tier 0.5` ไม่ใช่ endpoint · **ไม่ได้ตัดเน็ตของเครื่องจริง** — จำลองแบบที่ระบบจะเจอจริง: endpoint ถูกตั้งไว้แต่ไม่มีใครตอบ (พอร์ต 9) ซึ่งคือหน้าตาเดียวกันกับเน็ตล่ม/ปิดฝา/งบหมด · เช็คอัตโนมัติใน `MLXCheck`: (1) endpoint ต่อไม่ได้จริง (2) **งาน high-impact ตกลงมาที่ Tier 0.5 และได้ JSON กลับมา** (3) **ตรวจข้อขัดแย้งแล้วโมเดลตอบ ไม่ใช่เงียบ** — ข้อ 3 คือหัวใจของ U13: tier ที่ต่อไม่ได้ต้องไม่อ่านว่า "สองแหล่งตรงกัน" · **Tier 0.5 ใช้ Qwen3-VL-4B ได้แล้ว** — VL checkpoint สร้างด้วย `VLMModelFactory` คนละตัวกับ text (เดิมตกที่ `unsupportedModelType("qwen3_vl")`) และบนเครื่องนี้มันคือโมเดลเดียวที่ใช้ได้จริง · **สี่อย่างที่ต้องแก้เพื่อให้โมเดล 4B ใช้งานได้จริง**: (1) schema เป็น **ตัวอย่างให้ลอก** ไม่ใช่ spec ให้ตีความ (โมเดลคืน `{"role: "engineer"` ขาดทั้ง quote และปีกกา) (2) **temperature 0 ตอนขอ JSON** + retry ที่ sample ต่างออกไป (retry ที่ temp 0 ซ้ำความผิดเดิมทุกโทเคน) (3) **ซ่อมวงเล็บ/quote แบบมีขอบเขต** — ซ่อมเฉพาะเครื่องหมาย ไม่แตะค่า (4) **repetition penalty 1.05** — ไม่มีมันโมเดลตอบ "ไมรนบน" สามร้อยรอบบน transcript ภาษาไทยที่ย่อแล้ว · **คุณภาพที่ได้จริงบน 4B**: ตอบเลขถูก (17×3 = 51) แต่สะกดไทยเพี้ยน ("เทากบ") และตัดสินข้อขัดแย้ง metformin ว่า "ไม่ขัดแย้ง" — ตรงกับตาราง §9.4 ที่บอกว่าเครื่อง <16GB ได้โมเดลระดับ fallback ไม่ใช่ระดับนักวิเคราะห์ · **ค้าง**: ยังไม่ได้ทดสอบตัดเน็ตจริงทั้งเครื่อง (ปิด Wi-Fi = เปลี่ยน system setting ไม่ทำให้เอง) · KB/embedding ทำงาน local อยู่แล้วแต่ยังไม่ได้รวมในเช็คเดียวกัน |
| **P5.5** Endpoint registry + validation | `kind: selfHosted/paid`, probe, **validate model กับ `/v1/models`** ([ARCH E.9](docs/VERIFICATION_LOG.md#e9-vllmexecutor-spike--tier-1-ผ่าน-openai-compatible-endpoint) เคส 8a) | ตั้งชื่อโมเดลผิด → เตือนตอนบันทึก ไม่ใช่ไปพังตอนใช้ | ✅ **แท็บที่หก "ระยะไกล" — กดจริงแล้ว** · หลายๆ endpoint พร้อมกัน แต่ละอันประกาศ `kind` ซึ่งเป็นเส้นแบ่งเดียวระหว่างฟรีกับเสียเงิน · **จุดสถานะถาวร + ปุ่มตรวจใหม่ทั้งหมด** (probe ไม่กิน token) · **บันทึกได้ต่อเมื่อ probe ผ่าน** — หลักฐานบนหน้าจอ: กดบันทึก endpoint ที่ต่อไม่ได้ → ขึ้นแดง "ยังไม่บันทึก — ต่อไม่ได้" และไม่ถูกเพิ่มเข้ารายการ (Done-when ข้อนี้พอดี เพราะ E.9 เคส 8a บอกว่าเซิร์ฟเวอร์ยอมรับชื่อโมเดลที่ไม่มีอยู่) · **ของเดิมย้ายเข้ามาเอง** — `selfHostedEndpoint`/`selfHostedModel` ใน bootstrap.plist กลายเป็น endpoint แรกในทะเบียนโดยผู้ใช้ไม่ต้องทำอะไร (เห็นบนหน้าจอ: "Self-hosted · ค่าเริ่มต้น") · **API key ไม่เคยลงไฟล์** เก็บแค่*ชื่อตัวแปร* env (Keychain จริงคือ P9.2) · ตั้ง override ต่อ role ได้ในโครงสร้างแล้ว (ยังไม่มี UI) · 14 เทส · **ค้าง**: เส้นทางบันทึกสำเร็จยังไม่ได้กดจริง เพราะไม่มี endpoint ที่รันอยู่ตอนนี้ |
| **P5.6** Budget Governor | เพดาน 4 ชั้น, ประเมินก่อนยิง, เกิน → fallback/approval ([ARCH §9.5](ARCHITECTURE.md#95-budget-governor--คุมค่าใช้จ่ายของ-tier-1b)) | test: ตั้งเพดานต่ำ → ระบบไม่ยิง paid endpoint และตกไป tier อื่นเอง | ✅ **ผ่าน Done-when ด้วยเทสตรงตัว** — ตั้งเพดานต่ำ → router ข้าม paid ไปใช้ tier ที่ฟรี และงาน**สำเร็จ** ไม่ใช่ error (เกินเพดานเป็นสัญญาณ routing ตาม §9.2 ข้อ 4) · เพดานสี่ชั้น (ครั้ง/session/วัน/เดือน) ชั้นไหนถึงก่อนบล็อกก่อน และ**ข้อความบอกว่าชั้นไหน** — "เกินงบ" เฉยๆ คือข้อความที่ทำให้คนปิดระบบงบทิ้ง · **ประเมินก่อนยิง** จาก `maxTokens` (มองโลกในแง่ร้ายโดยตั้งใจ) แล้ว**ลงบัญชีจริงจาก usage ที่ endpoint คืนมา** · **endpoint ที่คิดเงินแต่ไม่ได้ตั้งราคา = ไม่ยอมใช้** ดีกว่าเดาด้วยเงินคนอื่น · `SpendLedger` อยู่ใน AgentKit (Config เขียนเพดาน, Persistence เก็บยอด, LLMProviders บังคับ — รูปแบบเดียวกับ `Embedder`) และยอดเก็บลง SurrealDB เพราะเพดานต่อวัน/เดือนที่รีเซ็ตตอนปิดแอปไม่ใช่เพดาน · 8 เทส |
| **P5.7** Cost/usage UI | แถบงบคงเหลือ + รายงานย้อนหลังต่อ session/role/โมเดล | ดูได้ว่าเงิน/token หมดไปกับอะไร | 🔶 **อยู่บนแท็บเดียวกับ endpoint** เพราะเป็นการตัดสินใจเดียวกัน (สวิตช์กับฟิวส์ควรอยู่ห้องเดียวกัน) — ช่องเพดานทั้งสี่พร้อมแถบ "ใช้ไป $X / $Y" ข้างๆ และรายงานเดือนนี้แยกตามโมเดล·role พร้อมจำนวน token · หน้าจอขึ้นถูกต้องแล้วว่า **ยังไม่มี endpoint ที่คิดเงิน เพดานจึงยังไม่มีผลกับอะไร** — ซึ่งเป็นความจริงของเครื่องนี้ · **ค้าง**: ยังไม่เคยมีตัวเลขจริงให้ดู เพราะยังไม่มี paid endpoint (จะเห็นค่าจริงเมื่อ GX10 หรือ hosted API พร้อม) · แถบงบยังไม่โผล่บนหัวแชท มีแต่ในแท็บนี้ |

---

## P6 — Analysis

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P6.1** DuckDB store | `duckdb-swift` + query API | query จริงได้จากในแอป | 🔶 **`AnalysisStore` ทำงานกับ DuckDB จริง 9 เทส ไม่มี stub** — เปิดไฟล์/in-memory, query, `SHOW TABLES`, schema, import CSV/Parquet · **ผลลัพธ์เป็นค่า ไม่ใช่ cursor** — materialise ให้จบก่อนคืน ไม่มี handle ค้างข้าม await (ซ้ำรอย D6) · **NULL ไม่เท่ากับสตริงว่าง** · error แนบ SQL มาด้วยเสมอ · **สองกับดักที่เจอกับของจริง**: (1) `cast(to: String.self)` ของ duckdb-swift **ไม่ fail แต่คืน nil ทั้งคอลัมน์**ถ้าชนิดไม่ตรง — ตารางจะว่างเปล่าแทนที่จะ error จึงต้องอ่านตามชนิดที่ DuckDB บอก แล้วให้ DuckDB เอง `CAST(... AS VARCHAR)` เฉพาะชนิดที่เราไม่ได้รองรับ (list/struct/interval) (2) **`DESCRIBE` รับ quoted identifier ที่มี `\"` ไม่ได้** ("Unterminated quote in qualified name") ตารางที่ชื่อแปลกจึงสร้างได้แต่ describe ไม่ได้ → อ่านจาก `information_schema` ด้วย **bound parameter** แทน ซึ่งไม่มีปัญหาการ quote ตั้งแต่แรก · **หน้าจอมีแล้ว** — DB Explorer อยู่ในหน้าวิเคราะห์ (รายชื่อตาราง กางดูคอลัมน์และชนิด แยกภายใน/ภายนอกตาม P10.12) ข้อความเดิมในแถวนี้เขียนไว้ก่อนที่หน้าจอนั้นจะถูกสร้าง |
| **P6.2** Extension + federated query | ยืนยัน `INSTALL/LOAD` จาก Swift (postgres/sqlite/mssql scanner) | ต่อ external DB จริงแล้วดึงตารางได้ | ✅ **ผ่าน Done-when กับฐานข้อมูลภายนอกจริง** — สร้างไฟล์ SQLite ด้วย `/usr/bin/sqlite3` (ของที่เครื่องมีอยู่ ไม่ใช่ของที่เราเขียนเอง) → `INSTALL/LOAD sqlite_scanner` จาก Swift → `ATTACH` → เห็น schema → **join ตาราง local กับ remote ในคำสั่งเดียว** → `pull` เข้ามาเป็นสำเนา → `DETACH` แล้วสำเนายังอยู่ · 5 เทส · **attach เป็น read-only โดยปริยาย** และมีเทสยืนยันว่า INSERT ลงฐานที่ attach มาถูกปฏิเสธ — ข้อมูลของคนอื่นบ่อยกว่าของเรา · **ค้าง**: postgres/mysql ใช้เส้นทางเดียวกันเป๊ะ (สอง statement เหมือนกัน) แต่**ยังไม่ได้ยืนยันกับ server จริง** เพราะยังไม่มี · mssql ไม่มี extension ทางการ ([§12.2](ARCHITECTURE.md#122-db-connectors) เขียนไว้แล้วว่าไปทาง ATTACH) · การโหลด extension ครั้งแรกต้องมีเน็ต — เทสจึง skip ดังๆ ถ้าโหลดไม่ได้ |
| **P6.3** DB connectors | PG/MySQL/SQLite/MSSQL + MongoDB (native) + scope | เพิ่ม connector ผ่าน UI แล้ว explore schema ได้ | 🔶 **ตรรกะครบ 7 เทส · หน้าจอมีแล้วแต่ยังไม่ได้กด (U19)** — P6.2 พิสูจน์กลไกไว้แล้ว สิ่งที่ขาดคือทุกอย่างรอบๆ: **แหล่งที่บันทึกไว้แล้วยังอยู่พรุ่งนี้** (ไฟล์ `connectors.json` ข้างๆ สมุดงาน), **มี scope ต่อแหล่ง** (โปรเจกต์เห็นของตัวเอง + ของส่วนกลาง ไม่เห็นของโปรเจกต์อื่น), และ **รหัสผ่านไม่ได้อยู่ในไฟล์** — เก็บแค่*ชื่อตัวแปรสภาพแวดล้อม* รูปแบบเดียวกับทะเบียน endpoint (§9.3/P5.5) เทสเปิดไฟล์อ่านแล้วยืนยันว่าไม่มีคำว่า `password=` · **จุดที่ต้องมีเทสจริงๆ คือการรั่วของรหัสผ่าน**: `AnalysisError.queryFailed` แนบ SQL มาด้วยตามที่ออกแบบไว้ และ DuckDB เองก็คืน statement ที่ล้มเหลวกลับมาในข้อความ — ซึ่งสำหรับ `ATTACH 'host=… password=…'` แปลว่ารหัสผ่านสดๆ จะไปโผล่ใน log, ใน span และบนหน้าจอ · ตอนนี้ `AnalysisStore` มี `secretInFlight` ที่ลบรหัสผ่านออกจากทั้งบรรทัด log และ error ที่โยน (เทสยิงผ่านเส้นทางจริงกับ host ที่ไม่มีอยู่ แล้วยืนยันว่าข้อความที่ได้ไม่มีรหัสผ่าน) · **ต่อเมื่อสั่ง ไม่ใช่ตอนบูต** — การ attach ยื่นออกเน็ตและอาจต้องใช้รหัสผ่าน ทั้งสองอย่างไม่ควรอยู่ในเส้นทางของการเปิดหน้าจอ หน้าจอบอกสถานะแทนว่าต่ออยู่หรือไม่ · ปุ่ม "ต่อ" ถูก disable พร้อมบอกชื่อตัวแปรที่ยังไม่ได้ตั้ง แทนที่จะให้ไปล้มที่ไดรเวอร์ · ดึงตารางเข้ามาวิ่งผ่าน `NotebookRunner` เส้นเดียวกับทุกคำสั่ง จึงถูก `SQLGuard` ถามยืนยันก่อนเขียนทับ · **ค้าง**: **SQL Server และ MongoDB ยังต่อไม่ได้จริง** — ขึ้นในรายการพร้อมเหตุผล (`UnsupportedConnector`) แทนที่จะเป็นตัวเลือกที่ล้มเงียบ: DuckDB ไม่มี extension ทางการสำหรับ MSSQL และ MongoDB ต้องใช้ไดรเวอร์ native ที่ยังไม่ได้เพิ่มเป็น dependency · postgres/mysql ยังไม่ได้ยืนยันกับ server จริง (เหมือน P6.2) — เส้นทางเดียวกับ sqlite ที่พิสูจน์แล้ว |
| **P6.5** Shared SQL guard | mutating-statement confirm **sub-module เดียว** ใช้ทั้ง Notebook + DB Explorer (v1 ก็อปกัน) | มีที่เดียวจริง — grep แล้วไม่เจอโค้ดซ้ำ | ✅ **ผ่าน Done-when แบบบังคับด้วยเครื่อง ไม่ใช่ด้วยวินัย** — `SQLGuard` ตัวเดียว (18 เทส) และ `check.sh` มีกฎใหม่ที่ **fail ถ้าเจอคำกริยา mutating เป็น string literal นอก `SQLGuard.swift`** ใน `Sources/Analysis` หรือ `Sources/CoAIWorkspaceApp` · **ทั้งสามทางเรียกใช้ตัวเดียวกันจริง**: เซลล์ SQL, DB Explorer และการนำเข้า CSV ต่างวิ่งผ่าน `NotebookRunner.run` เส้นเดียว — และ **runner เป็นคนปฏิเสธ ไม่ใช่หน้าจอ** (`run()` โยน `.needsConfirmation` พร้อม assessment; เทสยืนยันว่าตอนที่ยังไม่ยืนยัน **ตารางไม่ถูกสร้างจริง** ไม่ใช่แค่คืนค่า error) · **สิ่งที่แยกตัวคุมนี้ออกจาก `split(";")`**: semicolon ในสตริง/คอมเมนต์/dollar-quote ไม่ตัดคำสั่ง · ตารางชื่อ `"delete"` ที่ quote ไว้ไม่ถูกอ่านเป็นคำกริยา · **CTE ที่ลบข้อมูล** (`WITH x AS (DELETE …)`) ไม่ใช่ read · `DELETE`/`UPDATE` ที่ไม่มี WHERE = destructive แยกจากที่มี WHERE · `CREATE OR REPLACE` = เขียนทับ · **คำสั่งที่ไม่รู้จัก = ถือว่าเปลี่ยนข้อมูล** (กฎเดียวกับ P1.7 ที่ให้ทูลที่ไม่รู้จัก = High) |
| **P6.4** Notebook kernel | Python persistent kernel (stdin/stdout protocol) + SQL cell | state คงข้าม cell; kill kernel แล้ว restart ได้ | ✅ **ผ่าน Done-when ทั้งสองข้อกับ Python จริง (10 เทส)** — `x = 41` แล้วเซลล์ถัดไป `x + 1` ได้ 42, โมดูลที่ import ไว้ยังอยู่ · restart แล้วตัวแปรหายจริง (`NameError`) และเคอร์เนลยังใช้ได้ · โปรโตคอลเป็น JSON บรรทัดละคำสั่ง ผ่าน `KernelProcess` ใหม่ใน Execution (§13 "stdin piping สำหรับ notebook kernel") · **สี่บั๊กที่เจอเพราะรันกับ Python จริง**: (1) **`exit()` ในเซลล์ฆ่าเคอร์เนล** — `site.Quitter` **ปิด `sys.stdin`** ก่อนโยน SystemExit ช่องรับคำสั่งจึงตายไปด้วย แก้ด้วยการอ่านคำสั่งจาก `os.dup(0)` ส่วนตัว แล้วชี้ fd 0 ไป `/dev/null` (2) **SIGINT ไม่ถึงลูก** — `posix_spawn` ไม่ได้ตั้ง `SETSIGDEF` ลูกจึงรับ SIG_IGN ของ SIGINT มาจากเชลล์ที่ไม่ interactive และ **CPython ไม่ติดตั้ง handler ทับของที่ถูก ignore ไว้** → `time.sleep(60)` ขัดจังหวะไม่ได้เลย (แก้ที่ `Spawn` ทั้งไฟล์ — `run_shell` ได้ผลนี้ไปด้วย) (3) เซลล์ที่เขียน fd 1 ตรงๆ ปลอมเป็นคำตอบของโปรโตคอลได้ → เก็บ fd 1 ตัวจริงไว้ส่วนตัวแล้วชี้ fd 1 ไป stderr (4) SIGPIPE ตอนเขียนหาเคอร์เนลที่ตายแล้วจะฆ่าแอปทั้งตัว · **cell ที่รันนานถูก interrupt ไม่ใช่ restart** — KeyboardInterrupt คือผลลัพธ์ และตัวแปรทั้ง session ยังอยู่ · เซลล์ SQL วิ่งผ่าน `NotebookRunner` + `SQLGuard` (P6.5) · สมุดงานเก็บเป็นไฟล์ JSON ต่อเล่ม มี scope ต่อเล่มตาม §12.5 |
| **P6.6** Statistical Verification Gate | assumption check ต่อ test → structured warning → วนกลับ Analysis Plan | รัน t-test บนข้อมูลที่ไม่ normal → ระบบเตือนและเสนอ non-parametric | 🔶 **ผ่าน Done-when ตรงตัว (33 เทส) · ยังไม่ได้กดบนหน้าจอ** — t-test บนข้อมูลเบ้ → `Shapiro–Wilk p < 0.05` → เตือนพร้อมตัวเลข → เสนอ Mann–Whitney → `requiresPlanReapproval` เป็นจริง · **โครงสร้างที่ทำให้ข้ามไม่ได้**: ไม่มีทางเรียกให้ได้ค่า p โดยไม่ได้ผลตรวจข้อสมมติมาด้วย — `StatResult` มีทางออกทางเดียวคือ `report` ซึ่งพ่วงผลตรวจเสมอ · **คณิตศาสตร์เขียนเองใน Swift ทั้งหมด** เพราะ gate ต้องทำงานบนเครื่องที่ไม่มี Python/เน็ต/แพ็กเกจ: Shapiro–Wilk (Royston AS R94), Levene แบบ Brown–Forsythe, incomplete beta/gamma สำหรับ t/F/χ², OLS + VIF + Tukey non-additivity, logistic ด้วย IRLS, Mann–Whitney/Wilcoxon/Kruskal–Wallis/Fisher's exact · **เทสเทียบกับค่าที่เปิดตารางดูได้** ไม่ใช่กับผลของตัวเอง (t(10)=2.228→0.05, F(3,10)=3.708→0.05, χ²(3)=7.815→0.05, ชาของ Fisher = 0.4857, KW = 7.2) · **ข้อสมมติที่ตรวจไม่ได้ ≠ ข้อสมมติที่ผ่าน** — proportional hazards และ linearity ของ logit รายงานเป็น "ยังไม่ได้ตรวจ" และทำให้ผลไม่ clean · ต่อถึง Analyst แล้วผ่านทูล `run_stat_test` บน `ToolGateway` (6 เทสยิงผ่าน gateway จริง) ซึ่งเป็นหลักฐานที่ Definition of Done ของ Analyst เรียกร้องอยู่แล้ว · **survival ผ่านด่านแล้ว (P19.2/P19.3)** — `StatGate.survival` มี `.survival` เป็นการทดสอบของตัวเอง และ `run_stat_test` รับ `test: survival` ⇒ ข้อความเดิมในแถวนี้ล้าสมัยตั้งแต่ P19 · ยังไม่มีหน้าจอเฉพาะ — ผลออกทางทรานสคริปต์ของทีม · ✅ **logistic รายงานค่า p รายสัมประสิทธิ์แล้ว** — Wald z = β/SE จาก inverse ตัวเดียวกับที่ fit ใช้ พร้อม OR และ 95% CI ต่อตัวแปร (รายตัวไม่ใช่ค่าเดียวของทั้งโมเดล เพราะค่าเดียวไม่บอกว่าตัวแปรไหนเป็นคนทำงาน) และไม่มีความหมายเมื่อเกิด separation ซึ่งบอกไว้แล้วในผลตรวจ |
| **P6.7** Gap Detection + Analysis Plan | parse proposal → 3 ระดับ gap → origin tag → approve เป็นก้อน | Analysis Plan ที่ approve แล้ว **ไม่มี `agent_suggested` ค้าง** | 🔶 **ผ่าน Done-when โดยบังคับที่ type (18 เทส) · ยังไม่ได้กดบนหน้าจอ** — `AnalysisPlan.approve(by:)` **โยน error ถ้ายังมี `agent_suggested` เหลืออยู่** จึงไม่มีทางมีแผนที่อนุมัติแล้วแต่ยังมีข้อเสนอค้าง · **แบ่งงานกับโมเดลให้ชัด**: โมเดลอ่านร้อยแก้วอย่างเดียว ส่วนการเทียบกับสคีมาจริงและการตัดสินว่าอะไรบล็อกการอนุมัติเป็นการเปรียบเทียบล้วน เทสได้โดยไม่ต้องมีโมเดล · gap 3 ระดับตาม §12.4: ไม่มีคอลัมน์เลย = critical · ชื่อชนกันหลายตาราง/มีแต่ชื่อใกล้เคียง = ambiguous พร้อมตัวเลือก · โครงร่างไม่ได้ระบุวิธี = assumption-needed **ซึ่งกลายเป็น decision ที่ติดป้าย `agent_suggested`** จึงบล็อกการอนุมัติผ่านกฎเดียวกัน · **ค่าที่โมเดลสรุปมาแต่ไม่ปรากฏในโครงร่างจริง = ของ agent ไม่ใช่ของโครงร่าง** (กฎเดียวกับ `RelationExtractor` §11.4 — ปลายที่ไม่อยู่ในข้อความถูกทิ้ง) · **แผนที่อนุมัติแล้วแก้ไม่ได้โดยยังอนุมัติอยู่** — แก้แล้วการอนุมัติถูกถอนเองและบันทึกเหตุผลไว้ นี่คือความหมายของ pre-registration และเป็นทางที่ §12.3 วนกลับมา (assumption ไม่ผ่าน → `methodologyChanged` → วิธีใหม่เข้ามาเป็น `agent_suggested` → ต้องมีคนยืนยันก่อนอนุมัติใหม่) · ลง SurrealDB แล้ว (ตาราง `analysis_plan`, 5 เทสกับ DB จริง) · **`doc_type` ทำแล้ว (2026-08-16, 4 เทส + 1 กฎ)** — `Provenance.documentKind` ถูกประกาศตอนอัปโหลด (picker อยู่ข้างปุ่มเพิ่มเอกสาร) และ**ไม่เคยถูกเดา**: §12.4 ใช้ `doc_type: proposal` เป็นตัวจุดชนวนแผนวิเคราะห์ ระบบที่เดาจากชื่อไฟล์จะเปลี่ยน literature review ให้กลายเป็นแผนวิเคราะห์ของใครสักคน (มีกฎห้ามอ่านเนื้อหามาตัดสิน) · "ยังไม่มีใครบอก" (`nil`) ต่างจาก "บอกแล้วว่าอื่น ๆ" และแถวที่เขียนก่อนมีฟิลด์นี้ยัง decode ได้ · หน้าวิเคราะห์เอาโครงร่างขึ้นก่อนและติดป้ายให้ **แต่ยังลิสต์ทุกเอกสาร** เพราะเอกสารเก่าไม่มีชนิด และตัวกรองที่ซ่อนมันคือการซ่อนคลังทั้งคลังจากคนที่ไม่รู้ว่าทำไม · **สมุดงาน**: เซลล์ SQL/Python สลับชนิดได้, ป้ายบอกผลกระทบก่อนกดรัน, รันทีละเซลล์หรือทั้งเล่ม (หยุดที่เซลล์แรกที่ล้ม), แถบสถานะเคอร์เนลพร้อมปุ่มเริ่ม/เริ่มใหม่/ปลดออก · **ฐานข้อมูล**: รายชื่อตารางพร้อมชนิดคอลัมน์และจำนวนแถว, กดตารางแล้วได้ `SELECT * … LIMIT 100`, นำเข้า CSV/Parquet, รายชื่อฐานที่ ATTACH อยู่ · **ตารางผลลัพธ์**: NULL แสดงเป็น NULL ไม่ใช่ช่องว่าง (ตามสัญญาของ `AnalysisStore`) และ **บอกว่าตัดที่กี่แถวจากกี่แถว** ไม่ตัดเงียบ · **หนึ่งเซลล์ = หนึ่งตารางต่อหนึ่งคำสั่ง** ไม่ซ่อนผลของคำสั่งที่ 2 และ 3 · **แผ่นยืนยันแสดงคำสั่งคำต่อคำ** ไม่ใช่จำนวน (เหตุผลเดียวกับ Conflict Card §11.6) · **ค้าง**: **ยังไม่ได้กดบนหน้าจอจริง** — จอเครื่องหลับตอนถึงคิว แล้วพอปลุกจอมีผู้ใช้ทำงานอยู่หน้าเครื่องพอดี จึงหยุดไว้ (P1.10/U1 พิสูจน์แล้วว่ารอบนี้เจอบั๊กที่เทสมองไม่เห็นเสมอ) · **ตอบแล้วด้วยการวัด (P9.6, 2026-08-12): แอปที่ sandbox มองไม่เห็น `/opt/homebrew` และ `/usr/local` เลย** ล่ามเดียวที่รันได้คือตัวจาก Command Line Tools (ไม่มี pandas/numpy) เคอร์เนลตอนนี้ resolve ไปที่ตัวนั้นแล้ว — เดิมเขียนว่า — ถ้าไม่เห็น หน้าจอจะขึ้นว่าไม่มี Python และเซลล์ SQL ยังทำงานได้ตามปกติ |

---

## P7 — Channels & DocGen

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P7.1** Telegram channel | long polling + inline keyboard approval | approve งานเสี่ยงจากมือถือได้จริง (สิ่งที่ v1 ค้างเป็น Task K1) | 🔶 **โค้ดครบและขับจบทั้งเส้นทางในเทส 13 ตัว · ยังไม่ได้ยิงกับบอทจริง** — long polling ตาม §8.1 (เป็น request ขาออก จึงไม่ต้องเปิดพอร์ต ไม่ต้องมี tunnel และไม่พังเมื่อย้ายเครือข่าย) · transport เป็น protocol ที่ฉีดเข้าไปได้ เทสจึงป้อน JSON แบบเดียวกับที่ Telegram ส่งจริงแล้วตรวจว่า channel ทำอะไรกับมัน — บทเรียนจาก P3.3: โค้ดที่รันได้เฉพาะกับ API ของคนอื่นจะถูกรันครั้งเดียวด้วยมือแล้วไม่ถูกรันอีกเลย · **การ์ดอนุมัติแสดงคำสั่งคำต่อคำ** ไม่ใช่สรุป (§5.4 — อนุมัติจากมือถือก็ยังคือการอนุมัติ) · ตอบแล้ว**แก้ข้อความเดิมทิ้งปุ่ม** เพราะปุ่มที่ยังกดได้แต่ไม่ตัดสินอะไรคือวิธีที่คนเชื่อว่าตัวเองอนุมัติสิ่งที่ไม่ได้อนุมัติ · **โทเคนอยู่ใน URL path ของทุก request** ที่ Telegram รับ ทุกอย่างที่ log จึงผ่าน `redacted` ก่อน · คำตอบวิ่งกลับไปที่ห้องที่ถาม ไม่ใช่กระจายทุกห้อง · `ok:false` มาพร้อม HTTP 200 จึงถือเป็น error ไม่ใช่คำตอบว่าง · loop ไม่ตายเพราะเน็ตล่มหนึ่งนาที · **เจ้าของมีโทเคนแล้วและจะกรอกเอง (2026-08-16)** — ที่ ระบบ → ช่องทาง → เพิ่มบอท: ตั้งชื่อที่เก็บ (เช่น `TELEGRAM_BOT_TOKEN`) พิมพ์ค่าลง `SecretField` แล้วกดบันทึกลง Keychain · แอปไม่เคยเห็นค่า ไม่มีปุ่มแสดง และไม่มี API ไหนอ่านกลับ · ต้องใส่ chat id ที่อนุญาตด้วย (ว่าง = ไม่รับใคร ไม่ใช่รับทุกคน) · มีผลรอบถัดไปที่เปิดแอป · **ค้าง**: การยิงกับ Bot API จริงเป็นของเจ้าของเครื่อง · ✅ **หน้าจอเพิ่มบัญชีทำแล้ว (2026-08-15)** ที่ ระบบ → ช่องทาง — ก่อนหน้านี้ตั้งค่าได้ทางเดียวคือแก้ไฟล์ `channels.json` + ตัวแปรสภาพแวดล้อม · ปุ่มมีแค่ อนุมัติ/ไม่อนุมัติ ไม่มี "แก้อาร์กิวเมนต์ก่อน" (§2.6) เพราะการแก้คำสั่งเชลล์บนคีย์บอร์ดมือถือคือวิธีที่การอนุมัติกลายเป็นการพิมพ์ผิด |
| **P7.2** Discord + LINE | Gateway WS / webhook+HMAC + ปุ่ม approval | ทั้ง 3 channel ใช้ `Channel` protocol เดียวกันไม่มี special case | 🔶 **ผ่าน Done-when ในเชิงโครงสร้าง (13 เทส) · ยังไม่ได้ยิงกับแพลตฟอร์มจริง** — ทั้งสามตัวเป็น `RunnableChannel` ตัวเดียวกัน ใช้ `ChannelAccount`/allow-list/`SecretStore` ชุดเดียวกัน และ `Engine` สร้างทั้งสามด้วย `switch` เดียว ไม่มี special case ที่ไหน · **Discord**: Gateway WS ตาม §8.1 (Hello op10 → Identify op2 → Heartbeat op1 → MESSAGE_CREATE) ฉีด socket ได้ เทสจึงขับ handshake จริงโดยไม่ต้องมีโทเคน · **ขอ intent `MESSAGE_CONTENT` (1<<15)** ไม่งั้น Discord ส่งข้อความมาโดยเนื้อความถูกตัดออก ทุกอย่างจะดูเหมือน prompt ว่าง · **กรองข้อความของตัวเอง** — ถ้าไม่กรอง คำตอบแรกจะกลายเป็น prompt ถัดไปแล้วบอทคุยกับตัวเองไม่จบ ซึ่งบนระบบที่รันเชลล์ได้ไม่ใช่บั๊กที่ตลก · heartbeat หยุดตามเมื่อ socket ตาย · **LINE**: เป็น channel เดียวที่ต้อง**ฟัง** (ไม่มี polling API) จึงเปิด `NWListener` เอง · **ลายเซ็นคือของจริงที่ต้องมี** — ใครก็ตามที่รู้ URL ยิงเข้ามาได้ ทุก request ต้องมี `x-line-signature` (HMAC-SHA256 ของ**ไบต์ดิบ** ใต้ channel secret) **เทียบแบบ constant-time** เพราะการคืนค่าเร็วบอกคนเดาว่าเดาถูกไปกี่ตัว · **HMAC ต้องคิดจากไบต์ที่ LINE ส่งมาจริงๆ** — เอา JSON ที่ parse แล้วมา encode ใหม่จะได้ digest คนละตัวสำหรับข้อความเดียวกัน (เทสมีเคสนี้ตรงๆ) · เทสยิงของปลอมเข้าไปแล้วยืนยันว่าได้ 401 และ**ไม่มีอะไรถึง core** · allow-list ทำงาน**หลัง**ลายเซ็นผ่าน — secret ไม่ใช่สิ่งที่ตัดสินว่าใครคุยได้ · ตอบด้วย **push API ไม่ใช่ reply token** ตาม §8.1 (token อายุราวหนึ่งนาที สั้นกว่าเทิร์นที่มี tool call กับ approval) · **สถานะจากเจ้าของเครื่อง (2026-08-16)**: **LINE — ปิดเคสว่าไม่ได้ทดสอบ** เจ้าของแอปไม่มีบัญชี LINE Official Account และการทดสอบต้องไปสมัครก่อน จึงไม่นับเป็นของค้างที่รอโค้ด แต่เป็นเรื่องที่ยังไม่ได้ตรวจและ**ห้ามรายงานว่าใช้ได้** · **Discord — เจ้าของมีโทเคนแล้วและจะกรอกเอง** ที่ ระบบ → ช่องทาง (แอปไม่เคยเห็นค่า มันลง Keychain ตรงจากช่องกรอก) · **ค้าง (โค้ด)** · **Discord ต่อกลับเองและ RESUME แล้ว (2026-08-16, 3 เทส)** — เดิม**ไม่มีการต่อกลับเลย**: read loop จบพร้อมกับ socket แล้วบอทก็เงียบจนกว่าจะเปิดแอปใหม่ ⇒ ฝาแล็ปท็อปหรือเราเตอร์ = บอทที่ "เลิกตอบ" ในสายตาคนอีกฝั่ง · ต่อกลับพร้อม backoff ทวีคูณจนถึง 60 วิ เพราะ gateway ที่กำลังปฏิเสธเราคือ gateway ที่การยิงรัวทำให้โกรธกว่าเดิม (Discord จำกัดอัตรา identify และโทษของการรัวคือแบนนานกว่าตัวเหตุขัดข้อง) · **ต่อกลับอย่างเดียวไม่พอ**: identify คือการเริ่ม session ใหม่ ซึ่ง**ทิ้งข้อความที่เข้ามาระหว่างหลุด** — มีแต่ resume (op 6 พร้อม `session_id` + `seq`) ที่ทำให้ gateway เล่นย้อนให้ · resume ไปที่ `resume_gateway_url` ของ session ไม่ใช่ประตูหน้า · op 9 (session ใช้ไม่ได้) ลืม session ทิ้ง ไม่งั้นจะ resume เข้าหา session ที่ Discord ทิ้งไปแล้ววนไม่จบ · LINE listener ยังไม่มี TLS — ของจริงต้องมี reverse proxy หรือ tunnel หน้า |
| **P7.3** Multi-account + allow-list | หลาย bot/หลาย chat ต่อ platform | test: ข้อความจาก chat นอก allow-list ถูกทิ้ง | ✅ **ผ่าน Done-when** — หลายบัญชีต่อแพลตฟอร์ม แต่ละบัญชีมี allow-list ของตัวเอง (7 เทส) · ข้อความจาก chat นอกรายการ**ไม่ถึง core และไม่ถูกตอบกลับด้วย** — การตอบคือการยืนยันว่าบอทมีอยู่จริงให้คนที่กำลังลองของ · **รายการว่าง = ไม่รับจากใครเลย ไม่ใช่รับทุกคน** ชื่อบอทเป็นสาธารณะ โทเคนคือความลับเดียว ถ้าค่าเริ่มต้นเปิดกว้าง โทเคนที่หลุดหนึ่งครั้งคือคนแปลกหน้าที่มีเชลล์ · **โทเคนไม่ได้อยู่ในไฟล์** เก็บแค่ชื่อตัวแปรสภาพแวดล้อม (รูปแบบเดียวกับ endpoint §9.3 และ connector §12.2) เทสเปิดไฟล์อ่านแล้วยืนยัน |
| **P7.4** ตรวจ invariant ข้าม channel | ทุก channel ผ่าน CoreEngine เดียวกัน | **test พิสูจน์ว่าไม่มี channel ไหนเรียก tool เองได้** (ป้องกัน v1 bug B2 ซ้ำ) | ✅ **บังคับด้วยกราฟของโมดูล ไม่ใช่ด้วยเทส** — target `Channels` ขึ้นกับ AgentKit/Config/Observability เท่านั้น **ไม่มี ToolBelt ไม่มี CoreEngine** ดังนั้น `ToolGateway` และ `AgentTool` ไม่อยู่ในสายตาของ channel เลย จะเรียกก็เรียกไม่ได้ตั้งแต่คอมไพล์ · `check.sh` ตรวจสองฝั่ง: `import` ในไฟล์ของ M4 และบรรทัด dependency ใน `Package.swift` ที่จะเปิดทางให้ · สิ่งที่ channel ทำได้กับข้อความขาเข้ามีอย่างเดียวคือส่งต่อให้ `InboundHandling` และการอนุมัติส่งให้ `ApprovalAnswering` (ทั้งคู่ประกาศไว้ใน AgentKit) · ตัวเชื่อมจริงคือ `ChannelRouter` ในแอป ซึ่งเรียก **`AgentTurnRunner` ตัวเดียวกับที่หน้าแชทเรียก** ไม่ใช่เส้นทางที่คล้ายกัน — §8.2 จึงเป็นจริงโดยโครงสร้าง |
| **P7.5** Notifier + App Intents | native notification + Siri/Shortcuts | สั่งงานจาก Shortcuts ได้ | 🔶 **โค้ดครบและพิสูจน์ถึงชั้นที่พิสูจน์ได้โดยไม่ต้องมีคนพูดกับ Siri (15 เทส)** — `AppIntentsChannel` แปลง intent เป็น `IncomingMessage` ตัวเดียวกับที่ Telegram ส่ง แล้วเข้าทาง `ChannelRouter` เดิม §8.2 จึงยังจริงโดยโครงสร้าง (โมดูลนี้ยังมองไม่เห็น gateway) · **รูปร่างต่างจาก channel อื่นตรงที่มีคนรออยู่ปลายสาย** — Shortcuts ต้องได้ค่ากลับก่อน timeout ของมันเอง จึงมี `ask()` ที่จับคู่คำตอบผ่าน conversation ที่ router bind มา และมีเทสว่าถามพร้อมกันสองคำถามไม่สลับคำตอบกัน · **approval ไม่เกิดที่นี่** ตาม §8.1 (Siri ไม่มีที่ให้ดู diff) — แต่สิ่งที่ผู้เรียกต้องไม่ได้คือความเงียบแล้วตามด้วย "หมดเวลา" จึงแยก `needsApproval` ออกจาก `timedOut` และบอกว่าให้ไปอนุมัติในแอป · **Notifier แยกส่วนตัดสินใจออกจาก `UserNotifications`** เพราะ `UNUserNotificationCenter.current()` **trap** เมื่อโปรเซสไม่มี bundle — โปรเซส `swift test` ไม่มี ตัวตัดสินใจจึงอยู่ใน M4 ที่เทสได้ ส่วนการยิงจริงอยู่ในแอป · กฎของ Notifier คือกฎของการ**ไม่**พูด: ตอบที่มาถึงตอนคนมองจออยู่ไม่ใช่ข่าว, progress ไม่เคยคุ้ม, **แต่ approval ประกาศเสมอ** (เทิร์นค้างรออยู่) และถูก**ถอนเมื่อมีคนตอบจากที่อื่น** ตาม §5.4 · **`ChannelPlatform.isLocal`** เป็นข้อยกเว้นเดียวจาก token + allow-list พร้อมเหตุผลเขียนไว้ (intent ไม่มีผู้ส่งระยะไกลให้ list) และมีเทสวนทุก platform ว่าอันอื่นยังบังคับเหมือนเดิม · **`Metadata.appintents` ถูกสร้างจริงตอน `build-app.sh`** — Siri/Shortcuts อ่าน bundle นี้ ไม่ได้อ่าน binary แอปที่ไม่มีมันคือแอปที่ไม่มี intent เลยในสายตาระบบ ต่อให้เขียน `AppIntent` ไว้กี่ตัว (ความล้มเหลวทรงเดียวกับ D6) Xcode ใส่ flag ให้เอง SwiftPM ไม่ใส่ จึงต้องขอ `-emit-const-values` ใน Package.swift แล้วให้สคริปต์รัน `appintentsmetadataprocessor` และ**ตรวจว่ามี intent จริงในไฟล์ที่ออกมา** ไม่ใช่แค่ exit 0 — ตอนนี้ได้ 3 intent พร้อมวลีไทย/อังกฤษ · **ค้าง**: ยังไม่มีใครพูดกับ Siri จริง — เทสพิสูจน์ถึง channel และ metadata แต่การที่ Shortcuts.app แสดง action จริงต้องมีคนเปิดดู · ปุ่มบน notification (อนุมัติ/ปฏิเสธจาก banner) ยังไม่ได้ทำ — ตอนนี้ banner พาไปที่แอป |
| **P7.6** DocGen engine | template + fill + docx/pptx export | สร้างเอกสารจริงเปิดด้วย Word/Keynote ได้ | ✅ **ผ่าน Done-when — เปิดได้จริงทั้งสองฟอร์แมต** (`.pptx` เปิดใน PowerPoint บนเครื่องจริงแล้ว ไม่ขึ้น repair — 2026-08-12) — เทสเขียนไฟล์จริงแล้วส่งให้ `/usr/bin/unzip -t` (ตรวจทุก entry ทุก CRC) และ `/usr/bin/textutil` (ตัวอ่าน OOXML ที่ Apple ใส่มากับเครื่อง เป็นตัวเดียวกับที่ TextEdit ใช้) แล้วข้อความไทย, marker ของ citation, บรรณานุกรม และส่วนข้อจำกัดกลับออกมาครบ — เป็นหลักฐานที่ดีที่สุดเท่าที่ได้โดยไม่ต้องมีคนดับเบิลคลิก · **zip เขียนเอง (method 0 stored)** ไม่ shell out ไป `zip` เพราะการวางไฟล์ลง temp dir แล้วเรียกโปรเซสเป็นสิ่งที่ไม่ควรอยู่ในเส้นทางของคำว่า "บันทึกเอกสาร" โดยเฉพาะในแอปที่ sandbox · CRC-32 เทียบกับ test vector มาตรฐาน (`123456789` → `0xCBF43926`) · **แยกการ render ออกจากการจัดรูปแบบ**: `DocumentBuilder` ทำเป็นบรรทัดที่มี style ส่วน writer แปลงเป็น XML ของ Word หรือ Keynote — ครึ่งที่น่าสนใจจึงเทสได้โดยไม่ต้องแตะ zip และฟอร์แมตที่สามคือ writer ใหม่ ไม่ใช่กฎชุดที่สอง · **`w:cs` ต้องตั้งคู่กับ `w:ascii`** ไม่งั้น Word เลือก fallback ให้ภาษาไทยเองแล้วสระลอย · **แหล่งที่ไม่มีผู้เขียน/ปี หยุดการสร้างเอกสาร** ตาม §14.1 ไม่ใช่เตือนข้างๆ เอกสารที่เสร็จแล้ว (ข้ามได้ต้องส่งพารามิเตอร์ ไม่ใช่ค่าเริ่มต้นที่ลืมได้) · ต่อเข้าแอปแล้วที่ปุ่ม "ส่งออก .docx" บนหน้าแผนวิเคราะห์ — pre-registration คือสิ่งที่ต้องส่งให้คนอื่น · **`.pptx` เคยขึ้น repair แล้วแก้ไปแล้ว**: แพ็กเกจแรกมีแต่ `presentation.xml` กับสไลด์ ไม่มี slideMaster/slideLayout/theme ทั้งที่สไลด์จบด้วย `masterClrMapping` ที่ต้องมี master ให้ resolve — PowerPoint เสนอ repair แล้ว**สร้าง master ให้เอง** เนื้อหาจึงกลับมาครบ ทำให้อาการดูเหมือนเรื่องความสวยงาม ทั้งที่แพ็กเกจผิดจริง · เทสเดิมมองไม่เห็นเพราะ `unzip -t` กับ `xmllint` ผ่านได้สบายบนแพ็กเกจที่ไม่สมบูรณ์ ตอนนี้จึงมีเทสที่ตรวจ **กราฟของแพ็กเกจ** แทน: ทุก relationship ชี้ไป part ที่มีจริง, ทุก part มี content type, master เข้าถึงได้จาก presentation, ทุกสไลด์มี layout พอดีหนึ่งอัน (เทสนี้ fail กับโค้ดเดิม 9 จุด) · **ค้าง**: ยังไม่มี template (P7.9) — ตอนนี้ layout มาจากโค้ด · ไม่มีรูป/ตารางในเอกสาร · สไลด์ยังวางกล่องข้อความด้วยพิกัดของตัวเอง ไม่ได้ใช้ placeholder จาก layout |
| **P7.7** Citation engine | inline citation + bibliography ตาม style | ทุกประโยคจาก KB มี citation ผูก provenance จริง | ✅ **บังคับด้วย type** — `CitedText` มี initialiser เดียวและมันรับ `Provenance` ประโยคที่ไม่มีที่มาจึงไม่ใช่สิ่งที่ใครลืมใส่ แต่เป็นสิ่งที่เขียนไม่ได้ตั้งแต่แรก · 3 style ตาม §14.1: APA อ้างด้วยชื่อ+ปี, IEEE/Vancouver อ้างด้วยเลขตามลำดับที่ปรากฏ — **เลขจึงเป็นสมบัติของเอกสาร ไม่ใช่ของประโยค** การนับจึงอยู่ที่ `Bibliography` · **บรรณานุกรมนับ "งาน" ไม่ใช่ "ประโยค"** อ้างเปเปอร์เดียวสามครั้งได้เลขเดียว · แหล่งเว็บพก URL + วันที่เข้าถึงเสมอ (§11.3 — URL เดิมเดือนหน้าพูดอีกอย่าง) · **ไม่มีปีก็เขียน `n.d.` ไม่เดา** และ `CitationAudit` ยกธงก่อน generate ตามที่ §14.1 สั่ง · **corroboration ตามชั้นของแหล่ง**: T1–T2 สองแหล่งขึ้นไป = เขียนได้เต็มปาก · แหล่งเดียวไม่ใช่การยืนยันข้ามแหล่ง · **T5 สิบแหล่งไม่เท่ากับ T1 สองแหล่ง** ต้องมี T1–T3 ค้ำอย่างน้อยหนึ่ง · 18 เทส |
| **P7.8** Limitations อัตโนมัติ | assumption (`agent_suggested`) + ข้อความที่เคยมี conflict ขึ้นบัญชีเอง | draft มีส่วน Limitations ที่ถูกต้องโดยไม่ต้องสั่ง | ✅ **ผ่าน Done-when — และเจอกับดักที่ทำให้มันเกือบเป็นศูนย์เสมอ**: พอแผนถูกอนุมัติ `agent_suggested` ทุกตัว**กลายเป็น `human_confirmed` ไปแล้ว** (นั่นคือความหมายของการอนุมัติตาม §12.4) ตัวสร้างที่อ่าน `origin` จึงจะเขียนส่วน Limitations ว่างเปล่าให้กับแผนที่ต้องการมันที่สุดพอดี → เพิ่ม `wasAgentSuggested` ที่ตั้งครั้งเดียวและไม่เคยถูกล้าง · ทุกบรรทัดในส่วนนี้เป็นการพูดซ้ำสิ่งที่ระบบบันทึกไว้แล้ว ไม่มีอะไรถูกแต่งขึ้น: ข้อสมมติจาก Analysis Plan (§12.4) · ข้อความที่เคยขัดกันจาก Conflict Ledger (§11.6 ซึ่งเก็บทั้งสองฝั่งคำต่อคำไว้เพื่อการนี้) · ความหนาแน่นของหลักฐานจากกฎ tier · ข้อสมมติทางสถิติที่ไม่ผ่านจาก §12.3 · **ข้อขัดแย้งที่ยังไม่ตัดสินไม่ถูกรายงานว่าตัดสินแล้ว** และ run ที่ไม่มีข้อจำกัด**พูดออกมาว่าไม่มี** ซึ่งเป็นคนละเรื่องกับส่วนที่ไม่มีใครเขียน · เห็นได้จริงบนหน้าแผนวิเคราะห์ ("ข้อจำกัดที่จะขึ้นในเอกสารอัตโนมัติ") — ข้อสมมติอ่านต่างออกไปเมื่อเห็นประโยคที่มันจะกลายเป็น |
| **P7.9** Template editor + auto-parse | upload ตัวอย่าง → สกัดโครงสร้าง | ใช้ template ที่ parse มาสร้างเอกสารได้ | ✅ **ผ่าน Done-when (11 เทส)** — เทสหลักวิ่งครบวง: `.docx` เข้า → ได้แม่แบบ → เท draft **คนละเรื่อง คนละลำดับ** ลงไป → เขียนออกเป็น `.docx` → อ่านหัวข้อกลับด้วย `textutil` ว่ามาตามลำดับของแม่แบบจริง · **ต้องเขียนตัวอ่านตัวที่สอง** เพราะ `DocumentReader` (P2.3) ดึงแต่ *ข้อความ* ซึ่งถูกแล้วสำหรับคลังความรู้ แต่แม่แบบต้องการสิ่งที่ตัวดึงข้อความมีหน้าที่ทิ้ง — ย่อหน้าไหนเป็นหัวข้อ · **หัวข้อถูกเขียนสามแบบ เพราะเอกสารมาจากคน**: `w:pStyle`, `w:outlineLvl`, และ**ไม่มีอะไรเลย — บรรทัดสั้นที่เป็นตัวหนาล้วน** ซึ่งเป็นแบบที่พบมากที่สุดในไฟล์ที่คนพิมพ์เอง กฎที่สามใช้ต่อเมื่อสองข้อแรกไม่เจออะไรเลย (เอกสารที่มีสไตล์จริงต้องไม่โดนเลื่อนประโยคเน้นขึ้นเป็นหัวข้อ — มีเทสคุมทั้งสองทาง) · **เทสกับไฟล์ที่ไม่ใช่ของเรา**: `textutil -convert docx` เขียน Word file จริงจาก HTML แล้วผลคือ**ไม่มี `w:pStyle` และไม่มี `outlineLvl` เลยสักที่** หัวข้อเป็นแค่ run ตัวหนา — ตัวอ่านที่รู้จักแต่มาร์กอัปสวย ๆ ของ Word จะอ่านไฟล์ที่ macOS เขียนเองไม่ออก · เจอระหว่างทาง: `OfficeWriter` วาด bullet เป็นอักขระ "• " เพื่อเลี่ยง `numbering.xml` (การตัดสินใจของ P7.6) แปลว่าตัวอ่านที่รู้จักแต่ `w:numPr` **อ่านเอกสารที่แอปนี้เขียนเองไม่ออก** จึงต้องรู้จัก bullet ที่วาดด้วยอักขระด้วย · **แม่แบบคือรูปทรง ไม่ใช่ข้อความให้ใช้ซ้ำ** — ประโยคในไฟล์ตัวอย่างคืองานเขียนจริงของคนที่ให้ไฟล์มา เก็บไว้เป็น `guidance` ให้คนกรอกอ่าน และมีเทสว่าไม่เคยหลุดเข้าเอกสารใหม่ · **เนื้อหาที่แม่แบบไม่มีที่ให้ ไม่ถูกทิ้ง** — ต่อท้ายไว้เสมอ เพราะแม่แบบคือรูปทรงที่คนเลือก ไม่ใช่ใบอนุญาตลบงานส่วนที่ไม่พอดี และการลบเงียบ ๆ ตรงนี้จะถูกพบตอนอยู่ในต้นฉบับที่ส่งไปแล้ว · หัวข้อที่ขาดถูกรายงานและใส่บรรทัด "ยังไม่มีเนื้อหา" ไว้ให้เห็น · ต่อเข้าแอปแล้วที่หน้าแผนวิเคราะห์: ปุ่มเพิ่มแม่แบบจากไฟล์ + ตัวเลือกข้างปุ่มส่งออก และตอนส่งออกจะบอกว่าหัวข้อไหนยังว่าง · **แก้แม่แบบด้วยมือได้แล้ว (2026-08-16, 6 เทส)** — เปลี่ยนชื่อหัวข้อ · สลับลำดับ · บอกว่าหัวข้อไหนไม่จำเป็น ทั้งหมดจากในแอป (เดิมทางเดียวคืออัปโหลดไฟล์ใหม่ แม่แบบที่มีหัวข้อผิดลำดับหนึ่งอันจึงเป็นแม่แบบที่ต้องทนใช้) · **กฎอยู่บน type ไม่ใช่บนหน้าจอ**: หัวข้อว่างและหัวข้อซ้ำถูกปฏิเสธ เพราะหัวข้อที่ไม่มีชื่อหาไม่เจอในเอกสารที่เสร็จแล้ว (ด่าน "หัวข้อไหนยังว่าง" จะปล่อยผ่านตลอดไป) และชื่อซ้ำทำให้คำเตือนบอกไม่ได้ว่าอันไหน · **`guidance` ติดไปกับหัวข้อเสมอ** เพราะมันคือบันทึกเดียวว่าเอกสารที่ถูกอนุมัติเขียนอะไรไว้ใต้หัวข้อนั้น และไฟล์ตัวอย่างอาจไม่อยู่แล้ว · เขียนกลับที่ตำแหน่งเดิมในรายการ ไม่ต่อท้าย (แม่แบบที่กระโดดลงล่างทุกครั้งที่แก้ชื่อ คือรายการที่จำที่ไม่ได้) · `expectsBullets` เก็บไว้แล้วแต่ยังไม่มีใครใช้บังคับรูปแบบ |

---

## P8 — Roster & UI ครบ

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P8.1** Manifest loader | agent + skill frontmatter parser ตัวเดียวกัน, validate `tools:` | ชื่อ tool ผิด → reject พร้อม error ชัดตอนโหลด | ✅ **ผ่าน Done-when (12 เทส)** — parser ตัวเดียวใช้ทั้ง agent และ skill ตาม §7.2 (สองตัวจะเพี้ยนจากกัน และสิ่งแรกที่เพี้ยนคือ `tools:` ซึ่งเป็นฟิลด์ที่ตัดสินว่าอะไรแตะอะไรได้) · **ชื่อทูลที่ไม่มีอยู่ = ปฏิเสธทั้งไฟล์ตอนโหลด พร้อมบอกชื่อที่พิมพ์ผิดและรายการทูลที่มีจริง** — ไม่ใช่คำเตือน และไม่ใช่การเงียบๆ ตัดทูลนั้นทิ้ง เพราะ agent ที่หาย `run_shell` ไปเพราะพิมพ์ผิดจะดูเหมือนโมเดลที่เลิกทำตามคำสั่ง (ปรัชญาเดียวกับ Config ที่ reject ค่าผิดตรงที่มันถูกเขียน) · `base:` สืบทูลจาก role จริง และ **ตารางนั้นเขียนที่เดียวใน `SpecialistTools`** โดยมีเทสใน `RosterTests` คอยเทียบ (Roster เข้าถึง CoreEngine ไม่ได้ตามกฎเดียวกับ channel) · ไฟล์เสียหนึ่งไฟล์ไม่ทำให้ทั้งทะเบียนหาย · **ชื่อซ้ำกันสองไฟล์ = ถอนทั้งคู่** เพราะไฟล์ไหนชนะขึ้นกับลำดับไดเรกทอรี ซึ่งไม่ใช่การตัดสินใจของใคร · จำนวนรายการและไฟล์ที่โหลดไม่ได้ขึ้นบนหน้าสถานะระบบ |
| **P8.2** Invariant: risk-sensitive → gated | คำนวณจาก tool list จริง ไม่ใช่ field ใน manifest | test: manifest ที่พยายามข้าม gate ทำไม่ได้ | ✅ **ผ่าน Done-when** — `RosterEntry.riskCeiling` คำนวณจากรายการทูลที่ resolve แล้ว โดยใช้ risk ที่ `ToolGateway` ประกาศจริง และ **ทูลที่ไม่รู้จัก = High** (กฎเดียวกับ §5.3) · **ฟิลด์ที่พยายามให้คะแนนตัวเองถูกปฏิเสธ ไม่ใช่ถูกเพิกเฉย** — `risk` / `requires_approval` / `bypass_gate` / `autonomy` / `trusted` ทำให้ทั้งไฟล์โหลดไม่ผ่านพร้อมเหตุผล เพราะไฟล์ที่เขียน `risk: low` แล้วโหลดผ่านเงียบๆ คือไฟล์ที่บอกเจ้าของมันว่าเป็นเรื่องจริง (เทสวน 5 ฟิลด์) · agent ที่ขอ `run_shell` ได้ ceiling เป็น high เสมอไม่ว่าไฟล์จะเขียนว่าอะไร |
| **P8.3** MCP client | official Swift SDK + tools/resources/prompts + stdio cwd | **integration test พิสูจน์ว่า MCP tool ปรากฏใน tool list ของ session จริง** (v1 D6) | ✅ **ผ่าน Done-when (11 เทส)** — เทสหลักต่อเซิร์ฟเวอร์จริง แล้ว**เรียกผ่าน `ToolGateway` ด้วยชื่อที่โมเดลจะใช้** (`mcp__probe__echo`) ไม่ใช่แค่ list ทูลได้ · เซิร์ฟเวอร์ปลายทาง**ไม่ใช่ของเรา** — สคริปต์ Python ที่พูดโปรโตคอลตรง ๆ เพราะการคุยกับเซิร์ฟเวอร์ที่เขียนด้วย SDK เดียวกันพิสูจน์แค่ว่า SDK ตรงกับตัวเอง · **ครบทั้ง 3 primitive**: `tools/*` เป็น agent tool, `resources/*` เป็น**ทูลเดียวต่อเซิร์ฟเวอร์** (`read_resource` — เซิร์ฟเวอร์หนึ่งมี resource ได้เป็นพัน แต่ tool list คือสิ่งที่โมเดลอ่านทั้งหมดทุกเทิร์น), `prompts/*` **ไม่เป็นทูล** และมีเทสยืนยันว่าไม่อยู่บน tool list — prompt เป็นของผู้ใช้ในช่องพิมพ์ · **ทุกทูล MCP = High เสมอ** และ `readOnlyHint` ของเซิร์ฟเวอร์ไม่มีผล ด้วยเหตุผลเดียวกับ P8.2 (ไฟล์ที่ให้คะแนนตัวเองไม่ถูกเชื่อ) — เทสยืนยันว่าไม่มีช่องทางอนุมัติ = ไม่ได้รัน · **spawn ผ่าน `Execution`** (`PipedChild`) ไม่ใช่ผ่าน SDK: เซิร์ฟเวอร์ที่ไม่ได้เกิดจาก `Spawn` คือลูกที่อยู่ใน process group ของเราเอง ซึ่งปุ่ม Stop เอื้อมไม่ถึง — มีเทสว่า shutdown แล้วโปรเซสหายจริง (`pgrep`) · **cwd ถามจากเซิร์ฟเวอร์เอง** (ทูล `where` คืน `os.getcwd()`) ไม่ใช่ตรวจค่าที่เราตั้ง · **handshake มี deadline** พร้อม stderr ของเซิร์ฟเวอร์ในข้อความ — และกับดักที่เจอตอนเขียน: task group รอลูกทุกตัวเสมอ ส่วน `client.connect` ที่ค้างรอ `initialize` ไม่สนใจ cancel เลย timeout กลายเป็นการค้างแบบเดียวกับที่มันควรกัน (เทสค้างแทนที่จะ fail) — แก้ด้วยการให้ฝั่งที่หมดเวลา `disconnect` ปลดล็อกก่อนแล้วค่อยโยน และให้ deadline เป็นเจ้าของสาเหตุเมื่อจุดชนวนแล้ว ไม่งั้นข้อความจะสลับไปมาตามภาระเครื่อง · **บั๊กที่เจอตอนขับแอปจริง ไม่ใช่ตอนเทส**: เซิร์ฟเวอร์ที่**ออกทันที**ที่ถูกสั่งรัน (เช่นขาด dependency) ทำให้ปุ่มติดตั้งเงียบจนครบ timeout เพราะ deadline เฝ้าแต่ "ความเงียบ" ส่วนเซิร์ฟเวอร์ที่ตายแล้วเป็นข้อเท็จจริงที่รู้ได้ทันที — เพิ่ม watchdog ที่เฝ้าโปรเซสคู่กับตัวจับเวลา และพา stderr ของมันมาด้วย (31.8 วิ → ต่ำกว่า 1 วิ) · ต่อเข้าแอปแล้ว: อ่าน `mcp-servers.json` → connect → register ใน gateway **ก่อน**อ่านทะเบียน P8.1 (manifest จึงอ้าง `mcp__…` ใน `tools:` ได้) และ `scripts/check.sh` เพิ่ม `MCPRegistry` เข้ารายการ "built but never wired" ซึ่งเป็นกฎที่มีอยู่เพราะ D6 |
| **P8.4** Plugin = packaged MCP server | install/list/uninstall จากโฟลเดอร์ | ติดตั้ง plugin แล้ว tool ใช้ได้ทันที | ✅ **ผ่าน Done-when (11 เทส)** — ทั้งสองครึ่งของประโยคถูกเทสแยกกัน: **"tool ใช้ได้"** = เรียกผ่าน `ToolGateway` ด้วยชื่อที่โมเดลใช้ (กับดัก D6 ตัวเดิม สูงขึ้นมาอีกชั้น — ติดตั้งได้ ลิสต์ได้ ต่อได้ แล้วยังไปไม่ถึง session) และ **"ทันที"** = เทสเริ่มจาก gateway ที่ว่างและกำลังทำงานอยู่แล้ว ติดตั้ง → connect → register → เรียกได้ในเทิร์นเดียวกัน โดยไม่มี restart · **Roster ยังแตะทูลไม่ได้** — `MCPTool` เป็น `AgentTool` และ M3 ห้ามเอื้อมถึงเหมือนที่ channel ห้าม (กฎใน Package.swift) Roster จึง*บรรยาย*แพ็กเกจเป็นข้อมูลล้วน ส่วนการต่อจริงอยู่ที่แอป และรอยต่อนั้นถูกเทสในเทสที่ import ทั้งสองฝั่ง · **แพ็กเกจต้องอยู่ในตัวเอง**: manifest ระบุล่ามบน PATH หรือไฟล์ในโฟลเดอร์ตัวเองเท่านั้น — **พาธเต็มถูกปฏิเสธ** เพราะ "ปลั๊กอิน" ที่ชี้ไปที่อื่นบนเครื่องคือทางลัดไปหาของสิ่งนั้น ไม่ใช่แพ็กเกจ และการถอนจะรายงานว่าสำเร็จทั้งที่ทิ้งของไว้ · `..` ในคำสั่งหรืออาร์กิวเมนต์ถูกปฏิเสธด้วยเหตุผลเดียวกัน · **คัดลอกเข้ามา ไม่ใช่อ้างอิง** — โฟลเดอร์ต้นทางมักอยู่ใน Downloads และปลั๊กอินที่พังเพราะเจ้าของเก็บกวาดโฟลเดอร์คือความล้มเหลวที่ไม่มีใครโยงกลับไปหาสาเหตุได้ · ชื่อซ้ำ = ปฏิเสธ (ไม่ใช่เขียนทับของที่กำลังรันอยู่) · แพ็กเกจเสียหนึ่งอันไม่ทำให้ทั้งทะเบียนหาย ตามกฎเดียวกับ P8.1 · เจอตอนเทส: manifest ที่ไม่เขียน `arguments`/`environmentVariables` ถูกปฏิเสธเพราะ Codable สังเคราะห์ให้ทุกฟิลด์บังคับ — คือการปฏิเสธไฟล์เพราะมันธรรมดา · และ `connect` ตัวเดียวไม่ได้บันทึก failure แบบที่ `connectAll` บันทึก แปลว่าเซิร์ฟเวอร์ที่พังหลังบูตจะไม่ขึ้นหน้าสถานะเลย · ต่อเข้าแอปแล้ว: ปลั๊กอินที่ติดตั้งไว้ต่อพร้อม MCP server ปกติตอนบูต และมีปุ่มติดตั้ง/ถอนบนหน้าสถานะ ซึ่งบอกด้วยว่าปลั๊กอินนั้น**ให้เครื่องมืออะไรมาบ้าง** (ติดตั้งแล้ว กับ ให้เครื่องมือกับ session แล้ว เป็นคนละเรื่อง) · **แก้แล้วใน P9.6 (2026-08-12)**: ปัญหา "ปลั๊กอินรันไม่ได้ในแอปจริง" มาจาก `/usr/bin/python3` เป็น shim ที่เรียก `xcrun` ซึ่งรันใน App Sandbox ไม่ได้ — `ExecutableSearch` หาล่ามตัวจริงก่อน และ**ยืนยันบนแอป sandbox จริงแล้วว่า `mcp__weather__forecast` ขึ้น tool list** · **ค้าง**:  — ขับหน้าจอจริง (2026-08-12): ติดตั้งสำเร็จ ไฟล์เข้า container ครบ แถวขึ้นบนหน้าสถานะ แต่เชื่อมต่อไม่ได้ เพราะ `/usr/bin/python3` เป็น shim ของ Command Line Tools ที่โปรเซสใน App Sandbox เรียกไม่สำเร็จ (สคริปต์เดียวกันรันผ่านนอก sandbox) **เทสทุกตัวรันนอก sandbox จึงมองข้อนี้ไม่เห็นเลย** — Done-when "ใช้ได้ทันที" จริงในเทส แต่ยังไม่จริงในแอปที่ sandbox ทางแก้คือแพ็กล่ามมากับแพ็กเกจหรือผ่อน entitlement ซึ่งเป็นงาน P9.6 · ไม่มี `install_plugin` ให้ agent เรียกเอง — ตอนนี้ติดตั้งเป็นการกระทำของคน · **`install_package` ยังไม่มี** (checklist เทียบ v1 ผูกไว้กับ task นี้) — `RiskScorer` จัดชั้น High ไว้แล้วและบันทึกเองว่า "ต้องมีนโยบายเน็ตก่อน" · อยู่ใน[คิวถัดไปข้อ 6](#คิวถัดไป--เรียงตามลำดับที่ตั้งใจทำ) หลัง security review · ไม่มีการตรวจลายเซ็น/ที่มาของแพ็กเกจ · หน้า Plugins เต็ม ๆ อยู่ใน P8.6 |
| **P8.5** `write_skill` | agent เขียน skill เองผ่าน gate ปกติ | skill ที่ agent เขียน โหลดกลับมาใช้ได้ | ✅ **ผ่าน Done-when (8 เทส)** — เทสหลักเขียน skill **ผ่าน `ToolGateway`** อย่างที่ agent ทำ แล้วโหลดโฟลเดอร์กลับด้วย `ManifestParser` ตัวเดียวกับที่แอปใช้ตอนบูต ไม่ใช่เช็กว่ามีไฟล์โผล่มา แต่เช็กว่า**ทะเบียนมีมัน** และได้ `riskCeiling` ที่คำนวณจากทูลจริงตาม P8.2 · **ไม่ใช่ backdoor** ตาม §7.3 — และวิธีรับประกันคือไม่ทำอะไรพิเศษเลย: เป็น `AgentTool` เหมือน `run_shell`, `check.sh` บังคับว่ามีแต่ gateway ที่เรียกทูลได้, §5.3 จัดชั้นให้เป็น Medium อยู่แล้ว มีเทสว่าไม่มีช่องทางอนุมัติ = ไม่มีไฟล์เกิดขึ้น · **กฎที่ทำให้ Done-when เป็นจริงแทนที่จะเป็นความหวัง**: ไฟล์ถูก parse ด้วย loader ตัวที่จะโหลดมันจริง **ก่อน**เขียน และถ้าจะโหลดไม่ผ่านก็ไม่เขียน — ไม่งั้น agent ถูกบอกว่าสำเร็จแล้ว skill หายไปเงียบ ๆ ตอนเปิดแอปครั้งหน้า ซึ่งเป็นความล้มเหลวที่โปรเจกต์นี้เจอมาแล้วสามครั้งในชื่ออื่น · **รายชื่อทูลอ่านตอนเรียก ไม่ใช่ตอนสร้าง** — MCP server กับ plugin มาถึงระหว่างแอปทำงาน และ skill ที่อ้างทูลของมันต้องไม่ถูกปฏิเสธเพราะทูลตัวนี้ถูกสร้างก่อนหน้านั้น (มีเทส) · `ManifestParser.text` อยู่ข้างตัว parser เพราะสองที่ที่รู้จักฟอร์แมตจะเพี้ยนจากกัน และสิ่งแรกที่เพี้ยนคือ `tools:` — และมันไม่มีพารามิเตอร์สำหรับ `risk`/`bypass_gate` เลย skill จึงเขียนฟิลด์พวกนั้นไม่ได้แม้จะพยายาม · เจอตอนเทส: ชื่อ `../evil` ถูก sanitise เป็น `---evil.md` ซึ่ง *ปลอดภัย* แต่คือการบันทึกไฟล์ในชื่อที่ไม่มีใครขอ — เปลี่ยนเป็นปฏิเสธ เพราะตัวเขียนที่เปลี่ยนชื่อให้เงียบ ๆ คือตัวเขียนที่เดาผลจาก input ไม่ได้ · เช็กที่ตัดสินได้ก่อนถามคนถูกย้ายไป `precheck` ทั้งหมด (ชื่อ, การเขียนทับ, อาร์กิวเมนต์) — การอนุมัติที่ใช้ไปกับงานที่ยังไงก็ไม่เกิดคือความสนใจที่เสียเปล่า และครั้งที่สองคนจะเลิกอ่านคำขออนุมัติ |
| **P8.6** UI ที่เหลือ | Workflow Builder, Templates, File Viewer/Editor, Processes, Settings ทุกหมวด, Models, Budget, Sources | ทุกหน้าใน [ARCH §14.2](ARCHITECTURE.md#142-workspaceui--หน้าจอทั้งหมด) ใช้งานได้ — **ย้ายไปทำรวมกับการจัดหน้าใหม่ที่ [P10.12](#p10--project-เป็น-first-class--project-management)** แทนที่จะทำสองรอบ | 🔄 **ถูกกลืนเข้า P10.12 แล้ว** — หน้าจอส่วนใหญ่ทำในนั้นและอยู่ใน `IAInventory` · ✅ **File Viewer/Editor ทำแล้ว (2026-08-15)** — แท็บ "ไฟล์" คู่กับสมุดงานใน สคริปต์+คอนโซล · เปิด/แก้/บันทึกไฟล์ข้อความและโค้ด · `.docx`/`.pptx`/`.pdf` อ่านอย่างเดียว · ตรรกะอยู่ใน `WorkspaceFiles` (M6) 12 เทส · [ขับจริงแล้ว](docs/DRIVING_LOG.md) เจอ 2 ข้อ แก้ครบ · **ค้าง**: ยังไม่ได้กดปุ่มบันทึกบนหน้าจอ (ตัวขับพิมพ์ลง `TextEditor` ไม่ได้) · **สร้าง/เปลี่ยนชื่อ/ลบไฟล์ได้แล้ว (2026-08-16, 6 เทส)** — เดิมอะไรที่ต้องมีไฟล์ใหม่ต้องผ่าน `run_shell` ⇒ "เพิ่มโน้ตหนึ่งไฟล์" กับ "รันโค้ดอะไรก็ได้" กลายเป็นการตัดสินใจเดียวกันสำหรับคนที่กดอนุมัติ · ทุกคำสั่งผ่าน `WorkspaceFiles` ที่ยึด root เป็นขอบเขต — **ชื่อคือชื่อ ไม่ใช่พาธ** (`../` ในชื่อไฟล์คือวิธีที่ "ไฟล์ใหม่" ไปโผล่ที่ที่ไม่เคยอนุญาต) ตรวจสองชั้น: ชั้นแรกเรื่องสิ่งที่พิมพ์ ชั้นสองเรื่องที่มันไปตกจริง เพราะ symlink ทำให้เป็นคนละคำถาม · **ไม่เขียนทับ** ชื่อซ้ำถูกปฏิเสธ (ไฟล์ใหม่ที่ทับของเดิมคือการลบที่ไม่มีใครสั่ง) · **ลบ = ย้ายไปถังขยะ ไม่ใช่ unlink** ซึ่งเป็นความต่างทั้งหมดระหว่างปุ่มนี้กับ `rm` ในเชลล์ และหน้าจอพูดไว้ตรงนั้น · เมนูบนแถวมี `.accessibilityAction` คู่กัน เพราะเมนูที่เอื้อมไม่ถึงไม่ใช่ฟีเจอร์ · **ค้าง**: ตัวแสดงรูป · ✅ **Workflow Builder ทำแล้ว (2026-08-15)** — แท็บ "ลำดับงาน" เสาที่สามใน สคริปต์+คอนโซล · บันทึกลำดับขั้นแล้วรันซ้ำได้ ทุกขั้นเดินผ่าน `ToolGateway` (ปิดรูที่ Phase F ของ v1 เจอ) · palette อ่านจากทูลที่ต่ออยู่จริง · ตรรกะอยู่ใน `Workflow.swift` (M1) 11 เทส · [ขับจริงแล้ว](docs/DRIVING_LOG.md) เจอ 2 ข้อ แก้ครบ · **ตั้งใจไม่ทำ node canvas แบบ n8n** — เหตุผลอยู่ในหัวไฟล์และ driving log · **ค้าง**: ยังไม่ได้เห็นการอนุมัติกลางลำดับบนหน้าจอ · ยังพิมพ์อาร์กิวเมนต์แล้วรันจนสำเร็จด้วยตัวขับไม่ได้ |
| **P8.7** Accessibility | `accessibilityLabel` ทุกปุ่ม icon-only, keyboard nav, Dynamic Type | ใช้งานด้วยคีย์บอร์ดล้วนได้ครบทุกหน้า (v1 ต้องไล่แก้ทีหลัง 16 ปุ่ม) | 🔶 **บังคับด้วยสคริปต์แล้ว ไม่ใช่ความตั้งใจ** — `scripts/accessibility-audit.py` รันใน `check.sh` และ fail build ด้วย 3 กฎ เพราะ**ข้อกำหนดที่ไม่มีอะไรบังคับคือความชอบส่วนตัว** (บทเรียน v1 ที่ต้องไล่แก้ 16 ปุ่ม 8 ไฟล์ย้อนหลัง): (1) ปุ่มที่มีแต่ไอคอนต้องมี `accessibilityLabel` — ปุ่มที่ label เป็น SF Symbol ล้วน VoiceOver จะอ่านว่า "play.fill" (2) **ห้าม `.onTapGesture` เป็นทางเดียวไปหา action** — แถวที่กดได้ด้วยเมาส์อย่างเดียวไม่มี control ให้โฟกัส action นั้นจึงไม่มีอยู่จริงสำหรับคนที่ไม่ได้ชี้เมาส์ (3) **`.contextMenu` ต้องมีประตูที่สอง** — คลิกขวาคือเมาส์ และคีย์บอร์ด Mac ส่วนใหญ่ไม่มีปุ่มเมนู ทุกกฎมี `// a11y-ok: <เหตุผล>` ให้ยกเว้นได้แบบที่เหตุผลติดไปกับบรรทัด · **พิสูจน์ว่ากฎจับได้จริง** ด้วยการใส่ violation กลับเข้าไปทั้งสามแบบแล้วดูว่า fail (กฎที่ผ่านเพราะไม่มีอะไรให้จับ ไม่มีค่า) · แก้ที่ค้างอยู่: 5 `onTapGesture` เป็น `Button(.plain)` (เปิดสมุดงาน/เปิดแผน/ใส่ SELECT จากชื่อตาราง ×2/ปิดข้อความสถานะ), 5 context menu มี `.accessibilityAction` คู่กัน, จุดสีเขียว "ต่ออยู่" ของ connector ที่เดิมสื่อด้วย**สีกับรูปทรงเท่านั้น** ตอนนี้มีข้อความ · **⌘1–⌘7 สลับหน้าจอ, ⌘0 หน้าสถานะ** — segmented picker บน toolbar คีย์บอร์ดเข้าถึงได้ต่อเมื่อเปิด Full Keyboard Access ไว้ ซึ่ง "ไปถึงได้ถ้าไปเปลี่ยนค่าในระบบก่อน" ไม่ใช่สิ่งเดียวกับ "ไปถึงได้" · Dynamic Type: คอลัมน์ป้ายชื่อที่ตรึงไว้ 150/90pt เปลี่ยนเป็น `@ScaledMetric` — ความกว้างคงที่จะตัดข้อความตัวเองทิ้งที่ขนาดใหญ่ ซึ่งคือขนาดที่คนที่ต้องใช้มันเปิดอยู่พอดี · **ตรวจด้วยตัวขับบนสิทธิ์จริงแล้ว (2026-08-16, E.30)** — `ScreenCheck` อ่าน accessibility tree ซึ่งเป็นต้นไม้เดียวกับที่ VoiceOver อ่าน: รอบแรกเจอ **6 ปุ่มไร้ชื่อ** → สี่เป็น chrome ของ AppKit (scrollbar) สองเป็นปุ่มปิด/ย่อหน้าต่าง → **แก้ของเราแล้วกวาดทั้ง 5 พื้นที่ได้ 0 ทุกพื้นที่** · สิ่งที่เจอ: `.accessibilityLabel` บน `Button` ที่มี label เป็น `Image` ล้วน**ไม่ถึง VoiceOver** (ต้องอยู่บน `Image` — แก้ 13 จุด) และ placeholder ไม่ใช่ label · เพิ่ม `subrole` ลง `ScreenElement` เพราะเป็นสิ่งเดียวที่แยกเฟอร์นิเจอร์ของระบบออกจากปุ่มของเรา · **ค้าง**: ลำดับโฟกัสยังต้องมีคนกด Tab ไล่จริง — ต้นไม้บอกว่ามีชื่อครบ ไม่ได้บอกว่าลำดับดี · เดิมเขียนว่า — สคริปต์พิสูจน์ได้ว่าไม่มี action ที่เมาส์เท่านั้นเข้าถึงได้ แต่ "ลำดับโฟกัสสมเหตุสมผลไหม" ต้องมีคนลอง · sheet/panel ที่กว้างคงที่ (460–520pt) ยังไม่ได้ปรับตาม Dynamic Type |

---

## P9 — Hardening & Release

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P9.1** Golden-task eval harness | ชุด regression จากงานจริงที่สำเร็จ | รันใน CI, จับ plan ที่เบี่ยงจาก pattern เดิมได้ | — **ยังไม่เริ่ม** · ต้องมีงานจริงที่สำเร็จแล้วพอเป็นชุด regression ก่อน |
| **P9.2** Config migration | `schema_version` + migration ตอน boot | โหลด config เวอร์ชันเก่าแล้วไม่เสียค่าเดิม | ✅ **ผ่าน Done-when (11 เทส)** — และสิ่งที่ต้องแก้คือพฤติกรรมเดิมที่ทำ *ตรงข้าม* กับ Done-when ในเคสที่ธรรมดาที่สุด: `schemaVersion` เป็นฟิลด์บังคับ ไฟล์รุ่นแรกที่ยังไม่มีคีย์นี้จึง decode ไม่ผ่าน → เข้าทาง `repairedInvalid` → **เขียนค่าเริ่มต้นทับทับของผู้ใช้** ทั้ง endpoint, โมเดล, งบ หายหมด และที่ผู้ใช้เห็นคือแอปลืมการตั้งค่า ซึ่งคือ D5 ของ v1 เป๊ะ · กฎสามข้อ ทุกข้อเกี่ยวกับการตั้งค่าของคนอื่น: (1) **ไม่มีเวอร์ชัน = เวอร์ชัน 0 ไม่ใช่ไฟล์เสีย** (2) **ไฟล์จากรุ่นที่ใหม่กว่าไม่ถูกเขียนทับเลย** — ใช้ค่าเริ่มต้นรอบนี้ยังกู้ได้ แต่เขียนทับแล้วการตั้งค่าหายจากรุ่นที่เขาจะถอยกลับไปด้วย (เทสเทียบไบต์ว่าไฟล์ไม่ถูกแตะ รวมฟิลด์ที่รุ่นนี้ไม่รู้จัก) (3) **ไม่เขียนทับอะไรโดยไม่สำรองก่อน** — สำเนาอยู่ข้างไฟล์เดิม และหน้าสถานะบอกชื่อไฟล์สำรอง เพราะ "ค่าของคุณถูกย้ายแล้ว" น่าเชื่อได้ต่อเมื่อไปดูได้ · migration เป็น **ข้อมูล ไม่ใช่ `switch`** จึงมีเทสว่าไม่มีเวอร์ชันไหนระหว่าง 1 ถึงปัจจุบันที่ไม่มีขั้นรองรับ — เวอร์ชันที่ถูกบั๊มโดยลืมเขียน migration คือการตั้งค่าที่จะหายในรีลีสหน้า · ย้าย `effectiveEndpoints` มาเป็น migration step จริง (เดิมแปลงใหม่ทุกครั้งที่อ่าน ไฟล์จึงค้างรูปเก่าตลอดไป) · **และแก้บั๊กรูปเดียวกันในสี่ store ที่เก็บ list เป็น JSON** (channels, connectors, MCP servers, templates): ไฟล์ที่ decode ไม่ผ่านคืน list ว่าง แล้ว save ครั้งถัดไปเขียนรายการเดียวทับไฟล์ที่มีห้ารายการ — ตอนนี้สำรองไฟล์ก่อนเสมอ และ**เก็บสำเนาที่เก่าที่สุดไว้** เพราะรอบที่สองไฟล์จริงอาจเป็นเวอร์ชันที่ถูกตัดไปแล้ว · **สี่ไฟล์นั้นมี schema version แล้ว (2026-08-16, 7 เทส + 1 กฎ)** — `VersionedList` เขียนเป็นซอง `{schemaVersion, items}` และอ่านได้ทั้งสองรูป · **ไม่มีเวอร์ชัน = เวอร์ชัน 0 ไม่ใช่ไฟล์เสีย** เพราะไฟล์ทุกใบที่เขียนก่อนหน้านี้เป็น array เปล่า และเป็นส่วนใหญ่ · **ไฟล์จากรุ่นใหม่กว่าไม่ถูกเขียนทับ** — `save` ปฏิเสธพร้อมเหตุผล และข้อความบอกตรงข้ามกับกรณีไฟล์เสีย: ไฟล์ไม่เป็นไร แอปนี้ต่างหากที่เก่า และ**ไม่ได้แตะไฟล์เลย** (ใช้ default หนึ่งรอบยังกู้ได้ เขียนทับแล้วการตั้งค่าหายจากรุ่นที่จะกลับไปใช้) · กฎใน check.sh จับการกลับไปใช้ `encoder.encode(list)` ตรง ๆ ซึ่งคอมไพล์ผ่าน round-trip ผ่าน และพังในรีลีสที่เพิ่มฟิลด์ · setting ในตาราง `settings` ของ SurrealDB ยังไม่มี migration path |
| **P9.3** Secrets audit | ทุก secret อยู่ Keychain, ไม่มี plaintext ที่ไหน | grep แล้วไม่เจอ token ใน DB/ไฟล์ | ✅ **ผ่าน Done-when (23 เทส + สคริปต์)** — และสิ่งที่เจอตอนตรวจใหญ่กว่าที่ Done-when ถาม: [ARCH §15](ARCHITECTURE.md) เขียนไว้ตั้งแต่ต้นว่า secret อยู่ **Keychain** แต่โค้ดอ่านจาก **environment variable** มาตลอด · วัดด้วยของจริงก่อนสรุป: `export FOO=…` แล้ว `launchctl getenv FOO` **ไม่คืนอะไรเลย** (`PATH` ก็ว่าง) — `.app` ที่เปิดจาก Finder ไม่ได้รับ environment ของเชลล์ แปลว่า **paid endpoint · บอททั้งสามตัว · connector ที่มีรหัสผ่าน · MCP server ที่ต้องใช้โทเคน ไม่มีทางได้รับความลับเลยในแอปจริง** และไม่มีหน้าจอไหนให้พิมพ์ด้วย — **D6 ครั้งที่ 8** และครั้งแรกที่เกิดกับสิ่งที่สเปกเขียนถูกอยู่แล้ว · แก้: `SecretVault` + `KeychainVault` (พิสูจน์กับ Keychain จริงด้วย probe ที่คอมไพล์แยก: add/read/update/list/delete ครบและลบตัวเองทิ้ง) · `SecretField` ให้พิมพ์ค่าได้จริงในหน้า endpoint กับ connector — **เขียนอย่างเดียว ไม่มีปุ่ม “แสดง” และไม่มี API ไหนคืนค่าที่เก็บไว้กลับมาให้ UI** เพราะค่าที่ขึ้นจอคือค่าที่อยู่ในภาพหน้าจอ · **กติกาที่แยกงานนี้ออกจากการย้ายที่เก็บเฉย ๆ**: `.unreadable` ไม่ใช่ `.absent` — Keychain ที่เปิดไม่ได้ต้องไม่ถูกรายงานว่า “ยังไม่ได้ตั้งคีย์” เพราะจะส่งคนไปพิมพ์คีย์ทับของที่มีอยู่แล้ว (กติกาเดียวกับ U21-2 และ P1.8) · และค่าที่มาจาก environment **ขึ้นสีส้ม ไม่ใช่สีเขียว** เพราะ `launchctl setenv` อ่านได้จากทุกโปรเซสของผู้ใช้คนนั้น ต่างจาก Keychain item · **การตรวจเป็นเทสจริงไม่ใช่การอ่านโค้ด**: target `SecretsAuditTests` ปลูกค่า canary แล้วขับ store ทั้งสี่ตัวผ่านการ save ปกติ จากนั้น**ค้นทุกไบต์ใต้โฟลเดอร์** (ไม่ใช่ฟิลด์ที่นึกออก) · ตัวค้นเองมีเทสว่าหาไฟล์ที่มีความลับจริงเจอ เพราะ audit ที่ fail ไม่ได้คือ audit ที่ไม่ได้พิสูจน์อะไร · `scripts/secrets-audit.sh` ตรวจโฟลเดอร์ข้อมูลจริงบนเครื่อง (รันแล้ว 76 ไฟล์ ไม่พบอะไร) และพิสูจน์แล้วว่าจับ canary ที่ปลูกไว้ได้ทั้งสามแบบ · กฎใหม่ใน `check.sh` สามข้อ พิสูจน์ว่า fail ได้ทุกข้อ · **หน้าตั้งค่าบอทมีแล้ว** (`ChannelsView` + `ChannelEditor` พร้อม `SecretField` สำหรับโทเคนและ channel secret ของ LINE) — ข้อความเดิมในแถวนี้ล้าสมัย · `SecretField` ยังไม่ได้ขับบนหน้าจอจริง — ตัวขับพิมพ์ลง SwiftUI `SecureField` ไม่ได้ (ข้อจำกัดที่บันทึกไว้แล้ว) จึงยืนยันด้วย probe กับ Keychain จริงแทน |
| **P9.4** Crash/hang resilience | sidecar ล่ม, endpoint หาย, ไฟล์เสีย, disk เต็ม | ทุกกรณีมี error ที่อ่านรู้เรื่อง ไม่ crash | ✅ **ผ่าน Done-when (15 เทส)** — ครึ่ง “ไม่ crash” **จริงอยู่แล้ว**: ทั้ง `Sources/` ไม่มี `fatalError` และไม่มี `try!` เลยนอก executable ที่ใช้ตรวจ และ sidecar มีเทสเดิม 9 ข้อครอบตั้งแต่ถูกฆ่าจากข้างนอก จำกัดจำนวน restart ไปจนถึง readiness timeout — รายงานตามจริง ไม่ได้เขียนใหม่ให้ดูเยอะ · ครึ่งที่ **ไม่จริง** คือ “error ที่อ่านรู้เรื่อง” · `ReadableFailure` แปลสี่กรณีที่ Done-when ระบุ โดย**วัดรหัสจริงก่อนเขียน** ไม่ใช่เปิดเอกสาร: ดิสก์เต็ม `NSCocoaErrorDomain 640 ← POSIX 28` (สร้าง disk image 2 MB แล้วเขียน 5 MB ใส่) · ไม่มีสิทธิ์ `513 ← 13` · ไม่มีอะไรฟัง `URLError -1004` · ไฟล์เสีย `DecodingError` · **กติกาที่สำคัญที่สุดคือคำแนะนำที่ห้ามพูด**: ดิสก์เต็มต้องไม่ลงท้ายว่า “ลองใหม่อีกครั้ง” เพราะบนดิสก์ที่เต็มนั่นคือวงวน — `isTransient` เป็น `false` และมีเทสว่าประโยคไม่มีคำนั้น · สามในสี่กรณีเทส**สร้างเงื่อนไขจริง** (โฟลเดอร์ที่เขียนไม่ได้จริง · ไบต์ที่ไม่ใช่ JSON จริง · พอร์ตที่ไม่มีใครฟังจริง) ส่วนดิสก์เต็มใช้รูปที่วัดมา เพราะเทสไม่ควรไปสร้างวอลุมบนเครื่องคนอื่น · **และช่องว่างที่เจอระหว่างทาง**: ไฟล์ list ที่พังถูกสำรองไว้ถูกต้องตั้งแต่ P9.2 แต่**ไม่เคยบอกใคร** — ร่องรอยเดียวอยู่ใน unified log สิ่งที่ผู้ใช้เจอจริงคือรายชื่อบอทว่างเปล่าเช้าวันหนึ่งโดยไม่มีเหตุผล ตอนนี้ `FileStoreIncidents` เก็บรายงานไว้ให้หน้าสถานะแสดง · `ReadableFailure.message(for:)` **ปล่อยข้อความที่เราเขียนเองผ่านไปตรง ๆ** และแปลเฉพาะของ OS — ไม่งั้นคำปฏิเสธที่ดีอยู่แล้วจะถูกยุบเป็น “ไม่สำเร็จ” เหมือนกันหมด · สถานะ sidecar บอกด้วยว่า**อะไรจะใช้ไม่ได้**ระหว่างนั้น ไม่ใช่แค่ exit code · endpoint ที่ตอบแล้วปฏิเสธคีย์ (401/403) แยกเป็น verdict ของตัวเอง เพราะ “ต่อไม่ได้: HTTP 401” ส่งคนไปตรวจเครือข่ายที่ไม่ได้เสีย |
| **P9.5** Performance pass | Instruments + MetricKit — UI ไม่ block ระหว่าง agent ทำงาน | หน้า UI ตอบสนองระหว่างงานยาววิ่งอยู่ | 🔶 **วัดแล้วและแก้ที่เจอ · Instruments/MetricKit ยังค้าง (2026-08-16, E.29 + 1 กฎ)** — เปลี่ยนคำถามให้วัดได้ก่อน: ไม่ใช่ "ลื่นไหม" แต่คือ **งานที่ถึงคิวบน main actor ถูกดองนานแค่ไหน** (เฟรมละ 16.7 ms) · วัดด้วย archive 24 MB: decode บน main actor = **ค้าง 81.6 ms (ห้าเฟรม)** · ย้ายออก = **2.6 ms** — **งานใช้เวลาเท่ากัน ต่างกันที่ใครรอ** ซึ่งคือประเด็นทั้งหมด · แก้สี่จุดที่เจอ: export/import คลังความรู้ (ไม่มีอะไรจำกัดขนาด) และการเขียน `.docx` สามที่ · **กฎ**: การอ่าน/เขียนไฟล์แบบ synchronous ในไฟล์ของแอปต้องอยู่ใน `Task.detached` (พิสูจน์ว่า fail ได้) · **ค้าง**: Instruments + MetricKit บนแอปจริงระหว่าง agent ทำงานยาว — เป็นการวัดคนละแบบและต้องมีคนนั่งดู |
| **P9.6** Packaging | bundle sidecars, notarization, ติดตั้งครั้งแรกบนเครื่องสะอาด | เครื่องใหม่ติดตั้งแล้วใช้ได้เลยไม่ต้อง setup manual | 🔶 **แก้ตัวขัดขวางที่ใหญ่ที่สุดแล้ว และวัดข้อจำกัดที่เหลือด้วยของจริง** — วัดด้วย probe ที่เซ็น App Sandbox จริงก่อนเขียนโค้ด: `/usr/bin/python3`, `/usr/bin/git`, `/usr/bin/swift` เป็น**ไบนารีเดียวกัน** (SHA-256 ตรงกัน) คือ shim ที่เรียก `xcrun` และ `xcrun` รันใน sandbox ไม่ได้ (`cannot be used within an App Sandbox`) ส่วนตัวจริงใต้ shim รันได้ปกติ — จึงเพิ่ม `ExecutableSearch` ใน M9 ที่หาล่ามตัวจริงก่อน `PATH` และใช้ร่วมกันทั้ง notebook kernel กับ MCP/plugin (7 เทส) **ผลคือปลั๊กอินที่ติดตั้งในแอปจริง (sandbox) ขึ้นทูล `mcp__weather__forecast` บน tool list ได้จริงแล้ว** ซึ่ง P8.4 ทำไม่ได้มาก่อน · **เจอ landmine ของเครื่องอื่น**: binary อ้าง resource bundle ของ SwiftPM ด้วยพาธ build directory ของเครื่องที่คอมไพล์ และ `build-app.sh` ก๊อปเข้าไปแค่ bundle ของ Metal — อีกสามอัน (รวม `swift-transformers_Hub` ที่เรียก `Bundle.module` ตอนโหลดโมเดล) ไม่ได้เข้าไป แปลว่าแอปทำงานบนเครื่องนี้และ trap ที่เครื่องแรกที่ไม่ใช่เครื่องนี้ — ก๊อปครบแล้ว · **`package-audit.sh`** ตรวจเฉพาะสิ่งที่ผ่านบนเครื่องนี้เสมอไม่ว่าจะพาไปได้หรือไม่ (resource bundle ครบตามที่ binary อ้าง, ไม่มี dylib นอกระบบ, sidecar/Metal/AppIntents ครบ, ลายเซ็น+sandbox ผ่าน) และ `build-app.sh` เรียกมันเป็นขั้นสุดท้ายเลยลืมไม่ได้ — พิสูจน์ว่ากฎจับได้จริงด้วยการเอา bundle ออกแล้วดูว่า fail · **ปิดเคสตามการตัดสินใจของเจ้าของ (2026-08-16)**: ยังไม่มีและยังไม่สมัคร Developer ID จึง**ข้ามทั้ง notarization และการทดสอบบนเครื่องสะอาด** — ไม่ใช่ของค้างรอโค้ด และแอปเซ็น ad-hoc ซึ่งใช้บนเครื่องนี้ได้ แต่ Gatekeeper เครื่องอื่นจะปฏิเสธ · เดิมเขียนว่า: **notarization ทำแทนไม่ได้** — ต้องมี Developer ID ของเจ้าของแอป ตอนนี้เซ็น ad-hoc ซึ่ง Gatekeeper เครื่องอื่นปฏิเสธ (ขั้นตอนครบเขียนไว้ใน README แล้ว) · **ยังไม่ได้ทดสอบบนเครื่องสะอาดจริง** — container ของแอปถูก SIP ป้องกัน ย้ายออกไปทดสอบ first-run ไม่ได้ และผมไม่ลบข้อมูลจริงของผู้ใช้เพื่อจำลอง จึงพิสูจน์ได้แค่ว่า bundle พึ่งตัวเองครบ + แอปเปิดและเริ่ม DB จาก sidecar ใน bundle ได้ ข้อนี้ต้องเครื่องที่สองหรือ user account ใหม่ · **แพ็กเกจ Python ภายนอกยังใช้ในแอปไม่ได้**: ใน sandbox `/opt/homebrew` กับ `/usr/local` **มองไม่เห็นเลย** (วัดแล้ว) ล่ามเดียวที่รันได้คือตัวจาก Command Line Tools ซึ่งไม่มี pandas/numpy — notebook (P6.4) ในแอปจริงจึงได้ Python 3.9 เปล่า ๆ และ venv ของ SearXNG (Homebrew 3.14) ก๊อปเข้า `.app` ไม่ได้ (P3.1) ทางแก้เดียวคือพา Python framework เข้า bundle ซึ่งเป็นงานก้อนถัดไปของ P9.6 |

---

## P10 — Project เป็น first-class + Project Management

สเปก: [ARCH §19](ARCHITECTURE.md#19-project-environment--project-management-m14-projectkit) · เรียงตามหลัก "invariant ก่อน feature" — gate กับ tolerance ต้องมาก่อนสิ่งที่ต้องใช้มัน ไม่ใช่ตามมาแก้ทีหลัง

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P10.1** Project entity + สองสภาพแวดล้อม | `project` record, สลับ General/Project ที่หัวแอป, ทุก store ที่มี scope อ่าน `ProjectID` จริงแทน `"default"` ที่ hardcode | สร้าง 2 โปรเจกต์แล้ว **เอกสาร/ledger/notebook/span ของแต่ละอันไม่ปนกัน** — ทดสอบด้วยการ query ข้ามโปรเจกต์แล้วต้องไม่เห็นของอีกฝั่ง · `grep ProjectID("default")` ไม่เจอในโค้ดแอป | 🔶 **ทำแล้วบางส่วน (2026-08-13)** — `Project`/`ProjectStage`/`ScopeStatement` อยู่ใน AgentKit, `ProjectStore` ใน Persistence (schema v3), `ProjectService` ใน **M14 ProjectKit** ตัวใหม่ · หน้า "โปรเจกต์" สร้าง/เลือก/แก้ขอบเขตได้จริงในแอป และ Chat ผูก identity กับ scope ที่เลือก ดังนั้นสลับโปรเจกต์แล้วรายการบทสนทนาเปลี่ยนตาม · `grep ProjectID("default")` เหลือแต่ในคอมเมนต์ที่อธิบายว่าเคยมี · **แยกบนดิสก์แล้ว (2026-08-13)**: `AppPaths.project(id)` ให้โฟลเดอร์ต่อโปรเจกต์ (`projects/<id>/` → analysis · notebooks · documents · files) และ `WorkspaceStoreCache` เปิด DuckDB/notebook/connector ของโปรเจกต์นั้นครั้งเดียวแล้วถือไว้ — สอง `AnalysisStore` บนไฟล์เดียวไม่ใช่ทางช้า แต่คือผู้เขียนคนที่สอง · working directory ของ `run_shell` ตั้งเป็นโฟลเดอร์ `files/` ของโปรเจกต์เอง (อยู่ใน container จึงไม่ต้องขอสิทธิ์) · **ดูแยกต่อโปรเจกต์ได้แล้ว (2026-08-16, 1 เทสกับ SurrealDB จริง)** — `recent(limit:scope:)` และหน้าสายบังคับบัญชาถามเฉพาะ workspace ที่เปิดอยู่ · เดิมหน้านั้นถูกจุดจาก span **ทุกโปรเจกต์รวมกัน** แล้วเรียกว่างานของโปรเจกต์นี้ · ไม่ระบุ scope ยังเป็นคำถามจริง ("เครื่องนี้ทำอะไรอยู่บ้าง") และต้องไม่ถูกเปลี่ยนเป็นคำตอบของโปรเจกต์เดียวเงียบ ๆ · ลบโปรเจกต์ลบแค่แถว ไม่แตะโฟลเดอร์โดยตั้งใจ (ลบไฟล์คนอื่นย้อนไม่ได้) · ยังไม่มี worktree ต่อโปรเจกต์ |
| **P10.2** Stage machine + StageGate ใน hook chain | 5 ขั้น + G1–G4, StageGate แทรกก่อน Risk ในโซ่เดิม | โปรเจกต์ที่อยู่ขั้น `initiation` เรียก tool ที่มี side effect **ถูกปฏิเสธจาก path จริงของผู้ใช้** ไม่ใช่แค่ใน unit test · เปลี่ยน stage โดยไม่ผ่าน `StageGate.evaluate` ทำไม่ได้ (structural rule ใน `check.sh`) | ✅ **ผ่าน Done-when (12 เทส)** — StageGate อยู่ในโซ่จริงระหว่าง Critic กับ Risk Scorer และเทสทุกตัวยิงผ่าน `ToolGateway.call` ซึ่งเป็นประตูเดียวที่มี · **แกนการจัดชั้นเป็น "effect" ไม่ใช่ "risk"**: `save_document` กับ `analysis_execute` เสี่ยงเท่ากัน (medium) แต่ขั้นวางแผนอนุญาตตัวแรกและปฏิเสธตัวหลัง — ความเสี่ยงตอบว่าพังได้แค่ไหน ขั้นตอบว่างานนี้ถึงคิวหรือยัง · ทูลที่ไม่มีในตารางถูกถือว่า `.mutating` (เข้มที่สุด) ไม่ใช่ปล่อยผ่าน · โปรเจกต์ที่ไม่รู้จัก และ scope โปรเจกต์ที่อ่านขั้นไม่ได้ **ถูกปฏิเสธ** ตามกฎเดียวกับ HITL ที่ว่า "ไม่มีทางถาม ไม่เท่ากับอนุญาต" · `check.sh` เพิ่ม 2 กฎ: ตาราง risk กับ effect ต้องมีคีย์ตรงกัน และ `.stage =` เขียนได้เฉพาะใน `ProjectService` · **ค้าง**: G3/G4 ตรวจได้แค่เงื่อนไขที่มีของจริงวันนี้ (ใบงาน/บทเรียนถูกส่งเข้ามาเป็นพารามิเตอร์) เงื่อนไขที่เหลือของ Closing gate 8 ข้อรอ P10.8/P10.10 · ยังไม่ได้ขับหน้าจอจริงด้วยมือ |
| **P10.3** Scope statement + Project Brief | 5 ช่องบังคับ, ร่างจาก transcript ตอน promote จาก General | ขอบเขตที่ `outOfScope` ว่าง **ผ่าน G1 ไม่ได้** · promote บทสนทนา General แล้วได้ brief ที่มีเนื้อจาก transcript จริง ไม่ใช่ฟอร์มเปล่า | ✅ **ผ่าน Done-when (8 เทส)** — `BriefDrafter` แบ่งงานแบบเดียวกับ `GapDetector`: โมเดลอ่านความ ที่เหลือคำนวณเอง จึงเทสได้เป๊ะและยังใช้งานได้เมื่อโมเดลตอบไม่ได้ · **สิ่งที่บทสนทนาไม่ได้พูด กลายเป็นคำถามค้าง ไม่ใช่การเดา** — โดยเฉพาะ `outOfScope` ที่คนแทบไม่เคยพูดเอง ถ้าเดาให้ มันจะผ่าน G1 ทั้งที่ไม่มีความหมาย · โมเดลล่ม → ร่างยังออกมาพร้อมบรรทัดแรกที่บอกว่าร่างไม่สำเร็จ (ปุ่มที่กดแล้วเงียบ แยกไม่ออกจากปุ่มที่พัง) · `ConversationStore.reassign` ย้ายบทสนทนาพร้อมข้อความเข้าโปรเจกต์ และเทสตรวจทั้ง "หายจาก General" และ "โผล่ในโปรเจกต์" เพราะการย้ายที่เพิ่มอย่างเดียวคือการคัดลอก · **ค้าง**: เอกสารที่ ingest ตอน General ยังอยู่ `central` โดยตั้งใจ (โปรเจกต์อ่าน central ได้อยู่แล้ว) · ช่อง assumptions/constraints ยังไม่มีในแผ่นร่าง |
| **P10.4** WBS + ผูกกับ Assignment/Ledger | ต้นไม้ product-based, ใบ = 1 `Assignment`, `task` เพิ่ม `work_package` | ใบที่ไม่มี DoD สร้างไม่ได้ (บังคับที่ type) · node ที่ไม่มีใบ หรือ ใบที่ไม่ผูก `scopeRef` **ทำให้ G2 ไม่ผ่าน** · เปิดงานจาก WBS แล้วเห็นรอบการทำงานจริงใน ledger ผูกกลับมาที่ใบเดิม | ✅ **ผ่าน Done-when (15 เทส)** — `WorkPackage` ใน AgentKit สะกด `acceptanceCriteria` แบบเดียวกับ `Assignment` (ไม่ optional) และ `assignment()` คืน `nil` เมื่อเกณฑ์ว่าง ⇒ ใบที่ไม่บอกว่าเสร็จแปลว่าอะไร **ส่งให้ใครไม่ได้** · G2 อ่านแผนจริง 6 เงื่อนไข รวม **สองครึ่งของกฎ 100%**: งานที่ไม่ผูกขอบเขต และขอบเขตที่ไม่มีงานรองรับ · `task.work_package` ทำให้ ledger ตอบได้สองทาง — จากแผนว่าเกิดอะไร และจาก ledger ว่ารอบนั้นเป็นสัญญาข้อไหน · ลบงานแม่ลบทั้งกิ่ง (ทิ้งลูกไว้ = สร้างสถานะ "ใบลอย" ที่ WBS รายงาน ซึ่งจะเป็นความผิดของโค้ดไม่ใช่ของแผน) · `check.sh` เพิ่มกฎ: `status = .done` เขียนได้เฉพาะใน `WorkBreakdown.complete` ที่บังคับหลักฐาน · **ปุ่ม "เปิดงานใบนี้" ทำแล้ว (2026-08-16, 4 เทส + 1 กฎ)** — ใบงานอธิบายตัวเองเป็น `Assignment` ได้ตั้งแต่ P10.4 แต่**ไม่มีอะไรสั่งเริ่มได้** แผนกับทีมจึงเชื่อมกันด้วยฟิลด์ที่ไม่มีใครกดได้ ซึ่งคือ D6 ตรง ๆ · กดแล้ว**ลงเป็นร่างของทีม ไม่ใช่เริ่มทันที** ตาม §2.6 (ใบงานที่เขียนไว้เมื่อสองสัปดาห์ก่อน คนควรได้อ่านเกณฑ์ก่อนสั่งเดิน) · **ใบนั้นติดไปเป็น work package ด้วย** ⇒ ชั่วโมงลงที่สัญญาที่มันถูกใช้ไป ไม่ใช่ลอยอยู่เฉย ๆ · ใบที่ไม่มีเกณฑ์ตรวจรับ ปุ่มถูกปิดพร้อมเหตุผล (ส่งให้ใครทำไม่ได้เพราะไม่มีอะไรให้ตรวจว่าเสร็จ) · *(dependency ระหว่างใบงานมีมาก่อนแล้วผ่าน `dependsOn` + ตัวเลือกในหน้าแผน — ข้อความเดิมในแถวนี้ล้าสมัย)* |
| **P10.5** RACI | 1 A ต่อ work package, A เป็นคนเมื่อ risk สูง, `BoardRole` ที่ agent ถือไม่ได้ | ทดสอบว่าใส่ A สองคนไม่ได้ · agent ถือ Executive ไม่ได้แม้ในเทส (คอมไพล์ไม่ผ่าน — เขียนเป็น compile-fail test หรือ structural rule) | ✅ **ผ่าน Done-when (6 เทส + 1 กฎโครงสร้าง)** — สองข้อแรกไม่ได้เทส **เพราะเทสไม่ได้**: `RACI.accountable` เป็นฟิลด์เดี่ยวไม่ optional ⇒ "A สองคน" ไม่ใช่สถานะที่เขียนลงไปได้ และ `Accountable` เป็น enum ที่ไม่มี case รับ `Role` ⇒ `.agent(.engineer)` คอมไพล์ไม่ผ่าน · `BoardRole` ไม่มี case/initializer/ฟิลด์ใดรับ `Role` และ `check.sh` fail ถ้าไฟล์นั้นเอ่ยถึง `Role` นอกเคสผู้ร่วมงาน — กันไม่ให้กฎย้ายจาก compiler ไปเป็น validator เงียบ ๆ · ข้อที่สาม (เสี่ยงสูง → A ต้องเป็นคน) เป็น judgement ไม่ใช่รูปร่าง จึงตรวจที่ G2 พร้อมเหตุผล · G1 เพิ่มเงื่อนไข "มีชื่อ Executive ที่เป็นคน" และ**ไม่เติมชื่อให้อัตโนมัติ** เพราะประเด็นของกฎคือมีคนเอาชื่อตัวเองไปใส่ · **ตาราง RACI เต็มโครงการทำแล้ว (2026-08-16, 6 เทส)** — คนอยู่แกนหนึ่ง ใบงานอยู่อีกแกน เพราะคำถามที่ RACI ถูกสร้างมาตอบเป็นคำถามข้ามแถวทั้งนั้น: ใครรับผิดชอบทุกใบ (คือคอขวดจริงของแผน) · ใครอยู่ในโครงการแล้วไม่ถือตัวอักษรไหนเลย · ใบไหนมี A แต่ไม่มี R (มีคนรับผิดชอบผลแต่ไม่มีคนทำ) — ทั้งสามข้อดูจากหน้าจอรายใบไม่เห็น · **ช่องหนึ่งแสดงทุกตัวอักษรที่ตรง ไม่ยุบเหลือตัวเดียว** เพราะคนที่เป็นทั้ง A และ C บนใบเดียวกันคือสิ่งที่คนอ่านตารางนี้กำลังตามหา · แถวถูกอ่านออกเสียงเป็นประโยค ไม่ใช่ลำดับตัวอักษร · `riskClass` ต่อใบงานยังต้องตั้งเอง ยังไม่ได้ดึงจาก `RiskScorer` |
| **P10.6** Tolerance + Exception | 6 แกน ดึงค่าจาก span/BudgetGovernor/baseline/retry_count/RiskScorer/benefit | ทำ tolerance เวลาให้ทะลุจริงในเทส แล้วต้องได้ **Exception Report ที่ส่งออกทุก channel** และทีมหยุดรับ assignment ใหม่ในขั้นนั้น · Autonomy Slider แสดงกรอบเป็นตัวเลขจริง | 🔶 **ทำแล้ว กลไกครบ ตัวเลขยังไม่ครบ (9 เทส)** — `ToleranceCheck` เป็นฟังก์ชันบริสุทธิ์ 6 แกน (benefit เป็นแกนเดียวที่ "น้อยกว่า" คือทะลุ) · **การหยุดเป็นของจริง**: exception ที่เปิดอยู่ทำให้ StageGate ปฏิเสธทูลที่ไม่ใช่การอ่าน — "หยุด" ที่ tool layer ไม่บังคับ แปลว่า "เขียนไว้ในรายงานแล้วทำต่อ" · รายงานมีรูปแบบตามมาตรฐานครบ 5 ช่อง รวม **"ต้องการจากคุณ"** ที่เปลี่ยนรายงานสถานะให้เป็นคำขอตัดสินใจ · ส่งออกทุก channel ผ่าน `ChannelRouter.broadcast` ข้อความเดียวกับที่อยู่บนจอ · breach เดิมไม่ raise ซ้ำทุกครั้งที่ตรวจ · **การหยุดรอดข้ามการเปิดแอปใหม่** (`refreshExceptions` ตอน boot) · หน้าจอแสดงกรอบเป็นตัวเลข 6 บรรทัดพร้อมหน่วยของแต่ละแกน · **ค้าง**: อ่านค่าได้จริงแค่ **ขอบเขต** (นับจากแผน) — เวลา/เงิน/คุณภาพ/ความเสี่ยง ยังเป็น 0 เพราะยังไม่ได้ต่อกับ span store, BudgetGovernor, `retry_count` และ `RiskScorer` (ไปพร้อมแถบสถานะ P10.15) · ยังไม่ได้ผูก Autonomy Slider ให้ตั้ง preset ให้อัตโนมัติ — ตอนนี้กดเลือก preset เองในหน้าโปรเจกต์ |
| **P10.7** Baseline + change control | freeze ตอน G2, versioned, ห้ามทับ | แก้แผนหลัง baseline โดยไม่มี change request ทำไม่ได้ · baseline v1 ยังอ่านได้หลังสร้าง v2 | ✅ **ผ่าน Done-when (6 เทส)** — freeze อัตโนมัติตอนผ่าน G2 และตอน**อนุมัติ**คำขอเปลี่ยนแปลง (ไม่ใช่ตอนแก้แผน — แผนที่แก้แล้วกลายเป็นข้อตกลงใหม่เองคือสิ่งที่ baseline มีไว้กัน) · **ฐานข้อมูลบังคับความไม่เปลี่ยน**: index `(project_id, version)` UNIQUE ⇒ เขียนทับเวอร์ชันเดิมไม่ได้ · `BaselineDiff` แยก "งานเดินหน้า" ออกจาก "แผนเปลี่ยน" — status ไม่นับเป็น drift แต่การแก้เกณฑ์เสร็จ/scopeRef/ชื่อ นับ · **ตัดขอบเขตไม่ใช่ปัญหาเดียวกับขอบเขตบาน** — tolerance นับเฉพาะที่เพิ่ม · G3 เพิ่ม 2 เงื่อนไข: ไม่มีคำขอที่ยังไม่ตัดสิน และแผนต้องตรงกับ baseline ล่าสุด |
| **P10.8** Registers 5 ตัว | risk/issue/change/decision/lesson + ทางเข้าจากทั้ง agent และคน | agent เสนอ risk ได้แต่ตัดสิน change ไม่ได้ (ทดสอบจาก tool layer) · lesson ที่บันทึกไหลเข้า `central` KB ตอนปิด **แล้วค้นเจอจากโปรเจกต์ถัดไป** | 🔶 **ทำแล้ว กลไกครบ ยังไม่มีทูลให้ agent (9 เทส)** — ทะเบียนใช้โครงเดียวกันทั้ง 5 (ที่มา/เจ้าของ/สถานะ/ประวัติ) ต่างที่ `RegisterDetail` ซึ่งเป็น enum ⇒ เขียน risk ด้วยฟิลด์ของ change ไม่ได้ · **`decided(approve:by:)` รับชื่อคน ไม่มีอาร์กิวเมนต์ไหนที่ส่ง `Role` เข้าไปได้** — รูปแบบเดียวกับที่นั่ง Executive คือกฎอยู่ใน signature ไม่ใช่ใน validator · change เริ่มที่ `proposed` เสมอโดยไม่ต้องมีใครจำ · lesson ไหลเข้า `central` ตอนปิดโครงการ เขียนเป็น chunk ธรรมดาพร้อม provenance แบบ `authored` ⇒ โปรเจกต์ถัดไปค้นเจอผ่าน hybrid search เส้นเดิม ไม่ใช่ทางพิเศษ · **ทูลของ agent ทำแล้ว (2026-08-16, 5 เทส + 2 กฎ)**: `raise_risk` และ `propose_change` ⇒ ตอนนี้ทดสอบ "agent เสนอได้ แต่ตัดสินไม่ได้" **จาก tool layer** ได้จริงตามที่ Done-when ขอ · **กฎถูกรักษาด้วยการไม่มีทางพูดเป็นอย่างอื่น**: `origin` มาจาก `ToolContext` ไม่ใช่จาก argument ⇒ ไม่มีพารามิเตอร์ให้ agent อ้างว่าเป็นคน ซึ่งเป็นรูปเดียวของกฎนี้ที่รอดจาก manifest ที่คนอื่นเขียน · `propose_change` เขียนสถานะ `proposed` เสมอ และ `decided(approve:by:)` ต้องมีชื่อคน อยู่อีกฝั่งของประตู ไม่มีทูลไหนเอื้อมถึง · คะแนน 1–5 ที่เกินช่วง**ถูกปฏิเสธ ไม่ใช่ตัดให้พอดี** เพราะการตัดคือการบันทึกตัวเลขที่แต่งขึ้นไว้ในช่องที่บอกว่าประเมินมา · คำขอเปลี่ยนต้องตอบครบสามด้าน ("ไม่กระทบ" เป็นคำตอบที่ใช้ได้และใช้คำเดียว) · `raise_risk` อยู่ในรายการของ**ทุก role ที่ทำงาน** — ทะเบียนที่มีแค่คนเดียวเขียนได้คือทะเบียนที่เต็มหลังเหตุการณ์ · ยังไม่ได้ค้นบทเรียนซ้ำจากโปรเจกต์ใหม่จริงในเทส end-to-end |
| **P10.9** หน้า Plan — WBS · Gantt · Kanban · RACI | Gantt 3 แกน (dependency/actual จาก span/forecast p50–p90), Kanban ผูก ledger status + WIP = `maxAssignmentsPerRound` | ลากการ์ดเข้า Done ทั้งที่ QA ไม่ผ่าน **ทำไม่ได้** · โปรเจกต์ที่ยังไม่มีประวัติแสดง "ยังไม่มีข้อมูล" แทนตัวเลขเดา · critical path ตรงกับ dependency ที่ตั้งไว้ (ทดสอบกับกราฟที่รู้คำตอบ) | 🔶 **ทำแล้วบางส่วน (10 เทส + ขับจริง)** — หน้า Plan แตกเป็น 4 sub-tab (ภาพรวม · แผนงาน+ลำดับ · กระดานงาน · ทีม&RACI) ซึ่งแก้ปัญหา "หน้ายาวจนต้องเลื่อนผ่านทุกอย่าง" ที่เจอตอนขับรอบก่อน · `dependsOn` ต่อใบงาน + critical path + ลำดับ topological + `ready` ทดสอบกับกราฟที่รู้คำตอบ · การ์ดย้ายเข้า "เสร็จ" ผ่าน `WorkBreakdown.complete` เท่านั้น จึงถูกกฎหลักฐานปฏิเสธเหมือนที่ agent โดน · **แกนที่สามทำแล้ว (2026-08-16, 6 เทส + 1 กฎ)**: `ScheduleForecast.project` เดินหน้าไปข้างหน้าตามลำดับพึ่งพา แล้วบอกว่างานที่ยังไม่เริ่มจะไปตกตรงไหน — **เป็นช่วง p50–p90 เสมอ ไม่เคยเป็นวันเดียว** เพราะวันเดียวคือคำสัญญา ส่วนช่วงคือการวัด · **R9 บังคับด้วยกฎ**: ใบที่ไม่เคยมีงานชนิดเดียวกันเสร็จ **ไม่ได้วันที่เลย** และถูกเรียกชื่อไว้ว่าประมาณไม่ได้เพราะอะไร (รายการที่แอบตัดสิ่งที่ประมาณไม่ได้ทิ้ง อ่านเหมือนรายการของงานทั้งหมด) · **ความไม่รู้ต้องลามต่อ** — ใบที่ขึ้นกับใบที่ประมาณไม่ได้ ก็ประมาณไม่ได้ ถ้านับตัวที่ไม่รู้เป็นศูนย์ ใบถัดไปจะได้วันที่ดูมั่นใจโดยไม่มีอะไรรองรับ ซึ่งเป็นความผิดที่แพงที่สุดตรงนี้ · **งานที่เริ่มไปแล้วไม่ถูกประมาณ** — ของจริงวัดได้จากแกนเวลาแล้ว การวาดคำทำนายทับของที่วัดแล้วคือการเถียงกับข้อเท็จจริง · **ข้อจำกัดที่พูดไว้ตรง ๆ**: ยังใช้ band เดียวทั้งโปรเจกต์ (จากชนิดงานที่โปรเจกต์นี้ผลิตบ่อยที่สุด) เพราะ `WorkPackage` ยังไม่ถือชนิดผลงานรายใบ · **Kanban ลากได้แล้ว (2026-08-16)** — และการหย่อนเรียก `move` ตัวเดียวกับเมนู ⇒ ลากเข้า "เสร็จ" ยังได้คำปฏิเสธเรื่องหลักฐานเหมือนเดิม (มีกฎกันไม่ให้ตั้ง status ตรง ๆ ใน drop handler) · **ค้าง**: ยังไม่มีใครลากจริงบนหน้าจอ · **แต่เหตุผลเดิมหมดไปแล้ว (2026-08-15)**: span ผูกกับใบงานแล้ว, งานระดับ assignment ถูกบันทึกเป็น span แล้ว, `SurrealSpanSink.assignments(project:)` คืนแถวที่มีเวลาเริ่ม–จบ–ใบงาน ซึ่งเป็นข้อมูลที่แถบต้องใช้ทั้งหมด และ `Schedule.estimate` มีประชากรจริงป้อนให้แล้ว · เหลือคำถามเดียวที่ยังไม่ตอบ ไม่ใช่ข้อมูลที่ยังไม่มี: ใบงานที่แตะวันจันทร์แล้วแตะอีกทีวันพฤหัส **ไม่ได้ใช้เวลาสี่วัน** แต่แถบที่พาดจากจันทร์ถึงพฤหัสบอกว่าใช้ — ต้องตัดสินก่อนว่าแถบบนแกนปฏิทินหมายถึงอะไร (ช่วงปฏิทิน + ส่วนที่ทำงานจริงระบายทึบ เป็นทางที่น่าจะถูก) · คำอธิบายบนหน้าจอแก้ให้ตรงแล้ว — เดิมเขียนว่า "span ยังไม่ผูกกับใบงาน" ซึ่งไม่จริงอีกต่อไป · **ลากการ์ดได้แล้ว (2026-08-16, 1 กฎ)** — การหย่อนเรียก `move` ตัวเดียวกับเมนู ⇒ **การลากไม่ใช่ทางลัดข้ามกฎหลักฐาน** (§19.15 ข้อ 4) ลากเข้า "เสร็จ" ได้คำปฏิเสธเดียวกับที่ agent ได้ · ทั้งคอลัมน์รับการหย่อน รวมช่องว่างใต้การ์ดสุดท้าย เพราะคอลัมน์ที่หย่อนได้เฉพาะบนการ์ดคือคอลัมน์ที่ย้ายใบแรกเข้าไม่ได้ · **ยังไม่มีใครลากจริงบนหน้าจอ** ท่ามือเป็นส่วนที่เทสไม่ได้ · (R/C/I ตั้งได้จากหน้าจอรายใบอยู่แล้ว) |
| **P10.10** Benefits + Closing gate | benefit ledger + เงื่อนไขปิด 8 ข้อ | ปิดโปรเจกต์ที่ยังมีรายการค้างข้อใดข้อหนึ่งใน 8 ข้อ **ไม่ได้** — ทดสอบครบทั้ง 8 กรณี · ปิดก่อนกำหนดโดยคนสั่ง ถูกบันทึกเป็น "ยุติ" ไม่ใช่ "สำเร็จ" | ✅ **ผ่าน Done-when (15 เทส + ขับจริง)** — เงื่อนไขปิด 8 ข้อ **ทดสอบทีละข้อ** (gate ที่ทดสอบแต่กรณี "ขาดทุกอย่าง" ผ่านได้ทั้งที่ตรวจอยู่ข้อเดียว) และขับจริงกับฐานข้อมูลก็ไล่ทีละข้อเหมือนกัน · **เงื่อนไขที่ยังตรวจไม่ได้ ต้องไม่อ่านว่าผ่าน** — `ClosingFacts` ใช้ optional สำหรับข้อขัดแย้ง/สมมติฐาน/conformance ดังนั้นการต่อ store ไม่ครบทำให้ gate 8 ข้อกลายเป็น 4 ข้อไม่ได้ กฎเดียวกับ "ไม่มีทางถาม ไม่เท่ากับอนุญาต" · **หลักฐานที่ QA ตีกลับ ไม่ใช่หลักฐาน** — ข้อ 2 อ่าน `passed` ไม่ใช่นับ array · benefit วัด**ทิศทาง** จากค่าฐาน ⇒ ประโยชน์แบบ "ลดลง" (เวลา/ต้นทุน) ไม่ถูกนับเป็นสำเร็จตอนตัวเลขแย่ลง ซึ่งเป็นครึ่งหนึ่งของ benefit จริง · ประโยชน์ที่ยังไม่วัด **ไม่ใช่ 0** — ledger คืน nil และแถบกรอบขึ้นว่า "ยังไม่ได้วัด" ⇒ business case ที่ดูดีเพราะไม่มีใครดู ถูกกันไว้ · แกนประโยชน์อ่านจาก ledger ที่ service ถือเอง ไม่ใช่จากค่าที่หน้าจอส่งมา ⇒ **tolerance ครบ 6 จาก 6 แกน** (ปิดของค้างจาก P10.6/P10.15) · วัดผลได้หลังปิดโครงการ (post-project review) เพราะวันทบทวนมักอยู่หลังปิด · ข้อ 8 ระบบ**บันทึกการตัดสิน ไม่ลบไฟล์ให้เอง** · **ค้าง**: ปิดก่อนกำหนด (`terminate`) มีอยู่ก่อนแล้วและเทสไว้แล้ว แต่ยังไม่มีปุ่ม "ทบทวนประโยชน์" นอกโปรเจกต์ (ปิดแล้วต้องเข้าโปรเจกต์เดิม) · รายงานปิดโครงการเป็น P10.11 |
| **P10.11** Reporting 3 แบบ | highlight / end-stage / end-project ผ่าน DocGen | รายงานสร้างจาก ledger+span+register จริง — เปลี่ยนข้อมูลต้นทางแล้วรายงานเปลี่ยนตาม (ไม่ใช่ข้อความที่โมเดลแต่ง) | ✅ **ผ่าน Done-when (10 เทส + 2 ด่านขับจริง)** — `ReportBuilder` เป็นฟังก์ชันบริสุทธิ์ของ (kind, inputs, now) และเทสหลักคือ**เปลี่ยนแถวเดียวแล้วรายงานเปลี่ยน** (ปิดใบงาน → หลักฐาน α = 0.74 โผล่ในรายงานเอง) ไม่ใช่การตรวจว่ามีหัวข้อครบ · **สิ่งที่ยังไม่ได้วัด ต้องพิมพ์ว่ายังไม่ได้วัด** — รายงานถูกยกไปอ้างในที่ประชุม เลข 0 ในรายงานแย่กว่าเลข 0 บนจอ · "ใหม่ตั้งแต่รายงานฉบับก่อน" อ่านจากวันที่ของฉบับก่อนจริง ⇒ รายงานรอบสองไม่ซ้ำทะเบียนทั้งชุด · **บั๊กที่การขับจับได้**: เทียบเวลาแบบ `>` ทำให้ความเสี่ยงที่เกิด*วินาทีเดียวกัน*กับรายงานฉบับก่อนหายไปเลย (ISO-8601 ที่เก็บลง DB ไม่มีเศษวินาที) — แก้เป็น `>=` เพราะรายงานซ้ำหนึ่งรายการคือเสียงรบกวน แต่รายการใหม่ที่หายคือการ escalate ที่ไม่เกิด · เพิ่มเทสกันถอยหลัง · รายงานถูก**เก็บไว้** (ตาราง `report`, append-only) ⇒ "เดือนมิถุนาเราพูดว่าอะไร" ตอบได้ และ practice `reporting` มีของจริงแทนที่จะต้อง tailoring (ขับจริงเปลี่ยนจากของจริง 11 → **12** ข้อ) · ไฟล์ `.docx` เขียนผ่าน DocGen เข้าโฟลเดอร์ documents ของโปรเจกต์ และเทสตรวจว่า**ไม่มีหัวข้อไหนหายไปตอนกลายเป็นไฟล์** · ข้อความที่ส่งเข้า channel เป็นสตริงเดียวกับที่อยู่ในไฟล์ · **รอบอัตโนมัติทำแล้ว (2026-08-16, 6 เทส)** — เลือกรอบได้ (สัปดาห์/สองสัปดาห์/เดือน/ไม่ออกเอง) และเมื่อถึงรอบ หน้าจอขึ้นแถบว่าถึงเวลาแล้ว · **ไม่มี daemon และไม่ควรมี**: แอปนี้ไม่ได้รันตอนปิดอยู่ ตัวจับเวลาที่แกล้งว่ารันคือรายงานวันอังคารที่ออกวันพฤหัส — คำถามที่ตอบได้จริงคือ "ครั้งล่าสุดออกเมื่อไร ตอนนี้ถึงรอบหรือยัง และครอบคลุมช่วงไหน" · **รอบไม่ออกรายงานเอง** มันบอกว่าถึงกำหนด ส่วนปุ่มเดิมเป็นตัวออก ⇒ ไม่มีทางที่รายงานจะมาโดยข้ามสิ่งที่การกดปุ่มตรวจ · **รอบที่ขาดถูกพูด ไม่ถูกย้อนออก** — หายไปสามสัปดาห์ = รายงานหนึ่งฉบับกับหนึ่งประโยค ไม่ใช่สามฉบับ เพราะฉบับย้อนหลังคือตัวเลขที่ใส่ไว้ใต้วันที่ที่ไม่มีใครทำงาน · โปรเจกต์ที่ยังไม่เคยออกรายงานรอให้ครบรอบแรกก่อน (รายงานที่ครอบคลุมบ่ายวันแรกไม่มีอะไรอยู่ในนั้น และสอนคนให้เลิกอ่านฉบับถัดไป) · รายงานยังไม่แนบกราฟ/ตาราง เป็นบรรทัดข้อความล้วน |
| **P10.12** IA 4 พื้นที่ + sub-tab | Chat/Plan/Workbench/Knowledge ตาม [ARCH §19.2](ARCHITECTURE.md#192-information-architecture--พื้นที่-และ-sub-tab-ของแต่ละพื้นที่) · Workbench เรียงตามเส้นทางข้อมูล (เก็บข้อมูล→DB ภายใน→DB ภายนอก→สคริปต์+คอนโซล→ผลลัพธ์) · General ได้ 3 พื้นที่ + Workbench แค่ 3 sub-tab | ทุกเนื้อหาใน [ARCH §14.2](ARCHITECTURE.md#142-workspaceui--หน้าจอทั้งหมด) ยังเข้าถึงได้ครบ (checklist ต่อรายการ ไม่มีข้อไหนหาย — กฎ R13) · Workbench ใน General ใช้ query DB ได้จริงโดยไม่ต้องสร้างโปรเจกต์ | ✅ **ผ่าน Done-when (1 กฎโครงสร้าง + 1 ด่านขับ + ผังในแอป)** — 4 พื้นที่ + ระบบ (⌘1–⌘5) โดย**หน้าจอเดิมไม่ถูกเขียนใหม่เลย**: `screenView` ยังวาดของเดิมทุกหน้า พื้นที่แค่เลือกว่าจะวาดอันไหนที่ไหน — การจัดใหม่ที่แปลว่า "ย้ายที่อยู่" ไม่ใช่ "เขียนใหม่" คือเวอร์ชันเดียวที่กฎ R13 ยอม · **checklist เป็นข้อมูล ไม่ใช่คำอ้างใน commit**: `IAInventory` มีแถวต่อหน้าใน §14.2 บอกพื้นที่ · sub-tab · **สถานะจริง** (ครบ 6 / บางส่วน 7 / ยังไม่ได้ทำ 3 พร้อมเลข task) และ `check.sh` **parse ตาราง §14.2 จาก ARCHITECTURE.md** แล้วแดงถ้าหน้าไหนไม่มีแถว ⇒ หน้าจอหายเพราะถูกลืมไม่ได้ ต้องเป็นการตัดสินใจลบ (ทดสอบกฎแล้วด้วยการแก้ชื่อให้ผิด — จับได้) · ผังนี้**ดูได้ในแอป** ที่ ระบบ → ผังหน้าจอ เพราะ checklist ที่มีแต่ developer อ่านได้ ไม่ช่วยคนที่ต้องใช้ · Workbench เรียงตามเส้นทางข้อมูล 5 sub-tab (General ได้ 3) และ **แยกฐานข้อมูลภายใน/ภายนอกด้วย focus บน explorer เดิม** ไม่ใช่เขียน pane ใหม่ · **General query DB ได้จริงโดยไม่ต้องสร้างโปรเจกต์** — พิสูจน์ด้วยด่านขับที่สร้างตาราง เขียน อ่าน ผ่าน `SQLGuard` ตัวเดียวกับที่หน้าจอใช้ · **สลับ General/Project ย้ายขึ้นหัวแอป** (เมื่อก่อนต้องออกจากงานที่ทำอยู่ไปที่แถบซ้ายของ Plan) · Chat มีแถบขวาเฝ้าดูทีมแบบเปิด/ปิดได้ (ปิดเป็นค่าเริ่มต้น — คำถามหนึ่งคำถามไม่ต้องมีจอมอนิเตอร์ข้าง ๆ) · หน้า **แหล่งและ tier** เกิดใหม่ (ทะเบียนที่ตัดสิน tier ของทุก citation มาตั้งแต่ P3 แต่ไม่เคยมีใครเห็น) · **ค้าง**: กราฟ entity ยังเป็นรายการต่อส่วนใน "เอกสาร" ไม่ใช่ภาพกราฟ (จึงมี 3 sub-tab ไม่ใช่ 4) · เก็บข้อมูลเป็นแท็บที่บอกตรง ๆ ว่ารอ P11 · File Viewer/Workflow Builder ยังไม่ได้ทำ (P8.6) · แก้ทะเบียนแหล่งยังไม่บันทึกถาวร (P13) |
| **P10.14** ประวัติบทสนทนา | แถบซ้ายของ Chat: ค้นข้อความเต็ม (BM25 ตัวเดียวกับ KB), ปักหมุด, ตั้งชื่ออัตโนมัติ+แก้ได้, เปิดคุยต่อ | ค้นคำไทยในบทสนทนาเก่าเจอจริง · เปิดบทสนทนาเก่าแล้ว tier/ไฟล์แนบ/span กลับมาครบ · ใน Project เห็นเฉพาะของโปรเจกต์นั้น + ค้นข้ามโปรเจกต์ได้จากปุ่มเดียว | ✅ **ผ่าน Done-when (5 เทส + ขับจริง)** — ค้นด้วย **BM25 + tokenizer ตัวเดียวกับคลังความรู้** เพราะภาษาไทยไม่มีช่องว่าง `CONTAINS` จึงเจอทุกอย่างหรือไม่เจอเลย ทั้งสองอย่างไม่ใช่การค้นหา · ผลลัพธ์แสดง**ข้อความที่ตรง** ไม่ใช่แค่ชื่อบทสนทนา (คำถามคือ "พูดไว้ตรงไหน" ไม่ใช่ "มีบทสนทนาอะไรบ้าง") · บทสนทนาเดียวที่ตรง 5 ข้อความ = ผลลัพธ์เดียว · ปักหมุดอยู่บนสุดโดย**ไม่นับเป็นการใช้งาน** (ถ้าปักหมุดแล้ว `updated_at` ขยับ มันจะแยกไม่ออกจากการตอบ) · ตั้งชื่อจากข้อความแรกโดยใช้ตัวช่วยตัวเดียวกับ Project Brief · ค้นข้ามโปรเจกต์เป็น**คำถามคนละข้อ ไม่ใช่ค่าเริ่มต้นที่กว้างกว่า** · **ค้นแบบ semantic ทำแล้ว (2026-08-16, 6 เทส)** — ฝั่ง vector รวมเข้ากับ BM25 ด้วย **RRF ค่าเดียวกับ knowledge index** (สองกฎสำหรับ "คำกับ vector รวมกันยังไง" จะเพี้ยน และอันที่สองคืออันที่ไม่มีใครจูน) ⇒ ค้น "ยาปฏิชีวนะก่อนผ่าตัด" เจอบทสนทนาชื่อ "เตรียมผู้ป่วยก่อนเข้าห้องผ่าตัด" ที่ไม่มีคำนั้นเลย · **หนึ่ง embedding ต่อบทสนทนา ไม่ใช่ต่อข้อความ** — ฝังตอนบทสนทนาได้ชื่อ ซึ่งเป็นจังหวะเดียวที่มีอะไรให้ฝังและคุ้มค่า · **ข้อจำกัดที่พูดตรง ๆ**: มันหาจาก*เรื่องที่คุยกัน* ไม่ใช่ทุกประโยค — วลีที่ฝังอยู่กลางบทสนทนายาว ๆ เป็นงานของการค้นด้วยคำ · ผลที่มาจาก vector ล้วนบอกว่ามาจากเรื่อง ไม่ยกบรรทัดแรกมาแสดงราวกับเป็นคำที่ตรง · **ไม่มีโมเดล embedding = ค้นด้วยคำเหมือนเดิม** ไม่ใช่ค้นไม่ได้ · มี floor ของความคล้าย เพราะ vector ทุกตัวคล้ายกันนิดหน่อยเสมอ และอันดับที่ไม่มี floor คือรายชื่อบทสนทนาทั้งหมดเรียงมั่ว ๆ · ค้นครอบคลุม 300 บทสนทนาล่าสุด |
| **P10.15** แถบสถานะเป็นแดชบอร์ดที่กดได้ | ทุกช่องเปิด popover ที่มีที่มา + ปุ่มที่ทำได้จริง ([ARCH §19.2.3](ARCHITECTURE.md#1923-แถบสถานะเป็นแดชบอร์ดที่กดได้-ไม่ใช่ป้ายบอกสถานะ)) | ตัดสิน Exception ได้จาก popover โดยไม่เปลี่ยนหน้า · กดช่องงบแล้วเห็นการแยกตาม role/ทูลจากข้อมูลจริง · ทุกการกระทำใน popover เขียน decision record | 🔶 **ครึ่งแรกทำแล้ว: ตัวเลขเป็นของจริงและมีข้อมูลไหลเข้าจริง (3 เทส + 1 ด่านขับ)** — หน้า Chat มีตัวเลือก **"ใบงานที่กำลังทำ"** (ไม่มีใน General เพราะไม่มีแผน) ⇒ เทิร์นที่เลือกใบไว้ ทั้ง span ของเทิร์นและของทุก tool call ถูกนับเข้าใบนั้น · **เลือกไม่ผูกก็ได้ และเป็นค่าเริ่มต้น** — คำถามหนึ่งคำถามไม่ใช่งานตามสัญญา การเดาให้จะเอาเวลาไปใส่ใบที่ไม่มีใครทำ · — `span` ได้คอลัมน์ `work_package` และ `ToolContext` พามันไปเอง ⇒ **ทุก tool call ที่ผ่านประตูถูกนับเข้าใบงานโดยไม่มีใครต้องจำ** · `elapsedByWorkPackage` **บวกเวลาที่ใช้จริง ไม่ใช่จับนาฬิกาจากครั้งแรกถึงครั้งสุดท้าย** (งานที่ทำสามวันไม่ได้ใช้เวลาสามวัน) · tolerance อ่านค่าได้เพิ่มจาก 1 เป็น **5 จาก 6 แกน**: เวลา (span), ค่าใช้จ่าย (spend ledger), คุณภาพ (`attempts` ใน ledger), ความเสี่ยง (riskClass ของใบที่ยังเปิด), ขอบเขต (baseline diff) — แกน **ประโยชน์** ปิดไปกับ P10.10 (2026-08-13) — อ่านจาก benefit ledger ที่ `ProjectService` ถือเอง ⇒ ครบ **6 จาก 6 แกน** และแกนที่ยังไม่มีข้อมูลต้นทางยังบอกตรง ๆ ว่ายังไม่วัด · หน้าลำดับงานแสดงเวลาที่ใช้จริงต่อใบแล้ว · *(ข้อความสองบรรทัดที่เคยอยู่ตรงนี้ — "popover ยังไม่ทำ" และ "forecast ใช้ประวัติของ role" — ล้าสมัยทั้งคู่ ดูสองย่อหน้าถัดไป)* · **ครึ่งหลังทำแล้ว (2026-08-13): แถบสถานะกดได้จริง 6 ช่อง (5 เทส + ขับจริง)** — แถบอยู่ใต้ทุกหน้าจอ (ไม่ใช่ในหน้าโปรเจกต์) เพราะเรื่องที่ตัดสินว่าจะทำอะไรต่อ เคยอยู่ห่างจากงานที่ทำอยู่หนึ่งหน้าจอเสมอ · **ตัดสิน Exception จาก popover ได้โดยไม่เปลี่ยนหน้า** และปิดโดยไม่เขียนคำตัดสินไม่ได้ · ช่องงบแยก **ตามบทบาท + ตามโมเดล** จากบัญชีจริง และบอกตรง ๆ ว่า**แยกตามทูลไม่ได้เพราะเงินคิดต่อการเรียกโมเดล ไม่ใช่ต่อทูล** — แสดง "ทูลถูกเรียกกี่ครั้ง/กี่นาที" จาก span แทน (ครึ่งที่ซื่อสัตย์ของคำถามนั้น) · ช่องคุณภาพแสดง **เหตุผลจาก QA ทุกรอบ** ไม่ใช่แค่จำนวนรอบ · ช่องขอบเขตแสดงส่วนต่างรายใบ + ปุ่มเปิดคำขอเปลี่ยนแปลง · ช่องขั้นแสดงเงื่อนไข gate พร้อมปุ่มกระโดดไปหน้าแผน (จุดประ = ยังไม่มีอะไรให้ตรวจ ไม่ใช่ผ่าน) · **ทุกการกระทำเขียนลงทะเบียน** — เป็น `StatusAction` ที่รันได้ทางเดียวผ่าน `ProjectService.perform` และเทส**วนทุก case** ไม่ใช่เช็คสามอันที่มีวันนี้ ⇒ case ที่สี่ที่ลืมบันทึกจะทำให้เทสแดง + กฎใน `check.sh` ว่าไฟล์แถบสถานะห้าม mutate ทางอื่น · **ขยายกรอบที่ทะลุ = ปิดข้อยกเว้นนั้นด้วย** (ไม่ปิด = โครงการค้างอยู่กับเพดานที่ไม่มีอยู่แล้ว) และถ้าผ่าน G2 แล้วจะเปิดคำขอเปลี่ยนแปลงให้อีกใบ · **ค้าง**: สลับ tier ของ role จาก popover ยังไม่ทำ (ไปหน้าโมเดล) · สั่ง rework เพิ่มจาก popover ยังไม่ทำ (เป็นการสั่งทีม ไม่ใช่การตั้งค่า) · forecast band ยังใช้ประวัติของ role ไม่ใช่ของ deliverableType · ✅ **แถบพยากรณ์แก้แล้ว (2026-08-15, 3 เทส)** — และสิ่งที่เจอตอนแก้แย่กว่าที่จดไว้: `durations(forRole:)` ไม่ได้กรองชนิด span เลย จึงคืน span ทุกอันที่มี role ซึ่งส่วนใหญ่คือ `tool:kb_search` — **แถบ p50–p90 บนตารางเวลาถูกคำนวณจากระยะเวลาของการเรียกทูลทีละครั้ง** และหน้าจอเรียกมันว่า "งานชนิดเดียวกันเคยใช้" · p90 ของการค้นไม่ใช่ค่าประมาณของใบงาน · แถมยังตรึงไว้ที่ `.analyst` ตายตัว โปรเจกต์ที่ไม่มีนักวิเคราะห์เลยจึงเห็นแถบที่สร้างจากงานของคนอื่น · ตอนนี้กรองเฉพาะ span ชื่อ `turn` และถามเฉพาะบทบาทที่โปรเจกต์นี้มอบหมายงานจริง · **ป้ายบอกตามจริงว่าแถบทำจากอะไร** — "เทิร์นที่เสร็จแล้วของบทบาทที่โปรเจกต์นี้ใช้" ไม่ใช่ "งานชนิดเดียวกัน" เพราะยังไม่มีอะไรบันทึกระยะเวลาต่อชนิดส่งมอบ · แถบที่ติดป้ายเป็นประชากรหนึ่งแต่สร้างจากอีกประชากรหนึ่ง คือตัวเลขที่จะถูกยกไปใส่รายงาน · **เทสเดิมยืนยันพฤติกรรมที่ผิด** — มันใช้ helper ที่สร้าง span ชื่อ `tool:run_shell` แล้วผ่าน ทั้งที่ชื่อเทสบอกว่าอ่าน "งานที่เสร็จแล้ว" · ✅ **span ระดับ assignment ทำแล้ว (2026-08-15, 18 เทส)** — `TeamOrchestrator` ไม่เคยปล่อย span เลยแม้แต่อันเดียว ทั้งที่ทุก tool call ทุก model call และทุกเทิร์นแชทปล่อย ⇒ ส่วนที่ทำงานจริงคือส่วนเดียวที่ไม่มีเวลาให้ใครอ่าน · ตอนนี้ **1 span ต่อ assignment (พ่อ) + 1 span ต่อรอบ (ลูก)** พ่อคือประชากรของแถบพยากรณ์และ**นับทุกรอบรวมรอบที่ต้องแก้** — ค่าประมาณที่นับเฉพาะงานที่ผ่านตั้งแต่แรกคือแผนที่ใช้ได้เฉพาะวันที่ไม่มีอะไรพลาด · **ปิด span ทุกทางออก ไม่ใช่แค่ทางที่สำเร็จ**: escalate ปิดเป็น `failed` (span ที่ค้าง running ไม่มี `ended_at` จึงหลุดจากทุก query ระยะเวลา ⇒ ตัดเคสที่แย่ที่สุดออกจากประชากรพอดี และหน้า process ก็ยังขึ้นว่ากำลังทำอยู่ตลอดไป) · ผู้ใช้ยกเลิกปิดเป็น `cancelled` และไม่เข้าประชากร เพราะงานที่คนสั่งหยุดกลางคันไม่ได้บอกว่างานชนิดนั้นใช้เวลาเท่าไร · **สั่งแก้โดยคนเป็น attempt ไม่ใช่ assignment ใหม่** — ไม่งั้นรอบเดียวจะเข้าไปอยู่ในประชากรของงานทั้งงานและดึงแถบให้ต่ำลงทุกครั้งที่มีคนขอแก้เล็กน้อย · `deliverable_kind` อยู่บน span พ่อเท่านั้น (บนลูกด้วย = query ที่ลืมกรองชื่อจะนับงานเดียวเป็นสี่งาน) · ✅ **แถบพยากรณ์อ่านจากงานชนิดเดียวกันจริงแล้ว** — `durations(forDeliverableKind:)` และ `ScheduleEstimate.basis` พาประชากรติดไปกับตัวเลข ⇒ ประโยคบนหน้าจออ่านจาก `basis` ไม่ใช่เขียนไว้ข้าง ๆ (มันผิดมาแล้วสองครั้งด้วยเหตุนี้) · ประเภทที่ยังมีงานเสร็จไม่ถึงสามชิ้นถอยไปใช้เทิร์น **แต่บอกบนจอว่าถอย** และบอกว่าเทิร์นเป็นหน่วยเล็กกว่างาน แถบจึงต่ำกว่าความจริง · ✅ **`LedgerRow.work_package` มีคนเขียนแล้ว** — มีคอลัมน์ มี index ตั้งแต่ P10.4 และ**ไม่มีใครเขียนเลย** (D6 อีกครั้ง) ตอนนี้หน้าทีมมีตัวเลือกใบงานแบบเดียวกับหัวแชท และมันไปถึง `run` จริง (มีกฎใน `check.sh` เพราะพารามิเตอร์มีค่า default) · ✅ **หัวหน้าทีมตามขอบเขตโปรเจกต์แล้ว** — `scope` เป็น `let` ที่ตรึงไว้ตอน boot ⇒ งานทีมทุกชิ้นในแอปถูกลงบัญชีไว้ที่ General ไม่ว่าเปิดโปรเจกต์ไหนอยู่ ทั้งที่แถบกรอบ แถบพยากรณ์ และตารางเวลา อ่านตามโปรเจกต์ทั้งหมด · **ค้าง**: สลับ tier ของ role จาก popover · สั่ง rework จาก popover |
| **P10.16** Plan แก้ inline | WBS/dependency/RACI/tolerance/ขอบเขต/ทะเบียน แก้ได้ตรงหน้า · แก้หลัง baseline ขึ้นแถบคำขอเปลี่ยนแปลงทันที | แก้ RACI ให้มี A สองคน **ทำไม่ได้จาก UI** · แก้หลัง baseline แล้วได้ change request ที่บอกผลกระทบ 3 ด้านก่อนยืนยัน · **ลากแถบ Gantt เปลี่ยนวันจบไม่ได้** (เป็นผลการวัด ไม่ใช่ค่าที่ตั้ง) | ✅ **ผ่าน Done-when (9 เทส + 2 กฎโครงสร้าง + ขับจริง)** — ทุกการแก้ในหน้า Plan เดินผ่าน `PlanEdit` 4 แบบทางเดียว แล้ว `ProjectService.apply` **แก้และเปิดคำขอเปลี่ยนแปลงในคอลเดียว** เพราะมันคือเหตุการณ์เดียว (สองคอล = คอลที่คนลืม) · **ไม่บล็อก ไม่เตือนทีหลัง** ตาม §19.2.4 — ขึ้นแถบตรงนั้นว่า "จะกลายเป็นคำขอ #N · กระทบ: ขอบเขต/เวลา/เงิน" พร้อมปุ่มยืนยัน/ยกเลิก · **ตัวอย่างผลกระทบคือการลองแก้จริง** (`ChangeControl.applying` ให้แผนที่จะได้ แล้วคิดผลกระทบจากแผนนั้น) — preview ที่คำนวณคนละทางกับ commit คือ preview ที่ผิดได้ · **ประมาณการที่ไม่มีฐานให้ประมาณ พิมพ์ว่ายังประเมินไม่ได้** ไม่ใช่ 0 ("เวลา +0" จะถูกอ้างว่า "ไม่ช้าเพิ่ม") · **tolerance ก็อยู่ใน baseline** จึงต้องผ่าน change control ด้วย — เคสนี้ `BaselineDiff` มองไม่เห็นโดยตั้งใจ ถ้าเช็คแต่ diff จะหลุด · แก้แล้วไม่เปลี่ยนอะไรที่ตกลงไว้ (สถานะงานเดิน, บันทึกค่าเดิม) **ไม่ถาม** — ถามเรื่องที่ไม่ใช่เรื่อง คือวิธีสอนคนกดผ่านแถบยืนยัน · เพิ่มช่องแก้ inline ที่ยังขาด: ชื่อใบงาน (buffer + debounce แบบเดิม) · ชนิดสิ่งที่ส่งมอบ · ระดับความเสี่ยง · เส้นพึ่งพา (toggle ไม่ใช่ลากเส้น เพราะ §19.7 มีแต่ finish-to-start) · **R/C/I** (A ไม่อยู่ในแถวปุ่มเพราะเป็นค่าเดียวใน type) · ค่ากรอบทั้ง 6 แกนพิมพ์เองได้ · **2 กฎใน `check.sh`**: ห้ามมี `DragGesture` ในหน้า Plan (วันจบเป็นผลการวัด) และห้ามหน้าจอเขียนแผนตรงเข้า store ข้าม change control · **ลากจัดลำดับได้แล้ว (2026-08-16, 6 เทส)** — เดิม `order` แก้ได้ทางเดียวคือลบใบแล้วเพิ่มใหม่ต่อท้าย แผนที่เขียนสลับลำดับเล็กน้อยจึงค้างอย่างนั้นตลอด · **ลากได้เฉพาะในหมู่พี่น้องเดียวกัน** — หย่อนข้ามพ่อแม่คือการย้ายกิ่ง ซึ่งเปลี่ยน*สิ่งที่แผนพูด* ไม่ใช่ลำดับที่มันพูด และเป็นคนละเรื่องกัน (ปฏิเสธในโมเดล ไม่ใช่ในหน้าจอ) · **เขียนใหม่ทั้งชุดที่เลขเปลี่ยน** เพราะคืนแค่แถวที่ลากทำให้แถวอื่นถือเลขที่ไม่ตรงลำดับจริง แล้วการเพิ่มใบครั้งต่อไปจะไปโผล่ผิดที่ · แถวที่ไม่ขยับไม่ถูกเขียน (การเขียนทุกแถวคือรายการ change control ทุกแถว) · ยกเลิกแถบแล้ว reload ทั้งหน้า (buffer ในจอยังค้างค่าที่พิมพ์) |
| **P10.13** Conformance + tailoring | `Practice` enum 17 ตัว (ISO 21502) + `tailoring_record` | exhaustive switch ครบทุก practice · ปิดโปรเจกต์ที่มี practice ไม่มีทั้งของจริงและ record **ไม่ได้** | ✅ **ผ่าน Done-when (6 เทส + 1 กฎโครงสร้าง)** — `Practice` 17 ตัวตอบด้วย switch ที่ไม่มี `default:` และ `check.sh` fail ถ้ามี (default arm เปลี่ยน "ทุก practice มีคำตอบ" ให้เป็น "ทุก practice มี nil เหมือนกัน") · เทสอีกครึ่งคือ **facts เต็ม ⇒ ทั้ง 17 มีของจริง** ซึ่งจับ case ที่ผูกกับข้อเท็จจริงที่เป็นจริงไม่ได้เลย — exhaustive แต่ตายตัว · `TailoringRecord.decided(by:)` รับชื่อคน ไม่มีอาร์กิวเมนต์ที่ส่ง `Role` เข้าไปได้ และ**ปฏิเสธเหตุผลว่าง** ⇒ "ไม่เกี่ยว" เฉย ๆ ไม่ผ่าน · ตารางแยก "มีของจริง" ออกจาก "ตัดสินว่าไม่ทำ" บนหน้าจอ (17 ติ๊กที่ 16 อันเป็น tailoring ไม่ใช่ conformance ที่แข็ง) — ขับจริงได้ **ของจริง 11 · บันทึกว่าไม่ทำ 6** · **ค้าง**: `communication`/`reporting` นับจาก exception ที่ส่งออกจริง + รายงาน (รายงานมาใน P10.11) · `procurement`/`orgChange` ยังไม่มีของจริงให้ตรวจอัตโนมัติ จึงตอบด้วย tailoring ตามเจตนาของมาตรฐาน |

**หมายเหตุการเรียง**: P10.12 (UI) มาท้าย ๆ โดยตั้งใจ — P8.6 เดิม (หน้าจอที่ยังไม่ได้ทำ) ถูกกลืนเข้ามาที่นี่แทนที่จะทำสองรอบ

---

## P11 — Research Program & Instruments

สเปก: [ARCH §20](ARCHITECTURE.md#20-research-program--งานวิจัยที่เดินบนโครง-pm-m15-instruments) · **ขึ้นกับ P10** — งานวิจัยเดินบนโครง PM ไม่ใช่กลไกแยก

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P11.1** Project type manifest | `research.quantitative/qualitative/mixed` + `software` + `analysis` — โหลดด้วย parser ตัวเดิมของ M3 | สร้างโปรเจกต์ type `research.quantitative` แล้วได้ WBS ตั้งต้น + roster + gate ครบตามไฟล์ · **ไม่มี parser ตัวที่สอง** (structural rule) | ✅ **ผ่าน Done-when — ขับจริงแล้ว** (2026-08-14): 6 ชนิดตาม §20.2 เป็นไฟล์ใน `Resources/project-types/` โหลดเข้า picker ได้ครบทั้ง 6 พร้อมคำอธิบายว่าเลือกแล้วจะได้อะไร · สร้าง `research.quantitative` จริงแล้วได้ **แผน 5 บท 17 รายการ** พร้อม deliverable · เกณฑ์รับงานที่มีหลักฐานกำกับทุกข้อ · role ตามไฟล์ — และ **G1 ยังไม่ผ่าน** เพราะทุกใบยัง "ยังไม่ผูก" ขอบเขตและยังไม่มีผู้รับผิดชอบ (แผนตั้งต้นที่ผ่านประตูได้เองคือแผนที่ไม่มีใครเขียน) · **ไม่มี parser ตัวที่สอง**: ใช้ frontmatter reader ตัวเดิม เพิ่มแค่ "เก็บทุกค่าของ key ที่ซ้ำ" เพื่อให้เขียน `gate:` หลายบรรทัดได้ และ `check.sh` แดงถ้ามีไฟล์อื่นใน `Sources/` ไปอ่านรั้ว `---` เอง หรือถ้า type ชี้ไป template ที่ไม่มีอยู่ (**ทดสอบแล้วว่าจับได้จริง**) · **ข้อค้นพบเชิงออกแบบ**: manifest **ตัดสิน practice ทิ้งเองไม่ได้** — §19.15 ให้เป็นการตัดสินใจที่มีชื่อคนกำกับ และ `TailoringRecord` ปฏิเสธถ้าไม่มีชื่อ ไฟล์จึงทำได้แค่ *เสนอ* พร้อมเหตุผลที่เขียนไว้ให้ · **gate ที่ไฟล์ประกาศถูกบังคับแล้ว (2026-08-14 ค่ำ)**: `ProjectTypeGate` ย้ายมาอยู่ ProjectKit และกลายเป็นเงื่อนไขของประตูขาออกจาก "ขั้นดำเนินการ" · ตอบสามทางโดยตั้งใจ — **เงื่อนไขที่ระบบตอบได้** บล็อกจริงเมื่อเป็นเท็จ (`content_validity_passed` · `consent_approved` · `ethics_recorded` อ่านจากเครื่องมือของโปรเจกต์) · **ช่องว่างที่มีชื่อ** (เช่น `saturation_reached` ที่รอ P11.8) ไม่บล็อก แต่ขึ้นเป็น `vacuous` คือ "ยังไม่ได้ตรวจ" ไม่ใช่ติ๊กเขียว — ถ้าบล็อกจะทำให้ 4 ใน 6 ชนิดออกจากขั้นดำเนินการไม่ได้เลย ซึ่งเป็นบั๊กที่แต่งตัวเป็นความเข้มงวด · **ชื่อที่ไม่มีในทะเบียน** บล็อก เพราะเงื่อนไขที่แต่งขึ้นแล้วเงียบ ๆ ไม่ทำอะไร คือการทำให้บรรทัด `gate:` กลับไปเป็นของประดับ · มีเทสที่**อ่านไฟล์ชนิดโปรเจกต์ที่ ship จริง**แล้วยืนยันว่าทุกเงื่อนไขที่ไฟล์เอ่ยถึง อยู่ในทะเบียนใดทะเบียนหนึ่ง · **ค้าง**: โปรเจกต์ที่สร้างก่อนหน้านี้ยังไม่มี `typeName` จึงไม่มี gate ของชนิดใด ๆ มาบังคับ (ถูกต้องตามการออกแบบ แต่แปลว่าฟีเจอร์นี้มีผลกับโปรเจกต์ที่สร้างใหม่จากไฟล์ชนิดเท่านั้น) · ยังไม่ได้เห็นบนหน้าจอเพราะยังไม่มีโปรเจกต์ไหนบนเครื่องนี้อยู่ที่ขอบขั้นดำเนินการ · roster ต่อ type ยังไม่ผูกกับ agent จริง |
| **P11.2** Instrument Builder + Blueprint | ชนิดข้อครบตามสเปก, skip logic, สองภาษา, ผูกข้อ↔construct↔RQ | ข้อที่ไม่ผูก construct และไม่ติดป้าย `demographic` **ทำให้ publish ไม่ผ่าน** · ฟอร์มที่ AI ร่าง แก้ได้ทุกช่องจาก Workbench | ✅ **ผ่าน Done-when** — `Instrument`/`Item`/`Construct`/`ResearchQuestion` ครบ 9 ชนิดข้อ · skip logic ที่ชี้ไปข้างหน้าหรือชี้ไปข้อที่ไม่มีอยู่ถูก `Blueprint` จับ · สองภาษาตั้งแต่ชั้น type (`Bilingual`) · แก้ได้ทุกชั้นจาก Workbench จริง (ขับมือแล้ว) · **ข้อที่ไม่ผูก construct และไม่ติดป้าย `demographic` ทำให้ publish ไม่ผ่าน** ทั้งจากเทสและจากปุ่ม · **matrix/ranking สร้างได้จากหน้าจอแล้ว (2026-08-16)** — picker เสนอทั้งสองชนิด และมีช่องพิมพ์แถว/คอลัมน์ (หรือรายการที่จะจัดอันดับ) อยู่ข้างกันทันที เพราะคนที่กำลังเพิ่มตารางรู้ว่าแถวคืออะไรตั้งแต่ตอนเลือกชนิด · **รายการว่างปล่อยไปให้ด่านจับ ไม่เติม "แถว 1" ให้** — ตารางที่ไม่มีแถวคือความผิดพลาดที่ควรถูกบอก ส่วน placeholder จะถูกเผยแพร่ราวกับเป็นคำถามจริง · **ค้าง**: `fileUpload` ยังไม่มีทั้ง editor และการวาด (เหตุผลเดียวกับที่ renderer บอก) · ยังไม่มีตัวร่างด้วย AI |
| **P11.3** Validity (Swift) | IOC, CVI, α, item-total, ICC, κ + EFA ผ่าน Accelerate | ทุกตัวตรวจกับชุดข้อมูลที่รู้คำตอบ (เทียบค่าที่คำนวณมือ/จากตำรา) · **รันได้โดยไม่ต้องมี Python บนเครื่อง** · CFA บันทึกเป็นข้อจำกัดที่รู้ตัว ไม่ทำครึ่ง ๆ | ✅ **ผ่าน Done-when เกือบครบ** — IOC · I-CVI · S-CVI/Ave · Cronbach α · item-total · **ICC ทั้ง 6 รูปของ Shrout & Fleiss (1979)** · **Cohen’s κ และ Fleiss’ κ** — ทุกตัวตรวจกับตัวอย่างที่ตีพิมพ์แล้ว ไม่ใช่กับผลของตัวเอง (ICC เทียบกับตาราง 2 ของ Shrout & Fleiss ได้ .17/.29/.71 และ .44/.62/.91 ตรงทุกตัว · κ เทียบกับ 2×2 มาตรฐานที่คำนวณบนกระดาษได้) · รันใน Swift ล้วน ไม่ต้องมี Python · **ออกแบบให้ปฏิเสธเมื่อออกแบบไม่พอ**: ผู้ประเมินคนเดียว หรือหน่วยเดียว หรือทุกหน่วยได้คะแนนเท่ากัน คืน `nil` ไม่ใช่ตัวเลข เพราะตัวเลขที่ออกมาจะถูกเอาไปอ้างในเล่ม · รายงาน % ความตรงกันจริงคู่กับ κ เสมอ เพราะ κ ต่ำเพราะหมวดเดียวกินหมดเป็นคนละเรื่องกับผู้ลงรหัสไม่เก่ง · **EFA ครบแล้ว (2026-08-14 ดึก)**: KMO + MSA รายข้อ · Bartlett · eigenvalue ครบทุกตัว · principal axis factoring · varimax · McDonald's ω · และ `ConstructFit` ที่เทียบองค์ประกอบที่พบกับ construct ที่เครื่องมือประกาศไว้ — ผ่าน **LAPACK ของ Accelerate** ทาง C shim ตัวเดียว (ไม่มี API ที่ deprecated ในบิลด์) · ตรวจสองชั้นตามที่ R12 เรียกร้อง: **รูปปิดเชิงพีชคณิต** (equicorrelation → eigenvalue/determinant/KMO/น้ำหนัก √ρ) และ **implementation อิสระที่เขียนด้วย Python ก่อน** แล้ว Swift ต้องได้เลขเดียวกันถึง 1e-5 — ตรงทุกตัวตั้งแต่รันแรก · **มีหน้าจอแล้ว**: กล่อง “ความเที่ยงและความตรงเชิงโครงสร้าง” ในแท็บเก็บข้อมูล เรียก α/ω รายมาตรวัด + ตารางน้ำหนักองค์ประกอบ + คำเตือนจากขนาดตัวอย่าง · และมีเทสที่เดินทางยาว: **POST 40 ชุดผ่านฟอร์มจริงเข้า SQLite จริง อ่านกลับ แล้วได้โครงสร้างที่ปลูกไว้** · **ขับด้วยมือแล้ว** (2026-08-14 ค่ำ): สร้างเครื่องมือผ่านหน้าจอ → ผ่านประตู → เปิดเซิร์ฟเวอร์ → POST 40 ชุดเข้าฟอร์มจริง → กดคำนวณ แล้วได้โครงสร้างที่ปลูกไว้ครบ · **เจอ 3 ข้อที่เทสมองไม่เห็น (U25/U26/U27) แก้ครบ** และเพดานการลู่เข้าถูกยกจาก 100 เป็น 1000 รอบเพราะข้อมูลจริงขนาดเล็กต้องใช้ 183 · **ค้าง**: CFA บันทึกเป็นข้อจำกัดที่รู้ตัวตามแผน · ยังไม่มีหน้าจอเรียกใช้ ICC/κ (มาพร้อม P11.8) |
| **P11.4** InstrumentGate ก่อนเก็บข้อมูล | content validity ผ่าน + consent มี + ethics record ครบ ถึงจะ publish ได้ | พยายาม publish ตรง ๆ ข้าม gate **ถูกปฏิเสธ** (ทดสอบจาก API ไม่ใช่จากปุ่ม) · ผู้เชี่ยวชาญให้คะแนน IOC ผ่านโหมดผู้เชี่ยวชาญของ Host ตัวเดียวกัน | ✅ **ผ่าน Done-when** — `InstrumentGate.approve` เป็นผู้ผลิต `PublishedInstrument` เพียงตัวเดียว · เทสเรียก API ตรง ๆ โดยข้ามปุ่มแล้ว **ถูกปฏิเสธ** · ผู้เชี่ยวชาญให้คะแนน IOC ในหน้าจอเดียวกัน (ยังไม่ผ่านโหมดผู้เชี่ยวชาญของ Host — P11.5 จะต้องทำ) · **ขับมือแล้วเจอ 6 ข้อ แก้ครบ** (ตารางด้านล่าง) |
| **P11.5** M16 HTTPServer + FormRuntime | listener ในเครื่อง, LAN-only default, แปลง `PublishedInstrument` → หน้าเว็บ (HTML/CSS/JS ล้วน), validate ซ้ำฝั่ง server | คนอื่นในวงแลนกรอกฟอร์มได้จริงและคำตอบลงฐานข้อมูลของโปรเจกต์ · **ทดสอบบน `.app` ที่ build + sandbox แล้ว** ไม่ใช่ `swift run` (บทเรียน P8.4/P9.6) · ยิง field แปลกปลอมเข้ามาแล้วถูกทิ้งและบันทึก · ฟอร์มใช้ด้วยคีย์บอร์ด/screen reader และบนมือถือได้ | ✅ **ผ่าน Done-when — ขับจริงบน `.app` ที่ sandbox แล้ว** (2026-08-14): เปิดเซิร์ฟเวอร์จากในแอปแล้วได้ที่อยู่ LAN จริง · `curl` จากนอกแอปได้หน้าฟอร์ม 200 (7,974 bytes) ที่มีหน้าความยินยอมก่อนข้อแรก · **ตรวจซ้ำฝั่ง server จริง**: ไม่ติ๊กยินยอม → 400 · ตอบ Likert เกินระดับที่มี → 400 **พร้อมบอกว่าข้อไหน** · ส่งเวอร์ชันผิด → 400 · `/admin` → 404 (ไม่มี endpoint จัดการใด ๆ ให้หา) · ส่งครบ → 200 และคำตอบลง SQLite ของโปรเจกต์จริง · **field แปลกปลอม `is_admin` ถูกทิ้งและบันทึกเป็นแถวใน `dropped_field`** · ยิงพร้อมกัน 3 ชุดได้ 200 ทั้งหมด และแอปนับได้ 4 ชุด · ปิดรอบจากในแอปแล้ว POST **ได้ 410 ที่ endpoint** ไม่ใช่ซ่อนปุ่ม · ฟอร์มเป็น native control ล้วน (`fieldset`/`legend`/`radio`) จึงใช้คีย์บอร์ดและ screen reader ได้ พร้อม viewport และเป้ากด 44pt สำหรับมือถือ · ****resume token ทำแล้ว** (§20.7 SessionStore): กดปุ่ม "บันทึกไว้ก่อน" ได้ลิงก์ `?resume=<token 32 ตัว>` กลับมากรอกต่อโดยคำตอบเดิมอยู่ครบ · ไม่มีบัญชีผู้ใช้ ไม่มี cookie · ส่งแล้วร่างถูกลบ · ปิดรอบแล้วร่างที่ค้างถูกลบตามไปด้วย (§20.5) · ร่างของอีกเวอร์ชันถูกปฏิเสธ ไม่ใช่เทลงฟอร์มใหม่ · **matrix กับ ranking วาดได้แล้ว (2026-08-16, 12 เทส)** — ทั้งสองเป็น "หลายคำถามในพรอมป์เดียว" กฎจึงมาจากตรงนั้น: **grid ที่ตอบบางแถวไม่ใช่คำตอบครึ่งเดียวที่เอาไปเกลี่ยได้** (บังคับตอบครบเมื่อ required และบอกว่าแถวไหนหาย) และ**ranking ที่มีสองอันดับสามคือคำกล่าวอ้างคนละอย่าง ไม่ใช่เกือบถูก** (ต้องเป็น permutation 1…n ห้ามซ้ำห้ามข้าม) · แต่ละแถว/ตัวเลือกเก็บเป็นคำตอบของตัวเอง (`<id>#<index>`) ⇒ การวิเคราะห์แยกตัวแปรได้ · **grid เป็น `<table>` จริงพร้อม `scope=row/col` และ label ต่อ radio** เพราะหัวตารางไม่ใช่ label และ grid ที่ทำจาก stack อ่านออกมาเป็นปุ่มไร้ชื่อ · **ranking พิมพ์เลข ไม่ลาก** — การลากคีย์บอร์ดเข้าไม่ถึงและใช้นิ้วเดียวบนวอร์ดไม่ได้ · ฟอร์มที่กรอกต่อกลับมาเห็นของเดิมครบ · **`fileUpload` ยังไม่วาด และตั้งใจ**: มันคือ multipart, ไฟล์ที่มีข้อมูลคนอยู่บนดิสก์ และกฎการเก็บรักษาของตัวเอง (§20.5) — ครึ่งเดียวแย่กว่าช่องว่างที่พูดตรง ๆ |
| **P11.6** `PublishedInstrument` เป็น type ที่ปลอมไม่ได้ | ไม่มี public initializer — สร้างได้จาก `InstrumentGate.approve` ทางเดียว | เขียนเทสที่พยายามสร้าง `PublishedInstrument` เองแล้ว**คอมไพล์ไม่ผ่าน** (compile-fail test หรือ structural rule) — gate ที่เช็คตอน runtime อย่างเดียวคือ gate ที่จะถูกข้ามสักวัน | ✅ **ผ่าน Done-when** — ไม่มี public initializer และมีผู้ผลิตเดียว พิสูจน์ด้วย structural test ที่อ่านซอร์สของ `InstrumentGate.swift` · เวอร์ชันใหม่บันทึก `supersedes` ชี้กลับเวอร์ชันเดิม · **การผ่านประตูถูกบันทึกลงฐานข้อมูล** (`instrument_approval`) และเวอร์ชันที่ผ่านแล้วแก้ไม่ได้อีก |
| **P11.6b** ฐานข้อมูลภายใน 3 ชั้น | SQLite (WAL) เป็นทางเขียนของ M16 · DuckDB `ATTACH` อ่านต่อ · SurrealDB สำหรับกึ่งโครงสร้าง ([ARCH §19.17](ARCHITECTURE.md#1917-ฐานข้อมูลภายในของโปรเจกต์--sql-nosql-และช่องว่างจริงที่ต้องเติม)) | **ยิงฟอร์มพร้อมกัน 20 request แล้วไม่มีคำตอบหาย** และแอปอ่านได้ระหว่างที่ยังเขียนอยู่ · ไม่มี code path จาก M16 ไป DuckDB ตรง ๆ (structural rule) · materialize เขียน span ทุกครั้ง | ✅ **ผ่าน 2 ใน 3** — SQLite (WAL) เป็นทางเขียนของ M16 จริง (`PRAGMA journal_mode` = `wal` บนไฟล์ในโฟลเดอร์โปรเจกต์) · **ยิงพร้อมกัน 20 ชุดผ่าน HTTP จริงแล้วครบ 20 ไม่มีคำตอบหาย** และอ่านได้ระหว่างที่ยังเขียนอยู่ · **ไม่มี code path จาก M16 ไป DuckDB** — บังคับด้วย target แยก (`OLTP`) + กฎใน `check.sh` ที่**ทดสอบแล้วว่าจับได้จริง** เมื่อลองใส่ `Analysis` เข้า dependency · **ค้าง**: **materialize เข้า DuckDB ทำแล้วและขับจริงแล้ว** — กดปุ่มในหน้าคำตอบได้ตาราง `responses_<instrument>_v<n>` ใน DuckDB ของโปรเจกต์ เปิดใน DB explorer เห็น 15 แถว และ query แล้วค่าที่ถูกแก้มาเป็นค่าใหม่พร้อม `was_corrected = true` · เขียน span ทุกครั้ง |
| **P11.6c** คำตอบดิบแก้ทับไม่ได้ | ตาราง append-only · การแก้ค่าเก็บเป็น correction record (ค่าเดิม/ใหม่/เหตุผล/ใคร/เมื่อไร) | `grep` ไม่เจอ `UPDATE`/`DELETE` บนตารางคำตอบดิบในโค้ด · หน้า "คำตอบ" แสดงค่าที่แก้แล้วพร้อมเครื่องหมายและกดดูค่าเดิมได้ | ✅ **ผ่าน Done-when** — ตาราง `answer` เป็น append-only · การแก้ค่าเป็น `correction` (ค่าเดิม/ใหม่/เหตุผล/ใคร/เมื่อไร) และ**ปฏิเสธการแก้ที่ไม่มีเหตุผลหรือไม่มีชื่อผู้แก้** · `check.sh` แดงถ้ามี `UPDATE`/`DELETE` บนตารางคำตอบ · **หน้า "คำตอบ" ในแอปแล้ว**: ช่องที่ถูกแก้มีเครื่องหมาย (ไอคอน ไม่ใช่แค่สี) และกดดูค่าเดิม เหตุผล ชื่อผู้แก้ และเวลาได้ · **ยังไม่ได้ขับด้วยมือ** (ดูหมายเหตุท้ายรอบ P11.5) |
| **P11.7** ResponseStore + versioning + ปิดรอบ | ตารางต่อ instrument version, append-only, ปิดรอบแล้วปฏิเสธคำตอบใหม่ | แก้ฟอร์มที่มีคำตอบแล้ว **ไม่ทับข้อมูลเดิม** และวิเคราะห์แยก version ได้ · ปิดรอบแล้ว POST ตรงเข้า endpoint **ถูกปฏิเสธ** (ไม่ใช่แค่ซ่อนปุ่ม) | ✅ **ผ่านส่วนใหญ่** — คำตอบผูกกับ instrument + version และนับแยกกัน แก้ฟอร์มได้เวอร์ชันใหม่โดยคำตอบเดิมไม่ถูกแตะ · **รอบเก็บข้อมูลถูกบันทึกลงฐานข้อมูลแล้ว**: ปิดรอบแล้วรีสตาร์ตแอป รอบนั้น**ยังปิดอยู่** (เดิมกลับมาเปิดเอง) และ**การหยุดเซิร์ฟเวอร์ไม่ปิดรอบอีกต่อไป** — ปิดแล็ปท็อปตอนกลางคืนไม่ใช่ "เก็บข้อมูลเสร็จแล้ว" · รอบหนึ่งมีวันปิดวันเดียว กดปิดซ้ำไม่เลื่อนวัน · คำตอบทุกชุดบอกได้ว่าอยู่รอบไหน · **ค้าง**: resume token (กรอกต่อทีหลัง) · materialize เข้า DuckDB |
| **P11.7b** Linkage + waves | รหัสนิรนาม, ตารางเชื่อมแยกไฟล์, คีย์ใน Keychain, attrition | ตารางเชื่อมตัวตนกับตารางคำตอบ join กันได้เฉพาะผ่าน API ที่เขียน audit span · `grep` ไม่เจอคีย์ในไฟล์ DB | ✅ **ผ่าน Done-when ครบ — ชั้นข้อมูลและหน้าจอ ขับจริงแล้ว** — `LinkageStore` อยู่ในไฟล์ `linkage.sqlite` **คนละไฟล์กับคำตอบ** (สำเนาข้อมูลคำตอบจึงไม่มีตัวตนติดไปเลย ไม่ใช่ "มีแต่อ่านยาก") · ตัวตนถูกผนึกด้วย AES-GCM คีย์ 256-bit จาก Keychain **แยกต่อโปรเจกต์** (`kSecAttrAccessibleWhenUnlockedThisDeviceOnly` ไม่ sync ข้ามเครื่อง) · **เทสอ่านไบต์ดิบของไฟล์ (รวม `-wal`) แล้วหาไม่เจอทั้งชื่อผู้เข้าร่วมและคีย์** ซึ่งเป็นข้อเท็จจริงบนดิสก์ ไม่ใช่คำอ้างเรื่องโค้ด · `resolve` **เขียน span ทุกครั้ง** รวมถึงครั้งที่หารหัสไม่เจอ (คำถามที่ audit ตอบคือ "ใครเปิดดู" ไม่ใช่ "ใครเปิดดูสำเร็จ") · คีย์ของอีกโปรเจกต์เปิดไฟล์นี้ไม่ได้ · attrition ต่อรอบ = จำนวนที่เชิญเทียบจำนวนที่ตอบ · รหัสอ่านออกเสียงได้ (ไม่มีสระ ไม่มี O/0 I/1) เพราะงานภาคสนามมีคนต้องอ่านให้กันฟัง · **`check.sh` บังคับสองข้อ**: FieldServer ห้าม import `Linkage` (พื้นผิวที่รับคนนอกถามไม่ได้ว่าใครเป็นใคร) และไฟล์ตัวตนต้องไม่ใช่ไฟล์เดียวกับคำตอบ — **ทดสอบแล้วว่าจับได้จริง** · **รหัสเดินทางถึงคำตอบแล้ว**: ลิงก์ที่ลงท้าย `?code=P-…` ทำให้ฟอร์มพารหัสไปด้วย และคำตอบถูกเก็บคู่รหัส (M16 เก็บ*สตริง* ไม่ใช่ตัวตน และยัง import `Linkage` ไม่ได้) · ฐานข้อมูลเก่าที่ยังไม่มีคอลัมน์นี้ถูก `ALTER TABLE` เติมให้ ไม่ใช่เงียบ ๆ ขาดไป · **หน้าจอผู้เข้าร่วมทำแล้ว**: ลงทะเบียน → ได้รหัส · คัดลอกลิงก์เฉพาะคน · เชิญเข้ารอบ · attrition ต่อรอบ · "ดูว่าเป็นใคร" ต้องกรอกเหตุผลกับชื่อ และแสดงทีละคนแล้วลืมทันทีที่ปิด · **ขับจริงครบทั้งเส้นแล้ว** (2026-08-14 บ่ายแก่) — ดูรอบขับด้านล่าง |
| **P11.8** Qualitative | แนวสัมภาษณ์, ถอดเทป→KB พร้อม provenance, codebook, κ ระหว่างผู้ลงรหัส, saturation | ถอดเทปเข้า KB แล้ว citation ในต้นฉบับชี้กลับถึงช่วงข้อความจริง · κ คำนวณจากการลงรหัสสองชุดที่รู้คำตอบ | 🔶 **ครึ่งแรกผ่าน Done-when — ขับจริงแล้ว** (2026-08-14): สมุดรหัส (นิยาม + รหัสแม่ + รายงานรหัสที่ไม่มีนิยาม) · ช่วงข้อความที่**กำหนดครั้งเดียวแล้วทุกคนลงชุดเดียวกัน** ซึ่งเป็นเงื่อนไขที่ทำให้ κ คำนวณได้เลย · κ รายรวมและ**รายรหัส** (Cohen เมื่อสองคน Fleiss เมื่อมากกว่า) · หน่วยที่ลงไม่ครบถูกตัดออกและนับให้ดู · "ไม่เข้ารหัสไหนเลย" เป็นหมวดหนึ่ง ไม่ใช่ช่องว่าง · เส้นโค้งความอิ่มตัวที่**รายงานโดยไม่ตัดสิน** · schema v13 สามตาราง โดยแถวลงรหัสมีคีย์ (ช่วง, ผู้ลงรหัส) เพื่อไม่ให้คนหนึ่งคนเข้าไปอยู่ใน κ สองครั้ง · **หน้าจอใหม่ "ลงรหัส"** ซึ่งเป็นที่แรกที่ ICC/κ ถูกเรียกใช้จริง · **ขับจริง**: ผู้ลงรหัสสองคนลงสี่ช่วงผ่านหน้าจอ ได้ κ 0.50 · ตรงกัน 75% ตรงกับที่คำนวณบนกระดาษก่อนรัน · **ครึ่งหลังผ่าน Done-when แล้ว (2026-08-15)**: `Transcript` (รหัสผู้เข้าร่วม ไม่มีช่องใส่ชื่อ) · `TranscriptQuotation` ที่**สร้างเองไม่ได้** — ผู้ผลิตทางเดียวเฉือนข้อความจากบทถอดเทป ข้อความกับตำแหน่งจึงขัดแย้งกันไม่ได้ · `TextSpan` บน `Provenance` เป็นตัวระบุตำแหน่งของเอกสารที่ไม่มีเลขหน้า และขึ้นในบรรณานุกรม · `Origin.fieldwork` ที่ tier เป็น `nil` โดยเจตนา · `TranscriptIngest` แบ่งด้วย `Chunker` ตัวเดิมของ M7 และทุก chunk พกตำแหน่งของตัวเอง · หน้าจอเปลี่ยนเป็นวางบทถอดเทปแล้วแบ่งตามย่อหน้า (ตำแหน่งจริงแทนเลขที่พิมพ์เอง) · **ขับจริง**: สี่ย่อหน้า ได้ตำแหน่งต่อเนื่อง 0–32/33–76/77–100/101–144 และข้อความตรงต้นฉบับทุกช่วง · **ข้อค้นพบ**: บรรทัดไทยที่แสดง 43 รูป = 44 code point = 32 `Character` จึงปักหมุดหน่วยไว้ด้วยเทส · **เคยค้าง**: ปุ่มส่งบทถอดเทปเข้าคลังความรู้จากหน้าจอ · ✅ **ปุ่มส่งเข้าคลังความรู้ทำแล้ว (2026-08-15, 8 เทส)** — `TranscriptIngest.chunks` เสร็จตั้งแต่ P11.8 พร้อมเทสสามข้อ และ**ไม่มีอะไรเรียกมัน** คำสัญญา “ผลค้นอ้างกลับไปที่ย่อหน้าได้” จึงจริงกับฟังก์ชัน แต่ไม่จริงกับแอป · ทางที่ง่ายกว่าคือเขียนบทถอดเทปลงไฟล์ชั่วคราวแล้วเรียก `ingest(urls:)` เดิม — **ทำไม่ได้** เพราะ offset จะหายไปกับไฟล์ และ offset คือสิ่งเดียวที่ทำให้ chunk อ้างถึง*ย่อหน้า*แทนที่จะอ้างบทสัมภาษณ์สองชั่วโมงทั้งอัน · จึงเพิ่ม `IngestionPipeline.ingest(chunks:)` ที่รับ chunk ที่แบ่งมาแล้ว แล้วเดินต่อบนเส้นทางเดิมทั้ง dedup · embedding · near-duplicate เพราะบทถอดเทปที่ถูกจัดดัชนีคนละแบบจะถูกค้นเจอคนละแบบกับทุกอย่างในโครงการ · **การส่งซ้ำแทนที่ของเดิม ไม่ใช่เพิ่มเข้าไป**: บทถอดเทปเก็บ id เดิมไว้ตอนถูกแก้ การส่งครั้งที่สองจึงคือ*บทสัมภาษณ์เดิมที่พูดถูกขึ้น* ถ้าเก็บทั้งสองรุ่น คลังจะถือประโยคที่ถูกถอนกับประโยคที่แก้แล้วไว้พร้อมกันโดยไม่มีอะไรบอกว่าคำตอบมาจากรุ่นไหน (ต่างจากไฟล์อัปโหลดที่ id คิดจากเนื้อไฟล์ จึงเป็นคนละเอกสารจริง ๆ เมื่อเนื้อเปลี่ยน) · และ**ปฏิเสธถ้าไม่ได้อยู่ในโครงการ** — คลังกลางถูกค้นจากทุกโครงการ คำพูดของผู้เข้าร่วมจึงไม่ควรไปอยู่ตรงนั้น และคนที่เผลอจะกู้กลับเองไม่ได้ (§20.7) |
| **P11.9** ต้นฉบับ 5 บท | WBS template + DocGen template + bibliography | สร้างเอกสาร 5 บทที่ Word เปิดได้ โดยผลสถิติในบท 4 **ผูกกลับถึง cell ในสมุดงานที่รันจริง** ไม่ใช่ตัวเลขที่พิมพ์เข้าไป | 🔶 **ผ่าน Done-when ที่ชั้นข้อมูลและการส่งออก** (2026-08-15): ห้าบทเป็น `enum` ไม่ใช่จำนวน — ต้นฉบับที่ขาดบทที่ 4 สร้างไม่ได้ · ตัวเลขที่รายงานเป็น `ResultReference` ไม่ใช่ข้อความ และ `BoundResult` **ไม่มี public initializer** จึงเขียนเองไม่ได้ · ปฏิเสธทั้งเอกสารเมื่ออ้างเซลล์ที่ไม่เคยรัน หรือเมื่อ source ของเซลล์ถูกแก้หลังรัน (เคสที่ทำให้เล่มผิดโดยไม่มีอะไรดูผิด) · `CellRun` อยู่ใน M2 เพราะ M8 กับ M10 ใช้ร่วมกัน · schema v15 เก็บแถวเดียวต่อ (สมุดงาน, เซลล์) · บันทึกที่จุดที่เซลล์รันจริงในหน้าจอสมุดงาน · `exportManuscript` เขียน `.docx` พร้อม**ภาคผนวกที่มาของตัวเลข** · เซลล์ Python ไม่บันทึกอะไรโดยตั้งใจ (ตัวเลขที่ขูดจาก `print` ตามกลับไม่ได้) · **เคยค้าง**: หน้าจอสำหรับ*ประกอบ*ต้นฉบับ · ✅ **หน้าจอประกอบต้นฉบับทำแล้ว (2026-08-15, 16 เทส)** — ก่อนหน้านี้ `ManuscriptBuilder` กับ `exportManuscript` มีครบและ**ไม่มีอะไรสร้าง `Manuscript` เลย** จึงไม่เคยถูกเรียก: D6 อีกจุด · **การตัดสินใจที่สำคัญที่สุดของหน้าจอนี้คือ ตัวเลขถูก “ใส่” ไม่ใช่ “พิมพ์”** — ทางที่ตรงไปตรงมากว่าคือให้คนพิมพ์ `{ค่าเฉลี่ยอายุ}` ในประโยค แล้วกรอกฟอร์มบอกว่าเลขนี้มาจากไหน ซึ่งคือสตริงเดียวกันที่ต้องพิมพ์สองครั้งให้ตรงกันเป๊ะ ในภาษาที่ช่องว่างท้ายคำมองไม่เห็น · `SentenceComposer.insert` เขียนทั้ง placeholder และ reference ในการกระทำเดียว ทั้งสองจึงเพี้ยนจากกันไม่ได้ · ปฏิเสธสามอย่างที่ทำให้เลขหายเงียบ ๆ: ชื่อซ้ำในประโยคเดียว (เลขตัวแรกไปโผล่สองที่ ตัวที่สองไม่ถูกพิมพ์เลย) · ชื่อที่มีปีกกา · ชื่อว่าง · และ**รายงานแต่ไม่ปฏิเสธ** กรณี reference ที่ช่องถูกลบไปแล้ว — เลขนั้นไม่ได้ถูกอ้างในเนื้อ จึงไม่ควรบล็อกทั้งเล่ม และไม่ควรโผล่ในภาคผนวกที่มาของตัวเลขด้วย · `ManuscriptPreview` ตรวจ**ด้วยหลักฐานชุดเดียวกับตอนส่งออก** (`manuscriptEvidence()` ที่เดียว) เพราะหน้าจอที่บอกว่า “พร้อมส่งออก” ข้างปุ่มที่กดแล้วถูกปฏิเสธ แย่กว่าหน้าจอที่ไม่มี preview เลย · ตัวเลือกเซลล์เสนอ**เฉพาะเซลล์ที่รันแล้ว** คนจึงสร้าง reference ที่ผูกไม่ได้ไม่ได้ตั้งแต่แรก · `ManuscriptStore` เทสกับ SurrealDB จริง 4 ข้อ (binding รอดข้ามการบันทึก · ไม่รั่วข้ามโครงการ · save ซ้ำเป็น upsert · ลบแล้วหาย) · **ค้าง**: ยังไม่ได้ขับบนหน้าจอจริง — ตัวขับพิมพ์ลง SwiftUI `TextField`/`TextEditor` ไม่ได้ จึงสร้างเล่มด้วยตัวขับไม่ได้ |
| **P11.10** จริยธรรม/ความเป็นส่วนตัว | consent, ethics record, retention policy ใน `policy` scope | policy retention ที่ตั้งไว้ถูกบังคับตอนปิดโปรเจกต์ (ข้อ 8 ของ Closing gate) · ไม่มีทาง publish ฟอร์มที่ไม่มีหน้าความยินยอม | ✅ **ผ่าน Done-when ทั้งสองข้อ และต่อเข้าแอปแล้ว** (2026-08-15) — **ข้อ 2** ผ่านตั้งแต่ P11.4 (`InstrumentGateTests` มีเทส "no consent page, no publication") · **ข้อ 1**: เดิมเงื่อนไข 8 รับ*ข้อความอะไรก็ได้*ที่ไม่ว่าง — พิมพ์ "จะจัดการให้เรียบร้อย" ก็ผ่าน · ตอนนี้ชื่อนโยบาย**ต้องตรงกับกฎที่มีจริงใน `policy` scope** และวันครบกำหนดมาจากระยะเวลาที่กฎเขียนไว้ (อ่านทั้ง "5 ปี" และ "3 years" · ไม่ระบุ = บอกว่าไม่ระบุ ไม่เดา) · **บันทึกภาระ ไม่ลบไฟล์ให้เอง** ตาม P10.10 · ตอบสามทางเหมือน `ProjectTypeGate` · **`WorkspaceRetentionFacts` ต่อแล้ว**: อ่าน "เคยมีคนตอบไหม" จาก `responses.sqlite` ของโปรเจกต์เอง และอ่านนโยบายจาก**รุลบุ๊กตัวเดียวกับที่ hook chain บังคับ** (R14) — นโยบายที่หยุดคำสั่งกับนโยบายที่ผ่านประตูปิดจึงเป็นเอกสารเดียวกันเสมอ · ฐานข้อมูลคำตอบที่เปิดไม่ได้ = `nil` ไม่ใช่ "ไม่มีใครตอบ" · 15 เทส · **ค้าง**: ยังไม่ได้ขับหน้าจอกับโปรเจกต์ที่มีคำตอบจริงแล้วกดปิด (ต้องมีลำดับยาว: เปิดฟอร์ม → รับคำตอบ → ใส่นโยบาย → ปิดโครงการ) |

---

## P12 — Agent Competence & Knowledge View

สเปก: [ARCH §21](ARCHITECTURE.md#21-agent-competence-model--อะไรทำให้-agent-แต่ละตัวต่างกัน) · ทำหลัง P10 เพราะ role memory กินของจาก lesson register

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P12.1** Competence profile ใน manifest | competence claim (ICB4 Practice element) + knowledge view ในไฟล์เดียวกับ tool grant — parser ตัวเดิม | โหลด manifest ที่ประกาศ view แล้วการค้นของ agent ตัวนั้นเปลี่ยนจริง (ทดสอบจาก tool layer ไม่ใช่จาก unit ของ parser) | ✅ **ผ่าน Done-when (2026-08-15)** — `knowledge_view:` อ่านจาก frontmatter ตัวเดิม และ `Manifest` เก็บไว้เป็น**ข้อความ ไม่ใช่ชนิดที่ decode แล้ว** ด้วยเหตุผลเดียวกับที่ P8.4 ตัดสินเรื่องแพ็กเกจ: Roster *บรรยาย* ส่วนการทำให้เป็นความสามารถอยู่ที่ที่ความสามารถนั้นอยู่ — ถ้า Roster พึ่ง `Knowledge` `KnowledgeIndex` จะเข้ามาอยู่ในระยะเอื้อมของมัน และ module graph คือวิธีที่โปรเจกต์นี้บังคับว่าโมดูลไหนทำอะไรไม่ได้ (ทางแก้ของ B2) |
| **P12.2** Knowledge view ตอน retrieval | filter: scope / entity type / tier ขั้นต่ำ / ความสดใหม่ / hops / boost | 6 role ให้ผลการค้นต่างกันจริงบน KB ก้อนเดียวกัน — ทดสอบด้วย fixture ที่รู้คำตอบว่าใครควรเห็นอะไร | ✅ **ผ่าน Done-when (18 เทส)** — คำถามเดียวกัน หกบทบาท ได้ผลต่างกันจริงบน KB ก้อนเดียว · **กรองตอนค้น ไม่ใช่กรองผลลัพธ์ทีหลัง** เพราะการจัดอันดับแล้วค่อยตัดออกทำให้ `limit` โกหก (ขอสิบได้สี่ โดยไม่มีอะไรบอกว่าทำไม) และเอาของที่บทบาทนี้เห็นไปปนกับการจัดอันดับของคนอื่น · ต่อเข้า `kb_search` แล้ว โดยอ่าน role จาก `ToolContext` ที่มีอยู่แล้ว — Writer ที่ค้นจึงได้มุมมองของ Writer โดยไม่มีใครต้องจำว่าต้องส่งอะไร ซึ่งเป็นรุ่นเดียวที่ยังจริงอยู่หลังผ่านไปหกเดือน |
| **P12.3** `policy` มองไม่ข้าม | ทุก view ถูกบังคับเติม `policy` | exhaustive test ทุกค่าใน `Role` — ปิด policy ใน manifest แล้วต้องยังเห็นอยู่ดี | ✅ **ผ่าน Done-when (เทสครบทุกค่าใน `Role`)** — **แต่ไม่ได้ทำด้วยการ “เติมให้”** ทำด้วยการที่**ไม่มีวิธีเขียน view ที่ไม่มี `policy` ได้เลย**: `scopes` เป็น private และ `visibleScopes` union `.policy` เสมอ · manifest ที่ระบุ scope โดยไม่มี policy ถูกแก้ให้ และ `policyWasAddedBack` บอกว่าถูกแก้ ไม่ใช่แก้เงียบ ๆ · ครึ่งที่ลึกกว่า: chunk ใน `policy` ต้องรอดจากตัวกรอง*อื่น*ด้วย — กฎไม่มีผู้เขียน ไม่มีปี ไม่มี tier เงื่อนไข "อ้างอิงได้ครบ" ของ Writer กับพื้น tier ของ Researcher จะเขี่ยมันทิ้งทั้งคู่ ถ้าไม่ยกเว้นไว้ |
| **P12.4** Writer เห็นเฉพาะ chunk ที่ provenance ครบ | filter ที่ระดับ retrieval ไม่ใช่ตอนตรวจท้าย | ใส่ chunk ที่ไม่มีผู้เขียน/ปี แล้วค้นจาก view ของ Writer ได้ **0 ผล** · และ DocGen ยังหยุดการสร้างเอกสารเหมือนเดิม (สองชั้น ไม่ใช่ย้ายชั้น) | ✅ **ผ่าน Done-when** — chunk ที่ไม่มีผู้เขียนหรือปี Writer ไม่เห็นตั้งแต่ตอนค้น ไม่ใช่เห็นแล้วค่อยถูกบอกให้ตัดทิ้งตอนตรวจ — การอ้างอิงที่เติมให้ครบไม่ได้ คือสิ่งที่ Writer ไม่เคยมี แทนที่จะเป็นสิ่งที่ต้องรื้อย่อหน้าที่เขียนรอบมันไปแล้ว |
| **P12.5** QA เห็นเฉพาะ evidence + DoD + policy | ตัด view ของผู้ทำออกจากสายตา QA | QA ตัดสินได้จาก evidence ล้วน — ทดสอบว่า deliverable ที่ evidence ไม่พอ **ไม่ผ่าน** แม้ summary จะเขียนน่าเชื่อ | ✅ **ผ่าน Done-when** — QA เห็นเฉพาะ chunk ที่ระบบเขียนเอง (`userAuthored`) กับ `policy` · การตรวจที่อ่านสิ่งเดียวกับที่ผู้ทำอ่าน คือการตรวจที่ไปถึงข้อสรุปเดียวกับผู้ทำ |
| **P12.6** บันทึก view ที่ใช้ + ขอขยาย view | view ลงใน evidence · `widen_view` เป็นทูลที่ผ่าน hook chain | ผลค้นที่ 0 เพราะ filter รายงานต่างจาก "ไม่มีข้อมูลจริง" · การขยาย view โผล่ใน evidence ที่ QA อ่าน | 🔶 **ครึ่งแรกทำแล้ว** — ผลค้นที่ว่างเพราะ *มุมมองของบทบาท* รายงานต่างจาก "คลังไม่มีข้อมูล" และบอกจำนวนที่ถูกกรองออก เพราะสองอย่างนี้ส่งคนไปคนละที่ · **เคยค้าง**: ทูล `widen_view` · ✅ **ครึ่งหลังทำแล้ว (6 เทส)** — `widen_view` เป็นทูลจริงที่เดินผ่าน hook chain เพราะทางเลือกอีกสองทางแย่กว่า: ปล่อยให้ค้นโดยไม่มี view เมื่อไม่เจอ ทำให้ §21.2 ทั้ง section กลายเป็นคำแนะนำ ส่วนให้คนไปแก้ manifest กลางบทสนทนา ทำให้คำตอบของ "ผมมองไม่พอ" คือการออกไปแก้ไฟล์ห้านาที · **สี่อย่างที่จริงเสมอไม่ว่าอาร์กิวเมนต์จะเป็นอะไร**: ขยายได้อย่างเดียว แคบลงไม่ได้ (agent จึงซ่อนของจากตัวเองไม่ได้ รวมถึงซ่อนกฎ ซึ่ง `policy` ที่ถอดไม่ได้กันไว้อีกชั้น) · **หมดอายุพร้อมบทสนทนา ไม่แตะ manifest** — agent ที่แก้ manifest ตัวเองได้คือ agent ที่ให้สิทธิ์ถาวรกับตัวเองในการเห็นของที่มีคนตั้งใจกันไว้ · ต้องมีเหตุผลและเหตุผลถูกเก็บไว้ เพราะการขยายที่อธิบายทีหลังไม่ได้คือการขยายที่ตรวจไม่ได้ · และ **Reviewer ขยายเข้าไปดูของที่ผู้ทำใช้ไม่ได้** เพราะมุมมองแคบของผู้ตรวจคือเหตุผลที่มีผู้ตรวจ · **บันทึกลง span แล้ว (2026-08-16, 2 เทส)** — ตามที่แถวนี้เดาไว้เองว่าเป็นที่ที่ถูก: `Evidence` บังคับ `passed: Bool` และ "บทบาทนี้เห็นอะไรได้บ้าง" ไม่ใช่ผ่าน/ไม่ผ่าน การยัดลงไปจะได้ธงที่อ่านไม่ได้ความ · span ถือ role, scope และเวลาอยู่แล้ว ⇒ `view:widened` เก็บ**เหตุผลกับมุมมองที่ได้** ไว้ด้วยกัน เพราะการขยายที่อธิบายทีหลังไม่ได้คือการขยายที่ตรวจไม่ได้ · ไม่มี sink ก็ยังขยายได้ (sink ทุกตัวในโปรเจกต์นี้เป็น optional) |
| **P12.7** Role memory | lesson จาก `central` กรองด้วย role เข้ามาตอนเริ่ม assignment | ปิดโปรเจกต์ที่มี lesson แล้วเปิดโปรเจกต์ใหม่ — lesson ของ role นั้น**ปรากฏใน context จริง** และไม่ปรากฏกับ role อื่น | ✅ **ผ่าน Done-when (11 เทส)** — `LessonPublisher` เขียนบทเรียนลง `central` ตั้งแต่ P10.8 แล้ว จึง**ค้นเจอได้** · สิ่งที่ยังไม่มีคือมัน**มาถึง** และบทเรียนที่ต้องนึกได้เองว่าต้องไปค้น คือบทเรียนที่ไปถึงคนที่รู้อยู่แล้ว · ตอนนี้ไหลเข้า `Assignment.inputs` ก่อนงานเริ่ม · **สองกฎที่พลาดแล้วเงียบ**: บทเรียนที่**ไม่ระบุบทบาท ใช้ได้กับทุกบทบาท** — คนปิดโครงการกรอก "ใช้กับ:" ตามที่นึกออกตอนนั้น ถ้าตีความว่า "ไม่ระบุ = ไม่เกี่ยวกับใคร" บทเรียนส่วนใหญ่จะถูกเก็บไว้ในที่ที่ไม่สอนใคร ซึ่งเป็นความล้มเหลวเดียวกับที่ `LessonPublisher` เขียนไว้ในหัวไฟล์เอง เลื่อนมาอีกขั้นเดียว · และบทเรียนที่**ระบุบทบาทอื่น ถูกตัดออก** ไม่งั้นทุกบทบาทจะเริ่มงานด้วยการอ่านบทเรียนของคนอื่นทั้งหมด · อ่าน "ใช้กับ:" บรรทัดเดียวไม่ใช่ทั้ง chunk — บทเรียนที่*สาเหตุ*พูดถึงนักวิเคราะห์ ไม่ใช่บทเรียนที่เขียนถึงนักวิเคราะห์ · จำกัดจำนวนและ**บอกว่าตัดไปกี่ข้อ** เพราะสรุปที่แสดง 5 จาก 12 โดยไม่บอก อ่านได้ว่า "มีอยู่ 5" |
| **P12.8** Tool proficiency | อัตราสำเร็จต่อ (role × tool) จาก span + แสดงใน Plan → ทีม & RACI | ตัวเลขมาจาก span จริง · **ระบบไม่ปรับ tool grant ให้เอง** (structural rule: ไม่มี code path ที่เขียน manifest กลับ) | ✅ **ผ่าน Done-when (9 เทส)** — span มีคำตอบอยู่แล้ว (ทุกการเรียกทูลคือ span หนึ่งอัน พร้อม role · status · detail) สิ่งที่ span ไม่มีคือ**ความแตกต่างที่ทำให้ตัวเลขมีความหมาย** · **การเรียกที่ถูกกฎหยุดไว้ ไม่ใช่ความล้มเหลวที่ทูล**: gateway บันทึก policy hard stop · stage gate · คนไม่อนุมัติ · plan-only เป็น span ที่ไม่สำเร็จเหมือนกันหมด — นับเป็นความผิดของบทบาทจะทำให้ agent ที่ประพฤติดีดูไร้ความสามารถ และที่แย่กว่านั้นคือตัวเลขจะ**สูงขึ้น**เมื่อผ่อนราวกันตก ซึ่งตรงข้ามกับสิ่งที่คนอ่านอยากรู้ · นับแยกและ**แสดงแยก** เพราะ "บทบาทนี้ชอบพยายามทำสิ่งที่กฎห้าม" เป็นเรื่องที่ควรรู้และเป็นคนละเรื่อง · **ตัวหารน้อยไม่ได้เปอร์เซ็นต์**: 1 จาก 1 คือ 100% และหน้าจอที่พิมพ์แบบนั้นสอนคนให้เชื่อผิดหรือเลิกอ่าน — ต่ำกว่า 5 ครั้งตอบว่า "ยังใช้ไม่พอจะบอกอะไร" ซึ่งจริง และเป็นสิ่งเดียวกับที่แถบพยากรณ์เวลาทำอยู่แล้ว · เรียงตามจำนวนครั้งที่ใช้ ไม่ใช่ตามคะแนน เพราะเรียงตามคะแนนจะดัน 100%-จาก-1-ครั้ง ขึ้นบนสุดพอดี · แสดงในหน้า **แผนงาน → ทีม & RACI** ซึ่งเป็นที่ที่คนตัดสินว่าจะให้ใครทำอะไร · กฎใหม่ใน `check.sh` กันการผูกที่จะเน่าเงียบ ๆ: prefix ที่ตัวอ่านใช้แยก "กฎหยุด" ออกจาก "บทบาททำพลาด" ต้องยังเป็นข้อความที่ gateway เขียนจริง |

---

## P13 — ค้นเว็บชั้น T5

สเปก: [ARCH §1.2](ARCHITECTURE.md#12-web-search--มีของฟรีถาวรไหม-apple-ให้ด้วยไหม) · [§19.2.2](ARCHITECTURE.md#1922-chat-ต้องต่ออินเทอร์เน็ตได้ในทั้งสองสภาพแวดล้อม)

**สถานะ: ✅ ครบทั้ง phase (2026-08-14)** — ค้นและอ่านได้จริงใน `.app` ที่ build + sandbox แล้ว

**ทำไมเคยอยู่ท้าย**: โครงรองรับไว้หมดแล้ว — source registry, tier, `web_search`/`fetch_page` ที่เดินประตูเดียวกัน การเปิด T5 จึงเป็นการ*เติมแหล่ง* ไม่ใช่การ*แก้สถาปัตยกรรม* ทำตอนภาพรวมนิ่งแล้วได้ผลเท่ากัน และไม่บล็อกงานอื่นเลย — ซึ่งกลายเป็นการตัดสินใจที่ถูก เพราะทางที่เลือกตอนแรก (SearXNG) ใช้ไม่ได้จริง

**สิ่งที่เลือกจริง — `WKWebView` แบบไม่มีหน้าต่าง** (ชนะจาก 4 ทางที่เทียบใน P13.1): ให้ agent สั่งค้น เลือกลิงก์ แล้วโหลดหน้าเข้าท่อ ingestion เดิม · ได้เปรียบสองข้อที่ทางอื่นไม่มี — **อยู่ในแอปที่ sandbox แล้ว** (ไม่ต้องแพ็ก Python runtime แบบ SearXNG ซึ่งพิสูจน์แล้วว่าแพ็กไม่ได้) และ **รัน JavaScript ได้** จึงอ่านหน้าที่ render ฝั่ง client ซึ่ง `PageFetcher` คืน `FetchError.empty` · รูปร่าง ข้อจำกัด และกฎ "ไม่แก้ CAPTCHA / หน้าเว็บเป็นข้อมูลไม่ใช่คำสั่ง" อยู่ใน [§1.2.1](ARCHITECTURE.md#121-สะพานค้นเว็บด้วย-wkwebview-แบบไม่มีหน้าต่าง-p131)

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P13.1** เลือก backend ที่แพ็กเข้าแอป sandbox ได้ | เทียบ **4 ทาง**: **`WKWebView` แบบไม่มีหน้าต่าง ([§1.2.1](ARCHITECTURE.md#121-สะพานค้นเว็บด้วย-wkwebview-แบบไม่มีหน้าต่าง-p131)) — ตัวเต็ง** · SearXNG แบบ prebuilt binary · scraper ที่เขียนเอง · API ที่คิดเงินรายเรียก — เกณฑ์คือ *รันได้ในแอปที่ sandbox จริง* ไม่ใช่รันได้บนเครื่องนักพัฒนา | ทดสอบบน `.app` ที่ build + sandbox แล้ว ได้ผลค้นจริงคืนมา (บทเรียน P8.4/P9.6 — เทสนอก sandbox มองข้อนี้ไม่เห็น) · **หน้าที่เจอ CAPTCHA/ถูกบล็อก คืน error ที่แปลว่า "ต้องให้คนช่วย" ไม่ใช่ผลลัพธ์ว่าง** — และระบบไม่แก้ CAPTCHA เอง · หน้าผลลัพธ์ที่ parse ไม่ออกต้อง fail ดัง ไม่ใช่คืนรายการว่าง | ✅ **ผ่าน Done-when (14 เทส + ขับจริงใน .app ที่ sandbox)** — `WKWebView` แบบไม่มีหน้าต่างในแอป: ค้นได้ผลจริง 8 รายการ และ "อ่านหน้านี้" ได้บทความ 38 ย่อหน้าเข้าท่อ `Readability`/provenance เส้นเดิม · **`HeadlessPageReader` conform `PageReading`** ⇒ `fetch_page`/`ingest_url` ทั้งเส้นได้เบราว์เซอร์ไปใช้โดยไม่ต้องแก้อะไร (นี่คือเหตุผลที่ยอมทำเป็น protocol ตั้งแต่ P3) · **DOM เป็นตัว parse** (`querySelectorAll` ผ่าน `evaluateJavaScript`) ไม่ใช่ regex บน HTML แบบ v1 ที่พังทุกครั้งที่ markup ขยับ · **หน้าโหลดสำเร็จแต่อ่านไม่ได้เลย = error ของตัวอ่าน ไม่ใช่ 'ไม่มีผลลัพธ์'** (เทสยืนยันข้อความ) · **เจอ CAPTCHA/403/429/`/sorry/` = ขอให้คนช่วย** ไม่ใช่ผลว่าง และระบบไม่แก้ CAPTCHA · เทสแยกด้วยว่า*บทความเกี่ยวกับ* CAPTCHA ไม่ใช่ CAPTCHA (สแกนแค่หัวเอกสาร) · ล้าง cookie/storage ทุกครั้งที่ค้น + หน่วงต่อโดเมน + cache ต่อคำค้น · ค่าเริ่มต้นไม่ใช่ Google โดยตั้งใจ (เทสกันไว้) · **บั๊กที่การขับจับได้**: ผลค้นไทยเป็น T5 ทั้งหมด ⇒ งานวิจัยไทยอ้างอิงไม่ได้เลยตามกติกา §14.1 — เพิ่ม TCI/ThaiJO เป็น T2 และคลังมหาวิทยาลัย/ThaiLIS/วช. เป็น T3 แล้วค้นซ้ำได้ tier จริง |
| **P13.2** ต่อเข้า tier registry + tool | ผลจาก T5 ต้องได้ tier ที่ถูกต้องและเข้ากติกา corroboration เดิม | Researcher ที่ใช้ T5 สองแหล่ง **ยัง QA ไม่ผ่าน** ถ้าไม่มี T1–T3 ยืนยัน (กติกาเดิมต้องไม่ถูกผ่อนเพราะมีแหล่งเพิ่ม) | ✅ **ผ่าน Done-when (11 เทส + 1 ด่านขับ)** — **กติกา §14.1 เคยเป็นแค่ย่อหน้าในรายงาน ไม่ใช่ประตู**: `CrossSource.assess` ถูกใช้ที่เดียวคือส่วน Limitations ของเอกสาร ⇒ QA ของ Researcher นับจำนวน citation แต่**ไม่เคยดู tier** ⇒ บล็อกสองอันผ่านได้ · แก้โดยย้ายตัวกฎไป `AgentKit.Corroboration` (โมดูลที่ทั้งสองฝั่งพึ่งอยู่แล้ว) แล้วให้ทั้ง Limitations และ QA เรียกตัวเดียวกัน — สองสำเนาของกฎเดียวจะเพี้ยนครั้งแรกที่มีใครปรับ และสำเนาที่สำคัญคืออันที่ปล่อยงานผ่าน (§0.2 ข้อ 3) · **tier เดินทางจาก tool ถึง QA**: `Evidence` มี `tier` แล้ว · `fetch_page` เขียน `tier:` เป็น**บรรทัดแรก** (จึงรอดการตัด 200 ตัวอักษรตอนทำ evidence) · `CitationTier` เขียน/อ่านที่เดียวและมีเทส round-trip · **citation ที่ไม่มี tier ไม่ใช่ 'น่าจะโอเค'** — ถือว่าแบกข้อสรุปไม่ได้เหมือนบล็อก · **อ่านหน้าเดียวสองครั้ง = แหล่งเดียว** (retry อ่านซ้ำจะกลายเป็น consensus ปลอม) · ขับจริงผ่าน `FetchPageTool` ตัวจริง → evidence → `QAReviewer` ตัวจริง: T5×2 ไม่ผ่าน · เติม T2 แล้วผ่าน · **ยุบเป็น type เดียวแล้ว (2026-08-16, 7 เทส)** — `SourceTier` เป็น alias ของ `CredibilityTier` · **สิ่งที่เจอตอนยุบคือเหตุผลว่าทำไมต้องยุบ**: สอง enum นี้มี `<` **กลับทางกัน** (`t1 < t2` จริงในฝั่งหนึ่ง เท็จในอีกฝั่ง) ⇒ "แหล่งดีอย่างน้อย T2" อ่านได้คนละความหมายห่างกันหนึ่งโมดูล และมันไม่ crash แค่กรองผิด ซึ่งคือความผิดพลาดที่ §0.2 ข้อ 3 พูดถึง · เจอจุดที่กลับทางจริงสี่จุดตอนคอมไพล์+เทส: ตัวกรอง tier ของ `SourceRegistry` · `KnowledgeView.minTier` · การขยายสิทธิ์ (`widened`) · และกฎ "แหล่งที่ดีกว่าเปิดคำถามที่ตัดสินแล้วได้อีก" ของ conflict ledger — สามในสี่จุดนี้ทำงาน*ถูก*มาตลอดเพราะอยู่ในโมดูลที่ `<` แปลว่า "น่าเชื่อกว่า" · **decode ได้ทั้งสองรูปที่เคยเก็บไว้** (`"t3"` ของ provenance ตั้งแต่ P2 และ `3` ของ evidence ตั้งแต่ P1) เพราะตัว decoder ที่รับรูปเดียวคือการทำให้ครึ่งหนึ่งของคลังอ่านไม่ได้ (กฎเดียวกับ P9.2) · `TierParityTests` เปลี่ยนจากยามเฝ้าการเพี้ยน เป็นการยืนยันว่ามันเป็น type เดียวกันจริง |

---

## P14 — R ผ่านสะพานภายนอก

สเปก: [ARCH §12.7](ARCHITECTURE.md#127-r--เรียกจากข้างนอกแบบ-rstudio-พร้อมตัวช่วย-p14) · **ไม่บล็อกใคร** — ชั้นสถิติ Swift ไม่ได้พึ่ง R

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P14.1** ตัวช่วยตั้งค่าสะพาน | ตรวจว่าเครื่องมี R · สร้าง `r-bridge.R` (Plumber) + คำสั่งคัดลอกได้ + ไฟล์ launchd (ทางเลือก) | บนเครื่องที่มี R: ทำตามที่หน้าจอบอกแล้วสะพานขึ้นและ health เขียว · บนเครื่องที่ไม่มี R: หน้าจอบอกว่าต้องติดตั้งอะไร **ไม่ใช่ error ที่ให้เดา** | ✅ **ผ่าน Done-when (2026-08-16, 15 เทส + ขับกับ R จริง — E.28)** — ขับจริงก่อนเขียนโค้ด: R **4.5.3** ที่ `/usr/local/bin` มี `httpuv` + `jsonlite` แต่**ไม่มี `plumber`** ⇒ **เขียนสะพานบน httpuv แทน plumber ตามที่ §12.7 วาดไว้** เพราะไม่งั้นประโยคแรกที่ตัวช่วยพูดกับผู้ใช้คือ "ติดตั้งแพ็กเกจก่อน ตัวช่วยถึงจะทำงาน" — plumber เองก็เป็นชั้นบน httpuv · `/health` และ `/eval` ตอบจริง data frame กลับมาพร้อม `NA` เป็น JSON `null` และโค้ดที่ parse ไม่ผ่านได้ 400 พร้อมข้อความของ R เอง (`<text>:2:0`) · **สถานะแยกสี่แบบ และทุกแบบมีขั้นถัดไปเป็นคำพูด**: ไม่มี R ≠ ขาดแพ็กเกจ ≠ R ที่เสีย — ยุบรวมเมื่อไหร่ก็ส่งคนไปติดตั้ง R ที่ใช้ได้อยู่แล้วใหม่ · **ตัวช่วยไม่ติดตั้งอะไรให้** (กฎใน check.sh พิสูจน์ว่า fail ได้) เพราะตัวช่วยที่รัน `install.packages` เองคือตัวช่วยที่แก้เครื่องคนอื่นเพื่อให้ไฟของตัวเองเขียว · **แอปไม่เปิด R ให้** — สะพานเป็นของผู้ใช้ รันด้วยไลบรารีและโฟลเดอร์ของเขา ตามที่ §12.7 ขอ · เทส 15 ข้อ**ไม่เรียก R เลย** ใช้รูปคำตอบที่บันทึกไว้ เพราะเครื่องที่ไม่มี R คือเครื่องที่ฟีเจอร์นี้มีไว้ให้ |
| **P14.2** `r_eval` เป็นทูลที่เดินประตูเดิม | จัดชั้น `.mutating` ทั้งใน RiskScorer และ StageGate (กฎตารางตรงกันบังคับอยู่แล้ว) | ขั้นวางแผนเรียก `r_eval` **ถูกปฏิเสธ** · สะพานปิดอยู่แล้วเรียก ได้ข้อความที่บอกวิธีเปิด ไม่ใช่ connection refused | ✅ **ผ่าน Done-when (2026-08-16, 4 เทส)** — `r_eval` ลง `RiskScorer` เป็น **high** และ `StageGate` เป็น **`.mutating`** ⇒ ขั้นวางแผนปฏิเสธจริง (เทสยิงผ่าน gateway ตัวจริง ไม่ใช่เทียบตาราง) · สะพานเป็น HTTP ที่รัน R อะไรก็ได้ = เชลล์คนละไวยากรณ์ สิ่งเดียวที่ทำให้ยอมมีได้คือมันเดินประตูเดียวกับทูลอื่น · **สะพานปิด = "เปิดเทอร์มินัลแล้วสั่ง Rscript …"** พร้อมพาธจริงของไฟล์ ไม่ใช่ connection refused (เทสยิงกับพอร์ตที่ไม่มีใครฟังจริง) · error ของ R ส่งกลับแบบคำต่อคำ เพราะบรรทัดกับตำแหน่งคือสิ่งเดียวที่บอกได้ว่าผิดตรงไหน · ทูลลงทะเบียนในแอปตั้งแต่บูตแม้สะพานยังไม่เปิด — ทูลที่โผล่เฉพาะตอนสะพานขึ้นคือทูลที่โมเดลไม่มีวันรู้ว่ามี |
| **P14.3** ผลลัพธ์ลง DuckDB | data frame ที่คืนมากลายเป็นตารางของโปรเจกต์ | ผลจาก R ถูก query ต่อด้วย SQL ได้ และอ้างอิงในเอกสารได้เหมือนผลอื่น | ✅ **ผ่าน Done-when (2026-08-16, 5 เทส กับ DuckDB จริง)** — data frame → CSV → `AnalysisStore.importFile` ซึ่งเป็น**เส้นทางเดียวกับการนำเข้าไฟล์ทุกอันในแอป** ⇒ DuckDB เดาชนิดให้ (id เป็น INT, value เป็น DOUBLE — เทสยืนยัน) ถ้าเขียน `INSERT` เองจะกลายเป็นตัวเดาชนิดตัวที่สอง และสิ่งแรกที่มันจะเดาผิดคือ factor ที่หน้าตาเหมือนตัวเลข · **`NA` เขียนเป็นช่องว่าง ไม่ใช่ตัวอักษร NA** ไม่งั้นคอลัมน์ตัวเลขที่มีค่าว่างกลายเป็นข้อความทั้งคอลัมน์ · **เขียนตารางเมื่อสั่งเท่านั้น** (`into_table`) — ถ้าเขียนเองทุกครั้งโปรเจกต์จะเต็มไปด้วย `r_result_7` และประเด็นของ §12.7 คือผลจาก R ต้อง*อ้างอิงได้* ซึ่งแปลว่ามีชื่อที่คนตั้ง · **สั่ง into_table แต่โค้ดไม่ได้คืน data frame = error ไม่ใช่ตารางว่าง** เพราะตารางว่างจะถูกอ่านรอบหน้าว่า "ไม่มีแถวที่ตรงเงื่อนไข" |
| **P14.4** ขอบเขตของสะพาน | หน้าตั้งค่าบอกว่าสะพานเห็นโฟลเดอร์อะไร + `r_install_package` แยกเป็นความเสี่ยงสูงเสมอ | ติดตั้งแพ็กเกจต้องผ่าน HITL ทุกครั้งแม้อยู่โหมดทำงานเองทั้งหมด | 🔶 **ครึ่งของ `install_package` ทำแล้ว (2026-08-15, 4 เทส)** — `AlwaysAsk` ทำให้มันหยุดถามคนเสมอแม้อยู่โหมดอัตโนมัติเต็ม และรายการอยู่ข้าง `RiskScorer` ไม่ใช่บนตัวทูล เพราะ manifest ประกาศอะไรก็ได้ · **หน้าตั้งค่าบอกขอบเขตแล้ว (2026-08-16)**: หน้าสถานะระบบมีส่วนของสะพาน R ที่พูดตรง ๆ ว่าสะพานรันด้วยสิทธิ์ของผู้ใช้ เห็นไฟล์ทุกอย่างที่ผู้ใช้เห็น ใช้ไลบรารี R ของผู้ใช้ และฟังเฉพาะ 127.0.0.1 · **ครึ่งของ R ทำแล้ว (2026-08-16, 4 เทส)**: `r_install_package` เป็นทูลของตัวเองและอยู่ใน `AlwaysAsk` ⇒ หยุดถามคนแม้อยู่โหมดอัตโนมัติเต็ม · **และปิดช่องที่ทำให้กฎนี้ไร้ความหมาย**: `AlwaysAsk` ผูกกับ*ชื่อทูล* แต่ `install.packages("x")` ที่อยู่ในบล็อกโค้ดของ `r_eval` ไม่ใช่ชื่อทูล ⇒ `r_eval` ปฏิเสธคำสั่งติดตั้งทั้งหกแบบ (`install.packages` · `remotes::` · `devtools::` · `BiocManager::` · `renv::` · `pak::`) แล้วชี้ไปที่ทูลที่ถาม — จับแบบหยาบโดยตั้งใจเหมือน destructive pattern ของ `run_shell` เพราะเตือนเกินราคาหนึ่งครั้งที่ถูกส่งไปทางอื่น ส่วนพลาดคือโค้ดของคนอื่นรันบนเครื่องโดยไม่มีใครถาม · ชื่อแพ็กเกจต้องเป็น*ชื่อ* ไม่ใช่นิพจน์ R และ repository ต้องเป็น https |

---

## P15 — GX10 เป็นสมองหลักที่ใช้ได้จริง

สเปก: [ARCH §17.1](ARCHITECTURE.md#171-gx10--ค่าที่ตั้งจริง-และสิ่งที่ระบบต้องอ่านจากมัน-ไม่ใช่เดา) · **อยู่หัวคิวเพราะมันปลดล็อกทุก phase ที่เหลือ** — `check.sh` ข้ามด่านโมเดลท้องถิ่นมาตลอดเพราะแรมไม่พอ และงานทีมหลายชั้นของ P16 ไม่มีทางทดสอบจริงบนโมเดล 3B

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P15.0** ตั้งค่าเซิร์ฟเวอร์ให้ครบก่อน | ค่าที่ถูกวัดจนได้แล้ว: `--enable-auto-tool-choice --tool-call-parser qwen3_coder --reasoning-parser qwen3` | คำขอที่มี `tools` คืน `tool_calls` ที่ parse ได้ · `content` มีแต่คำตอบ | ✅ **ทำแล้ว (2026-08-16)** — ใส่ `--reasoning-parser qwen3` แล้วผ่านทุกด่านในรอบเดียว ([E.22](docs/VERIFICATION_LOG.md#e22-gx10--คำสั่งที่ครบแล้ว-ผ่านทุกด่าน-2026-08-16)): `tool_calls` parse ได้ด้วย `qwen3_coder` · **`content` เหลือแค่คำตอบ ส่วนความคิด 232 ตัวอักษรอยู่คนละที่** · `scripts/gx10-serve.sh` เลือกชื่อ parser ให้เองจาก `vllm serve --help` และ `scripts/gx10-check.sh` พิสูจน์ผลที่ parse แล้ว ไม่ใช่ status code — ซึ่งเป็นเหตุผลที่วันที่ parser ผิดถูกจับได้ ทั้งที่เซิร์ฟเวอร์ตอบ 200 ทุกครั้ง |
| **P15.1** ต่อ endpoint จริง | เพิ่ม GX10 (`192.168.1.205:8000`) ใน `EndpointRegistry` เป็น Tier 1 · **อ่านชื่อโมเดลจาก `/v1/models` ไม่ใช่ตั้งไว้ในคอนฟิก** — สลับ checkpoint แล้วชื่อเปลี่ยน และคอนฟิกที่ตั้งชื่อไว้จะพังเงียบ ๆ | ปิด Foundation Models แล้วแอปยังตอบได้จาก GX10 · ถอดสาย LAN กลางสตรีมแล้วได้ข้อความที่อ่านรู้เรื่องจาก `ReadableFailure` **ไม่ใช่ spinner ค้าง** | 🔶 **ต่อแล้วและพิสูจน์ด้วยเทส เหลือขับจริง (2026-08-16, 9 เทส + 2 กฎใน check.sh)** — **ชื่อโมเดลมาจาก `/v1/models` ไม่ใช่จากคอนฟิก**: เว้นช่องโมเดลว่าง = "อะไรก็ตามที่เซิร์ฟเวอร์เสิร์ฟอยู่" ซึ่งเป็นค่าเดียวที่รอดจากการสลับ checkpoint — ชื่อที่ปักไว้จะผิดตั้งแต่วันที่เปลี่ยน และ**อาการไม่ใช่ error**: endpoint หลุดจากสายการ route แล้วแอปไปตอบด้วยโมเดลเล็กบนเครื่องนี้แทน โดยไม่มีอะไรบนจอบอก (เกิดจริงมาแล้วตอนเลิกใช้ `--served-model-name`, [E.21](docs/VERIFICATION_LOG.md#e21-gx10--ค่าที่ถูกต้องจริง-วัดด้วย-gx10-checksh-2026-08-16)) · **เสิร์ฟหลายโมเดลแล้วเว้นว่าง = ปฏิเสธ ไม่ใช่เดา** — เดาบน endpoint ที่คิดเงินคือใบเรียกเก็บที่ไม่มีใครตกลงด้วย · **สลับ checkpoint ระหว่างแอปเปิดอยู่ก็ตามทัน**: 4xx ทำให้ลืมชื่อเก่าแล้วถามใหม่ · **สายหลุดกลางคำตอบพูดเป็นภาษาคน** — แปลที่ `VLLMExecutor` ที่ยังเห็น `URLError` อยู่ (อีกทอดเดียวมันกลายเป็นสตริงแล้วแปลไม่ได้) เทสยิงว่าข้อความมีชื่อ endpoint และไม่มีคำว่า `NSURLErrorDomain` — พิสูจน์แล้วว่าแดงกับโค้ดเดิม · **ชุด executor contract ทั้งชุดผ่านกับ GX10 จริงแล้ว** ([E.22](docs/VERIFICATION_LOG.md#e22-gx10--คำสั่งที่ครบแล้ว-ผ่านทุกด่าน-2026-08-16)) ผ่าน `COAI_TEST_ENDPOINT` · **ค้าง: ขับบนแอปจริง** — ปิด Foundation Models แล้วถามผ่านหน้าแชท และถอดสาย LAN จริงกลางสตรีม |
| **P15.2** พิสูจน์ tool-calling + structured output ของโมเดลนี้ | รันชุดเดียวกับ [E.9](docs/VERIFICATION_LOG.md#e9-vllmexecutor-spike--tier-1-ผ่าน-openai-compatible-endpoint) | **structured output ผ่านแล้ว** ([E.19](docs/VERIFICATION_LOG.md#e19-gx10-เสิร์ฟจริงแล้ว--วัดจาก-endpoint-เป็น-2026-08-15)) · เหลือ tool call ที่ต้องรอ P15.0 · ถ้าประกอบกลับไม่ได้ ต้องเขียนลง VERIFICATION_LOG ว่าไม่ได้ ไม่ใช่หาทางเลี่ยงเงียบ ๆ | ✅ **วัดครบแล้ว (2026-08-16, [E.22](docs/VERIFICATION_LOG.md#e22-gx10--คำสั่งที่ครบแล้ว-ผ่านทุกด่าน-2026-08-16))** — tool call ประกอบกลับได้จริงและผลของทูลเดินกลับเข้ารอบถัดไป · structured output decode ได้ · **และชุด executor contract ของแอปเองรันผ่านกับ GX10 ทั้งชุดใน 41.8 วินาที** ผ่าน `COAI_TEST_ENDPOINT` ซึ่งแปลว่าที่ตรวจคือโค้ดที่แอปใช้จริง ไม่ใช่ curl ที่เขียนใกล้เคียงกัน — ทุกรอบก่อนหน้านี้ขึ้น `SKIPPED: Tier 1 unchecked` เพราะชุดเทสมองแค่ `127.0.0.1:1234` |
| **P15.2b** สมุดทดของโมเดลต้องไม่โผล่บนหน้าจอ | ถ้า `--reasoning-parser` ตั้งได้ → อ่านจาก `reasoning_content` · ถ้าตั้งไม่ได้ → `VLLMExecutor` ตัดทุกอย่างก่อน `</think>` ทิ้ง | ถามคำถามไทยแล้ว**หน้าแชทไม่มีบทพูดกับตัวเองภาษาอังกฤษ** · การตัดต้องเป็นทางเดียว ไม่ใช่กระจายอยู่ในทุก view ที่แสดงข้อความ | ✅ **ทำแล้ว (2026-08-16, 2 เทส + 1 กฎใน check.sh)** — **กันไว้ทั้งสองทาง**: เซิร์ฟเวอร์ที่ตั้ง `--reasoning-parser` ส่งมาคนละฟิลด์อยู่แล้ว ส่วนที่ไม่ได้ตั้ง (เกิดจริงมาแล้วสองครั้ง — [E.21](docs/VERIFICATION_LOG.md#e21-gx10--ค่าที่ถูกต้องจริง-วัดด้วย-gx10-checksh-2026-08-16)) ส่ง `<think>` มาใน `content` เหมือนโมเดลท้องถิ่นเป๊ะ · **`ReasoningSplitter` ย้ายจาก MLXRuntime มาอยู่ LLMProviders** เพราะสิ่งที่คิดว่าเป็นปัญหาของ tier ท้องถิ่น จริง ๆ เป็นปัญหาของ*แฟล็กที่เซิร์ฟเวอร์* — ตัวตัดตัวเดียวใช้ทั้งสอง tier · ตัดระหว่างสตรีม และทนแท็กที่ถูกหั่นคร่อม chunk (`<thi` + `nk>`) ซึ่งเป็นวิธีที่มันมาถึงจริง · สิ่งที่ค้างในตัวตัดตอนจบสตรีมถูก flush ออก ไม่ใช่ทิ้ง เพราะแท็กที่ขาดกลางคือข้อความที่โมเดลพิมพ์จริง · **กฎใน `check.sh` ห้ามที่อื่นรู้จักหน้าตาของแท็ก** — พิสูจน์แล้วว่าแดงเมื่อ view model เริ่มตัดเอง ซึ่งเป็นวิธีแก้ที่ผุดขึ้นมาเองตอนเห็นแท็กบนจอ แล้วจบด้วยการตัดคนละแบบในสามที่ · **ค้าง**: ยังไม่ได้ถามจริงผ่านหน้าแชท (เป็นข้อเดียวกับที่ P15.1 ค้างไว้) |
| **P15.3** เพดานบริบทอ่านจาก endpoint | `ContextManager` เลิกถือ `16_384` ที่เขียนตาย — อ่าน `max_model_len` จาก `/v1/models` แล้วกันที่ให้ขาออก | เปลี่ยน `--max-model-len` ที่เซิร์ฟเวอร์แล้วแอปปรับตามโดยไม่ต้อง build ใหม่ · คำตอบยาวไม่ถูกตัดกลางประโยคเพราะงบเข้ากินที่ขาออกหมด | ✅ **ทำแล้ว (2026-08-16, 3 เทส + 2 กฎใน check.sh)** — `max_model_len` มาจากคำตอบเดียวกับที่บอกชื่อโมเดล จึงไม่มีทางเป็นคนละช่วงเวลากัน · `capabilities.contextWindow` ของแต่ละ endpoint เป็นค่าที่เซิร์ฟเวอร์ประกาศ ไม่ใช่ `32_768` ที่เขียนไว้ในไฟล์ทุกตัว — **ของเดิมผิดทั้งสองทาง**: ขยายที่เซิร์ฟเวอร์แล้วแอปไม่ได้ใช้ ลดที่เซิร์ฟเวอร์แล้วแอปยิงเกินหน้าต่างที่มันเชื่อว่าใหญ่กว่า · **งบขาเข้ากันที่ให้ขาออกหนึ่งในสี่** (`ContextManager.promptBudget`) เพราะหน้าต่างถูกแบ่งกับคำตอบ และบนโมเดลนี้สมุดทดกิน `max_tokens` ด้วย — วัดแล้วว่าคำขอที่ตั้ง 200 คืน `content` ว่างพร้อม `finish_reason: length` ([E.20](docs/VERIFICATION_LOG.md)) · เซิร์ฟเวอร์ที่ไม่บอกหน้าต่าง (LM Studio) ได้ค่า nil ไม่ใช่ 0 แล้วตกไปที่พื้น 16k ซึ่งเป็นตัวเลขที่วัดจากเครื่องนี้ ไม่ใช่การเดา · **ต้องเปิดแอปใหม่ ไม่ต้อง build ใหม่** — อ่านตอนบูตเพราะ router ถูกประกอบตอนนั้น |
| **P15.4** จัด prompt ให้ prefix cache ทำงาน | ส่วนคงที่ (system + รายการทูล) อยู่หน้าสุดเสมอ ส่วนที่เปลี่ยนอยู่ท้าย | วัด TTFT ของคำขอที่สองในบทสนทนาเดียวกัน **เร็วกว่าคำขอแรกอย่างมีนัย** และบันทึกตัวเลขจริง — ไม่ใช่อ้างว่า "ควรจะเร็วขึ้น" | ✅ **วัดแล้ว (2026-08-16, [E.24](docs/VERIFICATION_LOG.md#e24-prefix-cache-จ่ายจริง--335-วิ--032-วิ-2026-08-16) · 1 เทส)** — **TTFT 3.35 วิ → 0.32 วิ (เร็วขึ้น 90%)** เมื่อ prefix เดิมถูกใช้ซ้ำ · **มีตัวคุม**: prefix อื่นที่ยาวเท่ากันกลับไปใช้ 3.45 วิ ⇒ ที่ประหยัดได้คือ cache ไม่ใช่ "เครื่องอุ่นขึ้น" ซึ่งเป็นคำอธิบายที่ตัวเลขคู่เดียวแยกไม่ออก · โครงสร้าง prompt ถูกอยู่แล้ว (system เดียวหน้าสุด → รายการทูลเรียงตามชื่อ → ประวัติตามลำดับ) สิ่งที่เพิ่มคือ**เทสที่ล็อกลำดับรายการทูล** เพราะมันถูก render ไว้หน้าบทสนทนา — ปลั๊กอินที่โหลดเร็วกว่าเดิมเสี้ยววินาทีแล้วลำดับสลับ = ทุกบทสนทนาเสีย cache เงียบ ๆ **สามวินาทีต่อเทิร์นแขวนอยู่กับลำดับที่ไม่มีใครนึกว่าต้องรักษา** (พิสูจน์แล้วว่าเทสแดงเมื่อถอด `.sorted` ออก) |
| **P15.5** วัดตอนหลายสายพร้อมกัน | ยิง 8 สายขนานผ่าน `ModelRouter` · อ่าน `vllm:num_requests_running`/`num_requests_waiting` จาก `/metrics` ประกอบ | ได้ตัวเลขจริงว่ารองรับกี่สายก่อนเวลาตอบแย่ลงเกินรับได้ — **ตัวเลขนี้คือเพดานจริงของ span of control ใน P16** ไม่ใช่เลข 7 ที่ยืมจาก ICS | ✅ **วัดแล้ว (2026-08-16, [E.23](docs/VERIFICATION_LOG.md#e23-หลายสายพร้อมกัน--และเพดานที่เจอตัวแรกไม่ใช่ของ-gpu-2026-08-16) · 1 เทส + `swift run TierOneCheck`)** — **span of control = 16 สายขนาน** เวลาต่อคำตอบแย่ลงแค่ **1.5 เท่า** (7.3 → 11.0 วิ) แต่ปริมาณงานรวมขึ้น **11 เท่า** (11.4 → 128.5 โทเคน/วิ) · `num_requests_running` อ่านได้ 16 และ `waiting` เป็น 0 ตลอด ⇒ **ยังไม่เจอเพดานของ GPU เลย เพดานที่ 16 เป็นเพดานที่เราตั้งเอง** · **เพดานตัวแรกที่เจอไม่ใช่ของ GPU**: หกสายเร็วเท่าสายเดียว แต่สายที่เจ็ดทำเวลารวมพุ่ง 60% ทั้งที่เวลาต่อคำขอไม่ขยับ — `URLSession.shared` เปิดได้ 6 connection ต่อ host ⇒ **เพดานจริงของแอปเป็น 6 มาตลอดโดยไม่มีอะไรบอก** (ทีมสี่คน + แชท + ลำดับงาน = ครบโควตาพอดี และอาการของการเกินคือช้าลงเฉย ๆ ไม่ใช่ error) · `VLLMExecutor` ใช้ session ของตัวเองแล้ว และมีเทสที่ยิง 8 สายผ่านเซิร์ฟเวอร์ปลอมซึ่ง**ไม่ตอบใครจนกว่าจะครบแปด** — พิสูจน์แล้วว่าค้างจนหมดเวลาเมื่อเอาค่าที่ตั้งออก |
| **P15.6** สถานะ Busy อ่านจากเซิร์ฟเวอร์ | `/metrics` เปิดอยู่ ⇒ [§22.6](ARCHITECTURE.md#226-eoc-dashboard--command-tree-view) ไม่ต้องให้ agent รายงานตัวเอง | หน้า Command Tree แสดง Busy ที่ตรงกับคิวจริงของ vLLM · **ไม่มี code path ไหนที่สถานะ Busy มาจากคำกล่าวอ้างของ agent** (§2.5) | 🔶 **ตัวอ่านทำแล้ว หน้าจอรอ P16 (2026-08-16, 4 เทส)** — `ServerLoad`/`ServerLoadReader` ใน LLMProviders อ่าน `num_requests_running`/`waiting` จาก `/metrics` และ **`TierOneCheck` ใช้จริงแล้ว** จึงไม่ใช่ของที่ไม่มีใครเรียก (D6) · **"อ่านไม่ได้" ต่างจาก "ว่าง"** — ไม่มี metrics คืน nil ไม่ใช่ 0 เพราะหน้าจอที่โชว์ 0 อย่างมั่นใจกับเครื่องที่มองไม่เห็น คือการกล่าวอ้างที่ไม่มีใครตรวจ ซึ่งเป็นสิ่งเดียวกับที่ §2.5 ห้าม · บรรทัด `# TYPE …gauge` มีชื่อ metric เดียวกับตัวอย่างจริง เลยมีเทสกันไว้ว่าอ่าน comment เป็นค่าไม่ได้ · **ค้าง**: หน้า Command Tree เอง ซึ่งเป็นของ P16 — แต่ตัวเลขที่มันจะแสดงมีที่มาที่ตรวจได้แล้ว |
| **P15.7** ตัดสินเรื่องความเร็ว | วัดได้ **≈ 4.7 โทเคน/วินาที** ที่ bf16 ซึ่งเป็นเพดานแบนด์วิดท์ของเครื่อง ไม่ใช่การจูน · และสมุดทดกินโควตาเดียวกัน | **เป็นข้อตัดสินใจ ไม่ใช่ข้อทำ** — เลือกระหว่าง checkpoint ที่เล็กลง (NVFP4/FP8/AWQ) หรือใช้โมเดลเล็กกว่าสำหรับบทบาทที่ผิดแล้วไม่เสียหาย · **ตราบใดที่ยังเป็น 4.7 tok/s องค์กรสามชั้นของ P16 ยังไม่ใช่ของที่ใช้ได้จริง** | 🔶 **ข้อสันนิษฐานที่ตั้งข้อนี้ไว้ ถูกวัดจนเปลี่ยนไปแล้ว — ยังเป็นการตัดสินใจของผู้ใช้** (2026-08-16) · NVFP4 ให้ **11.4 โทเคน/วิ ต่อสาย** ([E.20](docs/VERIFICATION_LOG.md)) ไม่ใช่ 4.7 · และที่สำคัญกว่านั้น **งานหลายสายไม่ได้แบ่งเค้กก้อนเดิม**: 16 สายขนานได้รวม **128.5 โทเคน/วิ** โดยแต่ละสายช้าลงแค่ 1.5 เท่า ([E.23](docs/VERIFICATION_LOG.md#e23-หลายสายพร้อมกัน--และเพดานที่เจอตัวแรกไม่ใช่ของ-gpu-2026-08-16)) ⇒ **ข้อความเดิมที่ว่า "องค์กรสามชั้นยังใช้จริงไม่ได้" ตั้งอยู่บนเลขสายเดียว ซึ่งไม่ใช่รูปแบบงานของ P16** · สิ่งที่ยังต้องตัดสินคือคุณภาพ: 27B ที่ NVFP4 พอไหมสำหรับบทบาทที่ผิดแล้วเสียหาย หรือจะกันบทบาทนั้นไว้ให้ endpoint ที่คิดเงิน |

---

## P16 — AI Organization (M17 Command)

สเปก: [ARCH §22](ARCHITECTURE.md#22-ai-organization--จากทีมเดียวเป็นองค์กร-m17-command) · **ขึ้นกับ P15** — องค์กรหลายทีมบนโมเดลที่รับได้ทีละสาย คือการทำให้ทุกอย่างช้าลงเป็นเท่าตัว

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P16.1** การซ้อนอยู่ในไทป์ | `Team: Specialist` — ทีมย่อยคือลูกทีมหนึ่งรายของทีมแม่ ไม่ใช่ชนิดใหม่ | เทสที่พิสูจน์ว่า**ทีมแม่รับได้แค่ `Deliverable` ของทีมย่อย ไม่มีทางเข้าถึง transcript หรือ ledger ภายในของมัน** — และพิสูจน์ที่ระดับไทป์ ไม่ใช่ด้วยการตรวจค่า runtime | ✅ **ทำแล้ว (2026-08-16, 4 เทส)** — `SubTeam: Specialist` · ทีมย่อยถูกถือเป็น `any Specialist` จากฝั่งทีมแม่ ซึ่งมีสมาชิกเดียวที่คืนค่าคือ `execute(_:) -> Deliverable` ⇒ **ไม่มีสมาชิกให้เรียกเพื่อขอ transcript หรือ ledger เลย** — เทสที่สำคัญที่สุดในไฟล์นี้คือเทสที่*คอมไพล์ผ่าน* ไม่ใช่เทสที่รันแล้วเช็คค่า · หัวหน้าทีมย่อยตรวจก่อนส่งขึ้น หัวหน้าทีมแม่ตรวจซ้ำ — QA สองชั้นบนหลักฐานชุดเดิม · ผลงานถูกประทับ id ของใบงานทีมแม่ตอนส่งขึ้น เพราะสำหรับชั้นบน นี่คือคำตอบของใบงาน*ของมัน* |
| **P16.2** Span of control + แตกทีมอัตโนมัติ | เกิน 7 ใบ → แตกทีมย่อย โดยต้องผ่านสามเงื่อนไข (จำนวน · แตกได้จริง · งบรับไหว) | แผน 12 ใบที่อิสระต่อกัน → แตกเป็นทีมย่อยและจบงาน · แผน 12 ใบที่**ผูกกันแน่น** → ไม่แตก และบอกเหตุผล · **งบไม่พอ → ไม่แตก และบอกว่าต้องยกเพดานเท่าไร ไม่ใช่แตกไปแล้วหมดงบกลางทาง** | ✅ **กฎทำแล้ว (2026-08-16, 4 เทส)** — เกิน 7 ใบ = ต้องแตก · **สามเงื่อนไขต้องจริงพร้อมกัน ไม่ใช่ข้อใดข้อหนึ่ง** และมีเทสที่ยิงตรงข้อนี้: ตื้นพอ+จ่ายไหวแต่ผูกกัน → ไม่แตก · ตื้นพอ+อิสระแต่งบไม่พอ → ไม่แตกและ**บอกจำนวนที่ต้องยกเพดาน** ($4.20 ไม่ใช่ "งบไม่พอ") · ข้อความปฏิเสธพูดเหตุผลเชิงผลลัพธ์: แตกงานที่ผูกกันแน่นได้สองทีมที่แก้ขัดกันเอง และแตกเงียบ ๆ จนงบหมดกลางทางแย่กว่าทำช้า · **หัวหน้าทีมเรียกกฎนี้เองแล้ว (2026-08-16, 2 เทส)** — และตอนต่อสายก็เจอว่ามันมี**เลขของตัวเอง**: `maxFanOut = 4` ในหัวหน้าทีม ส่วน `CommandRules` ประกาศ 7 ⇒ "แผนกว้างได้แค่ไหน" มีสองคำตอบ และคำตอบที่ทำงานจริงคืออันที่ผู้เรียกบังเอิญไปชน · ตอนนี้ค่าเริ่มต้นคือ `CommandRules.spanOfControl` และการตรวจเดินผ่าน `needsSplitting` ตัวเดียวกัน (พารามิเตอร์ `cap` ยังอยู่ เพราะเทสที่จงใจบีบให้แคบลงเป็นคนละเรื่องกับความเห็นที่สอง) |
| **P16.3** เพดานความลึกและสิทธิ์ที่หดอย่างเดียว | ลึกได้ 3 ชั้น · ชั้นที่ 4 ต้องคนอนุมัติ · **ทีมมอบสิทธิ์ที่ตัวเองไม่มีให้ลูกทีมไม่ได้** | เทสที่ทีมย่อยพยายามใช้ทูลที่ทีมแม่ไม่มีสิทธิ์ → **ถูกปฏิเสธที่ gateway** · เทสที่แผนพยายามซ้อนชั้นที่ 4 → หยุดขออนุมัติ · เทสว่า Engineer ยังห้าม fan-out ทุกชั้น (§2.4 ไม่มีช่องหลบผ่านการตั้งทีม) | ✅ **ทำแล้ว (2026-08-16, 4 เทส)** — **สิทธิ์ไหลลงอย่างเดียวและหดได้เท่านั้น**: `childTools − parentTools` ต้องว่าง ไม่งั้นปฏิเสธพร้อมชื่อทูลที่เกิน — ถ้าไม่มีกฎนี้ "ตั้งทีมใหม่ที่ต้องใช้ `run_shell`" คือวิธีหลบ policy gate ที่ถูกที่สุด และเป็นรูปเดียวกับ privilege escalation ผ่าน process ลูก · **ตรวจซ้ำตอนทีมเขียนแผนของตัวเองด้วย** เพราะแผนเกิดหลังทีม — ได้สิทธิ์สามอันแล้วเขียนแผนที่ต้องใช้อันที่สี่ คือการยกระดับสิทธิ์ที่มาช้ากว่าหนึ่งก้าว · ความสามารถของทีม = union ของลูกทีม ∩ เพดาน charter ∩ สิทธิ์ทีมแม่ (ทุกขั้นตัดออกได้อย่างเดียว) · ชั้นที่ 4 ถูกปฏิเสธพร้อมเหตุผลเชิงต้นทุน (ทุกชั้นคูณจำนวน agent · R6 วัดไว้ ~15×) · **Engineer ห้าม fan-out ทุกชั้น** มีเทสวนทุก depth |
| **P16.4** Situation Board | `Scope.board(runID)` ใน M7 · เขียนได้ทางเดียวคือผ่าน QA ที่ผ่านแล้ว · อ่านแบบ pull ตอนเริ่มงาน | ข้อค้นพบที่ **QA ตีกลับ ไม่ปรากฏบนบอร์ด** (เทสยิงตรงข้อนี้ เพราะเป็นข้อเดียวที่กันไม่ให้บอร์ดกลายเป็นเครื่องขยายการหลอน) · สองทีมที่ได้งานทับกัน ทีมที่สองเห็นว่าทีมแรกทำแล้วและไม่ทำซ้ำ | ✅ **ทำแล้ว (2026-08-16, 6 เทส)** — `Scope.board(runID)` เป็น scope จริงใน M7 อ่านผ่านทางเดิม ไม่ใช่ระบบใหม่ · **ของที่ QA ตีกลับขึ้นบอร์ดไม่ได้** และคำปฏิเสธพูดเหตุผลไว้ในตัว: กระดานที่รับความเห็นที่ยังไม่ตรวจ ทำให้ความเข้าใจผิดของทีมเดียวกลายเป็น "สิ่งที่ทั้งองค์กรรู้" ภายในรอบเดียว · **ไม่มีหลักฐานติดมาก็ขึ้นไม่ได้** เพราะทีมอื่นจะได้แต่เชื่อ ไม่ได้ชั่งน้ำหนัก · ทีมที่สองที่ขอจับใบงานเดิมได้คำตอบว่าใครถืออยู่ (unity of command) พร้อมสิ่งที่ทีมแรกพบแล้ว · **เป็น pull ไม่ใช่ feed** — ถามตอนจะเริ่มงาน และถามเรื่องอื่นได้ค่าว่าง เพราะบอร์ดที่ตอบทุกคำถามด้วยทุกอย่างจะทำให้ context ของทุกทีมโตพร้อมกัน ซึ่งเป็นราคาที่ §22.2 มีไว้เพื่อเลี่ยง |
| **P16.5** Command Tree View | วาด span tree ตามสายบังคับบัญชา + สถานะ Busy จาก `AdmissionControl` ไม่ใช่จากที่ agent รายงานเอง | เปิดจอระหว่างงานจริงแล้วเห็นว่าใครสั่งใครและใครทำอะไรอยู่ · **ทีมที่ span ปิดแล้วต้องไม่ขึ้นว่า "กำลังทำ"** · เข้าถึงด้วยคีย์บอร์ดล้วนและอ่านออกเสียงได้ | ✅ **ทำแล้ว (2026-08-16, 7 เทส)** — อยู่ที่ **ระบบ → สายบังคับบัญชา** · สายบังคับบัญชาไม่ได้ถูกสร้างใหม่: span มี `parent` อยู่แล้ว ต้นไม้จึงเป็นสิ่งที่*บันทึกไว้* ไม่ใช่สิ่งที่เดาย้อนหลัง · **"กำลังทำ" ต้องจริงทั้งสองข้อ**: สถานะเป็น running **และ** ยังไม่มี `endedAt` — โปรเซสที่ตายระหว่างทำงานกับตอนเขียนผล ทิ้งคำว่า running ไว้ และกระดานที่อ่านแต่สถานะจะโชว์ทีมนั้นว่าทำงานอยู่ตลอดกาล (มีเทสยิงตรงข้อนี้) · **รอคนตัดสินใจถูกวาดคนละแบบกับกำลังทำ** เพราะอันหนึ่งต้องการความอดทน อีกอันต้องการคน · **นับสองที่ ไม่เกลี่ยให้เท่ากัน**: จำนวนกล่องที่ติดไฟ เทียบกับ `vllm:num_requests_running` ที่เซิร์ฟเวอร์รายงาน (P15.6) — เวลาไม่ตรงกันคือข้อมูลที่ควรเห็น เพราะมันบอกว่าฝั่งไหนเก่า · **ทุกแถวพูดชั้น สถานะ เวลา และจำนวนงานย่อยออกมาเป็นคำ** (§14.4) เพราะการเยื้องระยะไม่ได้บอกอะไรกับคนที่ฟัง และสีจุดสถานะไม่เคยเป็นสิ่งที่แบกความหมายไว้ลำพัง · แถวที่พับไว้เป็น "ที่ผู้ใช้พับเอง" ไม่ใช่ "ที่ยังไม่กาง" เพื่อให้กิ่งที่เพิ่งเกิดระหว่างรันโผล่มาเอง · กิ่งที่หัวอยู่นอกหน้าต่างเวลาถูกยกขึ้นเป็นราก ไม่ถูกทิ้ง — กระดานที่ขาดไปทั้งกิ่งคือกระดานที่ผิดแบบเงียบ ๆ ส่วนกระดานที่โชว์กิ่งกำพร้าคือกระดานที่ไม่ครบแบบเห็น ๆ · **ค้าง**: เปิดดูระหว่างงานจริงหลายทีม (ต้องมี P16.2 ที่ orchestrator เรียกกฎแตกทีมเองก่อน) |
| **P16.6** รายงานทางแชทจากหัวหน้าคนเดียว | IC เท่านั้นที่ส่งถึงผู้ใช้ · escalation เป็นข้อยกเว้นที่ทะลุขึ้นมาได้ทันที · เงียบเกินเวลาที่ตั้ง = ส่ง "ยังทำอยู่" | รันงานที่มี 3 ทีม → ผู้ใช้ได้ข้อความจากแหล่งเดียว · หนึ่งทีม escalate → ถึงผู้ใช้ทันทีโดยไม่รอสรุปรอบถัดไป | ✅ **ทำแล้ว (2026-08-16, 6 เทส)** — `maySpeakToUser` ตอบจริงว่ามีแต่ IC ที่พูดกับผู้ใช้ได้ · **escalation ทะลุขึ้นทันทีและไม่ถูกมัดรวม** — สองทีมที่หยุดรอ = สองข้อความ เพราะการรวมกันทำให้อันที่สองอ่านเป็นบริบทของอันแรก และแต่ละอันคือทีมที่หยุดอยู่จริง · สรุปรอบปกติถูกหน่วงตามช่วงเวลา ส่วน**ความเงียบที่นานเกินกำหนดเป็นข้อความในตัวมันเอง พร้อมบอกว่านานเท่าไร** เพราะ "ยังทำอยู่" เฉย ๆ คือประโยคที่โปรเซสที่ค้างก็ส่งได้เหมือนกัน · เวลาเป็นพารามิเตอร์ ไม่ใช่ `Date()` ที่ซ่อนอยู่ข้างใน — การตัดสินใจที่ขึ้นกับนาฬิกาที่มองไม่เห็นคือการตัดสินใจที่เทสไม่ได้ |
| **P16.7** Team Role & Team Skill | `TeamCharter { mission, domain, acceptanceCriteria }` · ความสามารถของทีม = union ของลูกทีม ∩ เพดานของ charter | charter ที่ไม่มี acceptance criteria **สร้างไม่ได้ตั้งแต่ไทป์** (กฎเดียวกับ `Assignment` ตั้งแต่ P1.1) | ✅ **ทำแล้ว (2026-08-16, 3 เทส)** — `TeamCharter` เป็น failable init: ไม่มีเกณฑ์ตรวจรับ หรือ mission ว่าง = สร้างไม่ได้ · เป็น init ที่ล้มเหลวได้ ไม่ใช่เมธอด validate ที่ต้องจำไปเรียก — charter ที่ตรวจรับไม่ได้ต้อง**ประกอบขึ้นมาไม่ได้** เพราะที่ระดับทีมมันคือทั้งกิ่งขององค์กรที่ผลิตงานซึ่งไม่มีใครตรวจได้ · `toolCeiling` ว่าง = ใช้เพดานของทีมแม่ ไม่ใช่ "ไม่จำกัด" |

---

## P17 — Machine Control (M18 ScreenDriver)

สเปก: [ARCH §23](ARCHITECTURE.md#23-machine-control--ให้ระบบทดสอบหน้าจอตัวเองได้-m18-screendriver) · **เหตุผลที่คุ้ม**: ทุกรอบของการขับหน้าจอด้วยมือเจอบั๊กที่เทสทั้งชุดมองไม่เห็น (8 · 10 · 5 · 7 · 7 · 3) และของค้างหลายข้อในแผนวันนี้ค้างที่ "ตัวขับพิมพ์ลง `TextField` ไม่ได้" ไม่ใช่ที่ "ยังไม่ได้ทำ"

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P17.1** ชั้น AX เป็นหลัก | `AXNavigator` — หาองค์ประกอบจาก label/role ไม่ใช่จากพิกัด | หาและกดปุ่มบนหน้าจอจริงได้จาก**ชื่อที่ VoiceOver อ่าน** · องค์ประกอบที่หาไม่เจอให้ข้อความที่บอกว่าหาอะไรไม่เจอ ไม่ใช่คลิกที่ว่างเปล่า | 🔶 **เขียนแล้วและกฎถูกเทสครบ เหลือขับจริงบนเครื่องที่ให้สิทธิ์ (2026-08-16, 9 เทส)** — target `ScreenDriver` ใหม่ · `AXNavigator` หาองค์ประกอบจาก**ชื่อที่ screen reader อ่าน** แล้วสั่ง `AXPress` (ไม่ใช่คลิกพิกัด ซึ่งพังเงียบเมื่อเลย์เอาต์ขยับหนึ่งพิกเซล และบางครั้งไปโดนตัวอื่นแทน) · **snapshot เป็น value** จึงค้นและเปรียบเทียบได้โดยไม่ต้องมีจอ ซึ่งเป็นเหตุผลที่กฎการค้นหาเทสได้จริง · **เจอสองอันขึ้นไป = error ไม่ใช่เอาอันแรก** เพราะตัวขับที่เงียบ ๆ เลือกปุ่มผิดคือเทสที่ผ่านทั้งที่กดผิด · **ปุ่มที่กดไม่ได้ ต่างจากปุ่มที่หาไม่เจอ** — อันหนึ่งคือหน้าจอบอกว่ายังทำไม่ได้ อีกอันคือตัวขับมองผิดหน้าต่าง · **ค้าง**: ขับหน้าจอจริง ต้องมีคนกดอนุญาต Accessibility ให้ก่อน (§23.2 ข้อ 1 — ระบบให้สิทธิ์ตัวเองไม่ได้) |
| **P17.2** CGEvent สำหรับสิ่งที่ AX เขียนไม่ได้ | พิมพ์ลง `TextField`/`TextEditor`/`SecureField` | **พิมพ์ข้อความไทยลงช่องค้นหาแล้วผลค้นเปลี่ยนจริง** (ข้อนี้คือสิ่งที่ตัวขับปัจจุบันทำไม่ได้ และเป็นเหตุผลทั้งหมดของ task นี้) · ปลดล็อกของค้าง: บันทึกไฟล์ใน File Viewer · `SecretField` · ประกอบต้นฉบับ | 🔶 **เขียนแล้ว เหลือขับจริง (2026-08-16)** — `CGEventKeyboardSetUnicodeString` ไม่ใช่ key code เพราะ **key code คือตำแหน่งบนคีย์บอร์ด US และ `ก` ไม่มีตำแหน่ง** — นี่คือกำแพงที่ตัวขับ AppleScript เดิมชนและเป็นเหตุผลทั้งหมดของ task นี้ · ลอง `AXValue` ก่อนเสมอ (ช่องที่เขียนค่าได้ เร็วกว่าและรบกวนน้อยกว่า) แล้วค่อยตกมาที่คีย์บอร์ด ไม่ใช่กลับกัน · โฟกัสช่องผ่าน AX ก่อนพิมพ์ ตัวอักษรจะได้ลงที่ที่ตั้งใจ ไม่ใช่ที่ที่โฟกัสบังเอิญอยู่ · **ค้าง**: ยืนยันว่าพิมพ์ไทยลงช่องค้นหาแล้วผลค้นเปลี่ยนจริง — ต้องรันบนเครื่องที่ให้สิทธิ์แล้ว |
| **P17.3** สิทธิ์และขอบเขต | ตรวจสิทธิ์ TCC ก่อนเริ่ม · **จำกัดที่ pid ของแอปตัวเองเป็นค่าเริ่มต้น** | ไม่มีสิทธิ์ → บอกว่าต้องเปิดอะไรที่ไหน **ไม่ใช่ล้มเหลวเงียบ** · พยายามขับแอปอื่นโดยไม่ได้เปิดสิทธิ์ไว้ → ถูกปฏิเสธ · **build ใหม่แล้วสิทธิ์หาย ต้องตรวจจับได้และบอก** (ลายเซ็น ad-hoc ทำให้เกิดจริง) | ✅ **ทำแล้ว (2026-08-16, 5 เทส)** — จำกัดที่ pid ของตัวเองเป็นค่าเริ่มต้น (API ชุดนี้ขับได้ทุกแอปบนเครื่อง รวมถึงโปรแกรมจัดการรหัสผ่าน) · ไม่มีสิทธิ์ = บอก**ชื่อ pane ตรง ๆ** และบอกว่าต้องใช้ทำอะไร เพราะ "ขออนุญาต accessibility" ไม่ใช่คำสั่งที่ใครทำตามได้บนเครื่องที่ไม่เคยตั้งมาก่อน · **ข้อที่แพงที่สุดคือสิทธิ์ที่หายหลัง build**: TCC ผูกกับลายเซ็น แอปเซ็น ad-hoc (R11) อาการคือตัวขับที่เคยทำงานเงียบไปเฉย ๆ — จำสถานะที่เคยได้รับไว้ (**ลืมไม่ได้ ไม่งั้นการถูกเพิกถอนจะอ่านเป็นการรันครั้งแรก**) แล้วรายงานว่า *เพิกถอน* พร้อมวิธีแก้ที่ถูกต้องคือ**เอารายการเดิมออกแล้วเพิ่มใหม่** — บอกให้ "เปิดสวิตช์" กับคนที่มองสวิตช์ที่เปิดค้างอยู่ คือคำแนะนำที่ใช้ไม่ได้ |
| **P17.4** หลักฐานที่ QA อ่านได้ | `Evidence.kind = .screenObservation` — AX snapshot + ภาพ ก่อน/หลัง | QA ปฏิเสธ deliverable ที่อ้างว่ากดปุ่มแล้วแต่ภาพก่อน/หลังไม่ต่างกัน · ทุกการกระทำของตัวขับเป็น span | ✅ **ทำแล้ว (2026-08-16, 4 เทส)** — `Evidence.kind` มี `.screenObservation` แล้ว และทุกการกระทำคืน `ScreenAction` ที่พก snapshot ก่อน/หลัง · **หน้าจอไม่เปลี่ยน = `nothingHappened` ไม่ใช่สำเร็จ** — "ผมกดแล้ว" คือคำกล่าวอ้าง ส่วนสอง snapshot มันต่างหรือไม่ต่างก็เท่านั้น · เทียบจาก**บรรทัดที่ screen reader จะอ่าน** ไม่ใช่พิกัด — หน้าต่างที่แค่ถูกลากไปสองพิกเซลไม่นับว่าเปลี่ยน มิฉะนั้นทุกอย่างจะ "เปลี่ยน" และการตรวจจะผ่านโดยไม่ตรวจอะไรเลย · หลักฐานบอกด้วยว่า*อะไร*ขึ้นมาใหม่และอะไรหายไป · **ตัวควบคุมที่ AX กดไม่ได้ถูกรายงานเป็นข้อค้นพบด้าน accessibility** เพราะสิ่งที่ตัวขับกดไม่ได้ คนใช้ VoiceOver ก็กดไม่ได้ — ถ้าไม่บันทึกไว้ ข้อบกพร่องจะถูกซ่อนเพราะทางอ้อมมันใช้ได้ · ทุกการกระทำเป็น span |
| **P17.5** สิ่งที่อ่านจากจอคือข้อมูลที่ไม่น่าเชื่อถือ | ข้อความจากจอผ่าน hook chain เหมือน tool output | เทสที่หน้าจอ (หรือหน้าเว็บที่เปิดอยู่) มีข้อความสั่งให้ agent ทำอะไรบางอย่าง → **agent ไม่ทำตาม** | ✅ **ทำแล้ว (2026-08-16, 4 เทส + 1 กฎใน check.sh)** — การป้องกันเป็น**เชิงโครงสร้าง ไม่ใช่เชิงถ้อยคำ**: ข้อความจากจอเข้ามาเป็น **tool output** ผ่าน `read_screen` ซึ่งโปรโตคอลบอกโมเดลอยู่แล้วว่าเป็น*ผลลัพธ์* ไม่ใช่*คำสั่ง* — การเอาไปใส่เป็น system/user message คือช่องโหว่ทั้งหมด และไม่มีการเขียนคำเตือนแบบไหนแก้ได้ · **กฎใน check.sh ห้ามไม่ให้ snapshot กลายเป็น system message** พิสูจน์แล้วว่าแดง · บรรทัดที่พยายามสั่งงาน AI ถูก**ชี้ให้เห็น ไม่ใช่ลบทิ้ง** เพราะการลบคือการซ่อนสิ่งที่เอกสารนั้นพยายามทำ และหน้าที่เหลือก็ยังต้องอ่านได้ · ข้อความธรรมดาไม่ติดธง เพื่อให้ธงยังมีความหมาย |

---

## P18 — Knowledge ที่เป็นหอสมุดจริง

สเปก: [ARCH §11.7](ARCHITECTURE.md#117-เกณฑ์ว่าอะไรคือ-ขัดแย้ง-จริง--และข้อผิดพลาดที่พบจากการใช้จริง)–[§11.9](ARCHITECTURE.md#119-การจัดหมวดความรู้--library-of-congress-classification)

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P18.1** เกณฑ์ข้อขัดแย้งตาม NLI | สามเงื่อนไข (คำถามเดียวกัน · ปฏิเสธกันได้จริง · บริบทเดียวกัน) + **กฎภาษา** | **คู่ไทย–อังกฤษที่แปลแล้วตรงกัน ต้องไม่ถูกยกเป็นการ์ด** (อาการที่พบจริง 2026-08-15) · ตัวเลขที่ต่างกันแต่ช่วงความเชื่อมั่นทับกัน → ไม่ใช่ข้อขัดแย้ง · คำแนะนำผู้ใหญ่ vs เด็ก → `bothInContext` ไม่ใช่ contradiction | ✅ **ทำแล้วและวัดกับโมเดลจริง (2026-08-16, 6 เทส + 1 กฎใน check.sh)** — schema ถามสามเงื่อนไข**แยกกัน** (`sameQuestion` · `mutuallyExclusive` · `sameContext`) แทน `contradicts` ตัวเดียวที่บรรยายเกณฑ์ไว้ใน prompt: แบบเดิมให้โมเดลทำทั้งการคิดและการบันทึก แล้วเหลือของให้ตรวจย้อนหลังศูนย์ชิ้น · แต่ละข้อปฏิเสธด้วยเหตุผลของตัวเอง (`differentQuestion` / `differentContext` / `notMutuallyExclusive`) เพราะ "ตอบคนละคำถาม" กับ "ขัดแย้งกัน" คือคนละข้อเท็จจริงเกี่ยวกับคลัง และ NLI ชี้ว่า *reference indeterminacy* คือสาเหตุติดป้ายผิดอันดับหนึ่ง · **กฎภาษาอยู่ในโค้ด ไม่ใช่แค่ใน prompt**: `isTranslation` เป็น true = ไม่ยกการ์ด ไม่ว่าโมเดลจะตอบข้ออื่นว่าอะไร · **คู่ข้ามภาษาต้องมั่นใจถึง 0.9** (ปกติ 0.7) เพราะโมเดลที่อ่านฝั่งหนึ่งไม่ออกจะตอบว่า "ขัดแย้ง" ด้วยความมั่นใจสูง ซึ่งคืออาการที่ทำให้ §11.7 ถูกเขียน · **วัดกับ GX10 จริง: คำแปลถูกยกผิด 0/4 · ข้อขัดแย้งจริงข้ามภาษาจับได้ 4/4 (0.92–0.98)** ผ่าน `swift run TierOneCheck` — ทั้งสองด้าน เพราะเกณฑ์ที่แน่นเกินไปจนข้อขัดแย้งจริงหลุด คือความเสียหายอีกด้านที่วัดไม่เจอถ้าดูแต่ false positive |
| **P18.2** ด่านตรวจภาษาก่อนถึงโมเดล | ใช้ `bge-m3` ที่มีอยู่: ใกล้กันมาก + ภาษาต่างกัน = สงสัยว่าเป็นคำแปล | คู่ที่เป็นคำแปลถูกกรองออกก่อนเรียกโมเดล — วัดจำนวนการเรียกโมเดลที่ประหยัดได้จริง | 🔴 **วัดแล้วว่าทำไม่ได้ — ไม่ใช่ยังไม่ได้ทำ (2026-08-16, [E.25](docs/VERIFICATION_LOG.md#e25-ด่านกรอง-คำแปล-ด้วย-embedding--วัดแล้วทำไม่ได้-2026-08-16))** — คู่ที่เป็นคำแปลได้ cosine **0.401–0.760** ส่วนคู่ที่**ขัดแย้งกันจริงข้ามภาษา**ได้ **0.322–0.770** ⇒ ทับกันเกือบทั้งช่วง ไม่มีเกณฑ์ไหนแยกได้ · เหตุผลเป็นเชิงโครงสร้าง ไม่ใช่เรื่องขยับตัวเลข: **embedding วัดว่าข้อความเกี่ยวกับอะไร และข้อความที่ขัดแย้งกันคือข้อความที่เกี่ยวกับเรื่องเดียวกันเป๊ะ** — คู่ที่ใกล้ที่สุดทั้งชุด (0.770) คือข้อขัดแย้งที่ชัดที่สุดทั้งชุด · **สิ่งที่เหลือและทำแล้ว**: `TextScriptReader` อ่านภาษาจากตัวอักษร (60/40 จากประโยคเวชปฏิบัติจริงที่มีชื่อยาอังกฤษปน) แล้ว P18.1 ใช้มัน**ยกเกณฑ์**ของคู่ข้ามภาษา แทนการข้ามโมเดล — เป็นกฎเรื่องหลักฐาน ไม่ใช่ทางลัดที่เดาแทนโมเดล · **เช็คใน `EmbeddingCheck` ยังวัดซ้ำทุกครั้งและจะแดงถ้าวันหนึ่งสองกลุ่มแยกกันได้** เพื่อให้ "ทำไมถึงไม่มีด่านกรอง" เป็นข้อเท็จจริงที่ยังถูกวัด ไม่ใช่ความทรงจำ |
| **P18.3** เอนทิตีข้ามภาษาเป็นโหนดเดียว | `EntityGraph` เก็บ `labels: [(text, lang)]` · alignment จาก embedding ไม่ใช่พจนานุกรม | `burnout` กับ `ภาวะหมดไฟ` เป็นโหนดเดียวที่มีสองชื่อ · **แยกคืนได้** และการรวมที่ระบบเดาบอกว่าเดา · ต่ำกว่าเกณฑ์ = ไม่รวม และไม่นับว่าขัดแย้ง | 🔶 **โครงสร้างทำแล้ว การรวมอัตโนมัติวัดแล้วว่าทำไม่ได้ (2026-08-16, 9 เทส · [E.26](docs/VERIFICATION_LOG.md#e26-รวมเอนทิตีข้ามภาษาด้วย-embedding--วัดแล้วทำเองไม่ได้-2026-08-16))** — โหนดเดียวหลายชื่อ (`EntityLabel` เก็บ text + script) · แสดงชื่อตามภาษาที่ผู้อ่านใช้ · **คีย์ของกราฟคงที่ ไม่ขยับตามภาษาที่เลือกดู** เพราะคีย์คือสิ่งที่เส้นเชื่อมกัน กราฟที่เปลี่ยนรูปเมื่อสลับภาษาคือกราฟคนละอัน · แยกคืนได้และ**การรวมพก score ติดตัว** เพราะ "ระบบคิดว่าเหมือนกัน" ไม่ใช่เหตุผลที่ใครชั่งน้ำหนักได้ · **แต่ระบบรวมเองไม่ได้**: วัดบนน้ำหนักจริงแล้วคู่ที่ได้คะแนนสูงสุดทั้งชุดคือ `ความดัน`↔`pressure` ที่ **0.919** ซึ่งเป็นคู่ที่ §11.8 เตือนไว้เองว่าห้ามรวม ส่วนคู่ที่ถูกต้องได้แค่ 0.485–0.620 ⇒ ไม่มีเกณฑ์ไหนแยกได้ · **ระบบจึงเสนอ และ `canonicalKey` นับเฉพาะที่คนยืนยันแล้ว** — คำแนะนำที่รวมโหนดเงียบ ๆ แยกไม่ออกจากข้อเท็จจริง · **หน้าจอให้กดยืนยัน/ปฏิเสธทำแล้ว (2026-08-16, 4 เทสกับ SurrealDB จริง)** — รายการอยู่ในหน้ากราฟ พร้อมคะแนนความคล้ายและประโยคที่บอกว่าคู่ที่คะแนนสูงสุดที่วัดได้เป็นคู่ที่ผิด (E.26) · **เก็บทั้งสองคำตอบ**: การปฏิเสธมีค่าไม่น้อยกว่าการยืนยัน เพราะรายการที่เสนอสิ่งที่คนปฏิเสธไปแล้วซ้ำ ๆ คือรายการที่คนเลิกอ่าน — และจากที่วัดมา การปฏิเสธคือกรณีปกติ ไม่ใช่ข้อยกเว้น · คำตอบผูกกับ*คู่* ไม่ผูกกับลำดับที่เห็น (ตอบกลับข้างก็ทับของเดิม ไม่เพิ่มแถวที่สอง) · เสนอตอนเปิดหน้ากราฟ ไม่ใช่ตอน ingest — การฝัง 200 ชื่อไม่ใช่สิ่งที่ควรทำลับหลังคน |
| **P18.4** จัดหมวดแบบ LC | คลาสหลัก 21 ตัว + subclass สองตัวอักษร · หนึ่งเอกสารหลายหมวดได้ · `assignedBy: .system\|.user` | หน้าคลังแบ่งสัดส่วนตามหมวดและกดเข้าไปดูได้ · **"ยังจัดหมวดไม่ได้" เป็นสถานะที่แสดง ไม่ใช่ยัดลง A** · หมวดที่ระบบเดาแก้ได้จากหน้าจอ | ✅ **ทำแล้ว (2026-08-16, 7 เทส)** — 21 คลาสหลักตาม LC จริง (ไม่มี I, O, W, X, Y เพราะ LC เองไม่ใช้ — เคสที่ไม่มีวันเป็นอะไรได้ คือเคสที่สุดท้ายมีคนยัดของลงไป) · **เก็บถึง subclass เท่านั้น**: `RA1234` ถูกอ่านเป็น `RA` เพราะเลขเรียกเต็มมีไว้ระบุตำแหน่งบนชั้นจริง ซึ่งไม่มีความหมายที่นี่ · **หนึ่งเอกสารหลายหมวด** — งานสาธารณสุขที่ใช้สถิติได้ทั้ง `RA` และ `HA` จริง ๆ · **"ยังจัดหมวดไม่ได้" เป็นชิปของตัวเองบนแถบสัดส่วน** กดกรองได้ ไม่ใช่ช่องว่างในแผนภูมิ และไม่ถูกยัดลง A ซึ่งจะทำให้ทั้งระบบหมวดหมู่ไร้ความหมายทั้งที่ดูเรียบร้อย · **ตัวจัดหมวดใช้คำ ไม่ใช่โมเดล** เพราะสิ่งที่จัดหมวดแล้วอธิบายไม่ได้ว่าทำไม ทำให้คนไม่มีอะไรให้เถียง — และ §11.9 ทั้งข้อมีไว้เพื่อให้หมวดที่ผิดถูกแก้ได้ · ป้ายบนแถวบอกว่า**ระบบเดาหรือคนจัด** และ hover เห็นคำที่ทำให้เดาแบบนั้น · แก้หมวดได้จากเมนูคลิกขวา **และจาก rotor ของ VoiceOver ทุกตัวเลือก** (กฎ accessibility ใน check.sh จับได้ตอนแรกว่ามีแต่คลิกขวา) · ตัวจัดหมวดไม่เคยเขียนทับสิ่งที่คนจัดเอง |
| **P18.5** เส้นข้ามหมวดถูกเน้น | กราฟเน้นความสัมพันธ์ที่ลากข้ามคลาส | เปิดกราฟของคลังที่มีทั้ง `RA` และ `QA` แล้วเส้นข้ามหมวดเห็นชัดกว่าเส้นในหมวดเดียวกัน | ✅ **ทำแล้ว (2026-08-16, 5 เทส)** — `Classifier.crossesClasses` แยกเส้นข้ามคลาสออกจากเส้นในคลาสเดียวกันได้ (`RA`↔`QA` ข้าม · `RA`↔`RT` ไม่ข้าม เพราะเป็น R ด้วยกัน) · **ปลายที่ยังจัดหมวดไม่ได้ = ไม่นับว่าข้าม** เพราะการเดาว่าข้ามจะไปเน้นเส้นที่ยังไม่มีใครจัดหมวดพอดี · ✅ **วาดแล้ว (2026-08-16, +4 เทส)** — entity ได้หมวดจาก*เอกสารที่เอ่ยถึงมัน* ซึ่งเป็นที่เดียวที่หมวดจะมาได้ (การถามโมเดลคือการสร้างมันขึ้นมา) · เส้นข้ามหมวดหนา 2.5 และทึบกว่า ส่วนเส้นในหมวดเดียวกันจางลงเป็นพื้นหลัง — **ใช้น้ำหนักและความทึบ ไม่ใช่สีอย่างเดียว** เพราะความต่างที่แบกด้วยเฉดสีคือความต่างที่ผู้อ่านบางคนไม่มี · **รายการข้อความพูดคำว่า "ข้ามหมวด" ออกมา** เพราะเส้นที่หนากว่าไม่ได้บอกอะไรกับคนที่ฟัง และรายการคือที่ที่ screen reader พบกราฟ · entity ที่อยู่ทั้งสองหมวดคือสะพาน ไม่ใช่การข้าม |

---

## P19 — สถิติการแพทย์และสาธารณสุขให้ครบ

สเปก: [ARCH §12.6.1](ARCHITECTURE.md#1261-ชุดสถิติที่งานแพทย์และสาธารณสุขต้องมี--และช่องว่างที่วัดแล้ว) · **กฎ R12 ใช้ทุกข้อ**: ตรวจกับค่าที่ตีพิมพ์แล้ว ไม่ใช่กับผลของตัวเอง

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P19.0** `survivalUnchecked` ต้องหายไปก่อน | วันนี้ `StatGate` มี `case survival` แต่ทางเดินจริงคือ `survivalUnchecked(summary:)` — ประกาศไว้แต่ไม่ได้คำนวณ | ระหว่างที่ยังไม่มีการคำนวณจริง **StatGate ต้องปฏิเสธ ไม่ใช่คืนสรุปที่ดูเหมือนคำตอบ** | ✅ **ทำแล้ว (2026-08-16, 1 เทส)** — `survivalUnchecked` หายไปแล้ว แทนที่ด้วย `survival()` ที่ **throw** `StatError.notImplemented(test:plannedIn:)` · ของเดิมซื่อสัตย์เรื่องเนื้อหาแต่**ผิดที่รูปร่าง**: มันคืน `StatResult` ที่มี `NaN` กับสรุปที่*ผู้เรียกส่งเข้ามาเอง* แล้วส่งกลับออกไป — สิ่งที่มีรูปร่างเป็นผลลัพธ์จะถูกเรนเดอร์เป็นผลลัพธ์ ถูกคัดลอกลงต้นฉบับเป็นผลลัพธ์ และแถวเดียวที่บอกว่า "ยังไม่ได้คำนวณ" คือแถวที่คนอ่านข้าม · ข้อความปฏิเสธบอกด้วยว่าต้องทำอะไรแทน (คำนวณจากเครื่องมือภายนอกแล้วบันทึกที่มา อย่าอ้างว่าเป็นผลของระบบนี้) เพราะการปฏิเสธที่ไม่บอกทางออกคือทางตัน · **ตามด้วย P19.3 ในวันเดียวกัน**: `survival(_:_:)` คำนวณจริงแล้ว ส่วน `notImplemented` ยังอยู่และยังเป็นคำตอบของทุกอย่างในตาราง §12.3 ที่โมดูลนี้ยังทำไม่ได้ |
| **P19.1** มาตรวัดระบาดวิทยา | RR · OR · RD · NNT · incidence/prevalence · age-standardised rate พร้อม 95% CI | ตรงกับตัวอย่างในตำราทุกตัว · **ทุกค่ามาพร้อมช่วงความเชื่อมั่น** เพราะค่ากลางเปล่า ๆ คือตัวเลขที่ถูกอ้างเกินจริง | ✅ **ทำแล้ว (2026-08-16, 14 เทส)** — ตรวจกับ **Physicians' Health Study** (แอสไพริน 104/11,037 · ยาหลอก 189/11,034) ซึ่งเป็นตัวเลขที่ตำราระบาดวิทยาใช้กันทั่วไป: **RR 0.550 (0.434–0.698)** · OR 0.546 · RD −0.0077 · **NNT 130 (93–213)** — ทุกช่วงคำนวณอิสระด้วยอิมพลีเมนต์นอกโค้ดเบสนี้ตาม R12 · **ไม่มีทางขอค่าเปล่า ๆ ได้เลย** เพราะ return type คือ `Estimate` ที่มีช่วงและ**ชื่อวิธีของช่วง**อยู่ในตัว ("95% CI" บอกแค่ระดับ ไม่ได้บอกว่าเป็นวิธีไหน และสองวิธีบนข้อมูลเดียวกันต่างกันพอจะเปลี่ยนข้อสรุป) · **Wilson ไม่ใช่ Wald สำหรับสัดส่วน** — ที่ 0/20 Wald ให้ [0,0] คือประกาศความแน่นอนสมบูรณ์จากการสังเกต 20 ครั้ง · **NNT ถูกปฏิเสธเมื่อช่วงของ RD คร่อมศูนย์** เพราะช่วงที่กลับด้านตรงนั้นไม่ใช่ช่วงเดียวที่ต่อเนื่อง แต่เป็นสองเส้นที่มีรูตรงกลาง — และรายงานเป็น NNT/NNH ตามทิศทาง ไม่ใช่ตัวเลขติดลบที่เครื่องหมายหายระหว่างตารางกับประโยค · **เซลล์ศูนย์ถูกปฏิเสธ ไม่ใช่บวก 0.5 เงียบ ๆ** · เรียกผ่าน `run_stat_test` ได้จริง ไม่ใช่มีแต่เทสของตัวเอง |
| **P19.2** ค่าการทดสอบวินิจฉัย | sensitivity · specificity · PPV/NPV · LR+/LR− · ROC/AUC | PPV/NPV เปลี่ยนตาม prevalence จริง (ตรงนี้คือที่คนอ่านผิดบ่อยที่สุด — หน้าจอต้องบอก prevalence ที่ใช้) | ✅ **ทำแล้ว (2026-08-16, 15 เทส)** — การทดสอบที่ sensitivity 90% specificity 90% ใช้ในที่ที่ความชุก 1% **ถูกเมื่อผลบวก 8.3% ของเวลา** · ตัวเลขเดียวกันที่ความชุก 10% ได้ 50% และที่ 50% ได้ 90% — **การทดสอบตัวเดิม สามคำตอบ** · `predictiveValues` **เรียกโดยไม่ระบุความชุกไม่ได้เลย** ไม่มี default ไม่มี overload ที่ละไว้ และผลลัพธ์**พกความชุกที่ใช้กลับมาด้วย** เพราะ PPV ที่ไม่มีความชุกกำกับไม่ใช่ตัวเลขที่ใครใช้ได้ · ความชุกของ*ชุดข้อมูลเอง* มีให้ใช้แต่ต้องเรียกชื่อมันตรง ๆ (`samplePrevalence`) เพราะการอ่าน PPV จากเซลล์ของตาราง = สมมติความชุกของงานวิจัยนั้นเงียบ ๆ ซึ่งเป็นค่าที่*การออกแบบงานวิจัยเลือกเอง* · LR+/LR− ไม่ขึ้นกับความชุก และ specificity = 1 พอดีถูกปฏิเสธแทนการรายงาน LR+ เป็นอนันต์ ซึ่งเป็นข้อเท็จจริงเรื่องขนาดตัวอย่าง ไม่ใช่เรื่องการทดสอบ · **AUC = Mann–Whitney** (โอกาสที่คนป่วยได้คะแนนสูงกว่าคนไม่ป่วย) พร้อมช่วง Hanley–McNeil — เสมอนับครึ่ง ไม่ใช่เข้าข้างการทดสอบ |
| **P19.3** Survival | Kaplan–Meier + log-rank · Cox PH · ตรวจสมมติฐาน PH (Schoenfeld) | เทียบกับชุดข้อมูลมาตรฐานที่มีผลตีพิมพ์ · **ข้อมูล censored ถูกใช้ ไม่ใช่ถูกตัดทิ้ง** (เทสยิงตรงข้อนี้) · PH ไม่ผ่าน → StatGate เตือนเหมือน assumption อื่น | ✅ **ทำแล้ว (2026-08-16, 10 เทส)** — ตรวจกับ **Freireich et al. (1963)** (6-MP เทียบยาหลอก คนละ 21 ราย) ซึ่งเป็นชุดที่ตำราทุกเล่มใช้: **มัธยฐาน 23 สัปดาห์ เทียบ 8** · **log-rank χ² = 16.79** · **HR ≈ 4.52 (2.03–10.09)** — ตรงกับค่าที่ตีพิมพ์ทั้งสามตัว และทุกช่วงคำนวณอิสระนอกโค้ดเบสนี้ (R12) · **เทสเรื่อง censored ยิงตรงตามที่ Done-when สั่ง** และยิงสามทาง: ตัดทิ้ง → มัธยฐานหด · นับเป็นเหตุการณ์ → เส้นตกเร็วเกิน · ใช้ให้ถูก → คนที่ censored ที่สัปดาห์ 11 ยังอยู่ในตัวหารที่ 10 และหายไปที่ 13 · **Cox = Newton–Raphson บน partial likelihood (Breslow)** เลือก Breslow เพราะเลขของมันตรวจมือกับตำราได้ ซึ่งเป็นกฎ R12 · **PH ตรวจด้วย Schoenfeld residuals และรายงานเป็น `AssumptionCheck` เหมือนข้ออื่น** — เหตุการณ์น้อยกว่า 5 = "ยังไม่ได้ตรวจ ไม่ใช่ผ่าน" · **ไม่มีเหตุการณ์เลย = ปฏิเสธ ไม่ใช่ fit** · เส้นที่ไม่เคยลงถึงครึ่ง = ไม่มีมัธยฐาน ไม่ใช่รายงานเวลาสุดท้ายที่สังเกตได้ (ซึ่งเป็นรูปของคำกล่าวอ้างเรื่องการรอดชีวิตที่มองโลกในแง่ดีเกินจริงส่วนใหญ่) · เรียกผ่าน `run_stat_test` ได้ และทูล**ขอธง censoring มาตรง ๆ ไม่อนุมานเอง** เพราะการอนุมานคือวิธีที่คนที่ยังไม่เกิดเหตุการณ์กลายเป็นเหตุการณ์เงียบ ๆ |
| **P19.4** Count/rate models | Poisson · negative binomial + ตรวจ overdispersion | ข้อมูล overdispersed ที่ยัด Poisson → gate เตือนและเสนอ negative binomial | ✅ **ทำแล้ว (2026-08-16, 8 เทส)** — **ความล้มเหลวที่ task นี้มีไว้จับ ไม่หน้าตาเหมือนความล้มเหลว**: ข้อมูลที่กระจายเกินยังให้สัมประสิทธิ์ที่ดูสมเหตุสมผล ผิดแค่ standard error ซึ่ง**แคบเกินจริง** ⇒ ผลที่ไม่จริงกลายเป็นมีนัยสำคัญโดยไม่มีอะไรบนจอดูผิด · จึงตรวจ **Pearson χ²/df ทุกครั้ง ไม่ใช่เป็นตัวเลือก** · ชุดที่กระจายเกิน (χ²/df = 15.0) ถูกเตือนพร้อม**ชื่อโมเดลที่แก้ได้** ส่วนชุดที่ปกติ (0.06) ผ่านเฉย ๆ เพราะคำเตือนที่ขึ้นทุกครั้งคือคำเตือนที่ไม่มีใครอ่าน · **negative binomial รันได้จริง ไม่ใช่ชื่อในคำเตือน** — บนชุดเดียวกันให้ผลเท่าเดิมแต่ช่วงกว้างขึ้น **3 เท่า** (SE 0.088 → 0.266) ซึ่งคือทั้งหมดที่การเตือนนี้มีไว้เพื่อ · ข้อมูลที่*ไม่ได้*กระจายเกิน ปฏิเสธ negative binomial แทนที่จะ fit ให้ เพราะมันคือ Poisson ที่มีพารามิเตอร์ทิ้งเปล่าหนึ่งตัว · ผลลัพธ์ที่ไม่ใช่จำนวนนับ (1.5 หรือ −1) ถูกปฏิเสธ ไม่ใช่ปัดเป็นจำนวนนับ · เรียกผ่าน `run_stat_test` แล้วคำเตือนกับชื่อโมเดลทางเลือกไปถึง Analyst จริง |
| **P19.5** Mixed / multilevel | random intercept/slope สำหรับข้อมูลซ้อนชั้นและการวัดซ้ำ | เทียบกับผลที่ตีพิมพ์ · **ข้อมูลซ้อนชั้นที่ยัด OLS → gate เตือนว่า CI จะแคบเกินจริง** ซึ่งเป็นความผิดพลาดที่พบบ่อยที่สุดในงานสาธารณสุข | 🔶 **random intercept ทำแล้ว · random slope ปฏิเสธไว้ (2026-08-16, 9 เทส)** — องค์ประกอบความแปรปรวนใช้เอกลักษณ์ ANOVA ทางเดียวแบบสมดุล ซึ่ง**แม่นตรงและตรวจด้วยมือได้** ชุดทดสอบจึงจงใจสมดุล · ชุดทดสอบ: 6 คลินิก คลินิกละ 5 คน → **ICC 0.981 · design effect 4.92 ⇒ ข้อมูล 30 ชิ้นมีค่าเท่าข้อมูลอิสระ 6.1 ชิ้น** และช่วงความเชื่อมั่นจากวิธีที่ถือว่าทุกชิ้นอิสระ **แคบเกินจริง 2.22 เท่า** · **ผลลัพธ์ที่ gate คืนคือค่าที่แก้แล้ว ไม่ใช่ค่าเดิมที่แปะคำเตือน** — อย่างหลังคือความผิดพลาดเดิมที่มีโน้ตติด · เกณฑ์เตือนอยู่ที่ **design effect ไม่ใช่ ICC** เพราะ ICC น้อยกับกลุ่มใหญ่ทำลายช่วงได้พอ ๆ กับ ICC มากกับกลุ่มเล็ก · ข้อมูลที่ไม่ได้ซ้อนชั้นจริง ผ่านเงียบ ๆ · ความแปรปรวนระหว่างกลุ่มที่ติดลบถูกตรึงเป็นศูนย์และรายงานว่า "ไม่มีการจับกลุ่ม" เพราะความแปรปรวนติดลบไม่ใช่ปริมาณ และการพิมพ์ออกมาคือการชวนให้มีคนตีความเครื่องหมายของมัน · **ค้าง: random slope** — ปฏิเสธด้วย `notImplemented` ตามกฎ P19.0 เพราะการประมาณแบบไม่ใช่ maximum likelihood จะให้ตัวเลขที่อ่านเหมือนของจริงทุกประการ |
| **P19.6** ความสอดคล้อง + ขนาดตัวอย่าง | κ · ICC · Bland–Altman · power/sample size | คำนวณขนาดตัวอย่างได้ก่อนเก็บข้อมูล และ **G2 ถามหามันตอนอนุมัติ baseline** | ✅ **ทำแล้ว (2026-08-16, 15 เทส)** — κ (Cohen/Fleiss) กับ ICC มีตั้งแต่ P11.8 · เพิ่ม **ขนาดตัวอย่าง** (สองค่าเฉลี่ย: ผลต่าง 5 SD 10 power 0.8 → **63 ต่อกลุ่ม** · สองสัดส่วน 30% vs 45% → **163 ต่อกลุ่ม**) และ **power ย้อนกลับ** สำหรับกรณีที่ตัวอย่างคือใครก็ตามที่หาได้ · **เศษของคนปัดขึ้นเสมอ** เพราะปัดลงคือการออกแบบให้ power ไม่พอโดยพลาดไปไม่ถึงหนึ่งคน · ตัวเลขพก**สมมติฐานที่ใช้คำนวณ**ติดไปด้วย เพราะขนาดตัวอย่างที่ไม่มีสมมติฐานคือตัวเลขที่คณะกรรมการจริยธรรมตรวจไม่ได้ · **Bland–Altman**: คำตอบคือ limits of agreement ไม่ใช่ bias — สองเครื่องที่ต่างกันเฉลี่ย 1.7 อาจต่างกัน 8 ในคนคนเดียว · ใช้ t ไม่ใช่ z เพราะงานที่ต้องใช้วิธีนี้คืองานเล็ก · **"ใช้แทนกันได้ไหม" ต้องมีเกณฑ์ทางคลินิกมาจากคน** — ไม่มีเลขคณิตไหนตัดสินว่าต่างกันเท่าไรถึงสำคัญ · เทสที่พิสูจน์ว่าสหสัมพันธ์ไม่ใช่ความสอดคล้อง: สองเครื่องที่อ่านห่างกัน 5 หน่วยทุกคน มี r = 1.0 · **G2 ถามหาขนาดตัวอย่างจริงแล้ว** — งานที่เก็บข้อมูลจากคนผ่าน G2 ไม่ได้ถ้ายังไม่ระบุ และ**ระบุตัวเลขลอย ๆ โดยไม่มีสมมติฐานก็ยังไม่ผ่าน** · งานที่ไม่ได้เก็บจากคนไม่ถูกถาม (แสดงเป็น vacuous ไม่ใช่ติ๊กเขียว) เพราะประตูที่ถามทุกคนคือช่องที่ทุกคนพิมพ์อะไรก็ได้ผ่าน |
| **P19.7** Meta-analysis | fixed/random effects · I² · forest/funnel | ตรงกับตัวอย่างที่ตีพิมพ์ · funnel plot ที่ไม่สมมาตรถูกรายงาน ไม่ใช่ปล่อยให้คนมองเอง | ✅ **ทำแล้ว (2026-08-16, 9 เทส)** — fixed effect (inverse variance) · random effects (DerSimonian–Laird) · **Q · I² · τ²** ซึ่ง I² ตรวจด้วยมือได้จากตัวเลขในเทส `(9.343 − 6)/9.343 = 35.78%` · **random effects ให้ช่วงกว้างกว่าเสมอ** เพราะมันเลิกสมมติว่ามีค่าจริงค่าเดียว และน้ำหนักเกลี่ยลง — งานใหญ่ถูกเชื่อน้อยลงเมื่อความต่างส่วนหนึ่งเป็นของจริงไม่ใช่ noise · **τ² ติดลบถูกตรึงเป็นศูนย์** เพราะมันไม่ใช่ปริมาณ แต่คือ "ไม่ต่างกันเลย บวกความบังเอิญ" · **ชุดที่ขัดกันคนละทางถูกบรรยายว่าอาจรวมไม่ได้** (I² > 95%) — เพราะการรวมยังคืนตัวเลขให้อยู่ดี ซึ่งคือตัวอันตราย · **funnel asymmetry ถูก*รายงานเป็นคำ* ไม่ใช่วาดแล้วให้คนมองเอง** ตาม Done-when: Egger's test พร้อมประโยคว่ามันแปลว่าอะไร (งานเล็กให้ผลต่างจากงานใหญ่อย่างเป็นระบบ เข้าได้กับ publication bias · ค่ารวมน่าจะเกินจริง) · ใช้เกณฑ์ p < 0.10 ตามธรรมเนียมของการทดสอบนี้ เพราะพลังต่ำ · **"ไม่พบความไม่สมมาตร" เขียนไว้ชัดว่าไม่ได้แปลว่าไม่มี** เพราะการทดสอบพลังต่ำเมื่องานน้อยกว่าสิบชิ้น · เรียกผ่าน `run_stat_test` แล้ว**ค่ารวมไม่เคยเดินทางลำพัง** — heterogeneity กับ funnel ติดไปด้วยเสมอ เพราะโมเดลที่อ่านตัวเลขเปล่าจะยกตัวเลขเปล่าไปใช้ |

---

## P20 — Design System · HIG · Liquid Glass

สเปก: [ARCH §24](ARCHITECTURE.md#24-design-system--human-interface-guidelines-m13) · **ทำหลัง P16/P17** เพราะ Command Tree กับหน้าจอที่ตัวขับต้องอ่าน เป็นสองสิ่งที่จะเปลี่ยนโครงหน้าจอมากที่สุด — จัดระบบก่อนแล้วรื้ออีกรอบคือการทำงานสองครั้ง

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P20.1** ตรวจ API จริงก่อนออกแบบ | `.glassEffect(_:in:isEnabled:)` · `GlassEffectContainer` บน SDK ที่เครื่องนี้มีจริง | บันทึกลง [`docs/VERIFICATION_LOG.md`](docs/VERIFICATION_LOG.md) ว่าใช้ได้แค่ไหนบน macOS 26 **ก่อน**เขียนดีไซน์ทับ | ✅ **วัดแล้ว ([E.27](docs/VERIFICATION_LOG.md#e27-liquid-glass-บน-sdk-ของเครื่องนี้--และเครื่องมือตรวจที่ตอบผิด-2026-08-16))** — `.glassEffect()` · `.glassEffect(_:in:)` · `GlassEffectContainer(spacing:)` · `.regular.tint().interactive()` · `.buttonStyle(.glass)` **มีจริงบน SDK 26.5** · **แต่ `.glassEffect(_:in:isEnabled:)` ที่ ARCH §24.2 เขียนไว้ ไม่มีพารามิเตอร์ `isEnabled` จริง** — คอมไพล์ไม่ผ่าน เปิด/ปิดแก้วต้องแยกสาขาใน view (แก้ใน ARCHITECTURE แล้ว) · **บทเรียนเรื่องเครื่องมือตรวจ**: grep ใน `.swiftinterface` เจอแค่ `GlassButtonStyle` และตอบว่าไม่มี `glassEffect` ทั้งที่มี — ไฟล์ interface ไม่ใช่รายการ API ต้องคอมไพล์โค้ดจริงถึงจะรู้ · แพ็กเกจตั้ง `.macOS(.v26)` อยู่แล้ว จึงไม่ต้องมี `@available` ห่อ |
| **P20.2** Design tokens + component กลาง | สี ระยะ ตัวอักษร ชุดเดียว · ชุด control กลาง (แถว · กล่อง · popover · sheet · แถบสถานะ) | **กฎใน `check.sh` ที่พิสูจน์ว่า fail ได้**: view ไฟล์ใหม่ที่ประกาศสีหรือระยะเองแทนที่จะใช้ token → แดง · ไม่งั้น §24 จะเป็นความตั้งใจอีกฉบับ | 🔶 **token + กฎทำแล้ว · หนี้เก่ายังอยู่ในรายการ (2026-08-16)** — `DesignTokens.swift` ถือระยะ (`Space`) มุม (`Radius`) และ**ชั้น** (`Surface`) ที่เดียว · **ไม่มีจานสีของตัวเอง** เพราะสีระบบตอบโหมดสว่าง/มืด/คอนทราสต์สูง/สีเน้นที่ผู้ใช้เลือกไว้แล้ว ส่วนจานสีที่เขียนเองตอบไม่ได้สักข้อแต่ดูเรียบร้อยในภาพหน้าจอเดียว · **กฎใน check.sh พิสูจน์แล้วว่าแดง** เมื่อ view ใหม่เขียน `.padding(11)` เอง · **รายการหนี้เก่าว่างเปล่าแล้ว (2026-08-16)** — ทุก view ในแอปเอาระยะและมุมจาก `DesignTokens` แล้ว · ตัวแปรรายการยังอยู่ เพราะรูปของกฎคือสิ่งสำคัญ: จะใส่ไฟล์กลับต้องมีคนเขียนเหตุผลกำกับ และรายการว่างคือรูปที่แข็งที่สุดของ "มีแต่หดได้" (พิสูจน์แล้วว่ากฎยัง fail ได้ด้วย `.padding(11)` ในไฟล์จริง) · **ค้าง**: ระยะสี่ค่าแทนค่าที่ตั้งกันเองห้าค่า กล่องบางใบจึงขยับ 2pt และ**ยังไม่มีใครไล่ดูทุกหน้าจอหลังย้าย** · ชุด component กลาง (แถว/กล่อง/popover/sheet) ที่ตอนนี้มีแค่ `contentBox()` กับ `SectionHeading` |
| **P20.3** ชั้นแก้วที่ทำหน้าที่ | แก้วเฉพาะสิ่งที่ลอยเหนือเนื้อหา · ตาราง/ตัวเลข/ผลสถิติอยู่บน Solid Layer เสมอ | ตรวจ contrast ของทุกหน้าจอที่มีตัวเลขทั้งโหมดสว่างและมืด — **ค่า p ที่อ่านผิดเพราะพื้นหลังโปร่งแสง คือความเสียหายที่แก้ไม่ได้** | 🔶 **บังคับเชิงโครงสร้างแล้ว · การมองด้วยตาสองโหมดยังค้าง (2026-08-16, 2 กฎใน check.sh)** — **เจอของจริงระหว่างทำ**: แถบสถานะวางเงิน เวลา และจำนวน rework ไว้บน `.bar` ซึ่งโปร่งแสง และมันอยู่ก้นหน้าจอที่เลื่อนได้ ⇒ เนื้อหาที่เลื่อนผ่านวิ่งทะลุตัวเลข — ย้ายไป solid แล้ว ส่วนแถบแท็บยังเป็นแก้วถูกต้อง เพราะมันลอยเหนือเนื้อหาและถือ*ชื่อ* ไม่ใช่ตัวเลข · **กฎสองข้อ พิสูจน์แล้วทั้งคู่**: พื้นหลังโปร่งแสงต้องประกาศผ่าน `Surface` เท่านั้น (ทำให้ "อันนี้อยู่บนแก้วไหม" ตอบได้ด้วย grep เดียว) · ไฟล์ที่ format ตัวเลข (`%.Nf`) ห้ามใช้ `Surface.floating` — heuristic หยาบโดยตั้งใจ เพราะราคาของการเตือนเกินคือคอมเมนต์หนึ่งบรรทัด ส่วนราคาของการพลาดคือค่า p ที่อ่านผิด · **ค้าง**: ไล่ดูด้วยตาทุกหน้าจอทั้งสองโหมด ซึ่งเป็นงานที่ต้องมีคนมอง |
| **P20.4** Confidence indicators | แสดงความมั่นใจของ `ConflictDetector` และ `ToolProficiency` ตรงจุดที่คนกำลังจะอนุมัติ | **ความมั่นใจต่ำ = ต้องมีคนดูก่อน ไม่ใช่แค่โชว์ตัวเลข** — เทสยิงว่าเส้นทางอนุมัติอัตโนมัติถูกปิดเมื่อความมั่นใจต่ำกว่าเกณฑ์ | ✅ **ทำแล้ว (2026-08-16, 6 เทส)** — `HookChain` อ่าน `ToolProficiency` ตอนตัดสิน และ**ต่ำกว่า 2 ใน 3 = ปิดทางอัตโนมัติ ไม่ว่าสวิตช์ autonomy จะตั้งไว้ยังไง** · ตัวเลขไปอยู่ตรงหน้าคนที่กำลังจะกดอนุมัติ ซึ่งเป็นที่เดียวที่มันเปลี่ยนอะไรได้ — ตัวเลขข้างปุ่มที่ไม่มีวันโผล่คือของประดับ · **"ยังใช้ไม่พอจะบอกอะไร" ต้องไม่เท่ากับ "แย่"** — ต่ำกว่า 5 ครั้งไม่ปิดทาง ไม่งั้นทูลใหม่ทุกตัวเกิดมาพร้อมการถาม และสิ่งแรกที่ทุกคนทำคือปิดประตูทิ้ง · build ที่ไม่มีคลังสถิติก็ไม่เริ่มถามทุกเรื่อง · **ไม่มีช่องทางถาม = ปฏิเสธ** ตามกฎเดิมของ chain ตั้งแต่ P1 · ต่อกับ span จริงผ่าน `ProficiencyCache` (แคช 1 นาที เพราะ gate อยู่บนทางของทุก call แต่คำตอบที่เก่ากว่านั้นคือการตัดสินด้วยสถิติสัปดาห์ที่แล้ว) |
| **P20.5** เหตุผลของการเลือกทำ | วันนี้เห็นเฉพาะเหตุผลของการ*ปฏิเสธ* (hook chain) | หน้าจอบอกได้ว่าทำไม agent เลือกทูลนี้/โมเดล tier นี้ — จาก `RoutingPolicy` และ risk score ที่มีอยู่แล้ว ไม่ใช่ให้โมเดลเขียนคำอธิบายย้อนหลัง | ✅ **ทำแล้ว (2026-08-16, 9 เทส · 2 กฎใน check.sh)** — **คำอธิบายคือการตัดสินใจเอง ไม่ใช่เรื่องเล่าเกี่ยวกับมัน**: `RoutingChoice` ออกมาจาก `choose(from:for:policy:)` ซึ่งเป็น pass เดียวกับที่คัดจริง — ตัวกรองข้อไหนตัด tier ไหนออก ข้อนั้นเขียนเหตุผลเอง ⇒ คำอธิบายจะเพี้ยนจากพฤติกรรมไม่ได้ถ้าตัวกรองไม่เปลี่ยน · เทสยึดมาตรฐานเดียวที่มีความหมาย: **เปลี่ยน policy แล้วประโยคต้องเปลี่ยน** และ `order` ที่โชว์คืออาร์เรย์เดียวกับที่จะไล่จริง · เหตุผลที่รู้ได้ตอนรันเท่านั้น (tier ที่ลองแล้วล่ม) ถูกเติมเข้าไปทีหลัง ไม่งั้นหน้าจอจะบอกว่า endpoint มาก่อน ทั้งที่คำตอบมาจากที่อื่น · ฝั่งทูลใช้ `RiskAssessment.reasons` ของ scorer ที่ตัดสินจริง — และ outcome ที่ไม่มีอะไรจะบอกก็ไม่บอก เพราะ "ปกติดี" ทุกใบคือวิธีสอนคนให้เลิกอ่านบรรทัดที่สำคัญ · **กฎ**: ตัวกรอง capability ใน `ModelRouter` ต้องมีชุดเดียว (สองชุด = มีคำอธิบายที่แอบเป็นคนละทางกับการตัดสิน) และเส้นทางส่ง `why` ถึง event ต้องยังอยู่ |
| **P20.6** Adaptive ตาม project type เท่านั้น | เน้นแผงที่เกี่ยวกับ project type ที่ผู้ใช้เลือกเอง | สลับ project type แล้วหน้าจอเน้นคนละแผง · **ไม่มีการปรับตามพฤติกรรมที่ระบบเดาเอง** — หน้าจอที่ย้ายของเองคือหน้าจอที่จำตำแหน่งอะไรไม่ได้ | ✅ **ทำแล้ว (2026-08-16, 6 เทส · 3 กฎใน check.sh)** — `PanelEmphasis` รับ input เดียวคือ `ProjectKind` ที่ผู้ใช้เลือกเองตอนสร้างโครงการ · **การปฏิเสธคือเนื้อของงานนี้**: span บันทึกทุกหน้าจอที่ใครเปิดอยู่แล้ว การเรียงแผงที่ใช้บ่อยขึ้นมาก่อนจึงห่างแค่ import เดียว — และนั่นคือวิธีที่หน้าจอเลิกเป็นสิ่งที่เรียนรู้ได้ คนหาของด้วยตำแหน่งก่อนจะหาด้วยการอ่านเสมอ · **เน้น ≠ ย้าย**: ลำดับแผงเท่ากันทุกชนิดโครงการ ชนิดโครงการตัดสินแค่ว่าอันไหนติดจุด และเปิดมาที่อันไหน · **"ที่ที่คุณค้างไว้" ชนะ "ที่ที่ชนิดโครงการแนะนำ"** — emphasis ใช้เฉพาะแท็บที่ยังไม่เคยเข้า · `blank` ไม่มีความเห็น ไม่เดาจากชื่อ · **กฎ**: ไฟล์ emphasis ห้ามแตะข้อมูลพฤติกรรม (ตัดคอมเมนต์ก่อนตรวจ เพราะไฟล์นี้เถียงเรื่อง usage-based adaptation ยาว) · รายการแท็บห้ามสร้างจาก emphasis · ชื่อแผงทุกอันต้องมีอยู่จริงในแอป |

---

## P21 — Project เป็นแท็บจริง

สเปก: [ARCH §19.1.1](ARCHITECTURE.md#1911-ความสัมพันธ์ที่ถูกต้องของสองอย่างนี้--และสิ่งที่-implementation-วันนี้ยังไม่ตรง) · **เป็นการแก้ความเข้าใจผิดที่สะสมมา ไม่ใช่ฟีเจอร์ใหม่** — และควรทำก่อน P16 เพราะองค์กรหลายทีมจะยิ่งผูกกับ scope เดียวที่สลับได้ทีละอัน

| Task | รายละเอียด | Done-when | สถานะ |
|---|---|---|---|
| **P21.1** หลายโปรเจกต์เปิดพร้อมกัน | เลิกใช้ `selection` เดี่ยว + เลิก rebuild ทั้งแอปด้วย `.id(scope.storageKey)` | เปิดสองโปรเจกต์ สลับไปมา **แต่ละอันจำสถานะของตัวเอง** · ไม่มีข้อมูลของอีกโปรเจกต์รั่วมา (เทสยิงตรงข้อนี้ เพราะ rebuild คือทางแก้เดิมของปัญหานั้น) | 🔶 **แบบจำลองทำแล้ว หน้าจอยังไม่ต่อ (2026-08-15, 13 เทส)** — `OpenWorkspaces` ใน ProjectKit ถือกฎทั้งสี่ข้อ: General ปิดไม่ได้ · เปิดซ้ำ = ย้ายไปแท็บนั้นไม่ใช่เปิดใหม่ (สองแท็บบนโปรเจกต์เดียวคือสองมุมมองที่เถียงกันว่าใครบันทึกทีหลัง) · **ปิดแท็บที่กำลังดูอยู่แล้วไปที่แท็บข้างเคียง ไม่ใช่เด้งกลับ General** · โปรเจกต์ที่ถูกลบ/ถูกปิดจากที่อื่น ถูก reconcile ทุกครั้งที่รายการโหลดใหม่ · ✅ **ต่อเข้าหน้าจอแล้ว (2026-08-16)** — `ProjectsViewModel` ถือ `OpenWorkspaces` แทน `selection` เดี่ยว (`selection` เหลือเป็นค่าที่*อ่านจาก* `workspaces.active` จึงไม่มีทางถือสองค่าที่ขัดกัน) · มีแถบแท็บอยู่เหนือทุกพื้นที่ ไม่ใช่ในหน้า Plan อย่างเดียว เพราะ workspace ไม่ใช่ state ของหน้าจอใดหน้าจอหนึ่ง · **แต่ละแท็บจำว่าค้างอยู่พื้นที่ไหน** (`areaByTab` คีย์ด้วย id ของแท็บ ⇒ ปิดแล้วเปิดใหม่เริ่มที่ Chat ไม่ใช่ทวงที่เดิมจากก่อนปิด) · แท็บที่เป็น archive ขึ้นแม่กุญแจและบอกด้วยเสียง เพราะคนที่มองไม่เห็นสถานะจะอ่านการปฏิเสธการแก้ว่าเป็นบั๊ก · เมนูเปิดโปรเจกต์แสดงโปรเจกต์ที่ปิดแล้วด้วย พร้อมป้าย "อ่านอย่างเดียว" — การไม่แสดงทำให้ "ปิดแล้ว" อ่านว่า "หายไปแล้ว" · **กฎใหม่สองข้อใน `check.sh` พิสูจน์แล้วว่าจับได้**: selection ที่กลับไปเป็นค่าเดี่ยวที่เก็บไว้ · แถบแท็บที่ไม่มีใครวาด (D6 รูปเดิม) · **ข้อที่ค้างไว้ (`.id(scope.storageKey)` 8 จุด) จบใน P21.2 แต่ไม่ใช่ด้วยการลบทิ้ง** — ดูแถวถัดไป |
| **P21.2** งานที่รันอยู่ไม่ตายเพราะสลับแท็บ | การรันผูกกับ scope ไม่ใช่กับหน้าจอ | เริ่มงานทีมในโปรเจกต์ A → สลับไป B → กลับมา A แล้วงาน**ยังเดินอยู่และ log ครบ** | ✅ **ทำแล้ว (2026-08-16, 4 เทส + 3 กฎใน check.sh)** — **หัวหน้าทีมมีคนละคนต่อ workspace** (`WorkspaceTeams` ใน CoreEngine): เดิมมี `TeamOrchestrator` ตัวเดียวทั้งแอปแล้วหมุนด้วย `use(scope:)` ทุกครั้งที่สลับแท็บ ซึ่ง**กลางรันจะถูกปฏิเสธ** (ถูกแล้ว — ครึ่งรันลงโปรเจกต์หนึ่ง อีกครึ่งลงอีกโปรเจกต์ คือบันทึกที่ไม่มีใครอ่านได้) **แต่การปฏิเสธเงียบ ๆ แปลว่าแท็บที่สลับ*ไป* ลงบันทึกใต้ชื่อโปรเจกต์ที่เพิ่งออกมา** — เทสยิงตรงบรรทัดนั้น และพิสูจน์แล้วว่าแดงเมื่อเอาแบบเดิมกลับมา · **แบบจำลองหน้าจอก็เป็นของ workspace** (`WorkspaceModels`): ของเดิมมี `TeamViewModel`/`AnalysisViewModel` อย่างละตัวแล้วหมุนตามแท็บ ⇒ แถวของโปรเจกต์ที่ออกมากับที่เข้าไปรวมเป็นรายการเดียว · **แชทเป็นเคสที่หนักที่สุด**: view model ถูกสร้าง*ในวิว*ที่ identity คือ scope ⇒ สลับแท็บ = โยนโมเดลทิ้งทั้งที่เทิร์นยังสตรีมอยู่ ตอนนี้โมเดลอยู่กับ workspace จึงอยู่ข้ามการ rebuild · **ปิดแท็บ = ปิดหน้าต่าง ไม่ใช่หยุดงาน** — ทั้งสองทะเบียนปฏิเสธการปล่อย workspace ที่ยังทำงานอยู่ เพราะปล่อยไปก็ไม่ได้หยุดรัน แค่ทิ้งสิ่งเดียวที่ยังมองเห็นมัน · **attach ครั้งเดียวต่อ workspace** ⇒ กลับมาที่แท็บเดิมไม่อ่านฐานข้อมูลใหม่อีกรอบ (นี่คือข้อที่ P21.1 ค้างไว้) · **`.id(scope.storageKey)` ยังอยู่ และตั้งใจให้อยู่** — มันคือ identity ที่ถูกสำหรับ state ของ*วิว* (ข้อความที่พิมพ์ค้าง ชีตที่เปิดอยู่ เป็นของแท็บนั้น) การลบทิ้งเฉย ๆ โดยไม่ทำที่เก็บ state ต่อแท็บ จะกลายเป็น**วิวเดียวใช้ `@State` ร่วมกันข้ามโปรเจกต์** ซึ่งคือการรั่วที่ P21.1 เขียนเทสกันไว้พอดี สิ่งที่เปลี่ยนคือไม่มีอะไรที่สำคัญห้อยอยู่กับมันแล้ว · **ค้าง**: state ของวิวเอง (ตำแหน่งสกรอลล์ ฟอร์มที่กรอกค้าง) ยัง reset เมื่อกลับมา เพราะมีวิวของแท็บเดียวที่มีชีวิตอยู่ในแต่ละขณะ — แก้ได้ด้วยการเลี้ยงวิวของทุกแท็บไว้พร้อมกัน ซึ่งแลกด้วยการรัน `task` ของทุกแท็บพร้อมกัน |
| **P21.3** ปิดโปรเจกต์ = archive เขียนไม่ได้ | ปิดแล้วอ่านได้ทุกอย่าง เขียนไม่ได้ | พยายามแก้ใบงาน/เพิ่ม assignment ในโปรเจกต์ที่ปิดแล้ว → **ถูกปฏิเสธที่ชั้น service ไม่ใช่แค่ซ่อนปุ่ม** | ✅ **ทำแล้ว (2026-08-15, 7 เทส + กฎใน check.sh)** — ประตูปิดโครงการมี 8 เงื่อนไขมาตั้งแต่ P10.10 และ **หลังผ่านแล้วไม่มีอะไรห้ามใครแก้ต่อเลย** ⇒ รายงานปิดโครงการเป็นคำกล่าวถึงสถานะที่ขยับได้หลังเขียน · `requireWritable` อยู่ที่ `ProjectService` ทุกทางเขียน (แก้โปรเจกต์ · ใบงาน · ลบใบงาน · ปิดใบงาน · ทะเบียน · ตัดสินคำขอเปลี่ยนแปลง) ไม่ใช่ซ่อนปุ่ม เพราะทูลของ agent กับช่องทางแชทก็เขียนได้เหมือนกัน · **ข้อยกเว้นเดียวคือการวัดผลประโยชน์** (§19.12 นัดทบทวนอยู่หลังปิดหลายเดือน — การวัดคือ*เพิ่มข้อเท็จจริง* ไม่ใช่แก้ข้อตกลง) และมีเทสคุมทั้งสองฝั่ง · **การปิดเองต้องไม่ถูกกั้น** — guard อยู่บน `update` สาธารณะ ไม่ใช่บน `persist` ที่ transition เขียนผ่าน ไม่งั้นการเปลี่ยนขั้นครั้งสุดท้ายจะเป็นสิ่งเดียวที่ระบบบันทึกไม่ได้ · กฎใน `check.sh` **พิสูจน์แล้วว่าจับได้** เมื่อมีคนเพิ่มทางเขียนใหม่แล้วลืม guard |
| **P21.4** ความรู้ย้ายขึ้นส่วนกลางตอนปิด | บทเรียน · precedent ที่ประกาศเป็น central · เอกสารอ้างอิงภายนอก+tier ย้ายขึ้น — **ข้อมูลผู้เข้าร่วมไม่ย้าย** | ปิดโปรเจกต์แล้วโปรเจกต์ถัดไปค้นเจอสิ่งที่ควรเจอ · **เทสที่พิสูจน์ว่าข้อมูลจาก M16 ไม่ตามขึ้นไป** (เป็นเรื่องจริยธรรม ไม่ใช่เรื่องการจัดเก็บ) | ✅ **ทำแล้ว (2026-08-16, 8 เทส + ขับผ่านฐานข้อมูลจริงใน `EmbeddingCheck` + 2 กฎใน check.sh)** — กฎอยู่ที่ `ClosingHandoverPolicy` (Knowledge, ตรวจได้โดยไม่ต้องมีฐานข้อมูล) ส่วนการย้ายอยู่ที่ `ClosingHandoverStore` (Persistence) และ `ProjectService` เรียกตอนปิด**พร้อมกับบทเรียน** ไม่ใช่คนละจังหวะ · **ขึ้น**: เอกสารอ้างอิงภายนอก (upload/web) **พร้อม tier เดิม** — ขึ้นไปโดยไม่มี tier คือถูกจัดอันดับใหม่เหมือนไม่เคยมีใครประเมิน · คำตัดสินข้อขัดแย้งที่**คนประกาศเป็น central** เท่านั้น (คำตัดสินที่ผูกกับนิยามของงานนี้ ไม่ใช่กฎของงานหน้า และคำตัดสินที่ระบบทำเองไม่ใช่ precedent ของใคร) · **ไม่ขึ้น**: `.fieldwork` — **นี่คือข้อจริยธรรม ไม่ใช่ข้อจัดเก็บ**: บทสัมภาษณ์กับ paper เป็นข้อความในดัชนีเดียวกัน รูปร่างเดียวกัน ขนาดพอกัน มีแต่ `Origin` ที่บอกได้ว่าอันไหนไปต่อได้ · ตารางที่ดึงจากฐานข้อมูลก็ไม่ขึ้น เพราะระบบบอกไม่ได้ว่ามีข้อมูลบุคคลอยู่ไหม — ปฏิเสธคือด้านที่ผิดแล้วแก้คืนได้ · ร่างระหว่างทางไม่ขึ้น (บทเรียนขึ้นทางของมันเอง เพราะ*การเป็นบทเรียน*คือการตัดสินใจของคน ไม่ใช่คุณสมบัติของข้อความ) · **ปิดซ้ำไม่ทำสำเนา** เพราะ id เดิม · **ของที่ไม่ขึ้นยังอ่านได้ในโปรเจกต์ที่ปิดแล้ว** — คำสัญญาคือเรื่องการเผยแพร่ต่อ ไม่ใช่การลบ · **กฎใน check.sh สองข้อ พิสูจน์แล้วทั้งคู่**: `.fieldwork` ที่เลิกตอบว่า `stays` · `default:` ที่โผล่มา ซึ่งจะทำให้แหล่งข้อมูลชนิดใหม่ได้คำตอบของคนอื่นโดยไม่มีใครถาม |

---

---

## Risk Register

| # | ความเสี่ยง | ผลกระทบ | แผนรับมือ | สถานะ |
|---|---|---|---|---|
| R1 | **Guardrail ปฏิเสธงานการแพทย์ 12.5% แบบสุ่ม** | Tier 0 ใช้เดี่ยวไม่ได้ | escalate อัตโนมัติ (P1.5) — **ต้องมี test ที่บังคับ refusal จริง ไม่ใช่ mock** | 🟡 คุมแล้ว · ยังเป็นข้อจำกัดถาวรของ Tier 0 |
| R2 | **macOS 27 API ยังไม่ออก** (ก.ย. 2026) | ถ้าผูกโค้ดตรงจะต้องรื้อ | `LLMExecutor` abstraction (P1.4) — เมื่อ macOS 27 ออก เพิ่ม executor ตัวที่ 4 ไม่แตะ CoreEngine | 🟡 เปิดอยู่ — รอ ก.ย. 2026 |
| R3 | **`surrealdb.swift` alpha** | ถ้าพึ่ง SDK จะโดน breaking change | เขียน client เอง ✅ ทำแล้ว — Plan B (SQLite+FTS5+sqlite-vec) บันทึกไว้ | ✅ ปิด — เขียน client เอง ไม่ได้พึ่ง SDK |
| R4 | **Thai tokenizer แตกคำทับศัพท์** | BM25 recall ตกในงานการแพทย์ | dictionary merge layer (P2.2) + วัดผลก่อน/หลัง | ✅ คุมแล้ว (P2.2) — วัดผลก่อน/หลังแล้ว |
| R5 | **เลือก embedding ผิด** | ต้อง re-index ทั้ง KB | ปิด D-2 **ก่อน** เริ่ม index (P2.1) — ห้ามข้าม | ✅ ปิด — D-2 ปิดก่อน index แรก |
| R6 | **Multi-agent กิน token 15×** | ค่าใช้จ่าย/เวลาบานปลาย | Budget Governor (P5.6) + cap fan-out (P4.2) + Tier 0/0.5 รับงานเบา | 🟡 คุมแล้ว (P5.6) — ยังไม่เคยวัดกับ endpoint ที่คิดเงินจริง |
| R7 | **SearXNG เป็น Python** | เพิ่มภาระ packaging — และกลายเป็นจริง: venv ย้ายที่ไม่ได้ จึงก๊อปเข้า `.app` ไม่ได้เลย | เดิม "ยอมรับไว้ชั่วคราว" แล้วเลื่อนไป P13 · **P13.1 แก้ด้วยการเปลี่ยนกลไก ไม่ใช่การแพ็กให้สำเร็จ** — `WKWebView` เป็นเฟรมเวิร์กของระบบ ไม่ต้องแพ็กอะไร ([ARCH §1.2.1](ARCHITECTURE.md#121-สะพานค้นเว็บด้วย-wkwebview-แบบไม่มีหน้าต่าง-p131)) | ✅ ปิด — แต่ปิดด้วยการ**เลิกใช้ SearXNG** ไม่ใช่ด้วยการแพ็กมันสำเร็จ · **บทเรียนที่เหลือไว้**: "ติดตั้งได้บนเครื่องนี้" ไม่ใช่คำตอบของ "แพ็กไปกับแอปได้" |
| R8 | **ทำ module เสร็จแต่ไม่ได้ต่อเข้า path จริง** (v1 D6) | มีโค้ดแต่ไม่มี feature | ทุก Task ที่ให้ความสามารถ agent ต้องมี integration test ระดับ session | 🟡 **เปิดถาวรโดยตั้งใจ** — เกิดซ้ำมาแล้ว 6 ครั้ง มีกฎใน `check.sh` คุม |
| R9 | **Gantt ของงาน AI ไม่มีหน่วยที่ซื่อสัตย์** — ถ้าให้โมเดลเดา "งานนี้ 3 วัน" คือแผนภูมิที่ดูดีแล้วผิดทุกวัน | วางแผนจากตัวเลขแต่ง | forecast มาจาก span จริงของ role+deliverableType เดียวกัน แสดงเป็นช่วง p50–p90 · ไม่มีประวัติ = แสดง "ยังไม่มีข้อมูล" ห้ามเดาแทน (P10.9) | 🟢 **ปิดแล้ว (2026-08-16)** — แกนเวลาวาดจาก span จริง (ช่องว่างคงเป็นช่องว่าง) และแกนคาดการณ์เป็นช่วง p50–p90 จากงานจริงเท่านั้น · ไม่มีประวัติ = ไม่มีวันที่ และบอกชื่อใบที่ประมาณไม่ได้ · มีกฎใน check.sh กันค่า default หลุดเข้าไปใน forward pass |
| R10 | **พิธีกรรม PM ท่วมเครื่องมือของคนเดียว** — gate/register/รายงานมากจนคนเลี่ยงไปใช้ General ตลอด | ฟีเจอร์ที่สร้างแล้วไม่มีใครใช้ | General ไม่มี PM machinery เลยโดยตั้งใจ · `tailoring_record` ให้ตัด practice ออกได้อย่างเป็นทางการ · gate มีเฉพาะจุดที่ย้อนกลับไม่ได้จริง (G1/G2/ก่อนเก็บข้อมูล/ปิด) | 🟡 คุมด้วยการออกแบบ — ยังไม่มีผู้ใช้จริงพอจะรู้ว่าพอไหม |
| R11 | **เปิดฟอร์มเก็บข้อมูลจากมนุษย์ บนเครื่องส่วนตัวที่ยังเซ็น ad-hoc และยังไม่มี security review** | ข้อมูลผู้เข้าร่วมรั่ว/หาย = ความเสียหายที่แก้ไม่ได้ | LAN-only เป็นค่าเริ่มต้น ไม่มี tunnel ในตัว · consent + ethics record บังคับที่ gate · คีย์เชื่อมตัวตนอยู่ Keychain · บอกตรง ๆ ในแอปว่างานเดิมพันสูงยังควรใช้บริการที่ผ่านการตรวจแล้ว (P11.4–P11.7, P11.10) · **P9.3/P9.4 ทำแล้ว**: ความลับย้ายเข้า Keychain จริง · มี audit ที่ค้นทุกไบต์ว่าไม่มี token ตกอยู่ในไฟล์ · สี่กรณีพังมีข้อความที่อ่านรู้เรื่อง | 🟠 **คุมแล้วบางส่วน ยังไม่ปิด** — สองข้อที่เป็นขั้นต่ำทำครบแล้ว แต่ **security review ที่แท้จริงคือคนนอกมาตรวจ ไม่ใช่คนเขียนตรวจงานตัวเอง** และสิ่งที่ยังไม่มี: notarization (ยังเซ็น ad-hoc) · threat model ของ M16 ที่เขียนเป็นเอกสาร · ยังไม่เคยรันบนเครื่องที่ไม่ใช่เครื่องนี้ |
| R12 | **สถิติความตรง/ความเที่ยงที่เขียนเองผิดแบบเงียบ ๆ** | ผลวิจัยผิดโดยไม่มีใครรู้ | ทุกตัวตรวจกับชุดข้อมูลที่รู้คำตอบจากตำรา · **CFA ไม่ทำในเฟสแรก** บันทึกเป็นข้อจำกัดแทนที่จะทำครึ่ง ๆ (P11.3) | 🟡 คุมแล้ว (P11.3) — ตรวจกับค่าที่ตีพิมพ์ + implementation อิสระ |
| R14 | **`KnowledgePolicyGate` ไม่เคยถูกต่อเข้าแอป — D6 ครั้งที่ 7 และอยู่บนประตูความปลอดภัย** *(เจอตอน P11.10, 2026-08-15)* | P2.6 สร้าง policy gate พร้อม 11 เทสที่ยิงผ่าน `ToolGateway` จริง **แต่แอปสร้าง `HookChain(stageGate:)` ซึ่งได้ `NoPolicyGate()`** ⇒ ในแอปจริงนโยบายใน `policy` scope ไม่เคยหยุดอะไรเลยเจ็ดเฟส | ✅ **ปิดแล้ว 2026-08-15** — `StoredPolicyGate` + `PolicyLibrarySource` อ่าน scope ตอนถูกถาม แคชไว้ และลืมเมื่อ ingest แตะ `policy` · **scope ที่อ่านไม่ได้ = ปฏิเสธ ไม่ใช่ปล่อยผ่าน** (นโยบายที่อ่านไม่ได้ ไม่เท่ากับไม่มีนโยบาย) · กฎใหม่ใน `check.sh` แดงถ้า `Engine` สร้าง hook chain โดยไม่มี `policyGate` — **ทดสอบแล้วว่าจับได้จริง** · ด่านใน `EmbeddingCheck` เขียนกฎลงฐานข้อมูลจริงแล้วยิงผ่าน gateway จริง | ✅ ปิดแล้ว |
| R15 | **องค์กรหลายชั้นทำให้ค่าใช้จ่ายโตแบบทวีคูณ** — R6 วัดไว้แล้วว่า multi-agent กิน ~15× และการซ้อนชั้นคูณซ้ำอีก | เผางบหมดกลางงานโดยไม่มีใครเห็นล่วงหน้า | เพดานความลึก 3 ชั้น · ชั้นที่ 4 ต้องคนอนุมัติ · **การแตกทีมต้องผ่าน `BudgetGovernor` ก่อนตั้ง ไม่ใช่ค้นพบตอนหมด** · Command Tree แสดงงบต่อ subtree (P16.2/P16.3/P16.5) | 🔴 **เปิด — ยังไม่มีอะไรคุมเลยจนกว่า P16 จะลงมือ** |
| R16 | **ตัวขับหน้าจอควบคุมได้ทุกแอป ไม่ใช่แค่แอปนี้** — CGEvent เป็น event ระดับระบบ | agent ที่หลงทางกดอะไรก็ได้บนเครื่องผู้ใช้ | จำกัดที่ pid ของแอปตัวเองเป็นค่าเริ่มต้น · ขับแอปอื่นเปิดเป็นรายครั้งและเป็น `high` · **สิ่งที่อ่านจากจอถือเป็นข้อมูลไม่น่าเชื่อถือ ผ่าน hook chain** (P17.3/P17.5) | 🔴 **เปิด — เป็นความเสี่ยงใหม่ที่ P17 สร้างขึ้นเอง ต้องคุมพร้อมกับที่สร้าง** |
| R17 | **สิทธิ์ TCC ผูกกับลายเซ็นไบนารี และแอปนี้ยังเซ็น ad-hoc** | build ใหม่แล้วตัวขับหยุดทำงานโดยไม่มีข้อความ — อาการเหมือนโค้ดพัง | ตรวจสิทธิ์ก่อนเริ่มทุกครั้งและบอกว่าต้องเปิดอะไรที่ไหน (P17.3) · **เป็นเหตุผลตรง ๆ อีกข้อที่ต้อง notarize** ซึ่งค้างอยู่ใน R11 อยู่แล้ว | 🟠 คุมได้บางส่วนด้วยการตรวจและบอก — รากของปัญหาปิดพร้อม R11 |
| R18 | **การจัดหมวด LC ที่ระบบเดาผิด = ความรู้ที่มีแต่หาไม่เจอ** | อาการเหมือนไม่มีความรู้นั้นเลย ซึ่งแย่กว่าไม่จัดหมวด | หมวดมี `assignedBy: .system\|.user` และแก้ได้จากหน้าจอ · **"ยังจัดหมวดไม่ได้" เป็นสถานะที่แสดง ไม่ใช่ยัดลง A** (P18.4) | 🟡 คุมด้วยการออกแบบ — ยังไม่มีข้อมูลจริงพอจะรู้ว่าอัตราเดาผิดเท่าไร |
| R13 | **ยุบ 14 หน้าจอเป็น 4 พื้นที่แล้วของหาย** — เป็นความผิดพลาดแบบเดียวกับ "มีโค้ดแต่เรียกใช้ไม่ได้" ที่เกิดมาแล้ว 4 ครั้ง | ความสามารถหายไปเงียบ ๆ ตอนจัดหน้าใหม่ | P10.12 ต้องมี checklist ต่อรายการของ [ARCH §14.2](ARCHITECTURE.md#142-workspaceui--หน้าจอทั้งหมด) ว่ายังเข้าถึงได้ครบ + กฎ reachability เดิมใน `check.sh` ยังบังคับอยู่ | ✅ คุมด้วยกฎ — P10.12 เสร็จ และ `check.sh` parse §14.2 ทุกครั้ง |

---

---

## งานถัดไป

**Phase ที่ปิดสนิท** (ทุก task ผ่าน Done-when ไม่มีของค้าง): **P0 · P1 · P13**
**Phase ที่ทุก task ลงโค้ดแล้วแต่ยังมีของค้าง**: P10 (16/16 task มีของ · 5 ใบยังมีช่อง "ค้าง") · P11 (เหลือ P11.10 ที่ยังไม่เริ่ม)
— "ลงโค้ดครบ" กับ "ปิดสนิท" ไม่ใช่คำเดียวกัน และตารางข้างบนแยกสองอย่างนี้ไว้แล้ว
ประวัติงานที่ทำไปแล้วรายรอบอยู่ใน [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md)

### คิวถัดไป — เรียงตามลำดับที่ตั้งใจทำ

| # | งาน | ทำไมอยู่ลำดับนี้ |
|---|---|---|
| ~~1~~ | ~~**หน้าจอประกอบต้นฉบับ** — ปิด P11.9~~ ✅ **ทำแล้ว 2026-08-15** | เหลือ: ขับบนหน้าจอจริง (ตัวขับพิมพ์ลง `TextField`/`TextEditor` ไม่ได้ จึงสร้างเล่มจากหน้าจอไม่ได้ด้วยตัวขับ) |
| ~~2~~ | ~~**ปุ่มส่งบทถอดเทปเข้าคลังความรู้** — ปิดของค้าง P11.8~~ ✅ **ทำแล้ว 2026-08-15** | **P11 ครบทุก task และไม่มีของค้างแล้ว** ยกเว้นการขับบนหน้าจอจริง ซึ่งติดข้อจำกัดของตัวขับ |
| ~~3~~ | ~~**P11.10** จริยธรรม/ความเป็นส่วนตัว~~ ✅ **ทำแล้ว 2026-08-15** · **P11 ไม่มี task ที่ยังไม่เริ่มแล้ว** (เหลือของค้างใน P11.8/P11.9) | เหลือ: ขับหน้าจอกับโปรเจกต์ที่มีคำตอบจริงแล้วกดปิด |
| ~~4~~ | ~~**P9.3 Secrets audit · P9.4 Crash/hang resilience**~~ ✅ **ทำแล้ว 2026-08-15** | เหลือ: หน้าจอตั้งค่าบอท (ข้อ 5) · notarization · ขับ `SecretField` บนหน้าจอจริงเมื่อตัวขับพิมพ์ลง `SecureField` ได้ |
| ~~5~~ | ~~**หน้าจอตั้งค่าช่องทาง (Telegram/Discord/LINE)**~~ ✅ **ทำแล้ว 2026-08-15 (11 เทส)** | อยู่ที่ **ระบบ → ช่องทาง** · ตรรกะที่มีกฎอยู่ใน `ChannelAccountDraft` ใน target `Channels` ไม่ใช่ใน view เพราะกฎพวกนี้ตัดสินว่าใครสั่งงานเครื่องนี้ได้ · **ที่ต้องระวังที่สุดคือการตัดรายการ chat id**: พิมพ์ `123, 456` แล้วได้ `"123"` กับ `" 456"` `allows(chat:)` เทียบตรงตัว คนที่สองจึงถูกบอทเมินเงียบ ๆ โดยบอทดูเหมือนตั้งค่าครบ — ตัดด้วยจุลภาคไทย/เต็มความกว้างด้วย เพราะคีย์บอร์ดมือถือให้จุลภาคคนละตัวและคนมองไม่เห็นความต่าง · รายการว่าง = **ไม่รับจากใครเลย** เตือนตรงจุดที่คนกำลังจะปล่อยว่าง ไม่ใช่ในเอกสาร · บอกตรง ๆ ว่าเปลี่ยนแล้วมีผลตอนเปิดแอปครั้งถัดไป (ช่องทางถูกเริ่มครั้งเดียวตอนบูต) เพราะหน้าจอที่ทำเหมือนมีผลทันทีทั้งที่บอทที่วิ่งอยู่ยังถือรายการเมื่อวาน แย่กว่าการบอกตามจริง · **กฎใหม่ใน `check.sh` พิสูจน์ว่า fail ได้**: property บน engine ที่ไม่มีไฟล์ view ไหนอ่าน = ตั้งค่าได้ทางเดียวคือแก้ไฟล์เอง (กฎเดิมจับแค่ “แอปไม่เคยสร้างเลย” ซึ่งเคสนี้ผ่าน) |
| ~~6~~ | ~~**หน้าจอเพิ่ม MCP server เอง**~~ ✅ **ทำแล้ว 2026-08-15 (11 เทส + ขับจริง)** | อยู่ที่ **ระบบ → สถานะระบบ** เหนือส่วนปลั๊กอิน · หน้าจอบอกสองอย่างที่ JSON ไม่เคยบอก: **คำนำหน้าที่ทูลจะได้จริง** (`mcp__<namespace>__` ซึ่งตัดทุกอย่างที่ไม่ใช่ ASCII ทิ้ง — ชื่อไทยล้วนสองตัวจึงกลายเป็น `server` ทั้งคู่แล้วทูลชนกัน) และ**ทำไมเซิร์ฟเวอร์ถึงไม่ขึ้น** · อาร์กิวเมนต์ตัดโดยเคารพเครื่องหมายคำพูด เพราะ `--root "/My Papers"` ที่ถูกตัดตามช่องว่างคือเซิร์ฟเวอร์ที่เสิร์ฟผิดโฟลเดอร์ โดยไม่มีอะไรบนจอชี้ไปที่ช่องที่ผิด |
| ~~7~~ | ~~**File Viewer/Editor** (P8.6)~~ ✅ **ทำแล้ว 2026-08-15** | เหลือ: กดปุ่มบันทึกบนหน้าจอจริง (ตัวขับพิมพ์ลง `TextEditor` ไม่ได้) · สร้าง/ลบ/เปลี่ยนชื่อไฟล์ · ตัวแสดงรูป |
| ~~8~~ | ~~**`install_package`** (P8.4)~~ ✅ **ทำแล้ว 2026-08-15 (19 เทส)** | กฎที่สำคัญที่สุดคือข้อที่ **argv ไม่ได้กัน**: ส่งเป็น argv แทน command line กัน `pandas; rm -rf ~` ได้จริง แต่ไม่กัน `--target=/ที่อื่น` ซึ่งไม่ใช่ shell injection — เป็นอาร์กิวเมนต์ธรรมดาที่ตัวจัดการแพ็กเกจอ่านเป็น*ตัวเลือก* ชื่อที่ขึ้นต้นด้วยขีดจึงถูกปฏิเสธแยกอีกชั้น · **ปฏิเสธ source distribution เป็นค่าเริ่มต้น** (`--only-binary :all:`) เพราะ wheel คือข้อมูลที่ถูกแตกออก ส่วน sdist **รัน `setup.py` ของตัวเองตอนติดตั้ง** ก่อนที่ใครจะได้ดูมัน — คนที่ขอ `pandas` ไม่ได้ขอข้อนี้ · เปิดได้ต่อการเรียกด้วย `allow_source_build` · **ติดตั้งลงโปรเจกต์ ไม่ใช่ลงเครื่อง** — งานวิจัยที่รันบนไลบรารีที่ติดตั้งทั้งเครื่อง คืองานที่สภาพแวดล้อมเลื่อนใต้เท้าตัวเองครั้งถัดไปที่มีคนติดตั้งอะไร · **บันทึกเวอร์ชันที่ resolve ได้จริง ไม่ใช่ที่ขอ** และถ้าอ่านไม่ได้ก็บอกว่าอ่านไม่ได้ เพราะ `pandas` วันนี้กับอีกหกเดือนคนละซอฟต์แวร์ · เทสเทียบกับผลลัพธ์ที่**วัดจาก pip จริง** ไม่ใช่จากความจำเรื่องรูปแบบ · เป็นทูลเดียวที่เปิดเน็ต (`RunShellTool` ปิดเป็นค่าเริ่มต้นเพื่อให้ข้อยกเว้นนี้ชัด) และ**บอกตรง ๆ เมื่อ seatbelt ซ้อนไม่ได้** เพราะการบอกว่า "ติดตั้งในกรอบของโปรเจกต์" ทั้งที่ไม่มีกรอบ คือการอ้างเรื่องความปลอดภัยที่ไม่เป็นจริง · **ค้าง**: ยังไม่ตรวจลายเซ็น/ที่มาของแพ็กเกจ · ยังไม่ได้ขับบนหน้าจอ (ต้องอนุมัติผ่าน HITL ซึ่งเป็นเส้นทางที่ขับแล้วในรอบก่อน) · P14.4 (บังคับ HITL แม้อยู่โหมดอัตโนมัติเต็ม) ยังไม่เริ่ม |
| 9 | **P12** Agent Competence & Knowledge View | ขึ้นกับ lesson register ของ P10.8 ซึ่งพร้อมแล้ว · เป็น phase เดียวที่ยังไม่เริ่มเลยและอยู่ในสเปกหลัก ([ARCH §21](ARCHITECTURE.md#21-agent-competence-model--อะไรทำให้-agent-แต่ละตัวต่างกัน)) |
| ~~10~~ | ~~**ครึ่งหลัง P10.15** · **Workflow Builder**~~ 🔶 **Workflow Builder ทำแล้ว 2026-08-15** · ครึ่งหลัง P10.15 (สลับ tier จาก popover) ยังค้าง | ธงแดงทั้งสามข้อจาก checklist ปิดครบแล้ว — เหลือของค้างที่ไม่บล็อกใคร |
| 11 | **P14** สะพาน R | ไม่บล็อกใคร — ชั้นสถิติ Swift ไม่ได้พึ่ง R |
| 12 | **P9.1 golden-task eval · P9.5 performance pass** | ทำก่อนปิด P9 · ต้องมีงานจริงที่สำเร็จแล้วพอเป็นชุด regression |

### คิวใหม่ที่ผู้ใช้กำหนด (2026-08-15) — เรียงตามการพึ่งพา ไม่ใช่ตามหมายเลข

| # | งาน | ทำไมอยู่ลำดับนี้ |
|---|---|---|
| A | **P15** GX10 เป็นสมองหลัก | **ปลดล็อกทุกอย่างที่เหลือ** — `check.sh` ข้ามด่านโมเดลท้องถิ่นมาตลอดเพราะแรมไม่พอ และ P16 ทดสอบจริงบนโมเดล 3B ไม่ได้ · **วัดแล้ว 2026-08-15** ([E.19](docs/VERIFICATION_LOG.md#e19-gx10-เสิร์ฟจริงแล้ว--วัดจาก-endpoint-เป็น-2026-08-15)): structured output ผ่าน · **tool calling ปิดอยู่ที่เซิร์ฟเวอร์ (P15.0)** · **4.7 โทเคน/วินาที ซึ่งเป็นข้อตัดสินใจ ไม่ใช่ข้อทำ (P15.7)** |
| B | **P18.1 · P18.2** เกณฑ์ข้อขัดแย้ง + ด่านภาษา | เล็กที่สุดในบรรดางานที่แก้อาการที่**เจอจริงแล้ว** — การ์ดข้อขัดแย้งที่เป็นคำแปลของกันและกัน ทำให้การ์ดจริงถูกปิดทิ้งโดยไม่อ่าน |
| C | **P19.0 · P19.1 · P19.2** | `survivalUnchecked` คืนสรุปโดยไม่ได้คำนวณ — ต้องปิดก่อน · แล้วมาตรวัดระบาดวิทยากับค่าการทดสอบวินิจฉัยเป็นเลขคณิตล้วนที่ตรวจกับตำราได้ตรง ๆ และใช้บ่อยที่สุด |
| D | **P21** Project เป็นแท็บจริง | แก้ความเข้าใจผิดเชิงโครงสร้างก่อนสร้างของทับลงไป — และ P16 จะยิ่งผูกกับ scope เดียวถ้าไม่แก้ก่อน |
| E | **P17** Machine Control | ปลดล็อกของค้าง "ขับไม่ได้" หลายข้อพร้อมกัน และทำให้รอบตรวจหน้าจอถูกลงทุกรอบหลังจากนั้น |
| F | **P16** AI Organization | งานใหญ่ที่สุด · ต้องมี A และ D ก่อนถึงจะทดสอบได้จริง |
| G | **P18.3–P18.5 · P19.3–P19.7** | ส่วนที่เหลือของคลังความรู้และสถิติ |
| H | **P20** Design System | หลังสุดโดยตั้งใจ — P16 กับ P17 เปลี่ยนโครงหน้าจอมากที่สุด จัดระบบก่อนแล้วรื้ออีกรอบคือทำงานสองครั้ง |

### สิ่งที่ต้องทำทุกครั้ง ไม่ใช่ครั้งเดียว

1. **ขับหน้าจอด้วยมือหลังทุก task ที่มี UI** — สถิติของโปรเจกต์นี้: ทุกรอบเจอบั๊กที่เทสทั้งชุดมองไม่เห็น (8 · 10 · 5 · 7 · 7 · 3) แล้วบันทึกลง [`docs/DRIVING_LOG.md`](docs/DRIVING_LOG.md)
2. **`./scripts/build-app.sh` ก่อนขับทุกครั้ง** — และเทียบเวลาไฟล์ binary กับเวลาเริ่มโปรเซสก่อนอ่านผล (เคยอ่านหน้าจอของ build เก่าแล้วสรุปผิดมาแล้ว)
3. **ปิดแอปด้วย ⌘Q เสมอ** — `kill -9` ทำให้ macOS restore เป็น "ไม่มีหน้าต่าง" ซึ่งหน้าตาเหมือน boot ล้มเหลวเป๊ะ (U24-2)

---

## Completeness Checklist — เทียบกับ v1

ย้ายไป [`docs/LEGACY_V1.md`](docs/LEGACY_V1.md#completeness-checklist--v1--v2-task) — ตารางเทียบ feature ของ v1 (Phase A–J + audit 21 ข้อ) กับ Task ของ v2 ทีละข้อ พร้อม feature inventory เต็มและบทเรียน D1–D7
